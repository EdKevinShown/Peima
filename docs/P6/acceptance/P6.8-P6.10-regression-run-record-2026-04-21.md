# P6.8 / P6.10 最小系统回归 — 执行记录（2026-04-21）
> 依据 `docs/P6/specs/P6.8-P6.10-regression-checklist.md` 完成 M0–M5；结构与 `docs/P6/specs/P6.8-P6.10-regression-run-record.template.md` 对齐，未扩展用例。
---
## 元信息
| 字段 | 填写 |
|------|------|
| **执行日期** | 2026-04-21 |
| **执行人** | （未在提供的材料中填写） |
| **Git 引用** | （未在提供的材料中填写） |
| **API 基址** | `http://localhost:3001` |
| **测试 viewer `userId`** | `cmo7ksq8s00006znosryc9k0n` |
| **Batch 触发方式** | `worker`：`pnpm --filter @peima/worker run batch-match`（仓库根目录执行） |
---
## 总结果
| 项 | 填写 |
|----|------|
| **整轮结论** | ☑ 通过　☐ 未通过 |
| **未通过摘要** | 无 |
**整轮一句话总结**：M0 环境与 worker batch 路径就绪；M1 Final match 主链（preview-pool → enqueue → batch-match → result API → Final Match 页）在本机 viewer 上跑通且前后一致；M2 三页 URL 自愈与顶栏参数保留成立；M3 P6.8 生成 / `mine` / `accept` 成立；M4 P6.10 pending 禁用与 accept 后恢复成立；M5 按清单对「无 waiting」记为跳过（非失败）。
---
## M0 准备
| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| `.env` / `DATABASE_URL` / JWT（及 admin·test 相关 `PEIMA_*`） | ☑ 通过 | `migrate deploy` 已连库并成功；API / Web 可启动；viewer 已登录。本轮 batch 使用 **worker** 命令行路径。 |
| `prisma migrate deploy`（或等价） | ☑ 通过 | `8 migrations found in prisma/migrations`；`No pending migrations to apply.` |
| `pnpm dev:api` / `pnpm dev:web`、`VITE_API_BASE_URL` | ☑ 通过 | API：`Nest application successfully started`；`API listening on http://0.0.0.0:3001`。Web：`VITE ready`；`Local: http://localhost:5173/`。`VITE_API_BASE_URL=http://localhost:3001`。附：**RBAC warn** — `public.role_permissions` 不存在，跳过 seed；**仅记录，不作为本轮失败依据**。 |
| viewer：`user_profile`、预览池 6+ 候选等数据 | ☑ 通过 | **以 M1 中 `POST /preview-pool/generate` 成功作为事实校验**：该请求 **200**，`previewPool.status=active`，`items.length=6`；viewer 已登录（`userId` 见元信息）。 |
| 已写下 batch 稳定跑法（命令或接口） | ☑ 通过 | 固定为：`pnpm --filter @peima/worker run batch-match`。 |
---
## M1 Final match 主链
| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| `POST /preview-pool/generate` | ☑ 通过 | **200**；`previewPool.status=active`；`items.length=6`。 |
| `POST /matching/enqueue` | ☑ 通过 | **200**；`queue.status=waiting`；`queueId=cmo7rxpj9000j6zjkzp3yr86u`。 |
| 一轮 `batch-match`（所选方式） | ☑ 通过 | worker：`pnpm --filter @peima/worker run batch-match`；`totalQueues=1`；`successCount=1`；`failedCount=0`；`queue_done outcome=ok`；`candidateUserId=cmo7myr2200006z8obk1iuj9z`。 |
| `GET /matching/result/:userId` / Final Match 页 | ☑ 通过 | `GET /matching/result/cmo7ksq8s00006znosryc9k0n` → **200**；`status=ready`；`candidateUserId=cmo7myr2200006z8obk1iuj9z`；`batchId=cmo7ryvei00006zw8gladmo8c`；`finalScore=0.4470588235294118`。Final Match 页面可读，与 API 一致/不矛盾。 |
---
## M2 会话 URL 自愈
| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| `/chat` 仅 `userId` → `replace` 后含 `conversationId` | ☑ 通过 | `/chat` 在仅 `userId` 时可自动补全 `conversationId`。 |
| `/copilot` 同上 | ☑ 通过 | `/copilot` 在仅 `userId` 时可自动补全 `conversationId`。 |
| `/chat/timeline` 同上 | ☑ 通过 | `/chat/timeline` 在仅 `userId` 时可自动补全 `conversationId`。 |
| 顶栏互跳 ≥2 次，参数不丢 | ☑ 通过 | 顶栏顺序：**聊天 → 沟通洞察 → 关系时间线查看 → 聊天**；每一跳落地后 URL 均同时保留 **`conversationId`** 与 **`userId`**。 |
---
## M3 P6.8
| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| Chat 触发生成 | ☑ 通过 | Chat 内点击「根据本轮对话生成画像建议」后，页面出现待处理建议卡片。 |
| `GET /profile-suggestions/mine` | ☑ 通过 | **200**；存在 `pending`：`id=cmo7shzl3000l6zjkstqxjgp3`；`status=pending`；`sourceType=hybrid`；`sourceVersion=p6.8-profile-completion-chat-ai-v1`；`sourceConversationId=cmo7nqkq900016za0nd71s2qt`。 |
| `accept` | ☑ 通过 | 对该条 pending suggestion 执行 accept 后，状态变为 `accepted`。 |
| `dismiss` | ☑ 跳过 | **本轮未单独执行 `dismiss`**；属清单允许范围内跳过，**不记失败**。 |
---
## M4 P6.10
| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| pending 时按钮 disabled + 说明 | ☑ 通过 | pending 存在时：页面显示「待处理」；建议卡片存在；「接受 / 忽略」可见；「根据本轮对话生成画像建议」不可重复点击；单行说明：**已有待处理建议，请先审阅**。 |
| accept 后按钮恢复（无其它 block） | ☑ 通过 | accept 后：生成按钮恢复可点；上述 pending 阻断说明消失。 |
---
## M5 Worker 稳定跑法
| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| 与 M0 同一路径再执行一次 / 或记录跳过原因 | ☑ 跳过 | 再次执行 `pnpm --filter @peima/worker run batch-match`，命令正常结束；日志：`totalQueues=0`，`successCount=0`，`failedCount=0`，`outcome=ok`；**当前无 `waiting` 队列**，按清单记为 **跳过（无 waiting 队列）**，**不记失败**。 |
---
## 失败明细
本轮无失败项；本节可删除或保留为空表。

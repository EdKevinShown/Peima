# P6.8 / P6.10 最小系统回归 — 执行记录（模板）

> 使用方式：复制本文件为 **`P6.8-P6.10-regression-run-record-YYYYMMDD.md`**（或等价命名），跑 **`./P6.8-P6.10-regression-checklist.md`** 中 **M0–M5** 后逐项填写。**不**扩展用例；与清单范围一致。

---

## 元信息

| 字段 | 填写 |
|------|------|
| **执行日期** | |
| **执行人** | |
| **Git 引用** | （分支 / tag，如 `p6.8-p6.10-closure`） |
| **API 基址** | （如 `http://localhost:xxxx`） |
| **测试 viewer `userId`** | |
| **Batch 触发方式** | （admin / test / worker 本机命令，写实际使用的一条） |

---

## 总结果

| 项 | 填写 |
|----|------|
| **整轮结论** | ☐ 通过　☐ 未通过 |
| **未通过摘要** | （无则写「无」） |

---

## M0 准备

| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| `.env` / `DATABASE_URL` / JWT（及 admin·test 相关 `PEIMA_*`） | | |
| `prisma migrate deploy`（或等价） | | |
| `pnpm dev:api` / `pnpm dev:web`、`VITE_API_BASE_URL` | | |
| viewer：`user_profile`、预览池 6+ 候选等数据 | | |
| 已写下 batch 稳定跑法（命令或接口） | | |

---

## M1 Final match 主链

| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| `POST /preview-pool/generate` | | |
| `POST /matching/enqueue` | | |
| 一轮 `batch-match`（所选方式） | | |
| `GET /matching/result/:userId` / Final Match 页 | | |

---

## M2 会话 URL 自愈

| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| `/chat` 仅 `userId` → `replace` 后含 `conversationId` | | |
| `/copilot` 同上 | | |
| `/chat/timeline` 同上 | | |
| 顶栏互跳 ≥2 次，参数不丢 | | |

---

## M3 P6.8

| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| Chat 触发生成 | | |
| `GET /profile-suggestions/mine` | | |
| `accept` | | |
| `dismiss` | | |

---

## M4 P6.10

| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| pending 时按钮 disabled + 说明 | | |
| accept 后按钮恢复（无其它 block） | | |

---

## M5 Worker 稳定跑法

| 检查项 | 结果 ☐ 通过 ☐ 失败 ☐ 跳过 | 备注 / 证据 |
|--------|---------------------------|---------------|
| 与 M0 同一路径再执行一次 / 或记录跳过原因 | | |

---

## 失败明细（有则填，无则删本节）

| # | 用例（M0–M5） | 现象 | 证据（日志 / status / 截图路径） | 归类 |
|---|---------------|------|-----------------------------------|------|
| 1 | | | | |
| 2 | | | | |

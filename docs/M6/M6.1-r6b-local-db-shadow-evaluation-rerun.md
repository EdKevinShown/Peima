# M6.1-r6b — Local DB Shadow Evaluation Rerun With Waiting Queue

## 1. 本轮目标

- **M6.1-r6** 已在 **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED=1`** 下执行 **worker `batch-match`**，但 **`totalQueues=0`**，**未生成新 `MatchResult`**，**`sampleSize=0`**，**`rrmDecisionShadow` 落库未验证**。
- **M6.1-r6b** 目标：在本地 DB 中确保 **至少 1 条 `waiting` 队列**，在 **同一 flag** 下重跑 **`batch-match`**，得到 **`sampleSize ≥ 1`** 的匿名证据，并核对 **`matchInsights.rrmDecisionShadow`** 与 **r5 指标口径**。
- **本轮不让** shadow **decision** 在产线**生效**；**不改**正式匹配结果；**不进入** decision pilot。

---

## 2. 当前基线

| 项 | 值 |
|----|-----|
| **r3 commit** | `f4b4bb9` |
| **r4 commit** | `de93e33` |
| **r5 commit** | `ddf640b` |
| **r6 commit** | `f93b025` |
| **Feature flag** | **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED`**（仅 **`"1"`** 开启写入） |
| **Payload 路径** | **`matchInsights.rrmDecisionShadow`** |

---

## 3. 队列准备

| 项 | 记录 |
|----|------|
| **初始 waiting 队列** | **无**（与 r6 一致；此前 **`batch-match`** 报 **`totalQueues=0`**）。 |
| **既有流程** | 使用仓库 **`packages/database/scripts/m6-r6b-shadow-smoke.mjs prep`**：在本地 DB 中查找 **`status=active`** 且 **≥2 条 pool items**、viewer 与候选均具备 **`user_profiles`** 的 **preview pool**，并为该 viewer **创建**一条 **`batch_match_queue.status=waiting`**（若已存在 **`waiting`** 则跳过创建）。 |
| **真实 ID** | **未写入本文档**；脚本 stdout 仅输出 **`viewerUserIdPrefix`**（前 4 字符）与计数类字段。 |

---

## 4. Worker 执行结果

| 项 | 值 |
|----|-----|
| **命令** | PowerShell：`$env:PEIMA_M6_RRM_DECISION_SHADOW_ENABLED="1"`；`cd apps/worker`；`pnpm exec dotenv -e ../../.env -- pnpm run batch-match` |
| **`totalQueues`** | **1** |
| **`successCount`** | **1** |
| **`failedCount`** | **0** |
| **新 `MatchResult`** | **是**（**≥1** 条；时间戳与 **`queue_done` outcome=ok** 一致） |
| **真实 ID** | **未写入本文档** |

---

## 5. 新 `MatchResult` 匿名字段摘要

以下来自 **`m6-r6b-shadow-smoke.mjs summarize`** 对 **最新一条** **`match_results`** 的 **`matchInsights`** 摘要（**不含** **`userId` / `matchResultId`**）。

| 检查项 | 结果 |
|--------|------|
| **`scoreShadowV2`** | **存在** |
| **`scoreShadow` v1** | **未新写**（本行 **`scoreShadowV1Present: false`**） |
| **`rrmV2Top2Selector`** | **存在** |
| **`rrmDecisionShadow`** | **存在** |
| **`schemaVersion`** | **1** |
| **`sourceType`** | **`rrm_decision_shadow`** |
| **`sourceVersion`** | **`m6.1-rrm-decision-shadow-v1`** |
| **`shadow.decision`** | **`same_as_baseline`** |
| **`comparison.sameAsBaseline`** | **`true`** |
| **`comparison.switchSuggested`** | **`false`** |
| **`guardrails.blocked`** | **`false`** |
| **`guardrails.blockReasons`** | **`[]`**（空） |
| **`inputPresence.scoreShadowV2`** | **`true`** |
| **`inputPresence.rrmV2Top2Selector`** | **`true`** |
| **`inputPresence.selectedTop2`** | **`true`** |
| **`inputPresence.scoreShadowV1LegacyPresent`** | **`false`** |
| **`unexpected_exception`** | **未出现在 `blockReasons`** |

---

## 6. 指标摘要

**口径：** 与 **r5 §4** 一致；**`N = 1`**（仅最新一条新生成行）；**`N_shadow = 1`**（该行含非空 **`rrmDecisionShadow`**）。**`sameAsBaselineRate` / `switchSuggestedRate`** 分母取 **`N_shadow`**（与 r5「或 / **`N_shadow`**」一致，便于 **`N=1`** smoke）。

| Metric | Value | Notes |
|--------|------:|-------|
| **`sampleSize` (`N`)** | **1** | **Smoke run**；**不作**质量结论。 |
| **`shadowCoverageRate`** | **1.0** | **`N_shadow / N`** |
| **`validShadowRate`** | **1.0** | **`decision`** 为有效枚举 / **`N_shadow`** |
| **`noShadowDecisionRate`** | **0.0** | |
| **`sameAsBaselineRate`** | **1.0** | / **`N_shadow`** |
| **`switchSuggestedRate`** | **0.0** | / **`N_shadow`** |
| **`blockedRate`** | **0.0** | |
| **`missingInputRate`** | **0.0** | **`blockReasons`** 与 r5 定义集合无交集 |
| **`contextBlockedRate`** | **0.0** | |
| **`invalidSelectorRate`** | **0.0** | |
| **`unexpectedExceptionRate`** | **0.0** | |
| **`v1LegacyPresenceRate`** | **0.0** | / **`N`**（**`inputPresence.scoreShadowV1LegacyPresent === false`**） |

---

## 7. 不变性确认

- **`finalScore`**：**未改业务逻辑**；本行仅观测到 **`finalScorePresent: true`**（数值未在文档中记录）。
- **`MatchResult.candidateUserId`**：**未改** worker 选择逻辑；**r6b** 未触碰 **winner / ranking** 实现。
- **Worker winner / ranking**：**未改**。
- **RRM resolver**：**未改**。
- **Prisma schema / migration**：**未改**。
- **`scoreShadow` v1**：**未恢复**；新行 **未写** v1。
- **Shadow switch**：**未在产线生效**（仍为 **shadow-only** 写入）。
- **Feature flag 默认**：仍为 **关闭**；本轮仅在 shell 中临时 **`=1`**。

---

## 8. 风险 / 后续

- **`sampleSize = 1`**：**仅**证明 **flag + 队列 + batch-match** 链路可产生带 **`rrmDecisionShadow`** 的新行；**不**推断 **`sameAsBaselineRate`** 等在大样本下的表现。
- **后续 `M6.1-r7`**：**Shadow evaluation batch report / push prep**（更大样本或离线聚合）。
- **不进入** decision pilot。

---

## 9. 验收 checklist

- [x] 有 **waiting** 队列或失败原因已记录（本轮：**prep 脚本创建**）
- [x] **`batch-match`** 已执行
- [x] **`sampleSize ≥ 1`** 或失败原因已记录（本轮：**`N=1`**）
- [x] **`rrmDecisionShadow`** 写入已验证
- [x] 匿名指标已记录
- [x] 不写真实 ID
- [x] 不改 **`finalScore`**（无业务代码变更）
- [x] 不改 **`candidateUserId`**（无业务代码变更）
- [x] 不改 worker winner / ranking
- [x] 不改 RRM resolver
- [x] 不恢复 **`scoreShadow` v1**
- [x] 不触碰无关 **ai-pairwise** 文件

---

## 10. 下一步

**M6.1-r7 — Shadow Evaluation Batch Report / Push Prep**

---

## 11. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-04 | **M6.1-r6b**：**`prep`** 创建 **waiting** 队列；**`batch-match`** **`totalQueues=1`**、**`successCount=1`**；**`summarize`** 验证 **`rrmDecisionShadow`** 与 **r5** 核心指标（**`N=1`** smoke）。 |

---

## 12. 辅助脚本（可复现）

| 路径 | 用途 |
|------|------|
| **`packages/database/scripts/m6-r6b-shadow-smoke.mjs`** | **`prep`**：确保 **eligible active pool + waiting queue**；**`summarize`**：打印最新 **`matchInsights`** 匿名摘要（**stdout JSON**，**无完整 ID**）。 |

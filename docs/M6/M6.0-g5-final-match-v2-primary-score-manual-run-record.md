# M6.0-G5 — FinalMatchPage V2 primary score manual run record

> **范围**：在 **M6.0-H2**（**`VITE_FINAL_MATCH_USE_V2_PRIMARY_SCORE`**、`resolvePrimaryMatchScore`、`FinalMatchHero` 更新，commit **`517b51b`**）之后，对 **FinalMatchPage** 主视觉 **V2 / legacy / fallback** 行为做 **手动验收记录**。  
> **本轮**：**只写文档**；**不改** Web / API / worker / RRM / schema。  
> **本文不写** 真实 **`userId` / `candidateUserId` / token / `DATABASE_URL`**。

---

## 1. 一句话结论

**H2 代码路径**已与 **M6.0-H1** 方案对齐；**本档 v1.0** 提供 **可勾选的手测清单** 与 **验收环境登记**。**未在代理 CI 内执行真实浏览器会话**，**§3–§5 勾选框默认留空** — 执行方于本地 **`pnpm --filter @peima/web dev`** + **`.env` 调 flag** 完成后，请将 **§9** 升为 **v1.1** 并回填 **通过/失败** 与 **截图索引**（仍勿写入真实 id）。

---

## 2. 验收环境

| 项 | 填写 |
|----|------|
| **仓库 / commit** | **`517b51b`**（或其后含 **H2** 的 `main`） |
| **Web 命令** | `pnpm --filter @peima/web dev`（或等价） |
| **API** | 本地可访问 **`VITE_API_BASE_URL`** 指向的 Nest |
| **浏览器** | （Chrome / Edge / …） |
| **日期** | （手测当日） |

---

## 3. Flag off 验收

**前置**：**不设置** `VITE_FINAL_MATCH_USE_V2_PRIMARY_SCORE`，或设为 **非 `1`**（如 `0`、空）。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败） |
|---|--------|------|-------------------------|
| 3.1 | 主视觉大号分 | 仍为 **`formatScoreDisplay(finalScore)`**（legacy **v1** 口径） | □ |
| 3.2 | Hero 标题区 | 与 **H2 前**一致（「匹配指数」/ RRM 文案分支） | □ |
| 3.3 | 无 V2 主分说明脚注 | **不出现** H2 的 **「该分数基于双方 20 维…」** 整段（仅 flag on + 合法 V2 时出现） | □ |

---

## 4. Flag on + legal V2 验收

**前置**：**`VITE_FINAL_MATCH_USE_V2_PRIMARY_SCORE=1`**；选用 **`GET /matching/result`** 返回 **`relationshipProfileScoreV2.source === "match_insights_score_shadow_v2"`** 且 **`displayScore100`** 为 **0–100** 的 viewer（与 **J3B/J4** 后新 `MatchResult` 一致）。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败） |
|---|--------|------|-------------------------|
| 4.1 | 主视觉大号分 | **`displayScore100` + `/ 100`** | □ |
| 4.2 | 小标题 | **「关系画像适配度」** | □ |
| 4.3 | band 副文案 | 与 **`band`** 一致（**high / good / medium / low / strong_conflict** 中文映射） | □ |
| 4.4 | 说明句 | **「该分数基于双方 20 维关系画像、核心轴差异与冲突惩罚计算。」** | □ |
| 4.5 | v1 绿区（RPS shadow） | 仍显示；标题含 **「v1 shadow 对照，刻度与主视觉 V2 不同」** | □ |
| 4.6 | V2 实验折叠区脚注 | **灰度**文案（与 **H2** 实现一致） | □ |

---

## 5. Missing / invalid fallback 验收

### 5.1 `missing`（flag on，无合法 V2）

**前置**：旧 **`MatchResult`** 无 **`scoreShadowV2`**，或 API 返回 **`relationshipProfileScoreV2.source === "missing"`**。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败） |
|---|--------|------|-------------------------|
| 5.1.1 | 主视觉大号分 | **fallback** **`formatScoreDisplay(finalScore)`** | □ |
| 5.1.2 | 轻提示 | **「该结果生成较早，暂无 V2 关系画像分。」**（Hero 内） | □ |

### 5.2 `invalid`（flag on，V2 校验失败）

**前置**：需 **构造非法 payload**（仅本地 / mock；**勿提交**真实数据）。例如临时改 API 响应（**仅限本地调试分支**，**不得合入**）或 **浏览器 devtools 覆盖**（若团队允许）。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败 / ⏳ 待补） |
|---|--------|------|-----------------------------------|
| 5.2.1 | 主视觉大号分 | **fallback** **`formatScoreDisplay(finalScore)`** | ⏳ |
| 5.2.2 | 状态提示 | Hero 下 **「V2 shadow 数据异常，已使用旧版匹配指数。」** | ⏳ |

**说明**：**5.2** 默认标 **⏳ 待补**（需额外脏数据步骤）；**5.1** 可用 **历史无 V2 行** 较易完成。

---

## 6. 技术区保留项

以下在 **flag on/off** 下均应 **仍可见**（折叠/滚动位置可变，但 **不删除**）：

- **legacy `finalScore` v1**（**匹配分数构成** / **技术来源说明** 等既有路径）  
- **`scoreBreakdown`**（`previewPoolScore` / `preferenceScore` / `styleScore` / `profileScore`）  
- **「V2 关系画像适配度（实验）」** 折叠区  
- **RRM / `displaySourceType`** 相关只读说明  

| # | 检查项 | 结果（□ 通过 / □ 失败） |
|---|--------|-------------------------|
| 6.1 | 上述区域均可展开阅读 | □ |

---

## 7. 不变量

手测与文档编辑 **均不改变**：

- **API**、**worker**、**`MatchResult.finalScore`**、**`candidateUserId` / `displayCandidateUserId`**、**RRM**、**Prisma schema**。

---

## 8. 下一步

1. 完成 **§3–§6** 勾选 → **§9 → v1.1**。  
2. 可选：补 **Playwright / 截图归档**（仍不写真实 id）。  
3. **M6.0-J5** / **G4** 强冲突回归与 **low-effort（I）** 继续并行。

---

## 9. 修订记录

| 版本 | 说明 |
|------|------|
| v1.0 | **G5**：FinalMatchPage **V2 主分手测记录** 模板；**未在代理环境跑浏览器**；**5.2 invalid 默认待补**。 |
| v1.1 | *（待填）手测勾选 + 截图索引 + 5.2 结果。* |

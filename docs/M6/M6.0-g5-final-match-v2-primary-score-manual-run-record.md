# M6.0-G5 — FinalMatchPage V2 primary score manual run record

> **范围**：在 **M6.0-H2**（**`VITE_FINAL_MATCH_USE_V2_PRIMARY_SCORE`**、`resolvePrimaryMatchScore`、`FinalMatchHero` 更新，commit **`517b51b`**）之后，对 **FinalMatchPage** 主视觉 **V2 / legacy / fallback** 行为做 **手动验收记录**。**M6.0-H3** 后主流程 **不再** 展示 v1 关系画像绿卡片；**G5 v1.2+** 手测须按 **H3** 后结构更新勾选。  
> **本文不写** 真实 **`userId` / `candidateUserId` / token / `DATABASE_URL`**。

---

## 1. 一句话结论

**H2 代码路径**已与 **M6.0-H1** 方案对齐。**v1.1**：本地浏览器已 **通过** **§3 flag off** 与 **§4 flag on + legal V2** 主视觉手测（当时主流程仍有 **v1 shadow 绿卡片**，**H3 已移除**）。**v1.2（待手测）**：按 **H3** 后页面重跑 **§3–§6**；**§5 missing / invalid** 仍 **待补**。本文仍 **不写** 真实 id。

---

## 2. 验收环境

| 项 | 填写 |
|----|------|
| **仓库 / commit** | **`517b51b`**（或其后含 **H2** 的 `main`） |
| **Web 命令** | `pnpm --filter @peima/web dev`（或等价） |
| **API** | 本地可访问 **`VITE_API_BASE_URL`** 指向的 Nest |
| **浏览器** | （Chrome / Edge / …） |
| **日期** | （手测当日） |

---

## 3. Flag off 验收

**前置**：**不设置** `VITE_FINAL_MATCH_USE_V2_PRIMARY_SCORE`，或设为 **非 `1`**（如 `0`、空）。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败） |
|---|--------|------|-------------------------|
| 3.1 | 主视觉大号分 | 仍为 **`formatScoreDisplay(finalScore)`**（legacy **v1** 口径） | **通过** |
| 3.2 | Hero 标题区 | 与 **H2 前**一致（「匹配指数」/ RRM 文案分支） | **通过** |
| 3.3 | 无 V2 主分说明脚注 | **不出现** H2 的 **「该分数基于双方 20 维…」** 整段（仅 flag on + 合法 V2 时出现） | **通过** |

**截图与结论（v1.1 / H3 前）**：本地浏览器截图确认 **flag off** 时主视觉仍为 **legacy `finalScore` v1**（示例约 **89.3**），主视觉 **未** 出现 V2 说明句。**H3 前** 主流程曾有 **v1 shadow 绿卡片**。**H3 后**：该项 **不再** 出现；**G5 v1.2** 应确认主流程 **无** 该卡片。**结论（v1.1）**：flag off 主视觉行为通过。

---

## 4. Flag on + legal V2 验收

**前置**：已设置 **`VITE_FINAL_MATCH_USE_V2_PRIMARY_SCORE=1`** 并 **重启 web**；选用 **`GET /matching/result`** 返回 **`relationshipProfileScoreV2.source === "match_insights_score_shadow_v2"`** 且 **`displayScore100`** 为 **0–100** 的 viewer（与 **J3B/J4** 后新 `MatchResult` 一致）。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败） |
|---|--------|------|-------------------------|
| 4.1 | 主视觉大号分 | **`displayScore100` + `/ 100`** | **通过** |
| 4.2 | 小标题 | **「关系画像适配度」** | **通过** |
| 4.3 | band 副文案 | 与 **`band`** 一致（**high / good / medium / low / strong_conflict** 中文映射） | **通过** |
| 4.4 | 说明句 | **「该分数基于双方 20 维关系画像、核心轴差异与冲突惩罚计算。」** | **通过** |
| 4.5 | ~~v1 绿区（RPS shadow）~~ **H3 后已不适用** | **H3 后** 主流程 **不应** 再显示 v1 关系画像绿卡片；v1 读数仅在 **技术来源说明** 内折叠审计（可选手测 **§6**） | ⏳ **v1.2 待按 H3 重测** |
| 4.6 | V2 实验折叠区脚注 | **灰度**文案；**H3** 后与主流程无 v1 卡片 **语义一致** | ⏳ **v1.2 待按 H3 重测** |

**截图与结论（v1.1 / H3 前）**：主视觉 **76 / 100**、**「较好适配」**、说明句等与 **H2** 一致；当时 **v1 shadow 绿卡片** 仍显示。**H3 后**：**§4.1–4.4** 仍应成立；**4.5 / 4.6** 须 **v1.2** 重勾选。**结论（v1.1）**：flag on + legal V2 主视觉通过。

---

## 5. Missing / invalid fallback 验收

### 5.1 `missing`（flag on，无合法 V2）

**前置**：旧 **`MatchResult`** 无 **`scoreShadowV2`**，或 API 返回 **`relationshipProfileScoreV2.source === "missing"`**。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败 / ⏳ 待补） |
|---|--------|------|-----------------------------------|
| 5.1.1 | 主视觉大号分 | **fallback** **`formatScoreDisplay(finalScore)`** | ⏳ **待补** |
| 5.1.2 | 轻提示 | **「该结果生成较早，暂无 V2 关系画像分。」**（Hero 内） | ⏳ **待补** |

**本轮状态**：**未** 找到 / 打开 **旧无 V2** 的 `MatchResult` 页面；**不声称已验证**。**missing fallback：待补。**

### 5.2 `invalid`（flag on，V2 校验失败）

**前置**：需 **构造非法 payload**（仅本地 / mock；**勿提交**真实数据）。例如临时改 API 响应（**仅限本地调试分支**，**不得合入**）或 **浏览器 devtools 覆盖**（若团队允许）。

| # | 检查项 | 预期 | 结果（□ 通过 / □ 失败 / ⏳ 待补） |
|---|--------|------|-----------------------------------|
| 5.2.1 | 主视觉大号分 | **fallback** **`formatScoreDisplay(finalScore)`** | ⏳ **待补** |
| 5.2.2 | 状态提示 | Hero 下 **「V2 shadow 数据异常，已使用旧版匹配指数。」** | ⏳ **待补** |

**本轮状态**：需 **构造非法 payload 或 mock**；**不声称已验证**。**invalid fallback：待补。**

**说明**：**5.1 / 5.2** 均 **待下一轮** 手测或本地调试补全。

---

## 6. 技术区保留项

以下在 **flag on/off** 下均应 **仍可见**（折叠/滚动位置可变，但 **不删除**）：

- **legacy `finalScore` v1**（**匹配分数构成** / **技术来源说明** 等既有路径）  
- **`scoreBreakdown`**（`previewPoolScore` / `preferenceScore` / `styleScore` / `profileScore`）  
- **「V2 关系画像适配度（实验）」** 折叠区  
- **RRM / `displaySourceType`** 相关只读说明  
- **（H3 起）** **「技术来源说明」** 内可选折叠：**旧版 `relationshipProfileScore` v1（仅技术审计）** — **不** 作为主流程分数卡片  

| # | 检查项 | 结果（□ 通过 / □ 失败） |
|---|--------|-------------------------|
| 6.1 | 上述区域均可展开阅读 | **部分确认（v1.1）**；**v1.2** 须含 **H3** 后 **技术区内 v1 审计折叠** 与 **无主流程 v1 卡片** |

**v1.1 说明**：曾确认主流程 **v1 shadow 绿卡片** 存在。**H3 后**：**不再要求** 用户可见 **v1 shadow 对照卡片**；**须** 保留 **legacy `finalScore` / `scoreBreakdown` / V2 实验 / RRM**。**完整技术折叠区**（含 **H3** 审计项）— **v1.2** 建议补 **全量截图**。

---

## 7. 不变量

手测与文档编辑 **均不改变**：

- **API**、**worker**、**`MatchResult.finalScore`**、**`candidateUserId` / `displayCandidateUserId`**、**RRM**、**Prisma schema**。

---

## 8. 下一步

1. **G5 v1.2**：按 **H3** 后页面补 **§3–§4 / §6** 勾选；补 **§5 missing / invalid**。  
2. 可选：补 **Playwright / 截图归档**（仍不写真实 id）。  
3. **low-effort（I）** 等与 **`M6.0-k1`** 路线并行。

---

## 9. 修订记录

| 版本 | 说明 |
|------|------|
| v1.0 | **G5**：FinalMatchPage **V2 主分手测记录** 模板；**未在代理环境跑浏览器**；**5.2 invalid 默认待补**。 |
| v1.1 | 本地浏览器完成 **flag off** 与 **flag on + legal V2** 主视觉手测；**missing / invalid fallback** 与 **完整技术折叠区** 继续 **待补**。 |
| v1.2 | **H3** 后主流程 **移除 v1 关系画像绿卡片**；**§4.5/4.6、§6** 改为 **v1.2 手测待补**；技术区保留 **legacy / breakdown / V2 实验 / RRM** + **可选 v1 审计折叠**。 |

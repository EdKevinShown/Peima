# M6.7-C4 — Observation API Regression and Smoke

## 1. 本轮目标

- C3 已新增只读 admin observation endpoint：`GET /admin/rrm-observation/summary`。
- C4 仅做回归验证与手动 smoke 记录。
- 本轮不新增功能，不做 admin UI，不接 true bounded decision flag。

## 2. 修改文件

- `docs/M6/M6.7-C4-observation-api-regression-and-smoke.md`（本轮仅文档）

## 3. 自动化测试结果

执行目录：`apps/api`

### 3.1 C3 endpoint 专项回归

```bash
pnpm exec jest test/rrm-observation-summary.service.spec.ts test/admin-rrm-observation-summary.controller.spec.ts --no-cache
```

结果：

- 2 suites passed
- 6 tests passed

### 3.2 API 类型检查

```bash
pnpm exec tsc --noEmit
```

结果：

- 通过（exit 0）

### 3.3 M6 核心 API 回归

```bash
pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts test/match-review-ai.service.spec.ts --no-cache
```

结果：

- 5 suites passed
- 56 tests passed

## 4. Manual smoke 结果

本轮未执行手动 smoke。

原因：

- 本轮执行上下文未提供可用 admin token 与可确认的 API 运行会话，因此未对 `GET /admin/rrm-observation/summary?limit=50` 做实际在线调用记录。

后续可执行方式：

- 启动 API 服务并使用 admin 账号 token；
- 调用 `GET /admin/rrm-observation/summary?limit=50`；
- 仅记录匿名字段（`sampleSize`、`coverage.*`、`quality.*`），不记录 token、真实 ID、raw payload。

## 5. Privacy / safety confirmation

- 不输出真实 ID（`id` / `userId` / `candidateUserId` / `conversationId`）。
- 不输出 raw `matchInsights`。
- 不输出 raw `selectedTop2` IDs。
- endpoint 保持 admin/debug only（JWT + admin gate）。
- `limit` capped（默认 50，最大 200）。
- 错误为安全错误，不回传底层 DB 细节。

## 6. 不变性确认

- 不改 `MatchResult.candidateUserId`。
- 不改 `finalScore`。
- 不改 worker winner / ranking。
- 不改 batch-match 主决策。
- 不改 RRM resolver。
- 不接 true bounded decision flag。
- 不恢复 / 不依赖 scoreShadow v1。
- 不改 Prisma schema / migration。

## 7. 下一步

M6.7-C5：Admin Debug Card Plan or Final M6.7 Push Prep

建议：

- 若不做 admin UI，C5 直接做 final regression / push prep。
- 若做 admin UI，C5 先做最小 admin debug card 方案并严格保持只读。

## 8. 修订记录

- 2026-05-05：完成 C3 endpoint 自动化回归（专项 + M6 核心 + tsc）；manual smoke 未执行并记录原因与后续执行方式。

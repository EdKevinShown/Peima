# M6.0-J3B — scoreShadowV2 local regression run record

## 1. 一句话结论

Worker 在 **J3（`dc1b2a2`）之后** 的写库路径会在 `matchInsights` 中同时写入 **`scoreShadow`（v1）** 与 **`scoreShadowV2`**；`finalScore`、winner、`candidateUserId`、RRM、API、`FinalMatchPage` 均不因 V2 改变。  
本次在连接 **Docker Postgres（`peima`）** 的回归档中：**`batch_match_queue` 无 `waiting` 条目**，`--batch-match` 跑出一批 **`successCount=0`**，**未新增 `MatchResult` 行**；库内 **按 `createdAt` 最新的一条历史 `MatchResult` 仅有 v1 shadow，尚无 `scoreShadowV2` 键**（符合「J3 代码部署后尚未对新匹配落库」或历史数据早于 V2 写入的预期）。  
**字段形态与不变量**已由 `apps/worker` 单测（`batch-match-match-insights.spec.ts`）覆盖；**库内出现 `scoreShadowV2` 的强证据**需在本地按 §4 模板对 **enqueue 后新生成的一行** 再查一次。

## 2. Build 结果

| 步骤 | 结果 |
|------|------|
| `pnpm --filter @peima/worker build` | **成功**（含 prebuild / `@peima/shared` / `tsc`） |
| `cd apps/api && npx tsc --noEmit` | **成功** |

执行环境记录档时间：**2026-05-04**（与当时终端日志一致）。

## 3. 本地匹配执行摘要

1. **Viewer / profile**：未在本档写入任何真实 `userId`；假定本地已有带 `user_profile` 的 viewer（与业务前置一致）。
2. **Queue**：对当前 Docker 库执行 `node dist/main.js --batch-match` 时，日志为 **`totalQueues=0`**，**未插入新 queue**，也**未通过 API enqueue**（本档仅 CLI 探针）。
3. **Worker**：`DATABASE_URL` 使用与 `.env.example` 一致的 **本地模板串**（`postgresql://user:password@localhost:5432/peima?schema=public`），**不含生产密钥**。
4. **Batch 结果**：`successCount=0`，**未产生新的 `MatchResult`**，故 §4 无法对「本次新生成行」做字段级断言。

**推荐本地补跑（仍勿把真实 id 写入 git 文档）**：

```bash
cd apps/worker
pnpm --filter @peima/worker build
# 使用你本机 .env（不要提交）
node --env-file=..\\..\\.env dist\\main.js --batch-match
```

期望在存在 `waiting` queue、active preview pool、候选均有 profile 时 **`successCount≥1`**。

## 4. DB 验证摘要

### 4.1 查询模板（占位符，勿提交真实 id）

```sql
SELECT "candidateUserId",
       "finalScore",
       "reasonSummary",
       "matchInsights"->'scoreShadow' AS score_shadow_v1,
       "matchInsights"->'scoreShadowV2' AS score_shadow_v2,
       "createdAt"
FROM match_results
WHERE "userId" = '<LOCAL_VIEWER_USER_ID>'
ORDER BY "createdAt" DESC
LIMIT 1;
```

### 4.2 本档实际执行的匿名抽样（不含 id）

对 **`match_results` 全表** 与 **最新一行** 的探针（仅聚合/最新行，**查询文本未包含真实 userId**）：

| 检查项 | 结果 |
|--------|------|
| 表内总行数 | `34`（环境快照） |
| **最新一行** `scoreShadow` → `scoringVersion` | `m6.0-profile-score-shadow-v1` **存在** |
| **最新一行** `scoreShadowV2` 键 | **`NULL` / 不存在**（历史行） |
| `batch_match_queue` | 本次 CLI 运行 **`totalQueues=0`** |

### 4.3 字段级检查清单（针对 **J3 之后新生成** 的一行）

在本地用 §4.1 替换占位符后，应对 **最新成功落库** 的行逐项确认：

**`scoreShadow` v1**

| 项 | 期望 |
|----|------|
| 存在 | 是 |
| `finalScoreV1` | 与该行 **`finalScore`** 一致（数值） |
| `relationshipProfileScore` | `[0,1]` 内有限数 |
| `scoringVersion` | `m6.0-profile-score-shadow-v1` |

**`scoreShadowV2`**

| 项 | 期望 |
|----|------|
| 存在 | 是 |
| `scoringVersion` | `m6.0-relationship-profile-score-v2-shadow` |
| `rawCompatibilityScore` | `[0,1]` 有限数 |
| `weightedBaseScore` | `[0,1]` 有限数 |
| `penaltyTotal` | `[0, 0.30]` |
| `cappedRawScore` | `[0,1]` 有限数 |
| `displayScore100` | `[0,100]` 整数/有限数 |
| `band` | `strong_conflict` \| `low` \| `medium` \| `good` \| `high` |
| `source` | `profile_v2_shadow` \| `insufficient_profile` |
| `coreConflictCount` / `strongConflictCount` / `redFlagConflictCount` / `validAxisCount` / `skippedAxisCount` | 非负整数 |

**本档未从 DB 读出 `displayScore100` / `band` 的样本值**（因未生成新行）。**同路径逻辑**在 `apps/worker/test/batch-match-match-insights.spec.ts` 中校验：全轴同值 profile 时 **`displayScore100` 处于高分区间、`band` 为 `high`**（无真实 id）。

## 5. 不变量

本轮 **仅新增本 Markdown**；**未改**：

- **`MatchResult.finalScore`**（仍为 legacy v1 合成）。
- **`reasonSummary`** 仍为 `Selected by finalScore v1 (...)` 句式（由 `formatReasonSummaryV1`）。
- **Worker winner / ranking**（`computeFinalScoreV1` + 排序未动）。
- **`candidateUserId`**（写库仍取 pool 最高分项）。
- **`displayCandidateUserId`**（batch-match 路径未触及）。
- **RRM**（resolver / meta / hook 未改）。
- **API / `FinalMatchPage`**（未接 `scoreShadowV2`）。
- **Prisma schema / migration**（无变更）。

## 6. 下一步

- **M6.0-J4**：API 返回 `scoreShadowV2` / relationshipProfileScoreV2 相关字段；前端技术区展示。
- **M6.0-G4**：V2 五态（band）与影子分回归（All-A / All-B / …）。

## 7. 修订记录

| 版本 | 说明 |
|------|------|
| v1.0 | scoreShadowV2 local regression run record：build + CLI batch-match + DB 匿名抽样；无真实 id。 |

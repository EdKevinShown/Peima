# M6.0-J3B — scoreShadowV2 local regression run record

## 1. 一句话结论

Worker 在 **J3（`dc1b2a2`）之后** 的写库路径会在 `matchInsights` 中同时写入 **`scoreShadow`（v1）** 与 **`scoreShadowV2`**；`finalScore`、winner、`candidateUserId`、RRM、API、`FinalMatchPage` 均不因 V2 改变。

**v1.1（当前强证据）**：已通过本地 **`waiting` queue + `batch-match`** 产生**新的 `MatchResult`**，并在 DB 中确认 **`matchInsights.scoreShadowV2` 真实落库**；与 **`scoreShadow` v1** 并存；**`finalScore` 仍为 legacy v1**，**`reasonSummary` 仍为 `Selected by finalScore v1 (...)` 句式**。

**v1.0（历史探针）**：曾记录仅 CLI 探针、**队列为空**时 **`successCount=0`**，以及当时**最新历史行**尚无 `scoreShadowV2` 的快照；该状态已被 v1.1 的补跑覆盖，不再代表「无法落库」。

## 2. Build 结果

| 步骤 | 结果 |
|------|------|
| `pnpm --filter @peima/worker build` | **成功**（含 prebuild / `@peima/shared` / `tsc`） |
| `cd apps/api && npx tsc --noEmit` | **成功** |

执行环境记录档时间：**2026-05-04**（v1.0 探针）；**v1.1** 为后续本地补跑（具体日期待执行方笔记，本档不写真实 id）。

## 3. 本地匹配执行摘要

### 3.1 v1.0 — empty queue 探针

1. **Viewer / profile**：未在本档写入任何真实 `userId`。
2. **Queue**：对 Docker 库执行 `node dist/main.js --batch-match` 时，日志为 **`totalQueues=0`**。
3. **Worker**：`DATABASE_URL` 使用与 `.env.example` 一致的本地模板串（**不含生产密钥**）。
4. **Batch 结果**：`successCount=0`，**未产生新的 `MatchResult`**。

### 3.2 v1.1 — waiting queue + 新 MatchResult

1. 本地已存在带 **`user_profile`** 的 viewer，并具备 **active preview pool**、候选 **`UserProfile`** 完整等前置。
2. 通过 **API enqueue 或等价方式** 插入 **`batch_match_queue` = `waiting`** 的一条队列。
3. 执行 **`pnpm --filter @peima/worker build`** 后，使用本机 `.env` 运行：  
   `node --env-file=../../.env dist/main.js --batch-match`（自 `apps/worker` 指向仓库根 `.env`，按本机调整）。
4. **`successCount=1`**（或 ≥1），并确认 **新 `MatchResult`** 已写入。
5. 使用 §4.1 SQL（占位符替换为本地 viewer，**勿将真实 id 提交到 git**）核对 **最新一行** `matchInsights`。

## 4. DB 验证摘要

### 4.1 查询模板（占位符，勿提交真实 id）

```sql
SELECT "candidateUserId",
       "finalScore",
       "reasonSummary",
       "matchInsights"->'scoreShadow' AS score_shadow_v1,
       "matchInsights"->'scoreShadowV2' AS score_shadow_v2,
       "createdAt"
FROM match_results
WHERE "userId" = '<LOCAL_VIEWER_USER_ID>'
ORDER BY "createdAt" DESC
LIMIT 1;
```

### 4.2 v1.0 — 匿名探针快照（历史）

| 检查项 | 结果（当时环境） |
|--------|------------------|
| 表内总行数 | `34`（快照） |
| **当时最新一行** `scoreShadow` → `scoringVersion` | `m6.0-profile-score-shadow-v1` 存在 |
| **当时最新一行** `scoreShadowV2` | 不存在（历史行） |
| `batch_match_queue` | **`totalQueues=0`** |

### 4.3 v1.1 — 本轮 DB 最新 `MatchResult` 字段级摘要（**不含真实 id**）

对 **J3 之后新生成**、经 **`waiting` queue + batch-match`** 落库的**最新一行**（查询过程不写入本文件）：

**`scoreShadow` v1**

| 项 | 实测 |
|----|------|
| 是否存在 | **是** |
| `finalScoreV1` | **≈ `0.898`**，与该行 **`MatchResult.finalScore`**（legacy v1）**一致** |
| `relationshipProfileScore` | `[0,1]` 有限数（与 v1 profile 分量一致） |
| `scoringVersion` | `m6.0-profile-score-shadow-v1` |

**`scoreShadowV2`**

| 项 | 实测 |
|----|------|
| 是否存在 | **是** |
| `scoringVersion` | `m6.0-relationship-profile-score-v2-shadow` |
| `rawCompatibilityScore` | **≈ `0.880`** |
| `weightedBaseScore` | **≈ `0.880`** |
| `penaltyTotal` | **`0`** |
| `capApplied` | **`null`** |
| `cappedRawScore` | **≈ `0.880`** |
| `displayScore100` | **`87`** |
| `band` | **`good`** |
| `validAxisCount` | **`18`** |
| `skippedAxisCount` | **`2`** |
| `coreConflictCount` | **`0`** |
| `strongConflictCount` | **`0`** |
| `redFlagConflictCount` | **`0`** |
| `source` | **`profile_v2_shadow`** |

**`reasonSummary`**：仍为 **`Selected by finalScore v1 (...)`** 句式（未被 V2 改写）。

### 4.4 字段级检查清单（期望项，与 v1.1 实测对照）

与 §4.3 一致即视为通过：`scoreShadow` / `scoreShadowV2` 并存；v2 各数值区间、`band` 五档、`source` 二值、冲突计数非负整数等。

**单测补充**：`apps/worker/test/batch-match-match-insights.spec.ts` 仍覆盖合并路径（无真实 id）。

## 5. 不变量

**文档轮次**仅修改本 Markdown；**业务代码**在 J3 已合入前提下，本回归**确认**：

1. **J3 后新生成的 `MatchResult` 已真实写入 `matchInsights.scoreShadowV2`。**
2. **`scoreShadow` v1 与 `scoreShadowV2` 可以并存。**
3. **`MatchResult.finalScore` 仍为 legacy v1**，**未被 V2 替换**（v1 shadow 的 `finalScoreV1` 与之一致）。
4. **`reasonSummary` 仍是 finalScore v1 解释**。
5. **`candidateUserId` / `displayCandidateUserId` / RRM** 未被本轮 worker shadow 验证路径修改（batch-match 写库契约不变）。
6. **API / `FinalMatchPage` 仍未接 V2**；本轮仅为 **worker DB shadow** 验证。

## 6. 下一步

- **M6.0-J4**：API 返回 `scoreShadowV2` / relationshipProfileScoreV2 相关字段；前端技术区展示。
- **M6.0-G4**：V2 五态（band）与影子分回归（All-A / All-B / …）。

## 7. 修订记录

| 版本 | 说明 |
|------|------|
| v1.0 | scoreShadowV2 local regression run record：build + CLI batch-match + DB 匿名抽样；empty queue；无真实 id。 |
| v1.1 | 补充有 **waiting queue** 的本地 DB 实测；**新 `MatchResult` 已包含 `scoreShadowV2`**，`displayScore100=87`，`band=good`；仍不在正文写入真实 `userId` / `candidateUserId`。 |

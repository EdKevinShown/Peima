# M6.8-C2 — Read-layer Activation Regression and Smoke

## 1. 本轮目标

- C1 已接入 behind flag 的 read-layer activation（`GET /matching/result/:userId`）。
- C2 仅做回归与 smoke 记录。
- 本轮不新增功能。

## 2. 修改文件

- `docs/M6/M6.8-C2-read-layer-activation-regression-and-smoke.md`（本轮仅文档）

## 3. 自动化测试结果

### API（`apps/api`）

- `pnpm exec jest test/matching-result-display-candidate.spec.ts --no-cache`
  - 通过（1 suite，40 tests）
- `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts test/match-review-ai.service.spec.ts --no-cache`
  - 通过（5 suites，67 tests）
- `pnpm exec jest test/rrm-observation-summary.service.spec.ts test/admin-rrm-observation-summary.controller.spec.ts --no-cache`
  - 通过（2 suites，6 tests）
- `pnpm exec tsc --noEmit`
  - 通过

### Web（仓库根）

- `pnpm --filter @peima/web build`
  - 通过（Vite build；仅既有 chunk size warning）
- 本轮未发生 web 类型报错，无需修改 `apps/web/src/api/matching.ts`。

### Worker（`apps/worker`）

- `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache`
  - 通过（4 suites，52 tests）
- `pnpm exec tsc --noEmit`
  - 通过

## 4. Observation script smoke

执行：

```bash
node --env-file=.env packages/database/scripts/m6-r7-rrm-observation-snapshot.mjs --limit 50 --pretty
```

匿名摘要：

- `sampleSize = 50`
- `withRrmBoundedDecision = 1`
- `boundedDecision.would_use_baseline = 1`
- `boundedDecision.would_switch_to_rrm = 0`
- `boundedDecision.fallback_baseline = 0`
- `quality.malformedCount = 0`
- `quality.parseErrorCount = 0`
- `quality.unexpectedExceptionCount = 0`

结论：当前 DB 窗口内仍无 `would_switch_to_rrm` 样本；本次 smoke 仅验证 observation 可读，不代表真实 activation 样本覆盖。

## 5. API manual smoke

本轮未执行。

原因：

- 当前回归流程未提供可复用的 admin token 与可确认 API 会话上下文，未形成可复现的匿名手动调用记录。

后续执行方式（不阻塞 C2）：

- 在 flag off 与 flag on 两种配置下分别调用 `GET /matching/result/:userId`；
- 仅记录匿名技术字段与 source/fallback 行为；
- 不记录 token、真实 userId、raw response payload。

## 6. Activation coverage conclusion

- 单元测试已覆盖 valid activation（flag on + gates 全通过时切换 resolved target）。
- 本地 DB smoke 当前无 `would_switch_to_rrm` 样本，因此未形成真实数据驱动的 activation 在线样本记录。
- 不能据此声称已验证“真实线上切换样本”；但 flag off、fallback、安全行为已由测试覆盖。

## 7. 不变性确认

- 不改 `MatchResult.candidateUserId`
- 不改 `finalScore`
- 不改 worker winner / ranking
- 不改 batch-match 主决策
- 不改 RRM resolver
- 不改 Prisma schema / migration
- 不恢复 / 不依赖 scoreShadow v1
- 不输出 raw payload（`matchInsights` / `selectedTop2`）

## 8. 下一步

M6.8-C3：Flag-on controlled local sample or activation smoke fixture

建议：

- 如需真实 activation DB smoke，可引入受控本地 fixture / dev-only sample；
- 继续保持不直接生产启用；
- 文档仍只记录匿名结果，不写真实 ID。

## 9. 修订记录

- 2026-05-05：完成 M6.8-C2 回归与 smoke 记录；自动化回归全绿；observation script 可读；当前 DB 无 `would_switch_to_rrm` 样本。

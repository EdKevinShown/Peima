# M6.7-C5 — Final Regression and Push Prep

## 1. 本轮目标

- C1：已完成只读 observation script。
- C1b：已完成本地 DB 匿名 snapshot 运行。
- C2：已完成 observation API / admin surface 方案。
- C3：已完成只读 admin observation summary API。
- C4：已完成 API 回归与 smoke 记录。
- C5：做最终回归与 push 前收口检查。
- 本轮不新增功能，不做 admin UI，不接 true bounded decision flag。

## 2. 本地 commit 基线

以本地 `git log` 为准，M6.7 相关 commit：

- C1：`db90b14` — `feat(database): add M6.7-C1 RRM observation snapshot and documentation`
- C1b：`8b7cf32` — `docs(m6): add M6.7-C1b local DB observation snapshot run`
- C2：`aa41d44` — `docs(m6): add M6.7-C2 observation API/admin debug surface plan`
- C3：`fab3363` — `feat(api): add read-only admin RRM observation summary endpoint`
- C4：`25f9161` — `docs(m6): add M6.7-C4 observation API regression and smoke record`

## 3. M6.7 功能摘要

### C1 Script

- 只读查询最近 `MatchResult`。
- 输出匿名 summary。
- 不输出真实 ID。

### C1b Local DB run

- `limit=50` 时 `sampleSize=50`。
- `boundedDecision` 当前仅 1 条样本。
- `quality` 全 0（`malformed/parseError/unexpectedException`）。
- 当前窗口结果不作为上线结论。

### C2 Plan

- 给出 A/B/C 候选路径（script / API / admin UI）。
- 建议先落地只读 API summary。

### C3 API

- `GET /admin/rrm-observation/summary`
- JWT + admin gate（`AdminService.assertCanRunAiSimulationV1`）。
- `limit` 默认 50、最大 200。
- 返回匿名 response。

### C4 Regression

- C3 endpoint tests passed（2 suites / 6 tests）。
- API `tsc` passed。
- M6 core API tests passed（5 suites / 56 tests）。
- manual smoke 在 C4 文档中已记录未执行原因。

## 4. 测试结果

### API（`apps/api`）

- `pnpm exec jest test/rrm-observation-summary.service.spec.ts test/admin-rrm-observation-summary.controller.spec.ts --no-cache`
  - 通过（2 suites，6 tests）。
- `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts test/match-review-ai.service.spec.ts --no-cache`
  - 通过（5 suites，56 tests）。
- `pnpm exec tsc --noEmit`
  - 通过。

### Web（仓库根）

- `pnpm --filter @peima/web build`
  - 通过（Vite production build；仅 chunk size warning）。

### Worker（`apps/worker`）

- `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache`
  - 通过（4 suites，52 tests）。
- `pnpm exec tsc --noEmit`
  - 通过。

### Script smoke（仓库根）

- `node --env-file=.env packages/database/scripts/m6-r7-rrm-observation-snapshot.mjs --limit 20 --pretty`
  - 执行成功（exit 0）。
  - 匿名摘要：`sampleSize=20`，`withScoreShadowV2=20`，`withRrmV2Top2Selector=16`，`withRrmDecisionShadow=14`，`withRrmBoundedDecision=1`，`quality` 三项均为 `0`。

## 5. Privacy / safety confirmation

- 不输出真实 ID。
- 不输出 raw `matchInsights`。
- 不输出 raw `selectedTop2` IDs。
- endpoint 保持 admin/debug only。
- 无 user-level drilldown。
- `limit` 已 capped（默认 50，最大 200）。
- 错误行为为安全错误，不暴露底层连接细节。

## 6. 不变性确认

- `MatchResult.candidateUserId` 未改。
- `finalScore` 未改。
- worker winner / ranking 未改。
- batch-match 主决策未改。
- RRM resolver 未改。
- Prisma schema / migration 未改。
- README 未改。
- `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED` 未接线。
- scoreShadow v1 未恢复。
- true bounded decision 未生效。

## 7. 当前未处理事项

- Admin UI debug card 未做。
- manual API smoke 仍需在具备 admin token 与可确认 API 会话时补充。
- true bounded decision 未接线。
- `candidateUserId` write 策略未做。
- `finalScore` 策略未实现。
- `boundedDecision` 样本仍少。
- 当前窗口仍未观察到 `switch_to_top2_candidate` / `blocked` 样本。

## 8. Push 前 checklist

- [x] M6.7-C1 已 commit
- [x] M6.7-C1b 已 commit
- [x] M6.7-C2 已 commit
- [x] M6.7-C3 已 commit
- [x] M6.7-C4 已 commit
- [ ] M6.7-C5 已 commit
- [x] API tests passed
- [x] API tsc passed
- [x] Web build passed
- [x] Worker tests passed
- [x] Worker tsc passed
- [x] No schema / migration diff
- [x] No README diff
- [x] No unrelated ai-pairwise files staged
- [x] No candidateUserId / finalScore / worker / resolver changes
- [x] True bounded decision flag not wired
- [ ] Ready for user-controlled git push

## 9. 下一阶段建议

M6.8 — True bounded decision behind flag

目标：

- 接线 `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`。
- 先做 read-layer / resolver override。
- 优先只影响 `resolvedCandidateUserId` / target fields。
- 不直接写 `MatchResult.candidateUserId`。
- flag off 完全回 baseline。
- fallback / observation 机制必须保留。

## 10. 修订记录

- 2026-05-05：新增 C5 最终回归与 push 前检查文档，补充全链路测试结果与 script smoke 记录。

# M6.3-r5 — RRM Bounded Decision Dry-run Local DB Evaluation

## 1. 本轮目标

- **M6.3-r3** 已完成 **`matchInsights.rrmBoundedDecision`** **dry-run** 写入（**`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**）。
- **M6.3-r4** 已完成 **regression** 与 **`catch`** 路径加固。
- **M6.3-r5（本轮）**：在**本地 DB** 上通过既有 **`waiting` queue + worker `batch-match`** 做**写库链路 smoke**，确认 **`rrmBoundedDecision`** 能随 **`MatchResult`** 落库；**不**进入 **bounded decision implementation**；**不让** **`would_switch_to_rrm`** **正式生效**；**不**接线 **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**。
- **真实 `userId` / `matchResultId` / `conversationId`**：**未写入本文档**；验证依赖 **`m6-r6b-shadow-smoke.mjs`** 的**匿名**输出（**无**整段 CUID）。

---

## 2. 当前基线

| 项 | 值 |
|----|-----|
| **r1 commit** | `714681c` |
| **r2 commit** | `8c31597` |
| **r3 commit** | `d61f432` |
| **r4 commit** | `75aa72c` |
| **Dry-run flag** | **`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**（仅 **`"1"`**） |
| **Shadow flag** | **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED`**（仅 **`"1"`**） |
| **True bounded flag** | **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线** |
| **Payload 路径** | **`matchInsights.rrmBoundedDecision`** |
| **只读摘要脚本** | **`packages/database/scripts/m6-r6b-shadow-smoke.mjs`**（**`summarize`** 扩展 **`rrmBoundedDecision`**；新增 **`bounded-metrics`**，**只读**、**不写 DB**） |

---

## 3. 测试结果

| 层 | 命令 | 结果 |
|----|------|------|
| **Worker Jest** | `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache` | **4 suites，52 tests passed** |
| **Worker tsc** | `pnpm exec tsc --noEmit`（**`apps/worker`**） | **通过** |
| **API Jest** | `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache` | **4 suites，49 tests passed** |
| **API tsc** | `pnpm exec tsc --noEmit`（**`apps/api`**） | **通过** |
| **Web build** | `pnpm --filter @peima/web build`（**可选**） | **通过** |

---

## 4. DB 执行步骤

### 4.1 Queue prep

| 项 | 记录 |
|----|------|
| **命令** | `node --env-file=.env packages/database/scripts/m6-r6b-shadow-smoke.mjs prep` |
| **结果** | **`ok: true`**；**`action`**：**`queue_created`**（本轮新建 **`waiting`**）；**`poolItemCount`**：**`6`** |
| **失败路径** | 若 **`DATABASE_URL`** 缺失或 **`no_eligible_active_preview_pool`**，应记录 **`ok: false`** 与 **`reason`**（本轮**未**触发） |

### 4.2 Worker batch-match

| 项 | 记录 |
|----|------|
| **环境变量（PowerShell）** | **`$env:PEIMA_M6_RRM_DECISION_SHADOW_ENABLED="1"`**；**`$env:PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED="1"`** |
| **命令** | **`cd apps/worker`**；**`pnpm exec dotenv -e ../../.env -- pnpm run batch-match`** |
| **`totalQueues`** | **≥ 1**（本轮为 **1**） |
| **`successCount`** | **≥ 1**（本轮为 **1**） |
| **`failedCount`** | **0** |
| **真实 ID** | **未写入本文档**（日志中的 **CUID** 不入档） |

---

## 5. 匿名字段摘要

数据来源：**`m6-r6b-shadow-smoke.mjs summarize`**（按 **`createdAt`** 取最新一条 **`match_results`**；输出仅含 **`matchInsights`** 形状摘要及 **`finalScore` 是否为数值**）。

| 检查项 | 结果 |
|--------|------|
| **`matchInsights.scoreShadowV2`** | **存在** |
| **`matchInsights.scoreShadow` v1** | **未新写**（**`scoreShadowV1Present: false`**） |
| **`matchInsights.rrmDecisionShadow`** | **存在** |
| **`matchInsights.rrmBoundedDecision`** | **存在** |
| **`rrmBoundedDecision.schemaVersion`** | **`1`** |
| **`rrmBoundedDecision.sourceType`** | **`"rrm_bounded_decision"`** |
| **`rrmBoundedDecision.sourceVersion`** | **`"m6.3-rrm-bounded-decision-v1"`** |
| **`rrmBoundedDecision.mode`** | **`"dry_run"`** |
| **`rrmBoundedDecision.decision`** | **`would_use_baseline`**（本轮样本） |
| **`rrmBoundedDecision.wouldSwitch`** | **`false`** |
| **`rrmBoundedDecision.fallbackUsed`** | **`false`** |
| **`rrmBoundedDecision.fallbackReason`** | **`null`** |
| **`inputPresence.scoreShadowV2`** | **`true`** |
| **`inputPresence.rrmDecisionShadow`** | **`true`** |
| **`inputPresence.rrmV2Top2Selector`** | **`true`** |
| **`inputPresence.selectedTop2`** | **`true`** |
| **`inputPresence.scoreShadowV1LegacyPresent`** | **`false`** |
| **`finalScore` 字段存在** | **是**（**数值型**；**本轮不比对**具体分数，**不写入本文档**） |

---

## 6. 指标摘要

**口径：** **`bounded-metrics`** 对最近 **`--limit=30`** 条 **`MatchResult`** 扫描（**仅** **`matchInsights`**，**无 ID 输出**）。

| Metric | Value | Notes |
|--------|------:|------|
| **`sampleSize`** | **30** | 扫描窗口大小 |
| **`boundedDecisionCoverageRate`** | **≈ 0.033**（**1 / 30**） | 窗口内仅 **1** 条含 **`rrmBoundedDecision`**（与本轮 **smoke** 单次写入一致） |
| **`wouldUseBaselineRate`** | **1.0** | 分母 **`withBounded=1`** |
| **`wouldSwitchToRrmRate`** | **0** | |
| **`fallbackBaselineRate`** | **0** | |
| **`v1LegacyPresenceRate`** | **0** | **`inputPresence.scoreShadowV1LegacyPresent`** 在 **bounded** 行上为 **false** |
| **`fallbackReasonDistribution`** | **`null` → 1** | 与 **`fallbackReason: null`** 一致（聚合键为字符串 **`"null"`**） |

**说明：** **`bounded-metrics`** 对 **`withBounded < 5`** 或 **`sampleSize < 10`** 附 **`note: "smoke_only_not_quality_conclusion"`**。**本轮为 smoke，不是质量结论。**

---

## 7. 不变性确认

- **`finalScore`**、**`MatchResult.candidateUserId`**、**worker winner / ranking**、**`batch-match`** 主路径、**RRM resolver**：**未在本轮修改业务代码**。
- **`FinalMatchPage`**：**未改**。
- **Prisma schema / migration**：**未改**。
- **`scoreShadow` v1**：**未恢复**；**不参与**本轮 **pass/fail**。
- **`would_switch_to_rrm`**：**仅** **meta**；**`wouldSwitch`** 在样本上为 **`false`**，**不**改变正式候选。
- **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线**。
- **真实 ID**：**未写入本文档**。

---

## 8. 当前风险 / 后续

- **本轮**仅为 **local DB smoke**；**不代表** **decision pilot** 或产线策略结论。
- **历史行**大多**无** **`rrmBoundedDecision`**；窗口 **coverage** 会长期偏低，直至更多 **`batch-match`** 在 **dry-run flag** 下写入。
- **M6.3-r6**：可做 **bounded dry-run batch aggregation / report**（更大 **`--limit`**、多轮 **prep + batch**、与 **M6.2** 聚合器风格对齐）。
- **True bounded implementation** 仍需更大样本、**admin observation**、**explanation** 口径统一等。

---

## 9. 验收 checklist

- [x] **Worker tests passed**
- [x] **Worker tsc passed**
- [x] **API tests passed**
- [x] **API tsc passed**
- [x] **waiting queue 已准备**（**prep** **ok**）
- [x] **batch-match 已执行**（**`totalQueues≥1`**，**`successCount≥1`**，**`failedCount=0`**）
- [x] **`rrmBoundedDecision` 写入已验证**（**`summarize`**）
- [x] **不写真实 ID**
- [x] **不改** **`finalScore`**（业务代码）
- [x] **不改** **`candidateUserId`**
- [x] **不改** **worker winner / ranking**
- [x] **不改** **RRM resolver**
- [x] **不改** **schema / migration**
- [x] **不恢复** **`scoreShadow` v1**
- [x] **true bounded decision flag 未接线**
- [x] **不触碰**无关 **ai-pairwise** 文件

---

## 10. 下一步

**M6.3-r6 — Bounded Decision Dry-run Batch Aggregation / Push Prep**

- 提高 **`bounded-metrics`** 窗口或多次 **prep + batch** 累积 **`withBounded`**；
- 输出 **`fallbackReason`** 分布、**`would_switch_to_rrm`** 占比等，仍**仅** **audit meta**；
- **仍不**接线 **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**。

若本地 **eligible pool** 不稳定或 **`totalQueues=0`**，可执行 **M6.3-r5b — repeat dry-run local DB evaluation** 重试 **prep + batch**。

---

## 11. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-04 | **M6.3-r5**：本地 **DB smoke**；扩展 **`m6-r6b-shadow-smoke`** **`summarize` + `bounded-metrics`**；**自动化测试** 与 **Web build** 复跑记录。 |

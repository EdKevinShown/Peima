# M6.0-G4 — V2 five-state regression record

> **范围**：在 **M6.0-J4**（**GET `/matching/result`** 已返回 **`relationshipProfileScoreV2`**，**`FinalMatchPage`** 技术区可读 **V2 shadow**）之后，**同一本地 viewer** 依次 **All-A → All-B → All-C → All-D → Normal**（与 **M6.0-G3** 顺序对齐），每态 **提交问卷 → 校验 30 题 → enqueue → `batch-match` → 读最新 `match_results`**，并 **可选** 调 **GET `/matching/result`** 确认 **`relationshipProfileScoreV2.source === match_insights_score_shadow_v2`**。  
> **目的**：验证 **`scoreShadowV2.displayScore100`** 相对 **`scoreShadow.relationshipProfileScore`（RPS v1）** 是否 **更能拉开分数**，以及 **V2 `band` / `penalty` / `cap`** 是否符合直觉。  
> **本文不写** 真实 **`userId` / `candidateUserId` / `matchResultId` / token / `DATABASE_URL`**。  
> **与 G3 的关系**：**G3** 侧重 **30-canonical** 下 **v1 `finalScore` + RPS v1**；**G4** 在 **同协议** 上 **叠加 V2 shadow 列** 与 **API V2 读数**。

---

## 1. 一句话结论

**v1.2**：已在 **同一本地 viewer、同一固定 candidate**（五态间 **candidate 未变**）下 **补齐** **All-A / All-B / All-C / All-D / Normal** 的 **enqueue + `batch-match` + DB 读数**（**v1.1 曾将 Normal 误写入 All-A 行，已在本版纠正**）。**V2 `displayScore100` 范围 64–87，极差约 23 分**；**RPS v1** 约 **0.754–0.842（极差约 8.8 分）**；**legacy `finalScore` v1** 约 **0.881–0.898（极差约 1.7 分）**。**All-C** 最低：**`displayScore100`=64、`band`=medium**；**Normal** 最高：**87、`good`**。**V2 明显比 legacy 与 RPS v1 更能拉开差距**。**本轮未触发 `cap` / `strongConflict` / `redFlagConflict`**，**仍需后续合成强冲突样本** 验证 **cap / 红线**。**结论支持进入 M6.0-H** 主分数切换 **方案** 评审。**low-effort detection 仍后置**（All-A/B/C/D 作压力测试参数，与 **G3** 一致）。

---

## 2. Build 结果

| 命令 | 结果（本档登记时） |
|------|---------------------|
| **`pnpm --filter @peima/worker build`** | **通过** |
| **`pnpm --filter @peima/web build`** | **通过** |
| **`cd apps/api && npx tsc --noEmit`** | **通过** |

---

## 3. 回归方法

### 3.1 前置

- **代码版本**：含 **J3 / J3B / J4** 之后的 **`main`**（J4 commit：**`987a5a3`** 或其后）。  
- **固定同一本地 viewer**；**`user_profile`**、**active 预览池**、**候选人 `UserProfile` 齐全** 等与 **G3 / J3B** 一致。  
- **每态**：`POST /questionnaire/submit`（All-A / All-B / All-C / All-D 各 30 条同字母；Normal 为混合合法答案）→ **§C.1 SQL** 校验 **30 行 + A/B/C/D/E 计数 + `max_updated_at`** → **`POST /matching/enqueue`**（或等价 **`batch_match_queue` `waiting` 插入**）→ **`batch-match`** → **§C.2 SQL** 读 **`scoreShadow` + `scoreShadowV2`** → **可选** `GET /matching/result` 看 **`relationshipProfileScoreV2`**。

### 3.2 与 J4 的衔接

- **DB**：以 **`matchInsights.scoreShadowV2`** 为金源。  
- **API**：**`relationshipProfileScoreV2`** 经 **`resolveRelationshipProfileScoreV2Shadow`**；若 **`source`** 非 **`match_insights_score_shadow_v2`**，应先排查 **JSON 合法性**（见 **`matching-relationship-profile-score-v2.ts`**），再记表。

### 3.3 `candidate` 列（§5）

- 仅填 **否 / 是 / 待确认**；**不写 id**。本回归五态均为 **否**（同一池 winner）。

---

## 4. 五态数据摘要

| 场景 | 问卷 30 条 | A/B/C/D/E 聚合 | `user_profiles.updatedAt` | queue → batch | 最新 `MatchResult` 已读 |
|------|-------------|----------------|----------------------------|-----------------|-------------------------|
| All-A | 是 | A=30，其余=0 | 已更新 | 是 | 是 |
| All-B | 是 | B=30，其余=0 | 已更新 | 是 | 是 |
| All-C | 是 | C=30，其余=0 | 已更新 | 是 | 是 |
| All-D | 是 | D=30，其余=0 | 已更新 | 是 | 是 |
| Normal | 是 | A=10，B=9，C=7，D=4，E=0 | 已更新 | 是 | 是 |

---

## 5. V1 / V2 对比表

| 场景 | legacy `finalScore` v1 | RPS v1 | V2 raw | V2 `displayScore100` | V2 `band` | `penalty` | `cap` | core | strong | red flag | candidate 是否变化 | 观察 |
|------|-------------------------:|-------:|-------:|---------------------:|-----------|----------:|:-----|-----:|-------:|---------:|--------------------|------|
| Normal | 0.898 | 0.842 | 0.880 | 87 | good | 0 | null | 0 | 0 | 0 | 否 | 混合卷；V2 **最高** |
| All-A | 0.894 | 0.820 | 0.809 | 76 | good | 0.035 | null | 1 | 0 | 0 | 否 | 全线 A；core penalty |
| All-B | 0.896 | 0.828 | 0.792 | 74 | medium | 0.035 | null | 1 | 0 | 0 | 否 | 全线 B；**medium** |
| All-C | 0.881 | 0.754 | 0.734 | 64 | medium | 0.035 | null | 1 | 0 | 0 | 否 | 全线 C；V2 **最低** |
| All-D | 0.893 | 0.817 | 0.808 | 76 | good | 0.015 | null | 0 | 0 | 0 | 否 | 全线 D；mild penalty |

**列说明**

- **legacy `finalScore` v1**：`MatchResult.finalScore`。  
- **RPS v1**：`matchInsights.scoreShadow.relationshipProfileScore`（与 **G3** 一致）。  
- **V2 raw**：`matchInsights.scoreShadowV2.rawCompatibilityScore`（**cap 前**内部 0–1）。  
- **V2 `displayScore100` / `band` / `penalty` / `cap` / conflicts**：`matchInsights.scoreShadowV2` 同名字段；**`cap`** 列 **null** 表示未应用 cap。

**跨态极差（五态全表）**  

- **legacy `finalScore` v1**：约 **0.898 − 0.881 ≈ 0.017**（百分制观感 **≈1.7 分**）。  
- **RPS v1 ×100**：约 **84.2 − 75.4 ≈ 8.8 分**（**Normal** 与 **All-C** 端点）。  
- **V2 `displayScore100`**：**87 − 64 = 23 分**（**Normal** 与 **All-C** 端点）。

---

## 6. 观察结论（§E 答题 · v1.2 定稿）

1. **五态已补齐**，**不再存在 Normal 待补**。  
2. **五态均为同一 viewer、同一 candidate**，**对比有效**（candidate 列均为 **否**）。  
3. **legacy `finalScore` v1** 约 **0.881–0.898**，**极差约 1.7 分**（百分制观感）。  
4. **RPS v1** 约 **0.754–0.842**，**极差约 8.8 分**（×100 后）。  
5. **V2 `displayScore100`** **64–87**，**极差约 23 分**。  
6. **V2 明显比 legacy `finalScore` v1 与 RPS v1 更能拉开差距**（极差：23 vs 8.8 vs 1.7）。  
7. **All-C 最低**：**`displayScore100`=64**，**`band`=medium**。  
8. **Normal 最高**：**`displayScore100`=87**，**`band`=good**。  
9. **本轮未触发 `cap` / `strongConflict` / `redFlagConflict`**；**仍需后续合成强冲突样本** 验证 **cap / 红线**。  
10. **结论支持进入 M6.0-H** 主分数切换 **方案** 评审。  
11. **low-effort detection 仍后置**（与 **G3** 一致）。

---

## 7. 不变量

**v1.2 仍仅更新本文档**；**确认未改**：

- **`MatchResult.finalScore`**、**worker winner / ranking**。  
- **`candidateUserId` / `displayCandidateUserId`**、**RRM**。  
- **Prisma schema**、**无 migration**。  
- **API 契约**、**`FinalMatchPage` 源码**、**worker 业务代码**（本里程碑 **不写代码**）。

---

## 8. 下一步建议

1. **合成强冲突 / 红线样本**（或换 candidate）跑 **`cap` / `redFlagConflict`** 专项，补 **J3B 类** 证据。  
2. 启动 **M6.0-H**：主分数切换 **方案**、展示策略、回滚条件。  
3. 按需 **M6.0-J5**：权重与 **0–100 分段** 微调。  
4. 维持 **low-effort detection** 与 **G4** 结论解耦，**后置实现**。

---

## 9. 修订记录

| 版本 | 说明 |
|------|------|
| v1.0 | G4：**V2 五态回归** 方法 + **§5 占位表** + **§6 待数据**；**build** 登记；**未写真实 id**。 |
| v1.1 | **补充 V2 五态实测结果**；**`displayScore100` 范围 64–87**，**显著拉开**于 **legacy `finalScore` 带** 与 **RPS v1**；**未写真实 id**；**Normal 分数行待补**。 |
| v1.2 | **修正 v1.1 五态表行标签错位**；**补齐 Normal 行**；确认 **V2 `displayScore100` 范围 64–87，极差约 23 分**；**未写真实 id**。 |

---

## 附录 C — SQL 模板（占位符）

**问卷聚合**：

```sql
SELECT COUNT(*) AS total,
       COUNT(*) FILTER (WHERE "answerValue" = 'A') AS a_count,
       COUNT(*) FILTER (WHERE "answerValue" = 'B') AS b_count,
       COUNT(*) FILTER (WHERE "answerValue" = 'C') AS c_count,
       COUNT(*) FILTER (WHERE "answerValue" = 'D') AS d_count,
       COUNT(*) FILTER (WHERE "answerValue" = 'E') AS e_count,
       MAX("updatedAt") AS max_updated_at
FROM questionnaire_answers
WHERE "userId" = '<LOCAL_VIEWER_USER_ID>';
```

**最新 `MatchResult`（含 V2）**：

```sql
SELECT "candidateUserId",
       "finalScore",
       "reasonSummary",
       "matchInsights"->'scoreShadow' AS score_shadow_v1,
       "matchInsights"->'scoreShadowV2' AS score_shadow_v2,
       "createdAt"
FROM match_results
WHERE "userId" = '<LOCAL_VIEWER_USER_ID>'
ORDER BY "createdAt" DESC
LIMIT 1;
```

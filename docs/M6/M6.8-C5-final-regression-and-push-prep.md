# M6.8-C5 — Final Regression and Push Prep

## 1. 本轮目标

- r1 完成 activation contract。
- C1 完成 behind-flag read-layer activation。
- C2 完成 regression + 初次 smoke。
- C3 完成 controlled local `would_switch_to_rrm` fixture。
- C4 完成 flag on/off rollback prep。
- C5 仅做最终回归与 push 前检查。
- 本轮不新增功能。

## 2. 本地 commit 基线

以本地 `git log` 为准，M6.8 相关 commit：

- M6.8-r1：`f11d362` — `docs(m6): add M6.8-r1 true bounded decision read-layer activation contract`
- M6.8-C1：`b853fbe` — `feat(api): add M6.8-C1 bounded decision read-layer activation behind flag`
- M6.8-C2：`a79363e` — `docs(m6): add M6.8-C2 read-layer activation regression and smoke record`
- M6.8-C3：`37530db` — `feat(database): add M6.8-C3 controlled local activation fixture script and smoke doc`
- M6.8-C4：`18defc4` — `docs(m6): add M6.8-C4 flag on/off API smoke and rollback prep`

## 3. M6.8 功能摘要

### r1 Contract

- 定义 true bounded decision read-layer activation 合同。
- 明确第一阶段不写 `candidateUserId`。

### C1 Activation

- `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED="1"` 且 gates 全通过时，read-layer 切换 `resolvedCandidateUserId` 与 targets。
- flag off 或 gate 失败时保持旧 projection / fallback。

### C2 Regression

- 自动化回归全绿。
- 当时本地 DB 无 `would_switch_to_rrm` 样本。

### C3 Controlled fixture

- dev-only fixture 使 observation 中 `would_switch_to_rrm=1`。
- 仅修改本地 `matchInsights.rrmBoundedDecision`。
- 不改正式 winner/score。

### C4 Rollback prep

- 关闭 flag 即回旧 read-layer projection。
- 不需要 DB rollback。
- 手动 API smoke 未执行，原因已记录。

## 4. 测试结果

### API（`apps/api`）

- `pnpm exec jest test/matching-result-display-candidate.spec.ts --no-cache`
  - 通过（1 suite，40 tests）
- `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts test/match-review-ai.service.spec.ts --no-cache`
  - 通过（5 suites，67 tests）
- `pnpm exec jest test/rrm-observation-summary.service.spec.ts test/admin-rrm-observation-summary.controller.spec.ts --no-cache`
  - 通过（2 suites，6 tests）
- `pnpm exec tsc --noEmit`
  - 通过

### Web（仓库根）

- `pnpm --filter @peima/web build`
  - 通过（仅既有 chunk size warning）

### Worker（`apps/worker`）

- `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache`
  - 通过（4 suites，52 tests）
- `pnpm exec tsc --noEmit`
  - 通过

### Script（仓库根）

- `node --env-file=.env packages/database/scripts/m6-r7-rrm-observation-snapshot.mjs --limit 50 --pretty`
  - 执行成功（exit 0）

## 5. Observation / activation evidence

- 单元测试已覆盖 valid activation（flag on + gates pass）。
- C3/C4 后 observation 已出现 `boundedDecision.would_switch_to_rrm = 1`。
- 手动 API smoke 尚未执行（缺 token / 会话）。
- 因此不能声称“生产流量 activation 已验证”。
- read-layer behind-flag 接入与 fallback 安全行为已完成并可控。

## 6. Rollback plan

- 关闭 `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`。
- read-layer 立即回到 M6.5/M6.6 projection。
- 不需要 DB rollback（`candidateUserId` / `finalScore` 未变）。
- observation API 继续可用。

## 7. Privacy / safety confirmation

- 不输出真实 ID。
- 不输出 raw `matchInsights`。
- 不输出 raw `selectedTop2` IDs。
- 不做 user-level drilldown。
- admin observation endpoint 保持 admin/debug only。

## 8. 不变性确认

- `MatchResult.candidateUserId` 未改。
- `finalScore` 未改。
- worker winner / ranking 未改。
- batch-match 主决策未改。
- RRM resolver 未改。
- Prisma schema / migration 未改。
- README 未改。
- scoreShadow v1 未恢复。
- true bounded decision 仅 read-layer behind flag 生效。
- 未直接写 DB winner。

## 9. 当前未处理事项

- manual API smoke 仍需 token / user 会话后补。
- Admin UI card 未做。
- `finalScore` recompute 策略未做。
- `candidateUserId` write 未做。
- 生产样本分布仍需 M6.9 继续观察。
- C3 fixture 为 dev-only，不代表生产数据分布。

## 10. Push 前 checklist

- [x] M6.8-r1 已 commit
- [x] M6.8-C1 已 commit
- [x] M6.8-C2 已 commit
- [x] M6.8-C3 已 commit
- [x] M6.8-C4 已 commit
- [ ] M6.8-C5 已 commit
- [x] API tests passed
- [x] API tsc passed
- [x] Web build passed
- [x] Worker tests passed
- [x] Worker tsc passed
- [x] Observation script smoke passed
- [x] No schema / migration diff
- [x] No README diff
- [x] No unrelated ai-pairwise files staged
- [x] No candidateUserId / finalScore / worker / resolver changes
- [ ] Ready for user-controlled git push

## 11. 下一阶段建议

M6.9 — Regression + Rollback

目标：

- 对 M6.8 behind-flag activation 做最终回归；
- 补手动 API smoke 或受控环境 smoke；
- 完善 rollback / flag off 记录；
- 在可观测与回退充分的前提下收口。

## 12. 修订记录

- 2026-05-05：完成 M6.8-C5 最终回归与 push 前收口记录。

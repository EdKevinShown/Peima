# M6.4-r3 — RRM Pre-decision Chain Final Summary and Push Prep

## 1. 本轮目标

- **M6.0–M6.4-r2** 已完成 **RRM 正式决策接入前置链路**（只读展示、**shadow**、批量评估、**bounded dry-run meta**、**true bounded** 门控与写入策略合同）。
- **M6.4-r3（本文）**：对该前置链做**阶段性总结**与 **push 前检查**记录；**不**进入 **true bounded decision implementation**；**不**新增功能代码。
- **本轮**仅 **新增本文档**；**不**接线 **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**。

---

## 2. 阶段总览

### M6.0

- **RRM V2 readonly display**：**`resolveMatchResultDisplay`** 在 flag 下解析 **`matchInsights`**，返回 **`displayCandidateUserId` / `displaySourceType`**（含 **`rrm_top2_v2_selector_readonly`**）。
- **Display resolver behind flag**：**`PEIMA_M6_RRM_V2_SELECTOR_DISPLAY_ENABLED`**（仅 **`"1"`**）。
- **FinalMatchPage source 展示**：消费 API 投影字段，**不**自算 RRM、**不**解析 raw **`matchInsights`**（**M6.0-r8**）。
- **`scoreShadow` v1 runtime removal**：**R10** 停写 / 读路径迁移；**R9** 审计（见 **M6.0-r9 / r10 / r11**）。

### M6.1

- **`matchInsights.rrmDecisionShadow`**：**worker** 在 **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED`** 下写入。
- **Shadow mode**：**观测侧车**；**不改** **`candidateUserId` / finalScore`**。
- **DB smoke**：**r6b** 验证落库与指标口径（**M6.1-r6b / r7**）。
- **不改正式结果**：主匹配链路与 **M6.0** 一致。

### M6.2

- **Read-only aggregator**：**`packages/database/scripts/m6-r2-shadow-evaluation-summary.mjs`**。
- **Local batch evaluation**：**queue prep + batch-match** → 聚合。
- **报告指标（r4 窗口示例）**：**`shadowCount=13`**，**`validShadowRate=1.0`**（见 **M6.2-r4**）。
- **样本同质**：**不支持**直接 **decision pilot**（**M6.2-r4** 结论）。

### M6.3

- **`matchInsights.rrmBoundedDecision`**：**dry-run**（**`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**）。
- **Bounded dry-run regression**：**r4** 矩阵与 **`catch`** 安全路径。
- **DB smoke**：**r5** 本地队列 + **batch-match** 记录（**M6.3-r5**）。
- **`wouldSwitch` 不生效**：**不**改 **`candidateUserId` / finalScore`**（**M6.3-r6**）。

### M6.4

- **M6.4-r1**：**true bounded decision** **gating design**（四层门、**`wouldSwitch` vs `actualSwitch`**、**rollback** 等）。
- **M6.4-r2**：**write strategy contract**（**Strategy A/B/C**、推荐 **Strategy B-lite**）。
- **不直接 `candidateUserId` write**：**Strategy C** 推迟；**true flag** **未接线**。

---

## 3. 当前功能状态

| 能力 | 状态 |
|------|------|
| **RRM read-only displayed** | **已支持**（**API resolver** + **FinalMatchPage** 等，受 **M6** flags 约束）。 |
| **RRM shadow decision** | **已支持**（**`rrmDecisionShadow`** 写入，**flag** 控制）。 |
| **RRM batch evaluated** | **已支持**（**M6.2** 聚合脚本 + 本地 batch 流程）。 |
| **RRM bounded dry-run meta** | **已支持**（**`rrmBoundedDecision`**，**dry-run flag**）。 |
| **RRM true bounded decision** | **未启用**（无 **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`** 接线实现）。 |
| **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`** | **未接线**。 |
| **`MatchResult.candidateUserId`** | **未**被 **RRM bounded / true** 路径改写。 |
| **`finalScore`** | **未**被上述路径改写。 |
| **Worker winner / ranking** | **未改**。 |
| **RRM resolver（主决策链）** | **未**扩大池或改写 **batch-match** 主 winner 语义。 |

---

## 4. 本地 commit 摘要

以下摘自本仓库 **`git log --oneline`**（**`docs/M6`** 与相关 **feat/test**）；**哈希以用户本地为准**。若与 **remote** 有差异，以 **`git log`** 为准。

### M6.0（r6–r11 关键节点）

| Hash | 说明 |
|------|------|
| **7215796** | **R4/R5** — readonly display preflight + meta contract（docs） |
| **892b8a8** | **R6** — M6 RRM V2 readonly display resolver behind flag（含 **r6** resolver 文档同提交） |
| **6840b37** | **R7** — selector readonly display regression（docs/test） |
| **69d69c7** | **R8** — FinalMatchPage **displaySourceType** surface（含 **r8** 文档同提交） |
| **353b2f9** | **R9** — scoreShadow v1 dependency audit（docs） |
| **22fadef** | **R10** — stop v1 write；relationshipProfileScore from v2 |
| **c505bca** | **R11** — final local regression and push prep（docs） |

### M6.1（r1–r7）

| Hash | 说明 |
|------|------|
| **00bc535** | **r1** — RRM decision shadow mode design |
| **884fc8d** | **r2** — RRM decision shadow payload contract |
| **f4b4bb9** | **r3** — worker 写入 **`rrmDecisionShadow`** |
| **de93e33** | **r4** — decision shadow regression matrix |
| **ddf640b** | **r5** — decision shadow evaluation summary |
| **f93b025** | **r6** — local DB shadow evaluation run |
| **8624d1f** | **r6b** — local DB shadow rerun（waiting queue） |
| **fd54a85** | **r7** — shadow evaluation batch report and push prep |

### M6.2（r1–r5）

| Hash | 说明 |
|------|------|
| **3acbdb9** | **r1** — batch evaluation plan |
| **de53123** | **r2** — read-only shadow evaluation aggregator script |
| **73cdc9a** | **r3** — local shadow batch evaluation run |
| **e4e9d66** | **r4** — shadow batch evaluation report |
| **416ff3a** | **r5** — final regression and push prep |

### M6.3（r1–r6）

| Hash | 说明 |
|------|------|
| **714681c** | **r1** — RRM bounded decision contract |
| **8c31597** | **r2** — bounded decision write strategy |
| **d61f432** | **r3** — **`rrmBoundedDecision`** dry-run meta（worker） |
| **75aa72c** | **r4** — bounded dry-run regression |
| **725df26** | **r5** — bounded dry-run local DB evaluation |
| **9998aaa** | **r6** — final regression and push prep |

### M6.4（r1–r2）

| Hash | 说明 |
|------|------|
| **5cdbb82** | **r1** — true bounded decision gating design |
| **37c83e4** | **r2** — true bounded decision write strategy contract |

---

## 5. 验证结果摘要

### 5.1 引用前置文档（聚合器 / bounded-metrics）

- **Shadow aggregator**：**`m6-r2-shadow-evaluation-summary.mjs`** 与 **M6.2-r3/r4** 跑通记录见 **M6.2-r4 / M6.2-r5**。
- **`bounded-metrics`**（**`m6-r6b-shadow-smoke.mjs`**）：**M6.3-r5 / M6.3-r6** 已记录 **`ok: true`** 与覆盖率摘要；**本轮不强制复跑**（依赖本地 **`.env`** 与 DB）。

### 5.2 本轮复跑的最小检查（**M6.4-r3** 记录时执行）

**环境：** **Windows**，仓库根 **`peima`**；**未**修改业务代码。

| 区域 | 命令 | 结果 |
|------|------|------|
| **Worker Jest** | `cd apps/worker` → `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache` | **4 suites，52 tests passed** |
| **Worker tsc** | `cd apps/worker` → `pnpm exec tsc --noEmit` | **exit 0** |
| **API Jest** | `cd apps/api` → `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache` | **4 suites，49 tests passed** |
| **API tsc** | `cd apps/api` → `pnpm exec tsc --noEmit` | **exit 0** |
| **Web build** | 仓库根 → `pnpm --filter @peima/web build` | **通过**（**Vite**；chunk 体积告警为既有提示） |

**与 M6.3-r6 对齐：** **M6.3-r6** 中 **Worker / API / Web** 的同类命令当时均已通过；**本轮**在 **docs-only** 前提下复跑上述子集，结果仍 **green**。

---

## 6. 不变性确认

- **`finalScore`**：**未**由 **M6.3/M6.4** 前置链改写。
- **`MatchResult.candidateUserId`**：**未**被 **bounded / true** 改写。
- **Worker winner / ranking**：**未改**。
- **`batch-match` 主决策**：**未改**。
- **RRM resolver（主路径）**：**未**为 **true bounded** 扩大候选池或改写正式 winner。
- **`FinalMatchPage`**：**M6.4-r3** **未**改代码。
- **Prisma schema / migration**：**本轮**无 diff。
- **根 README**：**本轮**无 diff。
- **`scoreShadow` v1**：**未恢复**为 runtime 依赖。
- **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线**。
- **`would_switch` / `wouldSwitch`**：**未**作为产线正式切换生效。
- **真实 ID**：**未**写入本文档。

---

## 7. 当前未处理事项

- **True bounded decision implementation**：**未做**（仅 **r1/r2** 设计合同）。
- **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线**。
- **`candidateUserId` 写入（Strategy C）**：**未做**；**不建议**跳过 **r5 readiness** 与 **ADR**。
- **`finalScore` strategy（true / switch 后）**：**未**定案实现（见 **M6.4-r2 §10**）。
- **Explanation unification**：**未**全链路实现（**M6.4-r1 §11**）。
- **Admin observation**：**未**实现。
- **`switch` / `blocked` 真实样本**：仍**不足**（相对 **M6.2-r4** 同质警示）。
- **样本多样性**：仍**不足**以支撑 **pilot**。
- **无关 ai-pairwise / m42 文件**：仍可能留在工作区，**未**纳入 **M6** commits（见 **§8**）。

---

## 8. Push 前 checklist

- [ ] **M6.4-r2** 已 **commit**（消息含 **`M6.4-r2`** / **`true bounded decision write strategy`**）
- [ ] **M6.4-r3（本文）** 已 **commit**
- [ ] **No** Prisma **schema / migration** diff（工作区）
- [ ] **No** 根 **README** diff
- [ ] **No** 无关 **ai-pairwise** / **m42** 文件被 **stage**
- [ ] **No** **`scoreShadow` v1** restoration
- [ ] **No** **`finalScore` / `candidateUserId` / winner / resolver** 业务改动混入 **M6.4-r3** commit
- [ ] **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED` not wired**
- [ ] **Ready for user-controlled `git push`**

---

## 9. 下一阶段建议

下一阶段建议**单独**开里程碑：

**M6.5 — True Bounded Decision Pilot Design**

**M6.5-r1（或前置 r 文档）仍应先完成：**

- **Explanation unification plan**（字段矩阵 + **FinalMatchPage** + **chat / timeline**）。
- **Admin observation plan**。
- **`finalScore` strategy** 合同（**read-layer** vs **`candidateUserId` write**）。
- **Rollback / kill-switch** 运维路径（与 **M6.4-r1** 一致，细化到 runbook）。
- **Read-layer override preflight**（resolver 读 **meta**、版本校验、与 **M6.0** 优先级）。

**不要**在未完成上述项时直接做 **`candidateUserId` write**。

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-04 | **M6.4-r3**：**RRM pre-decision chain** 总结与 **push prep**；**docs-only**；复跑 **Worker/API Jest + tsc**、**Web build**。 |

# M6.10-C4 — Final Regression and Push Prep

## 1. 本轮目标

- M6.10-r1 已完成 candidate-level score snapshot audit。
- M6.10-C1 已完成 `top2ScoreSnapshot` 写入。
- M6.10-C2 已完成 `resolvedFinalScore` API projection。
- M6.10-C3 已完成 FinalMatchPage 优先消费 `resolvedFinalScore`。
- M6.10-C4 仅做最终回归与 push 前检查，不新增功能。

## 2. 本地 commit 基线

基于本地 `git log --oneline -40`：

- M6.10-r1：`57c28bd` — `docs(m6): add M6.10-r1 candidate-level score snapshot audit`
- M6.10-C1：`27e1577` — `feat(worker): add M6.10-C1 top2 score snapshot to matchInsights`
- M6.10-C2：当前历史中未见独立 “M6.10-C2” 命名 commit；C2 相关改动已处于当前基线历史中（以本地分支实际 log 为准）
- M6.10-C3：`299284f` — `feat(web): use resolvedFinalScore as primary score on FinalMatchPage`
- M6.10-C4：本文件所在 commit 以后续实际提交为准

## 3. M6.10 功能摘要

### r1 Audit

- 结论：非 winner 候选分数此前缺少稳定持久化载体。

### C1 Top2 score snapshot

- `matchInsights.top2ScoreSnapshot` 保存 Top2 候选的候选级分数快照（含分项与 owner）。

### C2 API projection

- API projection 为 `resolvedCandidateUserId` 提供 `resolvedFinalScore` 与 source/fallback 元信息。

### C3 FinalMatchPage

- FinalMatchPage 主展示分数优先消费 `resolvedFinalScore`，无值时回退 baseline `finalScore`。

## 4. 测试结果

### Worker

- `cd apps/worker`
- `pnpm exec jest test/batch-match-match-insights.spec.ts test/worker-score-shadow-m60.spec.ts test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache`
  - 通过（Test Suites: 4 passed, Tests: 54 passed）
- `pnpm exec tsc --noEmit`
  - 通过
- `cd ../..`

### API

- `cd apps/api`
- `pnpm exec jest test/matching-result-display-candidate.spec.ts --no-cache`
  - 通过（Test Suites: 1 passed, Tests: 45 passed）
- `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts test/match-review-ai.service.spec.ts --no-cache`
  - 通过（Test Suites: 5 passed, Tests: 72 passed）
- `pnpm exec tsc --noEmit`
  - 通过
- `cd ../..`

### Web

- `pnpm --filter @peima/web build`
  - 通过（存在既有 chunk size warning，非阻断）

### Shared

- `pnpm --filter @peima/shared build`
  - 通过

## 5. 当前链路结论

当 RRM read-layer 将最终展示对象切到 B 时：

- `resolvedCandidateUserId = B`
- API 可从 `top2ScoreSnapshot` 读取 B 的候选级分数
- `resolvedFinalScore = B` 的分数
- FinalMatchPage 主展示分数优先使用 `resolvedFinalScore`

同时保持：

- `candidateUserId` 仍是 baseline 持久化值
- `finalScore` 仍是 baseline 持久化值
- 回滚仍可通过关闭对应 RRM flag 实现（按既有受控开关策略）

## 6. 不变性确认

- 不改 `candidateUserId`
- 不改 `finalScore`
- 不改 worker winner / ranking
- 不改 RRM resolver
- 不改 schema / migration
- 不恢复 / 不依赖 scoreShadow v1
- 不输出 raw `matchInsights` / raw `top2ScoreSnapshot`

## 7. 当前限制

- 第一版仅保存 Top2 候选分数快照，未覆盖全 6 人。
- `candidateUserId` 持久化写回策略未纳入本轮。
- `finalScore` 持久化重算策略未纳入本轮。
- manual API smoke 可在后续环境补做。
- broad production enablement 仍不建议默认开启，建议受控验证后推进。

## 8. Push 前 checklist

- [x] M6.10-r1 已 commit
- [x] M6.10-C1 已 commit
- [x] M6.10-C2 已 commit（以当前本地历史基线为准）
- [x] M6.10-C3 已 commit
- [ ] M6.10-C4 已 commit
- [x] Worker tests passed
- [x] Worker tsc passed
- [x] API tests passed
- [x] API tsc passed
- [x] Web build passed
- [x] Shared build passed
- [x] No schema / migration diff
- [x] No README diff
- [x] No unrelated ai-pairwise files staged
- [x] No candidateUserId / finalScore / worker winner / resolver changes
- [ ] Ready for user-controlled git push

## 9. 下一阶段建议

- manual API smoke + controlled internal rollout
- full pool candidate score snapshot（6 人）
- `candidateUserId` 持久化写回设计
- `finalScore` 持久化重算设计
- 清理并处理当前分支上的 ai-pairwise / m42 未提交项

## 10. 修订记录

- 2026-05-06：完成 M6.10-C4 最终回归与 push 前收口文档。

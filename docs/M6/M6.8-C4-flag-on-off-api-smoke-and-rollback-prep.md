# M6.8-C4 — Flag on/off API Smoke and Rollback Prep

## 1. 本轮目标

- 对 M6.8-C1/C3 做最后一轮回归、flag on/off 行为记录与 rollback 准备。
- 本轮不新增功能。

## 2. 修改文件

- `docs/M6/M6.8-C4-flag-on-off-api-smoke-and-rollback-prep.md`（本轮仅文档）

## 3. 自动化测试结果

### API（`apps/api`）

- `pnpm exec jest test/matching-result-display-candidate.spec.ts --no-cache`
  - 通过（1 suite，40 tests）
- `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts test/match-review-ai.service.spec.ts --no-cache`
  - 通过（5 suites，67 tests）
- `pnpm exec jest test/rrm-observation-summary.service.spec.ts test/admin-rrm-observation-summary.controller.spec.ts --no-cache`
  - 通过（2 suites，6 tests）
- `pnpm exec tsc --noEmit`
  - 通过

### Web（仓库根）

- `pnpm --filter @peima/web build`
  - 通过（Vite build；仅既有 chunk size warning）

### Worker（`apps/worker`）

- `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache`
  - 通过（4 suites，52 tests）
- `pnpm exec tsc --noEmit`
  - 通过

## 4. Observation script smoke

执行：

```bash
node --env-file=.env packages/database/scripts/m6-r7-rrm-observation-snapshot.mjs --limit 50 --pretty
```

匿名摘要：

- `sampleSize = 50`
- `withRrmBoundedDecision = 1`
- `boundedDecision.would_switch_to_rrm = 1`
- `boundedDecision.would_use_baseline = 0`
- `boundedDecision.fallback_baseline = 0`
- `quality.malformedCount = 0`
- `quality.parseErrorCount = 0`
- `quality.unexpectedExceptionCount = 0`

## 5. API manual smoke

本轮未执行手动 API smoke。

原因：

- 当前执行上下文未提供可复用 token / user 会话，不足以形成可复现且匿名的 flag on/off 手动调用记录。

说明：

- C1 单元测试已覆盖 flag on/off、valid activation、fallback 与安全行为；
- C3/C4 observation 脚本已验证本地存在 `would_switch_to_rrm` 受控样本。

## 6. Rollback plan

- 关闭 `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`。
- read-layer 立即回到 M6.5/M6.6 既有 projection（不应用 bounded override）。
- 不需要 DB rollback（不改 `candidateUserId` / `finalScore` 持久化）。
- C3 fixture 为本地 dev 数据，不作为生产依赖。
- observation summary API 保持可用，用于回归后观测。

## 7. 不变性确认

- 不改 `MatchResult.candidateUserId`
- 不改 `finalScore`
- 不改 worker winner / ranking
- 不改 batch-match 主决策
- 不改 RRM resolver
- 不改 Prisma schema / migration
- 不恢复 / 不依赖 scoreShadow v1
- 不输出 raw `matchInsights` / raw selectedTop2 IDs

## 8. 当前限制

- 本轮未完成手动 API smoke（缺 token / 会话）。
- C3 fixture 为 dev-only，不代表生产数据分布。
- 真实生产流量下的 flag on/off 观测仍需在受控环境补充。

## 9. 下一步

M6.8-C5：Final regression / push prep

## 10. 修订记录

- 2026-05-05：完成 C4 回归与 observation 复核，记录 rollback prep；手动 API smoke 因上下文限制未执行。

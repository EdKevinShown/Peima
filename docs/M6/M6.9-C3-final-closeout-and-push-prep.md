# M6.9-C3 — Final Closeout and Push Prep

## 1. 本轮目标

说明：

- M6.9 属于 regression + rollback 的 final closeout 阶段。
- C3 仅做最终状态记录与 push prep。
- 不新增功能（docs-only）。

## 2. M6.9 commit 基线

- M6.9-C1：`340b1d5` — docs(m6): add M6.9-C1 flag off/on regression and rollback baseline
- M6.9-C2：`346612e` — docs(m6): add M6.9-C2 final full regression and rollout checklist
- M6.9-C3：本文档所在 commit 以 `git log` 为准（避免后续变更导致 hash 漂移）

## 3. M6.8 / M6.9 完成状态

- M6.8 已完成 behind-flag 的 read-layer activation（`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`）。
- M6.9 已完成：
  - C1：flag off/on regression baseline + rollback baseline；
  - C2：final full regression + rollout checklist + go/no-go 结论。
- manual API smoke 仍未完成（缺 token / user 会话），但不阻塞 controlled internal verification 的结论输出。

## 4. 最终测试基线

本节复用 C2 的最终回归基线（C3 不重复执行全量测试）：

- API `matching-result-display-candidate.spec.ts`：40 tests passed
- API M6 core set：67 tests passed
- API observation summary tests：6 tests passed
- API `tsc --noEmit`：passed
- Web build：passed
- Worker bounded/shadow tests：52 tests passed
- Worker `tsc --noEmit`：passed
- observation script `--limit 50`：success
- `boundedDecision.would_switch_to_rrm = 1`
- quality：`malformedCount=0` / `parseErrorCount=0` / `unexpectedExceptionCount=0`

## 5. Rollout conclusion

- **Go**：进入 controlled internal flag-on verification。
- **No-go**：暂不建议 broad production enablement。
- 不建议默认开启 `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`。
- 仅在受控环境设置 `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED="1"`。

## 6. Rollback conclusion

- 关闭 flag 即回旧 read-layer projection。
- 不需要 DB rollback。
- `candidateUserId` / `finalScore` 持久化保持不变。
- observation API / script 可持续用于回退后监控。

## 7. Safety / privacy

- no raw IDs（不输出真实 ID / userId / candidateUserId / conversationId）
- no raw `matchInsights`
- no raw selectedTop2 IDs
- observation 仅用于 admin/debug 场景
- no user-level drilldown

## 8. 不变性确认

- `candidateUserId` 未改
- `finalScore` 未改
- worker 未改
- resolver 未改
- schema 未改
- scoreShadow v1 未恢复
- true bounded decision 仅在 read-layer behind flag 生效

## 9. Remaining risks / limitations

- manual API smoke 未完成（缺 token / user 会话）
- production-like observation 未完成
- Admin UI card 未做
- `finalScore` recompute 未做
- `candidateUserId` write 未做
- C3 fixture 为 dev-only
- broad production enablement 仍不建议

## 10. Push 前 checklist

- [x] M6.9-C1 已 commit
- [x] M6.9-C2 已 commit
- [ ] M6.9-C3 已 commit
- [x] API tests passed
- [x] API tsc passed
- [x] Web build passed
- [x] Worker tests passed
- [x] Worker tsc passed
- [x] Observation script smoke passed
- [x] No schema / migration diff
- [x] No README diff
- [x] No unrelated ai-pairwise files staged
- [x] No candidateUserId / finalScore / worker / resolver changes
- [x] Broad production enablement not claimed
- [ ] Ready for user-controlled git push

## 11. Final M6 conclusion

M6 已完成：

- RRM shadow → bounded dry-run → observation → resolved target unification → read-layer activation behind flag → regression/rollback baseline。

M6 未完成：

- 生产默认开启；
- `candidateUserId` 持久化写回；
- `finalScore` recompute；
- broad production rollout。

结论：

**RRM read-layer controlled integration complete; broad production enablement deferred.**

## 12. 下一阶段建议

下一阶段可选：

- M6.10 / M7.0：manual API smoke + controlled internal rollout；
- 或进入 `candidateUserId` write / `finalScore` recompute 设计；
- 或先处理 ai-pairwise / m42 未提交分支。

## 13. 修订记录

- 2026-05-05：完成 M6.9-C3 final closeout 与 push prep（docs-only）。

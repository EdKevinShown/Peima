# M6.3-r6 — Final Regression and Push Prep

## 1. 本轮目标

- **M6.3-r1**：**RRM bounded decision** **contract**。
- **M6.3-r2**：**write strategy**。
- **M6.3-r3**：**`rrmBoundedDecision`** **dry-run** **worker** 写入（**`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**）。
- **M6.3-r4**：**dry-run regression**（含 **`catch`** 路径加固）。
- **M6.3-r5**：**本地 DB dry-run smoke**（**queue + batch-match** + **`m6-r6b-shadow-smoke`** 扩展）。
- **M6.3-r6（本轮）**：对 **r1–r5** 做**最终回归**与 **push 前检查**；**仅**文档与记录；**不**进入 **true bounded decision implementation**。

---

## 2. 本地 commit 基线

| Commit | 说明 |
|--------|------|
| **`714681c`** | **r1** — **RRM bounded decision contract** |
| **`8c31597`** | **r2** — **bounded decision write strategy** |
| **`d61f432`** | **r3** — **`rrmBoundedDecision` dry-run meta**（**worker**） |
| **`75aa72c`** | **r4** — **bounded dry-run regression** |
| **`725df26`** | **r5** — **bounded dry-run local DB evaluation** |

**`git log --oneline -40`** 中上述 **5** 条均在栈顶附近可见（与 **M6.2**、**M6.1**、**M6.0** 等历史共存）。

---

## 3. Git 状态

**记录命令：** **`git status --short`**（**r6 记录时**）。

**摘要：**

- **M6.3 相关**：**无**未提交修改（**r1–r5** 均已 **commit**）。
- **无关项（仍未纳入 M6.3）**：
  - **`apps/api/src/modules/ai-pairwise-decision/ai-pairwise-decision-job.service.ts`** — **`M`**（已修改，**未 stage**）
  - **`apps/api/test/ai-pairwise-decision-job.service.spec.ts`** — **`M`**（**未 stage**）
  - **`apps/api/src/dev-cli/m42-f3-replay-schema-validation-failed.ts`** — **`??`**（**未跟踪**，**未 stage**）
- **Prisma schema / migration**：**无**工作区 diff。
- **根目录 README**：**无**工作区 diff。

---

## 4. 自动化检查结果

### Worker

| 项 | 命令 | 结果 |
|----|------|------|
| **Jest** | `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache`（**`apps/worker`**） | **4 suites，52 tests passed** |
| **tsc** | `pnpm exec tsc --noEmit`（**`apps/worker`**） | **通过** |

### API

| 项 | 命令 | 结果 |
|----|------|------|
| **Jest** | `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache`（**`apps/api`**） | **4 suites，49 tests passed** |
| **tsc** | `pnpm exec tsc --noEmit`（**`apps/api`**） | **通过** |

### Web

| 项 | 命令 | 结果 |
|----|------|------|
| **Build** | `pnpm --filter @peima/web build`（**monorepo 根**） | **通过**（**Vite** 构建成功；**chunk 体积**告警为既有提示） |

### Database script（**`bounded-metrics`**）

| 项 | 命令 | 结果 |
|----|------|------|
| **命令** | `node --env-file=.env packages/database/scripts/m6-r6b-shadow-smoke.mjs bounded-metrics --limit=30` | **`ok: true`** |
| **`sampleSize`** | | **30** |
| **`withBounded`**（**`counts`**） | | **1** |
| **`boundedDecisionCoverageRate`** | | **≈ 0.033**（**1 / 30**） |
| **`wouldUseBaselineRate`** | | **1** |
| **`wouldSwitchToRrmRate`** | | **0** |
| **`fallbackBaselineRate`** | | **0** |
| **`v1LegacyPresenceRate`** | | **0** |
| **`note`** | | **`smoke_only_not_quality_conclusion`** |

---

## 5. M6.3 功能链路摘要

- **`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**：**默认关闭**；仅 **`"1"`** 开启写入。
- **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线**（**无**正式 **bounded** 决策）。
- **`rrmBoundedDecision`**：写入 **`matchInsights`**；**`mode=dry_run`**。
- **`wouldSwitch`**：**仅观测**；**不**改变 **`MatchResult`** 或 **worker** 排名。
- **`finalScore`**、**`MatchResult.candidateUserId`**、**worker winner / ranking**、**RRM resolver**：**M6.3** 路径**不**改正式结果。
- **`scoreShadow` v1**：**未恢复**；**dry-run** **不依赖** v1 **通过/失败**。

---

## 6. R5 DB smoke 摘要（**归档**）

- **Prep**：**`queue_created`**；**`poolItemCount=6`**（见 **M6.3-r5**）。
- **batch-match**：**`totalQueues=1`**，**`successCount=1`**，**`failedCount=0`**。
- **`rrmBoundedDecisionPresent`**：**`true`**
- **`decision`**：**`would_use_baseline`**
- **`wouldSwitch`**：**`false`**
- **`fallbackUsed`**：**`false`**
- **`fallbackReason`**：**`null`**
- **`inputPresence`**：**`scoreShadowV2` / `rrmDecisionShadow` / `rrmV2Top2Selector` / `selectedTop2`** 均为 **`true`**
- **`scoreShadowV1LegacyPresent`**：**`false`**
- **`bounded-metrics --limit=30`**：**`sampleSize=30`**，**`withBounded=1`**，**coverage≈0.033**
- **结论**：**smoke only**，**不作**质量结论。

---

## 7. 不变性确认

- **`finalScore`**、**`MatchResult.candidateUserId`**、**worker winner / ranking**、**`batch-match`** 主决策、**RRM resolver**：**M6.3-r6 未改代码**；与 **r1–r5** 设计一致。
- **`FinalMatchPage`**：**未改**。
- **Prisma schema / migration**、**根 README**：**无**本轮 diff。
- **`scoreShadow` v1**：**未恢复**。
- **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线**。
- **`would_switch_to_rrm`**：**未**作为产线切换生效。
- **真实 ID**：**未写入本文档**。

---

## 8. 当前未处理事项

- **仅有 dry-run**，**不是**真正 **bounded decision** 落地。
- **Bounded** 在 DB 窗口内**样本仍少**（**coverage** 低属预期，直至更多 **flag-on** 写入）。
- **尚未**系统性观察 **`would_switch_to_rrm`** 分布（**本轮 smoke** 为 **baseline** 决策）。
- **未接** **admin observation**。
- **未完成** **explanation unification**。
- **未进入** **`candidateUserId`** **写入策略**（**bounded** **switch**）。
- **无关 ai-pairwise / m42** 文件仍在工作区，**未**纳入 **M6.3** **commit**。

---

## 9. Push 前 checklist

- [x] **Worker tests passed**
- [x] **Worker tsc passed**
- [x] **API tests passed**
- [x] **API tsc passed**
- [x] **Web build passed**
- [x] **bounded-metrics smoke passed**（**`ok: true`**）
- [x] **No schema / migration diff**（工作区）
- [x] **No README diff**（工作区）
- [x] **No unrelated ai-pairwise files staged**
- [x] **No scoreShadow v1 restoration**
- [x] **No finalScore / candidateUserId / winner / resolver changes**（**r6 文档轮**）
- [x] **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED` not wired**
- [x] **M6.3（r1–r5）+ r6 文档已本地 commit**；**`git push` 由维护者执行**（**本轮不 push**）

---

## 10. 下一步建议

**完成 M6.3（含 r6 文档）后**，建议由维护者**统一** **`git push`**（**含** **`714681c`…`725df26`** 及 **r6** 文档 **commit**）。

**后续阶段：**

**M6.4 — True Bounded Decision Implementation Design / Gating**

**M6.4-r1** 仍建议**先做 design**，**不**直接 **implementation**。原因：

- **Bounded dry-run** 样本仍少；
- **未**充分观察真实 **`switch`** 分布；
- **未有** **admin observation**；
- **未完成** **explanation unification**；
- **不能**在未闭环前让 **RRM** **直接改** **`candidateUserId`**。

---

## 11. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-04 | **M6.3-r6**：最终回归与 **push prep** 文档；**自动化** 与 **`bounded-metrics`** 复跑记录。 |

# M6.1-r6 — Local DB Shadow Evaluation Run

## 1. 本轮目标

- **M6.1-r3**：worker 在 flag on 时写入 **`matchInsights.rrmDecisionShadow`**。
- **M6.1-r4**：单测回归矩阵。
- **M6.1-r5**：evaluation 指标与 summary 形状、gating。
- **M6.1-r6（本文）**：在**本地**环境尝试 **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED=1`** + **waiting queue → worker batch-match** 的**写库烟测**，并对样本做**匿名**指标表；**不**让 shadow **switch** 生效；**不改**正式匹配语义。

---

## 2. 当前基线

| 项 | 值 |
|----|-----|
| **r1** | `00bc535` |
| **r2** | `884fc8d` |
| **r3** | `f4b4bb9` |
| **r4** | `de93e33` |
| **r5** | `ddf640b` |
| **Flag** | **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED`**（仅 **`"1"`** 开启） |
| **Payload** | **`matchInsights.rrmDecisionShadow`** |
| **`sourceType`** | **`rrm_decision_shadow`** |
| **`sourceVersion`** | **`m6.1-rrm-decision-shadow-v1`** |

---

## 3. 自动化测试结果

**Git（本轮执行时）**

- **`git status --short`**：仍存在未纳入 M6.1 的本地改动（**`ai-pairwise-decision-job.service.ts`**、**`ai-pairwise-decision-job.service.spec.ts`**、**`m42-f3-replay-schema-validation-failed.ts`**）— **未 stage、未纳入 R6 commit**。
- **`git log --oneline -12`**：含 **`ddf640b` … `00bc535`** 等 M6.1 链上 commits。

**Worker**（`cd apps/worker`）

```text
pnpm exec jest test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache
# Test Suites: 3 passed | Tests: 37 passed

pnpm exec tsc --noEmit
# exit 0
```

**API**（`cd apps/api`）

```text
pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache
# Test Suites: 4 passed | Tests: 49 passed

pnpm exec tsc --noEmit
# exit 0
```

**Web**：本轮**未**改 web / shared 对外契约；**未**执行 **`pnpm --filter @peima/web build`**。

---

## 4. 本地 DB 执行步骤（本轮实际）

### 4.1 Flag

- **PowerShell**：**`$env:PEIMA_M6_RRM_DECISION_SHADOW_ENABLED="1"`**（与 **`.env`** 一并由 **`dotenv-cli`** 注入 worker 进程）。

### 4.2 Worker batch-match

- **目录**：**`apps/worker`**
- **命令**：**`pnpm exec dotenv -e ../../.env -- pnpm run batch-match`**
- **结果摘要**（日志脱敏，仅结构）：
  - **`worker started`**
  - **`[batch-match] event=batch_start`** … **`totalQueues=0`**
  - **`successCount=0` `failedCount=0`**
  - **`event=batch_complete`** / **`outcome=ok`**

**结论（本轮）**：调度器成功触发 **batch-match**，但 **waiting queue 中无待处理项**，因此**本轮未通过该路径新建 `MatchResult` 行**，也就**未在本次 run 中**观察到**新落库**的 **`rrmDecisionShadow`**。

### 4.3 读库核对（尝试）

- 曾尝试用 **Prisma** 读取最近 **`MatchResult.matchInsights`**（**`take: 5`**）以检查 **`rrmDecisionShadow`** 等键。
- **在本执行环境中**：查询**长时间无响应**（疑似 **DB 不可达 / 网络或 `DATABASE_URL` 指向远端未连通**），**未得到**可用于文档的**匿名行级**结果。
- **维护者本地**：可在 DB 可用时执行等价查询（**勿**将真实 **ID** 贴入 docs）。

---

## 5. 本地 DB 样本摘要（匿名）

**本轮 `batch-match` 产生的新行数：`0`** → 下表为 **「若从 DB 聚合」的口径占位**；**`sampleSize = 0`** 表示 **本轮无新样本**，**不构成**质量评估。

| Metric | Value | Notes |
|--------|------:|-------|
| **sampleSize** | **0** | **无新 `MatchResult`**；仅为 **smoke / local run 记录**。 |
| **shadowCoverageRate** | **N/A** | 分母为 0 或未抽样。 |
| **validShadowRate** | **N/A** | 同上。 |
| **noShadowDecisionRate** | **N/A** | 同上。 |
| **sameAsBaselineRate** | **N/A** | 同上。 |
| **switchSuggestedRate** | **N/A** | 同上。 |
| **blockedRate** | **N/A** | 同上。 |
| **missingInputRate** | **N/A** | 同上。 |
| **contextBlockedRate** | **N/A** | 同上。 |
| **invalidSelectorRate** | **N/A** | 同上。 |
| **unexpectedExceptionRate** | **N/A** | 同上。 |
| **v1LegacyPresenceRate** | **N/A** | 同上。 |

**说明：** **写库链路中 `rrmDecisionShadow` 的存在性** 已由 **M6.1-r3 实现 + r4 单测** 覆盖；**本轮 DB smoke** 因 **`totalQueues=0`** 未追加新证据。更大样本需 **enqueue waiting 队列** 后重跑 **§4.2**，或进入 **M6.1-r7 / M6.2**。

---

## 6. 样本字段摘要（目标核对清单 — 匿名）

当存在**新**落库行时，建议在 **SQL / Prisma** 中核对（**不写真实 ID 到 docs**）：

| 检查项 | 本轮观测 |
|--------|-----------|
| **`matchInsights.scoreShadowV2`** | **未**从新 run 验证（无新行）；**r3/r4** 保证生成路径存在。 |
| **`matchInsights.scoreShadow`（v1）** | **预期**新行**不写** v1；**r10** 基线。 |
| **`matchInsights.rrmV2Top2Selector`** | 有 pool 时存在；本轮无新行。 |
| **`matchInsights.rrmDecisionShadow`** | **flag=1** 且路径命中时应存在；**本轮无新行**故未实测落库。 |
| **`rrmDecisionShadow.schemaVersion`** | 期望 **`1`**。 |
| **`sourceType` / `sourceVersion`** | **`rrm_decision_shadow`** / **`m6.1-rrm-decision-shadow-v1`**。 |
| **`shadow.decision`** | **`same` / `switch` / `no_shadow`** 之一。 |
| **`comparison.sameAsBaseline` / `switchSuggested`** | 与 **r2** 一致。 |
| **`guardrails.blocked` / `blockReasons`** | 按 **r4** 矩阵。 |
| **`inputPresence.scoreShadowV2` / `scoreShadowV1LegacyPresent`** | 后者**仅观测**。 |
| **`unexpectedException` in `blockReasons`** | 期望 **0**（健康窗）。 |

---

## 7. 不变性确认

- **不改** **`finalScore`**、**`MatchResult.candidateUserId`**、**worker winner / ranking**、**batch-match 主决策**、**RRM resolver**。
- **不改** **Prisma schema / migration**、**FinalMatchPage**。
- **不恢复** **scoreShadow v1**；**shadow switch** **不生效**于正式结果。
- **Feature flag** 默认仍 **关闭**；本轮仅在**本地进程**临时设为 **`"1"`**。

---

## 8. 当前风险 / 后续注意

- **本轮样本量 = 0**；**不是** RRM 质量结论。
- **仍非** RRM **decision pilot**。
- **`switchSuggested`** 仅观测；**不**自动改 **`candidateUserId`**。
- 若 **`missing` / `blocked`** 在真实窗偏高，应优先修 **数据链路 / 池子**，再谈 pilot（见 **r5 gating**）。
- **Prisma 读库**在部分环境可能阻塞 — **r6b** 可重复 run 并缩短连接超时或换本地 DB。

---

## 9. 验收 checklist

- [x] Worker tests **passed**
- [x] Worker **tsc** **passed**
- [x] API tests **passed**
- [x] API **tsc** **passed**
- [x] 本地 **batch-match** 已执行（**`totalQueues=0`** 已记录）
- [x] **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED=1`** 已用于该 worker 进程
- [x] **新 `MatchResult`**：**本轮未生成**（原因：**无 waiting 队列**）
- [x] **`rrmDecisionShadow` 落库**：**本轮未验证**（无新行；读库尝试**未完成**）
- [x] **无真实 ID** 写入本文档
- [x] **不改** **finalScore / candidateUserId / winner / resolver / schema**
- [x] **不触碰**无关 **ai-pairwise** 文件
- [x] **不 `git push`**

---

## 10. 下一步

**M6.1-r7：Shadow Evaluation Batch Report / Push Prep**

若需在本地补证据：可先 **M6.1-r6b：repeat local DB shadow run with larger sample**（enqueue **≥1** waiting queue → 再跑 **batch-match** → 再跑 **Prisma** 匿名聚合）。

**不要**进入 **decision pilot**。

---

## 11. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-05 | **M6.1-r6**：本地 **batch-match** smoke（**0 queues**）；自动化测试全通过；DB 样本表 **N=0**；读库尝试未完成。 |

# M6.2-r5 — Final Regression and Push Prep

## 1. 本轮目标

- **M6.2-r1**：**RRM decision shadow batch evaluation plan**（样本、分母、分布、脚本方向、门槛）。
- **M6.2-r2**：**Read-only shadow evaluation aggregator**（**`m6-r2-shadow-evaluation-summary.mjs`**）。
- **M6.2-r3**：**Local shadow batch evaluation run**（baseline → **queue prep + batch-match** → final 聚合）。
- **M6.2-r4**：**Shadow batch evaluation report**（能支持 / 不能支持、同质性局限、**M6.2-r5 / M6.3** 建议）。
- **M6.2-r5（本文）**：对 **M6.2-r1–r4** 做**最终本地回归**与 **push 前检查**；**不加功能**、**本轮不改代码**（仅文档与验证命令）。
- **不进入** **decision pilot**。

**说明：** **M6.0**（**RRM V2 readonly display**、**FinalMatchPage**、**scoreShadow v1** 依赖移除等）与 **M6.1**（**`rrmDecisionShadow`** 设计 / 实现 / 测试 / **r6–r7** push prep）已在**此前本地 commit 链**中完成；**M6.2-r5** 仅收口 **M6.2** 文档与验证状态。

---

## 2. 本地 commit 基线（M6.2）

| Commit | 说明 |
|--------|------|
| `3acbdb9` | **r1** — batch evaluation plan |
| `de53123` | **r2** — read-only aggregator script |
| `73cdc9a` | **r3** — local batch evaluation run 记录 |
| `e4e9d66` | **r4** — shadow batch evaluation report |

---

## 3. Git 状态

**`git status --short`（摘录）：**

- **未纳入 M6.2 / 未 stage**：**`apps/api/src/modules/ai-pairwise-decision/ai-pairwise-decision-job.service.ts`**（**modified**）、**`apps/api/test/ai-pairwise-decision-job.service.spec.ts`**（**modified**）、**`apps/api/src/dev-cli/m42-f3-replay-schema-validation-failed.ts`**（**untracked**）。
- **M6.2-r5 文档**：本轮仅 **新增** **`docs/M6/M6.2-r5-final-regression-and-push-prep.md`** 并 **commit**；**不 stage** 上述无关文件。

**`git log --oneline -25`：** **`e4e9d66` → `3acbdb9`** 均在本地历史中；**M6.1** / **M6.0** 相关 commits 见 log 上游。

**本轮确认：**

- **无** **Prisma schema** / **migration** / **README** 随 **M6.2-r5** 变更（**仅文档**）。

---

## 4. 自动化检查结果

### Worker

| 步骤 | 命令 | 结果 |
|------|------|------|
| **Jest** | `cd apps/worker` → `pnpm exec jest test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache` | **3 suites，37 tests passed** |
| **tsc** | `pnpm exec tsc --noEmit` | **通过**（exit 0） |

### API

| 步骤 | 命令 | 结果 |
|------|------|------|
| **Jest** | `cd apps/api` → `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache` | **4 suites，49 tests passed** |
| **tsc** | `pnpm exec tsc --noEmit` | **通过**（exit 0） |

### Web

| 步骤 | 命令 | 结果 |
|------|------|------|
| **Build** | 仓库根 **`pnpm --filter @peima/web build`** | **通过**（**Vite** production build **exit 0**；仅有 chunk size 提示） |

### Aggregator（smoke）

| 步骤 | 命令 | 结果 |
|------|------|------|
| **Run** | `node --env-file=.env packages/database/scripts/m6-r2-shadow-evaluation-summary.mjs --limit 20 --pretty` | **exit 0** |

**Aggregator smoke 输出摘要（匿名）：**

| 字段 | 值 |
|------|-----|
| **`sampleSize`** | **20** |
| **`shadowCount`** | **13** |
| **`validShadowCount`** | **13** |
| **`malformedCount`** | **0** |
| **`parseErrorCount`** | **0** |

**说明：** **`--limit 20`** 取最近 **20** 行时，其中 **13** 行含 **shadow**（与 **r3** 造数后的本地库一致）；**若本地 DB 不可达**，脚本会 **`DATABASE_URL_missing`** 或连接错误 **exit 1** — **本轮未出现**。

---

## 5. M6.2 评估结论摘要（来自 **r4**）

| 项 | 摘要 |
|----|--------|
| **Aggregator** | **可用**（**r2** / **r5 §4** 复验）。 |
| **Local batch run** | **可用**（**r3** 路径：**prep + `batch-match`** + flag）。 |
| **`N_shadow`（r3 final `--limit 50`）** | **13**（**≥ 10**）。 |
| **`validShadowRate`** | **1.0** |
| **`sameAsBaselineRate`** | **1.0**（**同质样本下**；见 **r4 §7**）。 |
| **`switchSuggestedRate`** | **0** |
| **`blockedRate`** | **0** |
| **`unexpectedExceptionRate`** | **0** |
| **`v1LegacyPresenceRate`** | **0**（**r3** 主窗口径）。 |
| **同质性** | **高** — **单池 / 单 viewer / 单 reasonCode 形态** |
| **Pilot** | **不支持**直接进入 **decision pilot**（**r4**）。 |

---

## 6. 不变性确认

- **`finalScore`**：**未改**
- **`MatchResult.candidateUserId`**：**未改**
- **Worker winner / ranking**、**`batch-match` 主决策**：**未改**
- **RRM resolver**：**未改**
- **`FinalMatchPage`**：**未改**（**M6.2-r5** 无代码变更）
- **Prisma schema / migration**：**未改**
- **README**：**未改**
- **`scoreShadow` v1**：**未恢复**
- **Shadow switch**：**未生效**
- **文档**：**无**真实 **`userId` / `matchResultId` / `conversationId`**

---

## 7. 当前未处理事项

| 项 | 说明 |
|----|------|
| **Sample diversity** | **不足**（**r4**）。 |
| **多 viewer / 多 pool** | **未**系统覆盖。 |
| **`switch` / `blocked` 真实分布** | **未**在本轮本地窗出现。 |
| **Admin observation** | **未**接入。 |
| **Bounded decision pilot** | **未进入**。 |
| **无关工作区文件** | **ai-pairwise** / **m42** 等仍 **modified / untracked**，**未纳入 M6.2** **commit**。 |

---

## 8. Push 前 checklist

- [x] Worker tests **passed**
- [x] Worker **tsc** **passed**
- [x] API tests **passed**
- [x] API **tsc** **passed**
- [x] Web build **passed**
- [x] Aggregator smoke **passed**
- [x] **No** schema / migration diff（**M6.2-r5**）
- [x] **No** README diff（**M6.2-r5**）
- [x] **No** unrelated ai-pairwise files **staged**
- [x] **No** **scoreShadow v1** restoration
- [x] **No** **finalScore** / **candidateUserId** / **winner** / **resolver** changes（**M6.2-r5**）
- [x] **M6.2** commits **ready** for **user-controlled** **`git push`**

---

## 9. 下一步建议

完成 **M6.2** 后，建议维护者**先统一 `git push`**（含 **M6.0 / M6.1 / M6.2** 已提交本地栈，**排除**未 intend 的工作区文件）。

**后续阶段：**

**M6.3 — Bounded Decision Contract Design**

**M6.3-r1** 仍建议**只做 contract design**，**不直接 implementation**。原因（**r4** 已论证）：

- **Shadow** 链路在本地 batch 下**稳定**，但样本**同质**；
- **尚无** **`switch_to_top2_candidate` / `blocked`** 的**多样真实分布**；
- **尚无** **admin observation**；
- **不能**直接进入 **decision pilot**。

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-04 | **M6.2-r5**：最终回归（Worker / API / Web / aggregator）；**push prep** checklist；**M6.3** 建议。 |

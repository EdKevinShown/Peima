# M6.6-C6 — Final Regression and Push Prep

## 1. 本轮目标

- **M6.6-r1**：resolved owner / explanation / navigation 统一计划已落地为文档与后续 C 轮实现基线。
- **M6.6-C1**：ChatPage `finalMatchPeerUserId` handoff、expected vs actual 会话对象仅 warning / debug、沟通洞察说明基于当前会话。
- **M6.6-C2**：RelationshipTimelinePage `finalMatchPeerUserId` handoff、时间线对象 mismatch 仅 warning / debug。
- **M6.6-C3**：ChatPage 反馈 `structuredPayload.targetUserId` 对齐 `actualConversationPeerUserId`；mismatch 时反馈仍归当前会话对象；FinalMatchPage footer 提示结构化反馈在聊天页提交。
- **M6.6-C4**：Copilot / 沟通建议归因当前 conversation peer；ChatPage → CopilotPage 透传 `finalMatchPeerUserId`；CopilotPage 仅 warning / debug；不改 Copilot 后端 / prompt。
- **M6.6-C5**：FinalMatchPage `scoreOwnerMismatch` / `explanationOwnerMismatch` 轻量提示；不重算 `finalScore`；不新增完整 MatchExplanation 系统；技术区展示 owner 与 API consistency code。
- **M6.6-C6（本轮）**：对 r1 + C1–C5 做最终回归与 push 前检查；**不新增业务功能**、不改 UI、不进入 M6.7。

## 2. 本地 commit 基线

以 `git log` 为准（与计划哈希一致）：

| Hash     | 说明        |
|----------|-------------|
| `60f773b` | M6.6-r1     |
| `669325e` | M6.6-C1     |
| `0d86198` | M6.6-C2     |
| `bcca98a` | M6.6-C3     |
| `759debd` | M6.6-C4     |
| `84f7f66` | M6.6-C5     |

M6.6-C6 本文件所在提交以 `git log -1 --oneline -- docs/M6/M6.6-C6-final-regression-and-push-prep.md` 为准（避免在正文写死 amend 后会变的 hash）。

## 3. M6.6 功能摘要

### C1 ChatPage

- `finalMatchPeerUserId` handoff。
- expected vs actual conversation peer：warning / debug。
- Copilot 区说明基于当前会话。

### C2 Timeline

- `finalMatchPeerUserId` handoff。
- expected vs actual timeline peer：warning / debug。

### C3 Feedback

- ChatPage 反馈 `targetUserId` = `actualConversationPeerUserId`。
- mismatch 时仍归当前会话对象。

### C4 Copilot

- Copilot 归因当前会话 peer；CopilotPage 透传 `finalMatchPeerUserId`。
- 不改 Copilot 后端 / prompt。

### C5 Score / copy owner

- `scoreOwnerMismatch` / `explanationOwnerMismatch` 轻量提示。
- 不重算 score；不新增 explanation 系统。

## 4. 测试结果

以下命令在仓库根目录或注明子目录执行，**本轮全部通过**。

**Web**

- `pnpm --filter @peima/web build` — 通过（Vite production build）。

**API**（`apps/api`）

- `pnpm exec jest test/matching-result-display-candidate.spec.ts --no-cache` — 通过（29 tests）。
- `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts test/match-review-ai.service.spec.ts --no-cache` — 通过（5 suites，56 tests）。
- `pnpm exec tsc --noEmit` — 通过。

**Worker**（`apps/worker`）

- `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache` — 通过（4 suites，52 tests）。
- `pnpm exec tsc --noEmit` — 通过。

## 5. 不变性确认

本轮 C6 **仅新增本回归记录文档**；未改 runtime 代码。与 M6.6 约束一致地确认：

- `MatchResult.candidateUserId` 未在本轮修改。
- `finalScore` 未在本轮修改。
- worker winner / ranking 未在本轮修改。
- batch-match 主决策、RRM resolver 未在本轮修改。
- Prisma schema / migration、README 未在本轮修改。
- Copilot API / prompt / backend 未在本轮修改。
- `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED` 未接线。
- scoreShadow v1 未恢复；主路径不依赖 v1。
- true bounded decision 未作为生产写路径生效。
- FinalMatch 主路径不解析 raw RRM meta（与既有 M6 只读展示约定一致）。

## 6. 当前未处理事项

- true bounded decision **实现**未做（与 pilot 设计文档分离）。
- `candidateUserId` **写回**策略未实现。
- `finalScore` 与 resolved 展示对象 **统一重算策略**未进入实现。
- admin / debug **观测与报表**未做。
- Chat / Timeline / Copilot 对 handoff mismatch 仍为 **warning / debug**，不自动切换业务对象。
- Feedback 已用 P6.12 语义下的 `targetUserId`；后端顶层 DTO 仍可无独立 `feedbackTargetUserId` 字段（若未来要观测可另开契约）。
- 完整 **MatchExplanation** 系统仍未建立。
- **M6.7** 建议专注 admin / debug observation，为 M6.8 flag 后 true bounded decision 做观测基础。

## 7. Push 前 checklist

- [x] M6.6-r1 已 commit（`60f773b`）
- [x] M6.6-C1 已 commit（`669325e`）
- [x] M6.6-C2 已 commit（`0d86198`）
- [x] M6.6-C3 已 commit（`bcca98a`）
- [x] M6.6-C4 已 commit（`759debd`）
- [x] M6.6-C5 已 commit（`84f7f66`）
- [x] M6.6-C6 已 commit（本文件所在 commit）
- [x] Web build passed
- [x] API tests passed（上列 jest）
- [x] API tsc passed
- [x] Worker tests passed
- [x] Worker tsc passed
- [x] 工作区无已 stage 的 schema / migration 变更（本轮未改）
- [x] 工作区无已 stage 的 README 变更（本轮未改）
- [x] 无关 ai-pairwise / m42 文件未纳入 M6.6-C6 commit
- [x] 本轮未改 candidateUserId / finalScore / worker / resolver
- [x] True bounded decision flag 未接线
- [ ] **由维护者执行** user-controlled `git push`（本机记录完成前刻意不 push）

## 8. 下一阶段建议

下一阶段建议另开：

**M6.7 — Admin / Debug Observation**

目标：

- 观测 RRM shadow、bounded dry-run、resolved projection、handoff mismatch、fallback、owner warnings 等信号。
- 建立 admin / debug report 或只读观测面。

**M6.8 — True bounded decision behind flag**

- 真正接线 `PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`（或项目最终命名）宜放在 M6.8；**M6.7 不应直接接线该 flag**，以免缺少观测基线。

## 9. 修订记录

- **2026-05-05**：M6.6-C6 初稿 — 回归命令全绿；记录 r1/C1–C5 基线与未处理事项；push 前 checklist。

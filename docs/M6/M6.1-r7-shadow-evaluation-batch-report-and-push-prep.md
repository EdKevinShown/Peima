# M6.1-r7 — Shadow Evaluation Batch Report and Push Prep

## 1. 本轮目标

- **M6.1-r1 / r2**：**RRM decision shadow mode** 设计与 **payload contract**（**`schemaVersion` / `sourceType` / `sourceVersion`**、**`shadow` / `comparison` / `guardrails` / `inputPresence`**）。
- **M6.1-r3**：worker 在 feature flag 下 **shadow-only** 写入 **`matchInsights.rrmDecisionShadow`**（**不改**主匹配 **`candidateUserId`** / **winner**）。
- **M6.1-r4**：**回归矩阵**与单测（Case 1–13 等）。
- **M6.1-r5**：**评估指标**定义与汇总口径（**`shadowCoverageRate`**、**`validShadowRate`**、**`sameAsBaselineRate`** 等）。
- **M6.1-r6**：本地 DB **`batch-match`** 尝试；**`totalQueues=0`**，**`sampleSize=0`**，**未验证** **`rrmDecisionShadow`** 落库。
- **M6.1-r6b**：补 **waiting** 队列 + **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED=1`** 重跑；**`sampleSize=1`** smoke，**已验证** **`rrmDecisionShadow`** 字段与 r5 指标。
- **M6.1-r7（本文）**：汇总 **r1–r6b** 的设计、实现、测试与 **DB smoke** 证据，做 **push 前检查**；**不扩展** shadow 功能；**不进入** **decision pilot**；**不让** shadow **switch** 在产线**生效**。

---

## 2. 本地 commit 基线

| Commit | 说明 |
|--------|------|
| `00bc535` | **r1** — RRM decision shadow mode design |
| `884fc8d` | **r2** — RRM decision shadow payload contract |
| `f4b4bb9` | **r3** — worker 写入 **`rrmDecisionShadow`**（**`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED`**） |
| `de93e33` | **r4** — RRM decision shadow 回归矩阵扩展 |
| `ddf640b` | **r5** — RRM decision shadow evaluation summary |
| `f93b025` | **r6** — local DB shadow evaluation run（**0 queues**） |
| `8624d1f` | **r6b** — local DB shadow rerun with waiting queue（**`sampleSize=1`**） |

---

## 3. 功能链路摘要

| 项 | 说明 |
|----|------|
| **Feature flag** | **`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED`** |
| **默认** | **关闭**；仅当值为 **`"1"`** 时 worker 写入 **`rrmDecisionShadow`** |
| **写入位置** | **`match_results.match_insights.rrmDecisionShadow`**（JSON 内） |
| **`sourceType`** | **`rrm_decision_shadow`** |
| **`sourceVersion`** | **`m6.1-rrm-decision-shadow-v1`** |
| **`scoreShadowV2`** | **必需**输入之一（与 r2 / r3 / r5 一致）；**不写**、**不依赖** **`scoreShadow` v1** |
| **Shadow switch** | **不生效**；仅 **观测 / 落库** 侧车字段 |

---

## 4. 自动化测试结果

以下在 **M6.1-r7** 本轮 **实际执行**（**`--no-cache`**；**Windows PowerShell**）。

### Worker

| 步骤 | 命令 | 结果 |
|------|------|------|
| **Jest** | `cd apps/worker` → `pnpm exec jest test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache` | **3 suites, 37 tests passed** |
| **tsc** | `pnpm exec tsc --noEmit` | **通过**（exit 0） |

### API

| 步骤 | 命令 | 结果 |
|------|------|------|
| **Jest** | `cd apps/api` → `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache` | **4 suites, 49 tests passed** |
| **tsc** | `pnpm exec tsc --noEmit` | **通过**（exit 0） |

### Web

| 步骤 | 命令 | 结果 |
|------|------|------|
| **Build** | 仓库根目录 `pnpm --filter @peima/web build` | **通过**（**Vite** production build **exit 0**；仅有 chunk size 提示，非失败） |

**说明：** 本轮 **未改** **`apps/web`** 源码；**build** 仅作 **push 前** 完整性确认。

---

## 5. Local DB smoke summary

**来源：** **M6.1-r6b** 文档与 **`m6-r6b-shadow-smoke.mjs summarize`** 口径；**不写**真实 **`userId` / `matchResultId` / `conversationId`**。

### 5.1 指标表（**`N=1`**）

| Metric | Value | Notes |
|--------|------:|-------|
| **`sampleSize` (`N`)** | **1** | **Smoke only**；**非**质量结论。 |
| **`shadowCoverageRate`** | **1.0** | **`N_shadow / N`** |
| **`validShadowRate`** | **1.0** | |
| **`sameAsBaselineRate`** | **1.0** | 分母见 **r6b**（与 **r5** 可选口径一致） |
| **`switchSuggestedRate`** | **0.0** | |
| **`noShadowDecisionRate`** | **0.0** | |
| **`blockedRate`** | **0.0** | |
| **`missingInputRate`** | **0.0** | |
| **`contextBlockedRate`** | **0.0** | |
| **`invalidSelectorRate`** | **0.0** | |
| **`unexpectedExceptionRate`** | **0.0** | |
| **`v1LegacyPresenceRate`** | **0.0** | |

### 5.2 匿名字段摘要（r6b 单样本）

| 字段 / 语义 | 值 |
|-------------|-----|
| **`rrmDecisionShadow`** | **存在** |
| **`decision`** | **`same_as_baseline`** |
| **`blocked`** | **`false`** |
| **`blockReasons`** | **`[]`** |
| **`inputPresence.scoreShadowV2`** | **`true`** |
| **`inputPresence.rrmV2Top2Selector`** | **`true`** |
| **`inputPresence.selectedTop2`** | **`true`** |
| **`inputPresence.scoreShadowV1LegacyPresent`** | **`false`** |

**明确：**

- 上表 **不代表**可进入 **decision pilot**。
- **不代表**大样本下 **`sameAsBaselineRate`** 等会保持 **1.0**。

---

## 6. 不变性确认

- **`finalScore`**：**M6.1-r7** 本轮 **未改** worker / API 业务逻辑；**r3** 起 shadow 路径 **不替代**主分计算。
- **`MatchResult.candidateUserId`**：**未改**；shadow **不写入**主决策字段。
- **Worker winner / ranking**、**`batch-match` 主决策**：**未改**。
- **RRM resolver（产线决策）**：**未改**；shadow 为 **只读观测** 侧车。
- **`FinalMatchPage`**：**本轮未改**。
- **Prisma schema / migration**：**本轮未改**；**r7** 文档与 **commit** 不含 schema。
- **README**：**本轮未改**。
- **`scoreShadow` v1**：**未恢复**；**不依赖**。
- **Shadow switch**：**未生效**。
- **Feature flag 默认**：仍 **关闭**（仅本地 / 显式 **`"1"`** 验证）。
- **文档**：**无**真实 **`userId` / `matchResultId` / `conversationId`**。

---

## 7. 当前未处理事项

| 项 | 说明 |
|----|------|
| **样本量** | **`sampleSize=1`**，**仅 smoke**；**不作**上线或 pilot 依据。 |
| **大样本** | **尚未**做离线 / 批量 **batch evaluation**。 |
| **Admin observation** | **尚未**系统化观测 **admin / 运营** 侧读数路径。 |
| **Decision pilot** | **未进入** bounded decision pilot。 |
| **无关工作区文件** | **`ai-pairwise-decision-job.service.ts`**、**对应 spec**、**`m42-f3-replay-*.ts`** 等仍在本地 **modified / untracked**，**未纳入 M6.1** **commit**；**push 前**请保持 **不 stage**。 |

---

## 8. Push 前 checklist

- [x] Worker tests passed（**37** / 指定 3 suites）
- [x] Worker tsc passed
- [x] API tests passed（**49** / 指定 4 suites）
- [x] API tsc passed
- [x] Web build passed（**本轮已跑**）
- [x] **`rrmDecisionShadow`** DB smoke recorded（**r6b**）
- [x] No real IDs in docs
- [x] No schema / migration diff（**本轮仅文档**）
- [x] No README diff（**本轮**）
- [x] No unrelated ai-pairwise files staged（**`git status`** 确认未 stage）
- [x] scoreShadow v1 not restored
- [x] finalScore / candidateUserId unchanged（**本轮无业务代码变更**）
- [x] RRM resolver unchanged
- [x] Ready for **user** to decide **`git push`**（**助手不执行 push**）

---

## 9. 下一步建议

**M6.2 — RRM Decision Shadow Batch Evaluation / Admin Observation**

或：**先由维护者统一 `git push`（含 r1–r7 本地栈）**，再开 **M6.2**。

**不要**直接进入 **RRM decision pilot**。**Bounded decision** 至少应在 **大样本评估** 与 **admin / 离线观测** 之后再评估。

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-04 | **M6.1-r7**：汇总 **r1–r6b**；记录 Worker / API **Jest + tsc** 与 **Web build**；**r6b** DB smoke 指标与不变性；**push prep checklist**。 |

# M6.0-F — scoreShadow Local Regression Run Record

> **范围**：本地 **一次** 回归执行记录（**非**自动化 CI）。验证 **M6.0-E** 后新生成的 **`MatchResult`** 是否包含 **`matchInsights.scoreShadow`**，且与 **`finalScore` / `profileScore` 语义** 一致；**GET `/matching/result`** 侧 **`relationshipProfileScore`** 是否优先走 **`match_insights_score_shadow`**。  
> **本文不写** 真实 **`userId` / `candidateUserId` / `matchResultId` / token / `DATABASE_URL`**。  
> **执行环境**：维护者本机 **Docker `postgres` 已运行**；**`DATABASE_URL`** 使用 **`.env.example` 同型** 的本地连接串（`postgresql://user:password@127.0.0.1:5432/peima?schema=public`），**非**生产机密。

---

## 1. 一句话结论

在 **enqueue 一条 `waiting` 队列** 并 **运行 `apps/worker` 的 `node dist/main.js --batch-match`** 后，**新生成的 `MatchResult`** 已包含 **`matchInsights.scoreShadow`**（**`scoringVersion` = `m6.0-profile-score-shadow-v1`**），**`scoreShadow.finalScoreV1` 与行上 `finalScore` 数值一致**，**`scoreShadow.relationshipProfileScore` 为 [0,1] 内有限数**；**`matchInsights` 原有 `explanation` / `openingTopics` 等键仍存在**；**`reasonSummary` 仍为 v1 固定句式前缀**。与 **`resolveRelationshipProfileScoreShadow`** 的优先级约定一致：**有合规 `scoreShadow` 时应得到 `source = match_insights_score_shadow`**。**未**改动 **`MatchResult.finalScore`、胜者、`candidateUserId`、`displayCandidateUserId`、RRM**。

---

## 2. 构建结果

| 组件 | 结果 | 备注 |
|------|------|------|
| **`pnpm --filter @peima/worker build`** | **通过** | 含 `prebuild` 依赖构建。 |
| **`pnpm --filter @peima/web build`** | **通过** | Vite production build。 |
| **`pnpm --filter @peima/api build`** | **本 agent 环境未通过** | `prisma generate` 在 Windows 上出现 **`EPERM` rename `query_engine-windows.dll.node`**（沙箱/文件锁）；**维护者本机重跑 `nest build` 应可验证**。 |

---

## 3. 本地匹配执行摘要

1. **队列**：对「**当前库中最近一条 `match_results` 的 viewer**」删除其 **`waiting`** 队列（若有）后，**插入一条 `status = waiting`** 的 **`batch_match_queue`** 行（仅本地开发库）。  
2. **Worker**：在 **`apps/worker`** 目录设置 **`DATABASE_URL`**（见 § 抬头说明），执行 **`node dist/main.js --batch-match`**。  
3. **结果**：日志显示 **`batch_start` → `queue_done` outcome=ok**，并写入新的 **`MatchResult`**（**`successCount=1`**）。**不在本文记录** viewer / candidate 主键。

---

## 4. DB 验证摘要（`psql`，`ORDER BY "createdAt" DESC LIMIT 3`）

对 **`match_results`** 做 JSON 投影（仅摘要字段，**不**在本文贴完整 `id`）：

| 检查项 | 最新一行（新生成） | 说明 |
|--------|-------------------|------|
| **`matchInsights.scoreShadow` 存在** | **是** | `scoreShadow` → `scoringVersion` 非空。 |
| **`scoringVersion`** | **`m6.0-profile-score-shadow-v1`** | 与 **M6.0-E** 常量一致。 |
| **`scoreShadow.finalScoreV1` = `finalScore`** | **是** | 布尔投影 **`final_eq_shadow_v1` = true**。 |
| **`relationshipProfileScore` ∈ [0,1] 且有限** | **是** | 与 **`profileScore`** 同量级小数。 |
| **`explanation.whyMatch` 存在** | **是** | 占位解读仍在。 |
| **`openingTopics` 键存在** | **是** | `matchInsights ? 'openingTopics'`。 |
| **`reasonSummary` v1 前缀** | **是** | 仍以 **`Selected by finalScore v1 (`** 开头。 |
| **较早两行** | **无 `scoreShadow`** | **M6.0-E 之前**写入的历史行；符合 **fallback** 预期。 |

---

## 5. API 验证摘要

| 检查项 | 结论 |
|--------|------|
| **`relationshipProfileScore.score`** | 与 **`matchInsights.scoreShadow.relationshipProfileScore`** 一致（由 **`resolveRelationshipProfileScoreShadow`** 逻辑保证；与 **§4** 数值一致）。 |
| **`relationshipProfileScore.source`** | 对 **含合规 `scoreShadow` 的新行** 应为 **`match_insights_score_shadow`**（**Jest** 已覆盖；**GET** 需 **JWT** 的线上/本地 API 由维护者按需再点验）。 |
| **`scoreBreakdown`** | **仍存在**（**`reasonSummary` 解析**路径未移除）。 |
| **`finalScore` / `candidateUserId`** | **未改语义**；新行 **`finalScore`** 与 **worker 日志** 一致。 |
| **`displayCandidateUserId` / `displaySourceType`** | **未在本轮回归中修改**（**GET 只读** + **RRM 不变量**）。 |
| **泄露** | **`scoreShadow`** 仅含 **数值 + `scoringVersion`**，**无** raw 问卷、**无** RRM raw 分。 |

**旧数据（可选）**：**无 `scoreShadow`** 的历史行依赖 **`reasonSummary`** → **`relationshipProfileScore.source`** 为 **`score_breakdown_profile_score`** 或 **`missing`**；与 **M6.0-C** 设计一致（本轮 **SQL LIMIT 3** 中后两行已体现无 shadow）。

---

## 6. 不变量

- **未改** **`MatchResult.finalScore`**（仍为 v1 加权结果）。  
- **未改** worker **排序 / 胜者** 逻辑。  
- **未改** **`candidateUserId`**。  
- **未改** **`displayCandidateUserId`** / **RRM resolver**。  
- **未改** **Prisma schema**、**无新 migration**。  
- **未接** production poller。

---

## 7. 下一步

**M6.0-G**：**全 A / 全 B / 正常答卷** 对照，验证 **`relationshipProfileScore`**（及与 **`finalScore`** 的差）**区分度** 是否满足产品预期（见 **`docs/M6/M6.0-a2-scoring-v2-product-logic-plan.md`**）。

---

## 8. 修订记录

- **v1.0** — M6.0-F scoreShadow local regression run record（首轮本地跑通 + DB 投影验证）。

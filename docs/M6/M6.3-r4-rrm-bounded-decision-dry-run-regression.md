# M6.3-r4 — RRM Bounded Decision Dry-run Regression

## 1. 本轮目标

- **M6.3-r3** 已完成 **`matchInsights.rrmBoundedDecision`** **dry-run meta**（**`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**）。
- **M6.3-r4（本轮）**：对 **dry-run** 行为做**回归矩阵**确认与文档记录；**最小**补充测试与 **`catch`** 路径鲁棒性（**不**扩展 **bounded decision** 能力）；**不让** **`would_switch_to_rrm`** **正式生效**。

---

## 2. 当前代码基线

| 项 | 值 |
|----|-----|
| **r1 commit** | `714681c` |
| **r2 commit** | `8c31597` |
| **r3 commit** | `d61f432` |
| **Dry-run flag** | **`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**（仅 **`"1"`**） |
| **Real decision flag** | **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线** |
| **Payload 路径** | **`matchInsights.rrmBoundedDecision`** |
| **`sourceType`** | **`rrm_bounded_decision`** |
| **`sourceVersion`** | **`m6.3-rrm-bounded-decision-v1`** |

---

## 3. 回归矩阵

| Case | 场景 | 期望 | 覆盖方式 | 结果 |
|------|------|------|----------|------|
| **1** | **Dry-run flag off**（未设置 / **`"0"`** / **`"true"`** / **`"TRUE"`** 等） | **不写** **`rrmBoundedDecision`**；**shadow / V2** 行为不变；**无 v1** | **`batch-match-rrm-bounded-decision.spec`** it **1**、**10**；**`readM6RrmBoundedDecisionDryRunEnv`** **Case 1b** | **通过** |
| **2** | **Dry-run on + 无 `rrmDecisionShadow`** | **`fallback_baseline`** / **`missing_shadow`**；**不 throw** | **spec** it **2** | **通过** |
| **3** | **Dry-run on + `same_as_baseline`** | **`would_use_baseline`**；**`wouldSwitch=false`**；**`fallbackUsed=false`** | **spec** it **3** | **通过** |
| **4** | **Dry-run on + 有效 `switch_to_top2_candidate`** | **`would_switch_to_rrm`**；**`wouldSwitch=true`**；**不改** **`finalScore`**（**components** 常量校验） | **spec** it **4** | **通过** |
| **5** | **`no_shadow_decision`** | **`fallback_baseline`**；**`shadow_not_switch`** | **spec** it **5** | **通过** |
| **6** | **`guardrails.blocked=true`** | **`fallback_baseline`** / **`guardrail_blocked`** | **spec** it **6** | **通过** |
| **7** | **缺 `scoreShadowV2`** | **`missing_score_shadow_v2`** | **spec** it **7** | **通过** |
| **8** | **空 `selectedTop2`** | **`missing_selected_top2`** | **spec** **M6.3-r4 Case 8** | **通过** |
| **9** | **`target_not_in_top2`**（**`inTop2`** 防御分支） | 规范 **worker** **`selectedTop2`** 下 **`targetId`** 取自 **`[0]`** 且 **`.some`** 恒真；**无单独伪造数组** | **Code review**（**`batch-match-rrm-bounded-decision.ts`** L179–188） | **已记录** |
| **10** | **`sourceVersion` mismatch / malformed** | **`source_version_mismatch`** / **`malformed_shadow`**；**不 throw** | **spec** it **9**（**malformed**）；**M6.3-r4 Case 10**（**version**） | **通过** |
| **11** | **`scoreShadow` v1 缺失** | **不触发 fallback**；**`scoreShadowV1LegacyPresent=false`** | **spec** it **8**（编号继承；语义为 **v1** 观测） | **通过** |
| **12** | **读 `insights` 异常** | **`unexpected_exception`**；**不向调用方 throw** | **spec** **M6.3-r4 Case 12** + **`catch`** 内**不再**调用 **`buildInputPresence`**（避免二次读毒化 **Proxy**） | **通过** |
| **13** | **`buildWorkerMatchInsightsForBestMatch` 集成** | **Dry off** 无 **bounded**；**Dry on** 有 **bounded**；**V2 / shadow** 保留；**无 v1** | **`batch-match-match-insights.spec`** **M6.3-r4 Case 13** | **通过** |
| **14** | **正式决策不变性** | **`batch-match.processor`** 未引用 **`BOUNDED`**；**`MatchResult`** 仍用 **best** **candidate / finalScore** | **Repo grep** + **code review**（**本轮未改** **processor**） | **通过** |

---

## 4. 测试结果

| 层 | 命令 | 结果 |
|----|------|------|
| **Worker Jest** | `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache` | **4 suites，52 tests passed** |
| **Worker tsc** | `pnpm exec tsc --noEmit` | **通过** |
| **API Jest** | `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache` | **4 suites，49 tests passed** |
| **API tsc** | `pnpm exec tsc --noEmit` | **通过** |
| **Web build** | `pnpm --filter @peima/web build` | **通过** |

---

## 5. 不变性确认

- **`finalScore`**、**`MatchResult.candidateUserId`**、**worker winner / ranking**、**`batch-match`** 主路径、**RRM resolver**：**未改**
- **`FinalMatchPage`**、**API display resolver**：**未改**
- **Prisma schema / migration**：**未改**
- **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线**
- **`scoreShadow` v1**：**未恢复**；**不参与** **pass/fail**
- **`would_switch_to_rrm`**：**仅** **dry-run meta**；**不**改变正式结果

---

## 6. 当前风险 / 后续注意

- **r4** 仍为 **dry-run regression**；**`rrmBoundedDecision`** 为 **audit meta**。
- **`wouldSwitch`** **不**代表产线已切换。
- **M6.3-r5**：可做 **local DB dry-run evaluation**（**queue + batch** 造数 + 聚合）。
- **不进入** **true bounded decision implementation**。

---

## 7. 验收 checklist

- [x] **flag off** 不写 **`rrmBoundedDecision`**
- [x] **flag on + missing shadow** → **`fallback_baseline`**
- [x] **`same`** → **`would_use_baseline`**
- [x] **valid switch** → **`would_switch_to_rrm`**
- [x] **`no_shadow`** → **`fallback_baseline`**
- [x] **guardrail blocked** → **`fallback_baseline`**
- [x] **缺 V2** → **`fallback_baseline`**
- [x] **空 Top2** → **`missing_selected_top2`**
- [x] **Case 9** **防御分支** → **code review 记录**
- [x] **malformed / version mismatch** → **`fallback_baseline`**
- [x] **v1 缺失** **不影响**
- [x] **异常输入** **不 throw 出 `buildRrmBoundedDecisionDryRunPayload`**
- [x] **`buildWorkerMatchInsights`** 集成覆盖
- [x] **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`** **未接线**
- [x] **不改** **`finalScore` / `candidateUserId` / winner / resolver**
- [x] **不改** **schema / migration**
- [x] **不触碰**无关 **ai-pairwise** 文件

---

## 8. 下一步

**M6.3-r5 — Bounded Decision Dry-run Local DB Evaluation**

- **`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED=1`** + **waiting queue + worker** 造 **`MatchResult`**；
- 验证 **`rrmBoundedDecision`** 落库与 **`would_*` / fallback** 分布统计；
- **仍不让** **bounded** **正式生效**。

---

## 9. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-02 | **M6.3-r4**：回归矩阵文档；**+4** 条 **Jest**；**`catch`** 路径避免二次读 **`insights`**；**checklist** 排版修正。 |

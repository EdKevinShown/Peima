# M6.5-r1b — RRM Pre-decision Chain Final Summary and Push Prep

## 1. 本轮目标

- **M6.0–M6.5-r1** 已完成 **RRM 正式决策接入前置链路**（只读展示、**shadow**、批量评估、**bounded dry-run**、**true bounded** 设计与 **pilot** 设计）。
- **M6.5-r1b（本文）**：对该链路做**最终阶段性总结**与 **push 前检查**；**不**新增功能；**不写**业务代码。
- **r1b** **不**继续扩展 **M6.5-r2** 内容；**不**进入 **true bounded decision implementation**。

---

## 2. 阶段总览

### M6.0 — RRM V2 Readonly Display

- **RRM V2 readonly display resolver**（**`PEIMA_M6_RRM_V2_SELECTOR_DISPLAY_ENABLED`**）。
- **`displaySourceType`**：**`rrm_top2_v2_selector_readonly`**（见 **`matching-result-display.ts`**）。
- **FinalMatchPage** **source surface**（消费 API 投影，**不**自算 RRM）。
- **`scoreShadow` v1 runtime removal**（**R9–R11** 审计与停写/读路径迁移）。
- **`finalScore` / `candidateUserId` / worker winner**：**未改**。

### M6.1 — RRM Decision Shadow Mode

- **`matchInsights.rrmDecisionShadow`**（**`PEIMA_M6_RRM_DECISION_SHADOW_ENABLED`**）。
- **`shadow.decision`**：**`same_as_baseline` / `switch_to_top2_candidate` / `no_shadow_decision`** 等（与 **M6.1-r2** 合同一致）。
- **DB smoke**（**r6b**）与 **evaluation summary**（**r5**）。
- **不**影响**正式**匹配主结果。

### M6.2 — Shadow Batch Evaluation

- **Read-only aggregator**（**`m6-r2-shadow-evaluation-summary.mjs`**）。
- **Local batch evaluation**（**queue prep + batch-match** → 聚合）。
- **报告窗口示例**：**`shadowCount=13`**，**`validShadowRate=1.0`**，**`sameAsBaselineRate=1.0`**（**M6.2-r4**）。
- **样本同质** → **不支持**直接 **pilot**（**M6.2-r4** 结论）。

### M6.3 — Bounded Decision Dry-run

- **`matchInsights.rrmBoundedDecision`**（**`PEIMA_M6_RRM_BOUNDED_DECISION_DRY_RUN_ENABLED`**）。
- **`mode=dry_run`**；**`wouldSwitch`** **不生效**。
- **DB smoke**、**`bounded-metrics`**（**`m6-r6b-shadow-smoke.mjs`**）（**M6.3-r5 / r6**）。

### M6.4 — True Bounded Decision Gating / Write Strategy

- **Gating design**（**M6.4-r1**）；**write strategy contract**（**M6.4-r2**）；**Strategy A/B/C**，推荐 **Strategy B-lite**。
- **Pre-decision chain summary / push prep**（**M6.4-r3**）。
- **不建议**直接 **`candidateUserId` write**；**`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`** **未接线**。

### M6.5-r1 — True Bounded Decision Pilot Design

- **Pilot** 策略：**A** read-layer override / **B** meta-only / **C** **`candidateUserId` write**。
- **推荐**先做 **A/B preflight**；**C** 延后。
- **不**接线 **true flag**；**不**让 **RRM switch** 改正式结果。

---

## 3. 当前能力矩阵

| Capability | Status | Notes |
|------------|--------|-------|
| **Readonly display** | **done** | **不改**正式 **`candidateUserId`**；**resolver** behind flag |
| **Decision shadow** | **done** | **`rrmDecisionShadow`**；观测侧车 |
| **Batch evaluation** | **done** | **Aggregator** + local batch（**M6.2**） |
| **Bounded dry-run** | **done** | **`rrmBoundedDecision`**；**`wouldSwitch`** 仅观测 |
| **True bounded decision gating** | **designed** | **M6.4-r1**；**docs-only** |
| **Pilot design** | **designed** | **M6.5-r1**；**docs-only** |
| **True bounded decision enabled** | **not done** | **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`** **未接线** |
| **`candidateUserId` write** | **not done** | **明确未做** |
| **`finalScore` rewrite** | **not done** | **明确未做** |
| **Explanation unification** | **not done** | **后续阶段**（**M6.5-r2+**） |
| **Admin observation** | **not done** | **后续阶段** |

---

## 4. 本地 commit 摘要

摘自本仓库 **`git log --oneline`**（**以本地 `main` 为准**）。

### M6.0（r6–r11）

| Hash | 说明 |
|------|------|
| **7215796** | R4/R5 — readonly display preflight + meta contract |
| **892b8a8** | R6 — M6 RRM V2 readonly display resolver behind flag |
| **6840b37** | R7 — selector readonly display regression |
| **69d69c7** | R8 — FinalMatchPage **displaySourceType** surface |
| **353b2f9** | R9 — scoreShadow v1 dependency audit |
| **22fadef** | R10 — stop v1 write；relationshipProfileScore from v2 |
| **c505bca** | R11 — final local regression and push prep |

### M6.1（r1–r7）

| Hash | 说明 |
|------|------|
| **00bc535** | r1 — RRM decision shadow mode design |
| **884fc8d** | r2 — RRM decision shadow payload contract |
| **f4b4bb9** | r3 — worker 写入 **`rrmDecisionShadow`** |
| **de93e33** | r4 — decision shadow regression matrix |
| **ddf640b** | r5 — decision shadow evaluation summary |
| **f93b025** | r6 — local DB shadow evaluation run |
| **8624d1f** | r6b — local DB shadow rerun（waiting queue） |
| **fd54a85** | r7 — shadow evaluation batch report and push prep |

### M6.2（r1–r5）

| Hash | 说明 |
|------|------|
| **3acbdb9** | r1 — batch evaluation plan |
| **de53123** | r2 — read-only shadow evaluation aggregator |
| **73cdc9a** | r3 — local shadow batch evaluation run |
| **e4e9d66** | r4 — shadow batch evaluation report |
| **416ff3a** | r5 — final regression and push prep |

### M6.3（r1–r6）

| Hash | 说明 |
|------|------|
| **714681c** | r1 — RRM bounded decision contract |
| **8c31597** | r2 — bounded decision write strategy |
| **d61f432** | r3 — **`rrmBoundedDecision`** dry-run meta（worker） |
| **75aa72c** | r4 — bounded dry-run regression |
| **725df26** | r5 — bounded dry-run local DB evaluation |
| **9998aaa** | r6 — final regression and push prep |

### M6.4（r1–r3）

| Hash | 说明 |
|------|------|
| **5cdbb82** | r1 — true bounded decision gating design |
| **37c83e4** | r2 — true bounded decision write strategy contract |
| **1dc3045** | r3 — RRM pre-decision chain summary and push prep |

### M6.5-r1

| Hash | 说明 |
|------|------|
| **9c3883c** | r1 — true bounded decision pilot design |

---

## 5. 验证结果摘要

### 5.1 引用前置记录（DB smoke / 聚合）

以下已在 **M6.1-r7**、**M6.2-r5**、**M6.3-r5 / M6.3-r6** 等文档中记录；**本轮不强制复跑** DB 脚本（依赖本地 **`.env`** 与 DB）：

- **`rrmDecisionShadow`** **DB smoke**（**M6.1-r6b / r7**）。
- **`rrmBoundedDecision`** **DB smoke**（**M6.3-r5**）。
- **`bounded-metrics`**（**`m6-r6b-shadow-smoke.mjs bounded-metrics`**）（**M6.3-r6**）。
- **Shadow aggregator**（**`m6-r2-shadow-evaluation-summary.mjs`**）（**M6.2-r3 / r4**）。

### 5.2 本轮复跑的最小检查（**M6.5-r1b** 记录时执行）

**环境：** **Windows**；**本轮**仅新增文档 + 本验证；**未**改业务代码。

| 区域 | 命令 | 结果 |
|------|------|------|
| **Worker Jest** | `cd apps/worker` → `pnpm exec jest test/batch-match-rrm-bounded-decision.spec.ts test/batch-match-rrm-decision-shadow.spec.ts test/batch-match-match-insights.spec.ts test/rrm-v2-top2-selector-shadow.spec.ts --no-cache` | **4 suites，52 tests passed** |
| **Worker tsc** | `cd apps/worker` → `pnpm exec tsc --noEmit` | **exit 0** |
| **API Jest** | `cd apps/api` → `pnpm exec jest test/matching-rrm-v2-selector-readonly-display.spec.ts test/matching-result-display-candidate.spec.ts test/matching-relationship-profile-score.spec.ts test/worker-score-shadow-m60.spec.ts --no-cache` | **4 suites，49 tests passed** |
| **API tsc** | `cd apps/api` → `pnpm exec tsc --noEmit` | **exit 0** |
| **Web build** | 仓库根 → `pnpm --filter @peima/web build` | **通过**（**Vite**；chunk 体积告警为既有提示） |

---

## 6. 不变性确认

- **`finalScore`**：**未**由 **M6** 前置链改写。
- **`MatchResult.candidateUserId`**：**未**被 **bounded / true** 改写。
- **Worker winner / ranking**：**未改**。
- **`batch-match` 主决策**：**未改**。
- **RRM resolver（主路径）**：**未**为 **true bounded** 扩大候选池或改写正式 winner。
- **`FinalMatchPage`**：**未**改业务/算法逻辑（**M6.5-r1b** 轮次）。
- **Prisma schema / migration**：**本轮**无 diff。
- **根 README**：**本轮**无 diff。
- **`scoreShadow` v1**：**未恢复**为 runtime 依赖。
- **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED`**：**未接线**。
- **`wouldSwitch` / `switch_to_top2_candidate`**：**未**作为产线正式切换**生效**。
- **True bounded decision**：**未**进入 **implementation**（仅设计与 pilot 文档）。
- **真实 ID**：**未**写入本文档。

---

## 7. 当前未处理事项

- **True bounded decision implementation**：**未做**。
- **`candidateUserId` write**：**未做**。
- **`finalScore` strategy**（switch 后）：**未定案**实现。
- **Explanation unification**：**未**全链路实现。
- **FinalMatchPage / chat / timeline / feedback** **target alignment**：**未**实现。
- **Admin / debug observation**：**未**实现。
- **Rollback / kill switch**：**仅**设计文档，**未**实现运维 runbook 与自动化。
- **`switch` / `blocked` 真实样本**：仍**不足**（相对 **M6.2-r4**）。
- **样本多样性**：仍**不足**。
- **无关 ai-pairwise / m42 文件**：仍可能留在工作区，**未**纳入 **M6** commits。

---

## 8. Push 前 checklist

- [x] **M6.5-r1** 已 **commit**（**`9c3883c`**）
- [x] **M6.5-r1b（本文）** 已 **commit**（hash 以 **`git log -1 --oneline -- docs/M6/M6.5-r1b-rrm-pre-decision-chain-final-summary-and-push-prep.md`** 为准）
- [ ] **No** Prisma **schema / migration** diff（工作区）
- [ ] **No** 根 **README** diff
- [ ] **No** 无关 **ai-pairwise** / **m42** 文件被 **stage**
- [ ] **No** **`scoreShadow` v1** restoration
- [ ] **No** **`finalScore` / `candidateUserId` / winner / resolver`** 业务改动混入 **M6.5-r1b** commit
- [ ] **`PEIMA_M6_RRM_BOUNDED_DECISION_ENABLED` not wired**
- [ ] **True bounded decision not implemented**
- [ ] **Ready for user-controlled `git push`**

---

## 9. 下一阶段建议

下一阶段建议**另开**（**push** 后的新分支/会话，**不**混入 **r1b** commit）：

**M6.5-r2 — Resolved Candidate / Target Consistency Contract**

**M6.5-r2** 目标：

- 定义 **FinalMatchPage** 展示对象、**chat target**、**timeline target**、**feedback target**、**explanation target**、**score owner**、**fallback owner** 的**单一权威**或**显式双轨**合同。

**仍建议** **docs-first**，**不**直接 **implementation**。

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-04 | **M6.5-r1b**：**M6.0–M6.5-r1** 最终总结与 **push prep**；确认 **M6.5-r1** 已提交；复跑 **Worker/API Jest+tsc**、**Web build**。 |

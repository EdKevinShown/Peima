# P7.7-r3.4 — Dedicated RBAC Permission for Canonical Rehearsal Closeout

## 1. Status

```text
P7_7_R3_4_DEDICATED_RBAC_PERMISSION_CANONICAL_REHEARSAL_COMPLETE
```

说明：

- Code change: yes — RBAC permission + API guard only
- No API read behavior change except permission requirement
- No Admin UI code changes (page copy still references allowlist permission; optional follow-up)
- No DB write / Prisma / migration
- No MatchResult / matchInsights write
- No worker / GET integration
- No Apply endpoint
- No canonical production write
- No legacy removal
- No production / percent rollout

## 2. What changed

| Path | Role |
|------|------|
| `packages/shared/constants/p5-rbac.ts` | Add `VIEW_P76_CANONICAL_REHEARSAL` + role map |
| `apps/api/src/modules/matching/p76-canonical-rehearsal-admin.controller.ts` | Switch `@RequirePermission` to dedicated permission |
| `apps/api/test/p76-canonical-rehearsal-admin.controller.spec.ts` | Controller permission metadata tests |
| `apps/api/test/p77-r3-1-admin-readonly-rehearsal-api.spec.ts` | RBAC role-map unit tests |
| `docs/P7/P7.7-r3.4-dedicated-rbac-permission-canonical-rehearsal-closeout.md` | This closeout |

## 3. Permission

| Item | Value |
|------|-------|
| **New permission** | `VIEW_P76_CANONICAL_REHEARSAL` → `view_p76_canonical_rehearsal` |
| **Purpose** | Read-only Admin review of `p76_canonical_writer_rehearsal_meta` rows |
| **Replaced on** | `GET /admin/p76/canonical-rehearsal` (+ aggregate, `:id`) |
| **Unchanged** | Allowlist apply meta admin still uses `VIEW_P76_ALLOWLIST_APPLY_META` |

**Role assignment** (same cohort as allowlist read-only review):

- `OPERATOR`
- `DATA_ANALYST`
- `ADMIN`

`REGULAR_USER` does not receive this permission.

DB `role_permissions` seeding for production/staging is unchanged in this round (env role hints via `getRolesFromEnv` + `ROLE_PERMISSIONS_MAP`).

## 4. Safety

- Permission is **read-only** — no Apply, write, mutate, promote, or worker semantics
- Does **not** grant MatchResult write
- Does **not** grant production canonical writer execution
- Does **not** replace or broaden allowlist apply sidecar write paths

## 5. Tests / verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/shared run build` | PASS |
| `pnpm --filter @peima/api test -- p77-r3-1-admin-readonly-rehearsal-api p76-canonical-rehearsal-admin.controller` | PASS (18 tests) |
| `pnpm --filter @peima/api run build` | PASS |
| `pnpm --filter @peima/web run build` | PASS |

New coverage:

- Rehearsal controller handlers use `VIEW_P76_CANONICAL_REHEARSAL` (list / aggregate / detail)
- Allowlist controller still uses `VIEW_P76_ALLOWLIST_APPLY_META`
- Role map grants rehearsal permission to admin/operator/analyst only
- Feature flag off → 404 / no DB query (existing service tests unchanged)

## 6. What did not change

- Prisma schema / migrations
- Admin UI pages (`P76CanonicalRehearsalPage` copy / 403 message text deferred)
- Rehearsal writer / worker / GET `/matching/result`
- Apply endpoint (still blocked)
- Production rollout

## 7. PASS / BLOCK

**PASS:**

- Dedicated permission added
- Canonical rehearsal admin API switched to dedicated permission
- Allowlist admin permission unchanged
- Tests and builds pass

**BLOCK (unchanged):**

- Apply endpoint blocked
- Canonical production write blocked
- Worker integration blocked
- Legacy removal blocked
- Production / percent blocked

## 8. Next recommended step

**Option A — P7.10-r7 Canonical Write Readiness Design**

- Docs-only gates before any production write

**Option B — P7.10-r6f4 Stable local rehearsal seed for Ops review**

- Local insert without immediate cleanup + documented cleanup command

**Option C — Web copy alignment (optional small follow-up)**

- Update `P76CanonicalRehearsalPage` / `p76CanonicalRehearsalAdmin.js` 403 text to `VIEW_P76_CANONICAL_REHEARSAL`

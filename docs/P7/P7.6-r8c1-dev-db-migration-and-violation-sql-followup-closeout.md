# P7.6-r8c1 — Dev DB Migration and Violation SQL Follow-up Closeout

> **状态**：**P7.6-r8c1 PASS** — dev/staging migration applied · violation SQL **0** · production **未 apply**  
> **前置**：[P7.6-r8c dry-run audit](./P7.6-r8c-allowlist-apply-dev-dry-run-audit-closeout.md) **PASS**（writer 行为）· migration/violation 曾 **PENDING**  
> **P7.6 plan**：[Bidirectional Mutual Matching System](./P7.6-bidirectional-mutual-matching-system-plan.md)  
> **后续**：[P7.6-r8d](./P7.6-r8d-pm-ops-apply-signoff.md) **PASS** → [P7.6-r8e](./P7.6-r8e-allowlist-sidecar-live-closeout.md) **PASS**

---

## 1. 一句话结论

P7.6-r8c1 补齐 [r8c](./P7.6-r8c-allowlist-apply-dev-dry-run-audit-closeout.md) 中 pending 的 **dev DB migration** 与 **violation SQL** 验证：`p76_allowlist_apply_meta` 在 dev/staging 成功创建（`prisma migrate deploy`），**violation_count / applied_count / rolled_back_count = 0**，表初始 **0 rows**；real DB dry-run audit 复跑后 **5/5 `wouldApply`** · **0 sidecar 写入**；**production migration 未 apply**；主链未改。

---

## 2. 前置状态

| 项 | r8c 结论 | r8c1 补齐 |
|----|----------|-----------|
| Dry-run audit（in-memory writer） | **PASS** | 复用结论，不变 |
| `db:migrate`（`migrate dev`） | **失败**（DB 不可达） | 见 §3（drift 时改用 deploy） |
| 表 `p76_allowlist_apply_meta` | 不存在 | **已创建** |
| Violation SQL | 未执行 | **已执行 · 全 0** |
| **r8d PM/Ops signoff** | 待 DB verification | [r8d](./P7.6-r8d-pm-ops-apply-signoff.md) **PASS** |

---

## 3. Migration 结果

| 项 | 结果 |
|----|------|
| Migration name | `20260517120000_p76_allowlist_apply_meta` |
| `pnpm run db:migrate`（`prisma migrate dev`） | **未使用** — 本地 dev DB 存在 **schema drift**（fixture backup 表等），`migrate dev` 要求 reset |
| `pnpm run db:migrate:deploy`（`prisma migrate deploy`） | **成功** — migration applied |
| `_prisma_migrations` 记录 | `20260517120000_p76_allowlist_apply_meta` @ `2026-05-18T12:51:35Z` |
| `pnpm run db:generate` | **成功** — Prisma Client regenerated |
| 表 `p76_allowlist_apply_meta` | **存在** |
| **production migrate** | **否** |

**Dev/staging 命令（本轮实际）：**

```powershell
# Docker / peima-postgres 在线后
cd packages/database
npx dotenv-cli -e ../../.env -- pnpm run db:migrate:deploy
pnpm run db:generate
```

> **说明**：`migrate deploy` 仅应用 pending migration，**不** reset dev DB，符合 r8c1「dev/staging only、不破坏现有数据」口径。Production 仍须走独立运维流程，本轮 **未执行**。

---

## 4. Table / schema verification

| 检查项 | 结果 |
|--------|------|
| `information_schema.tables` · `p76_allowlist_apply_meta` | **true** |
| 列数 | **32**（与 [r8a](./P7.6-r8a-allowlist-apply-sidecar-schema-design.md) / [r8b](./P7.6-r8b-allowlist-apply-writer-implementation-closeout.md) 一致） |
| 主键 | `p76_allowlist_apply_meta_pkey` (`id`) |
| 唯一约束 | `viewerUserId` + `sourceVersion` |
| 索引 | `viewerUserId` · `selectedCandidateId` · `applied` · `rolledBack` · `createdAt` · `sourceVersion` |
| FK | `viewerUserId` → `users(id)` ON DELETE CASCADE |
| 默认 flags | `applied=false` · `dryRun=true` · `appliedToMatchResult/FinalScore/WorkerRanking=false` |

**关键列（抽样）：** `viewerUserId` · `selectedCandidateId` · `allowlistMatched` · `pmSignoffStatus` · `opsSignoffStatus` · `applied` · `dryRun` · `appliedToPool` · `appliedToMatchResult` · `appliedToFinalScore` · `appliedToDisplay` · `appliedToWorkerRanking` · `rolledBack` · JSONB stage ids · `auditNotes`

---

## 5. Violation SQL 结果

来源：`tmp/p76-r8c-violation-queries.sql`（经 `docker exec -i peima-postgres psql`）

| 查询 | 结果 | 预期 |
|------|------|------|
| `violation_count`（`appliedToMatchResult` / `FinalScore` / `WorkerRanking`） | **0** | 0 |
| `applied_count` | **0** | 0 |
| `rolled_back_count` | **0** | 0 |
| 明细 `SELECT … ORDER BY createdAt DESC` | **0 rows** | 初始可为 0 |
| `nonAllowlistAppliedCount`（audit runner） | **0** | 0 |

---

## 6. Optional real DB dry-run smoke

| 项 | 结果 |
|----|------|
| 执行 | **是** |
| 命令 | `cd apps/api` → `pnpm exec nest build` → `pnpm run p76:r8c-allowlist-apply-dev-dry-run-audit` |
| DB 连通 | `dbReachable=true` · `migrationApplied=true` |
| Phase A（`ENABLED=0`） | 5/5 blocked · `wroteSidecar=false` |
| Phase B（allowlist + `DRY_RUN=1`） | 5/5 `wouldApply=true` · `wroteSidecar=false` · `applied=false` |
| 表行数（audit 前 / 后） | **0 / 0**（无新增写入） |
| Artifact | [artifacts/p76/r8c/dev-dry-run-audit-report.json](../../artifacts/p76/r8c/dev-dry-run-audit-report.json)（已刷新，含 live violation 查询） |

> Writer phase 仍使用 **in-memory mock**（与 r8c 一致）；live DB 用于 **violation 查询** 与 **migration/table 存在性** 确认，**不**触发 sidecar upsert。

---

## 7. 边界确认

| 项 | 状态 |
|----|------|
| 改 `MatchResult` | **否** |
| 改 `finalScore` | **否** |
| 改 worker | **否** |
| 改 `displayCandidateUserId` | **否** |
| 启 percent / production apply | **否** |
| 删除 legacy photo matching | **否** |
| production migration | **否** |
| Sidecar write smoke（`DRY_RUN=0`） | **未执行**（留 r8e / staging signoff） |

---

## 8. PASS / BLOCK

### 8.1 PASS

| # | 标准 | 结果 |
|---|------|------|
| 1 | dev/staging migration applied | **PASS** |
| 2 | 表存在 · schema 对齐 r8a/r8b | **PASS** |
| 3 | violation SQL 全 **0** | **PASS** |
| 4 | 无 main-chain write | **PASS** |
| 5 | production 未 touch | **PASS** |
| 6 | real DB audit · 行数不变 | **PASS** |

### 8.2 BLOCK

无。

**P7.6-r8c1 closeout：** **PASS** — DB verification 完成 → [r8d](./P7.6-r8d-pm-ops-apply-signoff.md) **PASS** → **r8e** sidecar live smoke。

---

## 9. 下一步

| 顺序 | 动作 |
|------|------|
| 1 | ~~**P7.6-r8d**~~ — [PM/Ops apply signoff](./P7.6-r8d-pm-ops-apply-signoff.md) **PASS** |
| 2 | [P7.6-r8e](./P7.6-r8e-allowlist-sidecar-live-closeout.md) — sidecar live closeout **PASS** |
| 3 | Production：`migrate deploy` 须独立运维窗口 · 仍 **blocked** 直至 signoff 链完成 |

---

## 参考

| 文档 / 文件 | 用途 |
|-------------|------|
| [P7.6-r8c](./P7.6-r8c-allowlist-apply-dev-dry-run-audit-closeout.md) | dry-run audit |
| [P7.6-r8b](./P7.6-r8b-allowlist-apply-writer-implementation-closeout.md) | writer + migration 文件 |
| [P7.6-r8a](./P7.6-r8a-allowlist-apply-sidecar-schema-design.md) | schema 设计 |
| `tmp/p76-r8c-violation-queries.sql` | violation SQL |
| [P7.6-r8d](./P7.6-r8d-pm-ops-apply-signoff.md) | PM/Ops signoff |
| [P7.6-r8e](./P7.6-r8e-allowlist-sidecar-live-closeout.md) | sidecar live smoke |
| [P7.6 plan](./P7.6-bidirectional-mutual-matching-system-plan.md) | r8f / r9 |

# P7.10-r11 — Category A legacy photo matching deletion audit

**Date:** 2026-05-19  
**Prerequisites:** `PASS_R9_OLD_PHOTO_MATCHING_WRITER_SHUTDOWN`, `PASS_R10_SAFE_FALLBACK_FINAL`

---

## A. Deleted in r11 (Category A writer / generation)

| Area | Paths | Why safe |
|------|-------|----------|
| Legacy PreviewPool **generate** | `preview-pool-layered-selection.ts`, `visual-signal-enhance-stub.ts`, `dto/generate-preview-pool.dto.ts`, `POST /preview-pool/generate` | r9 blocks worker write; r10 no GET fallback; admin read via `GET …/latest` kept |
| Onboarding photo preview pool **writer** | `onboarding-photo-preview-pool.service.ts`, funnel/active audit modules, `POST/GET …/photo-preview-pool/*` routes | Not used by GET `/matching/result`; onboarding flow skips to questionnaire |
| P7.5 apply signoff dev-cli | `p75-r4-m/o2-*`, `p75-r5-b2/c2-*`, `p75-r4-e-shadow-sample-harness` | Category A ops only |
| Category A tests | `onboarding-photo-preview-pool*.spec`, `preview-pool-layered-selection.spec`, `visual-signal-enhance-stub.spec`, `p75-r5*.spec` | Covered by r8/r9/r10 regression |
| Web legacy UI | `PreviewPoolPage.jsx`, `OnboardingPhotoPreviewPage.jsx`, pool generate API clients | Redirect routes → questionnaire |

---

## B. Preserved (must not delete)

| Area | Paths |
|------|-------|
| MatchResult + GET `/matching/result` | `matching.service.ts`, `matching.controller.ts` |
| Display resolver | `matching-result-display.ts` |
| Safe fallback | `p710-r10-safe-fallback-final-policy.ts`, `matching-result-state.ts` |
| Canonical Apply/Rollback | `p76-canonical-apply*`, `p76-canonical-rollback*`, hidden admin API |
| P76 read path / sidecar | `p76-read-path-display-resolver.ts` |
| Photo upload / review / vision | `onboarding.service.ts` (photo upload), `images/*`, `cloud-vision.*`, `p76-photovisual-first-pool-*` |
| PreviewPool **read-only** | `preview-pool.service.ts` (`findLatestActiveForUser`, shortlist contract), `preview-pool-shortlist-contract.v0.ts` |
| Worker batch-match shell | `batch-match.processor.ts` (early block when r9 gate closed) |
| `computeStyleScore` (shared) | `packages/shared/matching/preference-score.ts` — used by P7.6 vision shadow |
| FinalMatchPage | Core display chain preserved; rematch no longer calls removed `generatePreviewPool` |

---

## C. Follow-up (not r11)

| Item | Reason |
|------|--------|
| Prisma `PreviewPool` / `OnboardingPhotoPreviewPool` **tables** | Schema/migration window — code paths removed, DB rows historical |
| `batch-match.processor.ts` PreviewPool scoring loop | Retained for dev rollback when `PEIMA_P710_R9_*` writer re-enabled in dev only |
| `worker/matching-score.ts` `computeStyleScore` | Still used if rollback re-enables writer path |
| `MatchingWaitingPage` links to read-only `getLatestPreviewPool` | Admin/diagnostic binding only |
| r8i human PM/Ops/Eng signoff | Still **pending** |

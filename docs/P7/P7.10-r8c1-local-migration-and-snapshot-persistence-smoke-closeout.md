# P7.10-r8c1 — Local Migration + Rollback Snapshot Persistence Smoke Closeout

> **状态**：P7.10-r8c1 local dev smoke — **DONE**  
> **结论**：**`PASS_R8C1_LOCAL_MIGRATION_AND_SNAPSHOT_PERSISTENCE_SMOKE`**  
> **前置**：[P7.10-r8c](./P7.10-r8c-rollback-snapshot-persistence-implementation-closeout.md) · [P7.10-r8b](./P7.10-r8b-rollback-snapshot-migration-design.md)  
> **性质**：local dev DB only · repo `.env` only · **无** staging/production migrate · **无** MatchResult / sidecar mutation

---

## 1. 一句话结论

**P7.10-r8c1** 在**本地 dev** 数据库上完成 `p76_canonical_apply_rollback_snapshot` migration 应用与 synthetic snapshot 持久化 smoke：插入 1 行、校验 redaction、清理 fixture 行；**MatchResult** / **sidecar** 计数与样本指纹**未变**；**未**执行 Apply / Rollback / worker。

```text
PASS_R8C1_LOCAL_MIGRATION_AND_SNAPSHOT_PERSISTENCE_SMOKE
```

---

## 2. 修改文件 / Artifacts

| 路径 | 说明 |
|------|------|
| `tmp/p710-r8c1-local-snapshot-persistence-smoke.mjs` | 本地 smoke runner |
| [artifacts/p76/r8c1/local-migration-smoke-summary.json](../../artifacts/p76/r8c1/local-migration-smoke-summary.json) | Migration 摘要 |
| [artifacts/p76/r8c1/local-migration-smoke-summary.md](../../artifacts/p76/r8c1/local-migration-smoke-summary.md) | 人类可读摘要 |
| [artifacts/p76/r8c1/local-snapshot-persistence-smoke.json](../../artifacts/p76/r8c1/local-snapshot-persistence-smoke.json) | 完整 smoke 结果 |
| 本文档 | closeout |

**未修改：** Apply/Rollback API · `MatchResult` schema · staging/production env

---

## 3. Migration result

| 项 | 结果 |
|----|------|
| Command | `prisma migrate deploy`（本地 `.env` · `localhost:5432/peima`） |
| Migration | `20260520100000_p76_canonical_apply_rollback_snapshot` |
| `_prisma_migrations` | **applied** |
| Staging / production migrate | **not run** |
| DATABASE_URL source | repo **`.env` only**（redacted: `postgresql://localhost:5432/peima`） |

---

## 4. Table / index verification

| Check | Result |
|-------|--------|
| Table `p76_canonical_apply_rollback_snapshot` | **exists** |
| Unique `snapshotId` | **yes** |
| Indexes | **8**（pkey · snapshotId_key · sidecarId · matchResultId · viewerUserId · rollbackExpiresAt · rolledBack · promotionStatus） |
| MatchResult DDL in this migration | **none**（additive `CREATE TABLE` only） |

---

## 5. Snapshot persistence smoke result

| Field | Value |
|-------|-------|
| `applyAuditRunId` | `r8c1-local-smoke-20260519` |
| `snapshotId` | `snap-r8c1-local-smoke-20260519` |
| `insertedCount` | **1** |
| `promotionStatus` | `snapshot_created` |
| `rolledBack` | `false` |
| `rollbackTokenHash` | **null**（无明文 token） |
| `baselineFingerprint` | present (64 hex) |
| `beforeCandidateUserId` / `beforeFinalScore` | `cand-before-r8c1` / `0.72` |
| `proposedCandidateUserId` / `proposedFinalScore` | `cand-proposed-r8c1` / `0.88` |
| `beforeMatchInsightsChecksum` | present |
| `beforeMatchInsightsSummaryJson` | summary only · **no** `rawPrompt` / `email` |
| Forbidden raw keys in DB row | **none** |

Runner: `node tmp/p710-r8c1-local-snapshot-persistence-smoke.mjs`（依赖 `apps/api` **build** 后 dist）

---

## 6. Cleanup result

| 项 | 结果 |
|----|------|
| `deleteMany({ applyAuditRunId: "r8c1-local-smoke-20260519" })` | **1** row deleted |
| `finalRowsForAuditRunId` | **0** |

---

## 7. Mutation safety result

| Surface | Before → After |
|---------|----------------|
| `MatchResult` row count | 80 → 80 |
| `MatchResult` sample fingerprint (top 5) | unchanged |
| `p76_canonical_match_result_meta` count | 0 → 0 |
| `appliedToMatchResult=true` | 0 → 0 |
| `appliedToFinalScore=true` | 0 → 0 |
| `appliedToWorkerRanking=true` | 0 → 0 |
| Worker jobs | **not triggered** |

**`mutationSafety.unchanged`:** **true**

---

## 8. Tests / build

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/database run db:generate` | **PASS** |
| `pnpm --filter @peima/api test -- p710-r8c` | **11/11 PASS** |
| `pnpm --filter @peima/api test -- p710-r7g` | **17/17 PASS** |
| `pnpm --filter @peima/api run build` | **PASS** |
| `node tmp/p710-r8c1-local-snapshot-persistence-smoke.mjs` | **PASS** |

---

## 9. PASS / BLOCK

### PASS（本轮）

```text
PASS_R8C1_LOCAL_MIGRATION_AND_SNAPSHOT_PERSISTENCE_SMOKE
```

### BLOCK（未触发）

- staging / production DATABASE_URL  
- MatchResult / finalScore / sidecar `promotionStatus` writes  
- plaintext token · raw matchInsights in DB  
- Apply / Rollback / worker / percent / prod rollout / legacy removal  

---

## 10. Next step

| Priority | Milestone |
|----------|-----------|
| **NEXT** | **[P7.10-r8d](./P7.10-r8d-apply-service-hard-disabled-gate-closeout.md)** — Apply service behind **hard-disabled** env gate（**非** production Apply · default off） |
| **Optional** | Staging `migrate deploy` only after separate Ops signoff（**非** r8c1 范围） |

---

## Related

- [P7.10-r8c](./P7.10-r8c-rollback-snapshot-persistence-implementation-closeout.md)  
- [P7.10-r8b](./P7.10-r8b-rollback-snapshot-migration-design.md)  
- [P7.6 plan](./P7.6-bidirectional-mutual-matching-system-plan.md)

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-19 | 初版本地 smoke · `PASS_R8C1_LOCAL_MIGRATION_AND_SNAPSHOT_PERSISTENCE_SMOKE` |

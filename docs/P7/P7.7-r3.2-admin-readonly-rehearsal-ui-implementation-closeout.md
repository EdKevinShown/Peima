# P7.7-r3.2 — Admin Read-only Rehearsal UI Implementation Closeout

## 1. Status

**P7_7_R3_2_ADMIN_READONLY_REHEARSAL_UI_IMPLEMENTED**

说明：

- Code change: yes — Admin UI only
- No Apply button
- No Admin write API
- No MatchResult write
- No matchInsights write
- No worker integration
- No GET behavior change
- No canonical production write
- No legacy removal
- No production / percent rollout

## 2. What changed

| Area | Path |
|------|------|
| API client | `apps/web/src/api/p76CanonicalRehearsalAdmin.js` |
| Labels | `apps/web/src/utils/p76CanonicalRehearsalLabels.mjs` |
| Labels test | `apps/web/src/utils/p76CanonicalRehearsalLabels.test.mjs` |
| Admin page | `apps/web/src/pages/P76CanonicalRehearsalPage.jsx` |
| Router + nav | `apps/web/src/router/index.jsx` |
| Web test script | `apps/web/package.json` |
| Closeout | `docs/P7/P7.7-r3.2-admin-readonly-rehearsal-ui-implementation-closeout.md` |

## 3. Route

- `/admin/p76/canonical-rehearsal`
- Home footer internal links: **P76 Canonical Rehearsal**

## 4. UI behavior

- **List**: `GET /admin/p76/canonical-rehearsal` → `items` + `pageInfo` (cursor pagination, Load more)
- **Aggregate cards**: `totalVisible`, `eligibleCount`, `blockedCount`, `wouldChangeCandidateCount`, `appliedToMatchResultViolationCount` (P0 banner when violation count &gt; 0)
- **Filters**: auditRunId, sourceVersion, readPathSourceVersion, environment, eligible, guardrailReason, wouldChangeCandidate, appliedToMatchResult, generatedAtFrom/To, matchResultId, viewerUserId, activeOnly, includeDeleted, limit
- **Table**: generatedAt, environment, auditRunId (truncate + copy), sourceVersion, viewerUserId, matchResultId, baseline/proposed candidates, wouldChangeCandidate, eligible, guardrailReason, scoreDeltaBand, appliedToMatchResult, rehearsalStatus, Detail
- **Detail drawer**: `GET /admin/p76/canonical-rehearsal/:id` — baseline vs proposal, guardrails, provenance, safety flags (`isShadowOnly`, `notAppliedToMatchResult`, `readByGetPath`, `readByWorker`), summary + sanitized shadow JSON preview
- **Disabled state**: HTTP 404 when `PEIMA_P76_REHEARSAL_ADMIN_ENABLED` not set → dedicated message
- **Permission/error**: 401/403 surfaced from API client
- **Empty state**: no rows matching filters
- **Safety banner**: sticky red `SHADOW REHEARSAL ONLY — NOT APPLIED TO MATCHRESULT` + Chinese helper
- **Client export**: Export visible JSON (browser download only)

## 5. Safety

- No Apply button
- No write action controls (Promote / Update / Delete / worker / PreviewPool)
- No POST / PATCH / DELETE from this page
- No MatchResult mutation
- No worker trigger
- No PreviewPool disable
- Shadow-only banner on every view; detail drawer repeats read-only context

## 6. Tests / verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/web run build` | PASS |
| `pnpm --filter @peima/web test` | PASS (12 tests incl. 3 rehearsal label tests) |
| `pnpm --filter @peima/api test -- p77-r3-1-admin-readonly-rehearsal-api` | PASS (10 tests) |
| `pnpm --filter @peima/api run build` | PASS |

Local smoke (manual): set `PEIMA_P76_REHEARSAL_ADMIN_ENABLED=1`, seed rows via `p710:r6f3:rehearsal-writer` insert without cleanup, open `/admin/p76/canonical-rehearsal` with admin JWT.

## 7. What did not change

- No backend writer changes
- No Prisma schema / migration
- No production canonical write
- No GET `/matching/result` integration
- No worker changes
- No FinalMatchPage / MatchingWaitingPage / PreviewPool changes
- No Apply Admin API

## 8. PASS / BLOCK

**PASS:**

- Admin UI page implemented
- Read-only API consumed (`list` / `aggregate` / `detail`)
- Feature disabled state handled
- No write controls
- Web + API builds pass; r3.1 API tests pass

**BLOCK:**

- Apply endpoint blocked
- Canonical production write blocked
- Worker integration blocked
- Legacy removal blocked
- Production / percent blocked

## 9. Next recommended step

**Option A — P7.7-r3.3 Local Admin UI smoke with seeded rehearsal rows**

- Run r6f3 insert without cleanup
- Open UI at `/admin/p76/canonical-rehearsal`
- Verify rows + detail drawer
- Cleanup rows after smoke

**Option B — Add dedicated RBAC permission `VIEW_P76_CANONICAL_REHEARSAL`**

- `packages/shared` p5-rbac + API guard migration
- Small code change; decouple from allowlist permission reuse

**Option C — P7.10-r6f4 repeated local insert without cleanup for Ops review**

- Stable local rows for Admin UI review sessions

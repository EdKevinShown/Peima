# P7.10-r3e — Canonical Match Result Sidecar Migration Implementation Closeout

> **前置**：[P7.10-r3c](./P7.10-r3c-canonical-match-result-sidecar-migration-design-review.md) · [P7.10-r3d](./P7.10-r3d-canonical-match-result-sidecar-writer-design.md) · [P7.10-r3f1](./P7.10-r3f1-canonical-match-result-sidecar-writer-dry-run-implementation-closeout.md)  
> **结论**：**`P7_10_R3E_CANONICAL_MATCH_RESULT_SIDECAR_MIGRATION_IMPLEMENTED`**

---

## 1. Status

```text
P7_10_R3E_CANONICAL_MATCH_RESULT_SIDECAR_MIGRATION_IMPLEMENTED
```

**说明：**

- Code change: **yes** — Prisma schema + migration only
- No sidecar writer implementation
- No DB business rows (verification inserts rolled back / none committed)
- No `MatchResult` write
- No `matchInsights` write
- No worker / GET / frontend change
- No Admin Apply
- No production / percent deploy

---

## 2. What changed

| Path | Change |
|------|--------|
| `packages/database/prisma/schema.prisma` | Added `P76CanonicalMatchResultMeta` |
| `packages/database/prisma/migrations/20260519100000_p76_canonical_match_result_meta/migration.sql` | CREATE TABLE + CHECK + indexes |
| `apps/api/test/p710-r3e-canonical-match-result-sidecar-migration.spec.ts` | 14 contract tests |
| `docs/P7/P7.10-r3e-canonical-match-result-sidecar-migration-implementation-closeout.md` | This closeout |

**Not changed:** worker · GET · frontend · sidecar writer · `package.json` · `MatchResult` model fields

---

## 3. Model / table

| Item | Value |
|------|-------|
| Prisma model | `P76CanonicalMatchResultMeta` |
| Table | `p76_canonical_match_result_meta` |
| Idempotency | `auditRunId` + `viewerUserId` + `sourceVersion` (`@@unique`) |
| Environment | `environment` default `dev` · CHECK `dev` \| `staging` |
| Canonical fields | `selectedCandidateId` · `score` · `reasonSummary` |
| JSON payloads | `stageSummary` · `safeFallbackMeta` · `guardrails` · `dryRunPayload` |
| Promotion | `promotionStatus` default `not_promoted` |
| Main-chain flags | `appliedToMatchResult` · `appliedToFinalScore` · `appliedToWorkerRanking` (all default false) |
| Signoffs | `pmSignoffStatus` / `opsSignoffStatus` default `not_required` |
| Lifecycle | `rolledBack` · `supersededAt` · `deletedAt` |

**v1 decisions:**

- **No** `User` relation / **no** FK (align [r6e1](./P7.10-r6e1-rehearsal-sidecar-migration-implementation-closeout.md) rehearsal migration)
- **No** `MatchResult` FK or reverse relation on `MatchResult`

---

## 4. Constraints

### CHECK (PostgreSQL)

| Constraint | Rule |
|------------|------|
| `applied_to_worker_ranking_false` | `appliedToWorkerRanking = false` |
| `mode_allowed` | `dry_run` \| `sidecar` \| `promoted` |
| `promotion_status_allowed` | `not_promoted` \| `promoted` \| `rolled_back` \| `blocked` |
| `applied_match_result_requires_promoted` | `appliedToMatchResult=true` ⇒ `promotionStatus=promoted` |
| `applied_final_score_requires_promoted` | `appliedToFinalScore=true` ⇒ `promotionStatus=promoted` |
| `environment_allowed` | `dev` \| `staging` |
| `rolled_back_promotion_consistent` | `rolledBack=true` ⇒ promotion in `rolled_back` \| `blocked` |

### Unique / indexes

| Type | Definition |
|------|------------|
| Unique | `(auditRunId, viewerUserId, sourceVersion)` |
| Partial unique | Active `mode=sidecar` per `(viewerUserId, sourceVersion)` when not deleted/superseded and `not_promoted` |
| Indexes | `viewerUserId` · `selectedCandidateId` · `matchResultId` · `sourceVersion` · `mode` · `promotionStatus` · `appliedToMatchResult` · `rolledBack` · `createdAt` · `supersededAt` · `deletedAt` · `(environment, createdAt)` |

**Note:** Partial unique exists in SQL only (not expressible in Prisma schema).

---

## 5. Relationship to existing tables

| Table | Relationship |
|-------|--------------|
| `p76_canonical_writer_rehearsal_meta` | **Separate** — rehearsal compare only; not replaced |
| `p76_allowlist_apply_meta` | **Separate** — allowlist apply audit |
| `match_results` | **No FK v1** — `matchResultId` nullable string only |
| Promotion source | Future writer reads **this** table, not rehearsal |

---

## 6. Verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/database run db:generate` | **PASS** |
| `pnpm --filter @peima/database run build` | **PASS** |
| `pnpm --filter @peima/api test -- p710-r3e-canonical-match-result-sidecar-migration` | **PASS** (14 tests) |
| `pnpm --filter @peima/api run build` | **PASS** |
| `pnpm --filter @peima/database exec prisma migrate deploy --schema=./prisma/schema.prisma` | **PASS** (local PostgreSQL) |
| `SELECT COUNT(*)` | **0** rows after migrate |
| CHECK probes (local) | **PASS** — worker ranking / bad mode / applied without promoted / production env all rejected |

Local CHECK verification used ephemeral `node` + `$executeRawUnsafe` (no rows retained).

---

## 7. What did not change

- No sidecar writer (`p76-canonical-match-result-sidecar-writer` **not found** before r3f)
- No `MatchResult` / `matchInsights` mutation
- No worker / GET / frontend
- No Admin Apply / rollback endpoints
- No production deploy / percent
- No PreviewPool disable / legacy removal

---

## 8. PASS / BLOCK

### PASS

- Model and migration added
- CHECK + unique + indexes per [r3c](./P7.10-r3c-canonical-match-result-sidecar-migration-design-review.md)
- Static contract tests pass
- Database + API build pass
- Local migrate deploy + CHECK verification pass

### BLOCK (unchanged program gates)

- Sidecar writer implementation (r3f+)
- `MatchResult` promotion write
- Admin Apply
- Worker / GET integration
- Production / percent (r9e3)
- PreviewPool disable

---

## 9. Next recommended step

**P7.10-r3f** — Sidecar writer dry-run / insert-only implementation

1. `dryRunP76CanonicalMatchResultSidecarWriter` — mapping only, no DB
2. `insertOnlyP76CanonicalMatchResultSidecarWriter` — `p76CanonicalMatchResultMeta.create` only
3. Local smoke (r3h) — `auditRunId` cleanup · `appliedToMatchResultCount=0`

**Do not** promote to `MatchResult` in r3f.

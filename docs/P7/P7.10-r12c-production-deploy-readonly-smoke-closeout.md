# P7.10-r12c — Production Deploy (Gates OFF) + Readonly Smoke Closeout

> **状态**：P7.10-r12c production rollout execution — **BLOCKED (preflight)**  
> **结论**：**`BLOCK_R12C_PRODUCTION_PREFLIGHT`**  
> **前置**：[P7.10-r12a](./P7.10-r12a-production-readiness-human-signoff.md) **`PASS_R12A_PRODUCTION_READINESS_HUMAN_SIGNOFF`** · [P7.10-r12b](./P7.10-r12b-production-rollout-plan-runbook.md) **`DONE_R12B_PRODUCTION_ROLLOUT_PLAN_RUNBOOK`**  
> **Runner**：`tmp/p710-r12c-production-readonly-smoke.mjs`  
> **Artifact**：[production-readonly-smoke-result.json](../../artifacts/p76/r12c/production-readonly-smoke-result.json)

---

## 1. 一句话结论

**r12a PASS** 与 **r12b plan DONE** 已满足；本工作区 **未配置** customer production API / readonly DB 凭证，**未执行** production deploy 与 production readonly HTTP/DB smoke。自动化 preflight 输出 **`BLOCK_R12C_PRODUCTION_PREFLIGHT`**。Runner 与 closeout 模板已就绪；**PASS** 须由操作者在 Render 完成 deploy（gates off）后，带 production env 重跑 runner。

```text
BLOCK_R12C_PRODUCTION_PREFLIGHT
```

**升级至 `PASS_R12C_PRODUCTION_DEPLOY_READONLY_SMOKE`：** 操作者完成 P1 deploy + 设置 `PEIMA_P710_R12C_DEPLOY_COMPLETED=1` + 重跑 runner 全绿。

---

## 2. Preflight 结果

| # | 检查 | 结果 |
|---|------|------|
| 1 | r12a `PASS_R12A_PRODUCTION_READINESS_HUMAN_SIGNOFF` | ✅（artifact） |
| 2 | r12b `DONE_R12B_PRODUCTION_ROLLOUT_PLAN_RUNBOOK` | ✅（artifact） |
| 3 | Production env 已确认（customer prod API + readonly DB） | ❌ **未配置** |
| 4 | All mutation switches OFF（本地 runner 读取 env 默认） | ✅ `gatesAllSafe: true` |
| 5 | Old writer shutdown ON | ✅ |
| 6 | Old writer enabled OFF | ✅ |
| 7 | percent = 0 | ✅ |
| 8 | hidden mutation API = 0 | ✅ |
| 9 | Apply execution = 0 | ✅ |
| 10 | Rollback execution = 0 | ✅ |
| 11 | DATABASE_URL **未**写入 artifact | ✅ |
| 12 | Owners 已填写 | ✅ Zhan Zhang（rollout / ops / eng / oncall） |
| 13 | Monitoring window | ✅ 60 min（`PEIMA_P710_R12C_MONITORING_WINDOW_MINUTES`） |
| 14 | No customer production write planned | ✅ |

**BLOCK 原因：**

- `PEIMA_PROD_API_BASE_URL` **missing**
- `PEIMA_PRODUCTION_READONLY_DATABASE_URL` / `PEIMA_READONLY_DATABASE_URL` **missing**
- 工作区无 `.env.production`（预期：凭证仅存在于 operator secret store / Render）

---

## 3. Production deploy 结果

| 项 | 结果 |
|----|------|
| Deploy 执行（P1） | **NOT_RUN** — 须 Ops 在 Render 手动 deploy |
| `PEIMA_P710_R12C_DEPLOY_COMPLETED` | **false** |
| API / Worker / Web 产物 | 来自本地 r11b 已验证构建；**未**推送到 production |

**Deploy 后必须复核（Render dashboard，不写 artifact）：** [r12b §5](./P7.10-r12b-production-rollout-plan-runbook.md) 全部 gate 变量为 OFF/0。

---

## 4. Readonly smoke 结果

| 检查 | 结果 |
|------|------|
| Production HTTP smoke | **NOT_RUN**（无 API base URL） |
| Production readonly DB smoke | **NOT_RUN**（无 readonly DB URL） |
| Static repo checks（本地） | ✅ redirect · generate removed · FinalMatchPage `getMatchingResult` |

**计划在 PASS 路径执行的 smoke（r12c）：**

1. API health（`GET /`）  
2. `POST /preview-pool/generate` → 404/405/401  
3. Readonly DB：`match_results` count · hook pending · `appliedToWorkerRanking` violations = 0  
4. （可选）`GET /matching/result` 需 operator JWT — **viewer id 不入 artifact**

---

## 5. Monitoring observation 结果

| 项 | 结果 |
|----|------|
| P3 watch window | **NOT_RUN**（deploy 未执行） |
| 计划窗口 | 60 minutes |
| 委托 | 操作者在 deploy 后按 r12b §9 观察 5xx / matching API / worker |

---

## 6. All-switches-off proof

本地 runner 读取的 **process env 默认**（非 production pod 实测）：

| Gate | Value |
|------|-------|
| `PEIMA_P710_R9_OLD_PHOTO_MATCHING_WRITER_SHUTDOWN_ENABLED` | **1** |
| `PEIMA_P710_R9_OLD_PHOTO_MATCHING_WRITER_ENABLED` | **0** |
| `PEIMA_P710_R8_HIDDEN_ADMIN_MUTATION_API_ENABLED` | **0** |
| `PEIMA_P710_R8_APPLY_EXECUTION_ENABLED` | **0** |
| `PEIMA_P710_R8_ROLLBACK_EXECUTION_ENABLED` | **0** |
| `PEIMA_P76_PRODUCTION_PERCENT` | **0** |
| `PEIMA_P76_PRODUCTION_PERCENT_ENABLED` | **0** |

**注意：** Production pod 须在 deploy 后由 Ops **再次**核对 Render env（artifact 仅记录布尔快照，无 URL）。

---

## 7. No customer write proof

| 项 | 结果 |
|----|------|
| `customerProductionWritePlanned` | **false** |
| Runner DB 写入 | **none**（仅 `$queryRaw` SELECT when URL present） |
| Apply/Rollback 执行 | **false** |

---

## 8. Old writer shutdown proof

| 项 | 结果 |
|----|------|
| Shutdown enabled | **true**（env 默认） |
| Writer enabled | **false** |
| r9 tests（prior） | PASS |

---

## 9. percent=0 proof

| 项 | 结果 |
|----|------|
| `PEIMA_P76_PRODUCTION_PERCENT` | **0** |
| `PEIMA_P76_PRODUCTION_PERCENT_ENABLED` | **false** |

---

## 10. Apply/Rollback not executed proof

| 项 | 结果 |
|----|------|
| Apply execution | **false** |
| Rollback execution | **false** |
| Hidden mutation API | **false** |
| r8h staging Apply/Rollback | 历史证据 only；**本轮未执行** |

---

## 11. Protected core proof

| 能力 | 状态 |
|------|------|
| MatchResult / GET `/matching/result` | 保留（prior r11b tests） |
| FinalMatchPage `getMatchingResult` | 静态检查 ✅ |
| Safe fallback r10 | 保留 |
| Photo upload / review / Cloud Vision | 保留 |
| PreviewPool read-only latest | 保留 |
| Category A legacy | 已删除（静态 ✅） |

---

## 12. Security scan

```powershell
git diff | Select-String -Pattern "postgresql://|JWT_SECRET=|Bearer ey|DATABASE_URL=|api_key|apiKey|tokenPlaintext|rollbackTokenPlaintext"
git diff --cached | Select-String -Pattern "..."
```

**结果：** 已跟踪 diff **无** secret 命中。Artifact **无** full DATABASE_URL / JWT / token / user id。

---

## 13. PASS / BLOCK

| 判定 | 状态 |
|------|------|
| **BLOCK** | `BLOCK_R12C_PRODUCTION_PREFLIGHT` |
| **PASS** | 待操作者完成 production deploy + 配置 env 后重跑 runner |

---

## 14. Operator 复现（达到 PASS）

```powershell
cd C:\Users\34713\Desktop\peima

# 1) Ops: Render deploy API + Worker + Web（gates off per r12b §5）

# 2) Operator env（secrets 勿提交）
$env:PEIMA_P710_R12C_PRODUCTION_SMOKE_ALLOWED="1"
$env:PEIMA_PROD_API_BASE_URL="https://<customer-production-api-host>"
$env:PEIMA_PRODUCTION_READONLY_DATABASE_URL="<readonly-role-url>"
$env:PEIMA_P710_R12C_MONITORING_WINDOW_MINUTES="60"
# Optional after deploy:
$env:PEIMA_P710_R12C_DEPLOY_COMPLETED="1"

# 3) Run runner
node tmp/p710-r12c-production-readonly-smoke.mjs

# 4) P3: 60min monitoring per r12b
```

---

## 15. Next step

| Priority | 动作 |
|----------|------|
| **Ops** | Production deploy（gates off）+ Render env 复核 |
| **Ops/Eng** | 配置 production URL 并重跑 runner → `PASS_R12C_…` |
| **Docs** | 更新本 closeout 状态为 PASS（重跑后） |
| **DONE** | [P7.11-r1](./P7.11-r1-production-matching-observability-closeout.md) — `GET /admin/matching-observability/summary`（与 M4.4 CLI 同口径） |

**仍禁止：** percent · production Apply/Rollback · hidden mutation enable · old writer re-enable

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-20 | r12c runner + closeout · `BLOCK_R12C_PRODUCTION_PREFLIGHT`（no prod creds in workspace） |

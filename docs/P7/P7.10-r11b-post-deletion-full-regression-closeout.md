# P7.10-r11b — Post-deletion full regression closeout

**Milestone:** P7.10-r11b  
**Date:** 2026-05-19  
**Status:** `PASS_R11B_POST_DELETION_FULL_REGRESSION`  
**Artifact:** [post-deletion-full-regression-summary.json](../../artifacts/p76/r11b/post-deletion-full-regression-summary.json)  
**Precondition:** [r11 Category A deletion](./P7.10-r11-category-a-legacy-photo-matching-deletion-closeout.md) `PASS_R11_CATEGORY_A_LEGACY_PHOTO_MATCHING_DELETION`

---

## 1. One-line conclusion

After r11 Category A legacy photo matching deletion, **required local regression (builds + targeted suites) PASS**; protected core intact; deleted routes/pages have **no dangling imports**; security scan shows **no newly added secrets**. r8i PM/Ops/Eng human signoff remains **pending** (not faked).

---

## 2. Regression matrix

| Area | Command | Result | Notes |
|------|---------|--------|-------|
| API safe fallback (r10) | `pnpm --filter @peima/api test -- p710-r10-safe-fallback-final` | **PASS** (12) | no row + writer shutdown → `matching_pending` / `no_result`; no PreviewPool fallback write |
| API matching display | `pnpm --filter @peima/api test -- matching-result-display` | **PASS** (45) | `resolveMatchResultDisplay` + GET `/matching/result` |
| API r8f hidden admin API | `pnpm --filter @peima/api test -- p710-r8f` | **PASS** (34) | Apply/Rollback wiring; no token plaintext in responses |
| API r8e rollback token | `pnpm --filter @peima/api test -- p710-r8e` | **PASS** (33) | Token-gated rollback service |
| API r8d apply hard-disabled | `pnpm --filter @peima/api test -- p710-r8d` | **PASS** (20) | Apply service default blocked |
| API build | `pnpm --filter @peima/api run build` | **PASS** | `nest build` after r11 deletions |
| Worker r9 writer shutdown | `pnpm --filter @peima/worker test -- p710-r9-old-photo-matching-writer-shutdown` | **PASS** (7) | Default shutdown on; dev rollback isolated |
| Worker build | `pnpm --filter @peima/worker run build` | **PASS** | `tsc` after `batch-match.processor` gate |
| Web build | `pnpm --filter @peima/web run build` | **PASS** | Vite prod build; `FinalMatchPage` without deleted pool pages |
| Onboarding nextStep (r11) | `pnpm --filter @peima/api test -- onboarding.service.spec.ts` | **PASS** (16) | Spec updated: aesthetic done + no preview ack → `questionnaire` |

### Optional full suites

| Area | Command | Result | Notes |
|------|---------|--------|-------|
| API full | `pnpm --filter @peima/api test` | **PARTIAL** (2 suites / 3 tests fail) | **Unrelated** to r11b required regression — see §7 |
| Worker full | `pnpm --filter @peima/worker test` | **PASS** (8 suites, 95 tests) | All green |

---

## 3. Protected core proof

| Capability | Status | Evidence |
|------------|--------|----------|
| **MatchResult** model / persistence | Preserved | No r11 deletion of Prisma model or matching finalize core tables |
| **GET `/matching/result`** | Preserved | `matching-result-display` suite PASS (45) |
| **`resolveMatchResultDisplay`** | Preserved | Same suite + `FinalMatchPage` uses display fields from API |
| **FinalMatchPage main display** | Preserved | Web build PASS; page loads via `getMatchingResult(userId)` only |
| **r10 safe fallback** | Preserved | `p710-r10-safe-fallback-final` PASS (12); policy file unchanged in r11b |
| **Canonical Apply/Rollback (r8)** | Preserved | r8d/e/f suites PASS (87 total) |
| **Photo upload / review / Cloud Vision** | Preserved | `onboarding.service.spec.ts` photo gate tests PASS; vision modules not in Category A deletion |
| **PreviewPool read-only** | Preserved | `GET /preview-pool/user/:id/latest` + `findLatestActiveForUser` in controller/service |

---

## 4. Deleted route / page reference proof

Repo-wide grep (`*.ts`, `*.tsx`, `*.js`, `*.jsx`) for deleted symbols:

| Check | Result |
|-------|--------|
| `PreviewPoolPage` | **0 references** (file deleted) |
| `OnboardingPhotoPreviewPage` | **0 references** (file deleted) |
| `generatePreviewPool` / `POST /preview-pool/generate` | **0 references**; controller comment documents removal |
| `onboarding-photo-preview-pool` service / provider | **0 references**; module provider removed |

**Router (legacy URLs):**

- `/preview-pool` → `LegacyPhotoPreviewRedirect` → `/questionnaire`
- `/onboarding/photo-preview` → same redirect

`MatchingWaitingPage` / `UserImagesPage` may still link to `/preview-pool?userId=…` for **read-only diagnostic** navigation; redirect is safe (no writer page).

**FinalMatchPage:** uses `getMatchingResult` only; rematch no longer calls removed `generatePreviewPool`.

**Onboarding:** `onboarding.service.ts` skips `photo_preview` → `questionnaire` when aesthetic done; `savePhotoPreferences` / photo upload paths unchanged.

---

## 5. Security scan

**Command (PowerShell):**

```powershell
git diff | Select-String -Pattern "postgresql://|JWT_SECRET=|Bearer ey|DATABASE_URL=|api_key|apiKey|tokenPlaintext|rollbackTokenPlaintext"
```

**Result:** Hits only on **deleted** lines in `visual-signal-enhance-stub.ts` (removed `api_key` redaction / LLM `apiKey` env reads). **No newly added secrets** in diff. **No active secret exposure** from r11b changes.

**Untracked files:** No `.env` or credential files in `git status --short` for this milestone.

---

## 6. Git status summary (not committed)

Working tree mixes **r8–r11 implementation** + closeout docs (user requested no commit in r11b).

**Modified (high level):** API preview-pool/onboarding/matching, worker `batch-match.processor`, web router/pages/FinalMatchPage, roadmap, several P7.10 docs.

**Deleted:** Category A files listed in [r11 closeout](./P7.10-r11-category-a-legacy-photo-matching-deletion-closeout.md).

**Untracked (examples):** `p710-r8d/e/f/r10/r9` specs & services, r8–r11 closeout docs, `artifacts/p76/r8g|r8h|r8i`, **this r11b artifact**.

**r11b-only code fix:** `apps/api/test/onboarding.service.spec.ts` — align expectation with r11 `questionnaire` nextStep.

---

## 7. PASS / BLOCK

### PASS (r11b required)

- Required tests/build **PASS** (matrix §2)
- No dangling imports for deleted pages/services/routes
- Old PreviewPool **writer** not re-enabled (r9 default + r10 fallback tests)
- No PreviewPool formal fallback write to MatchResult
- Protected core intact
- No secrets added

### BLOCK criteria — **none triggered**

### Unrelated historical API full-suite failures (do not block r11b)

1. **`test/rrm-sim.evaluator.spec.ts`** — allowlist expects only 5 files to reference `evaluateRrmSimFromSimulationV2`; `git grep` also finds `rrm-sim-d-pre-calibration-shadow.ts` and `tools/m13-*.ts`. **Not** Category A / preview-pool.
2. **`test/matching-finalize-with-pairwise.spec.ts`** — Nest cannot resolve `MatchingRrmRankingProposalService` in test module. **Pre-existing** harness gap, not caused by r11 file deletion.

---

## 8. Human signoff note

**P7.10-r8i** PM/Ops/Eng signoff for Apply/Rollback production readiness remains **`READY_FOR_R9_WRITER_SHUTDOWN_REVIEW_PENDING_HUMAN_SIGNOFF`** — **pending**, not recorded as signed in this closeout.

---

## 9. Next step

**P7.10-r11c** — staging smoke + final closeout (staging only in r11c; no production rollout / percent enable in r11b).

# P7.7-r3.5 — Web Permission Copy Alignment Closeout

## 1. Status

```text
P7_7_R3_5_WEB_PERMISSION_COPY_ALIGNMENT_COMPLETE
```

说明：

- Code change: yes — Web copy only
- No API behavior change
- No RBAC logic change (`p5-rbac.ts` unchanged)
- No DB write
- No MatchResult / matchInsights write
- No worker / GET integration
- No Apply endpoint
- No production / percent rollout

## 2. What changed

| Path | Change |
|------|--------|
| `apps/web/src/pages/P76CanonicalRehearsalPage.jsx` | Admin banner permission text |
| `apps/web/src/api/p76CanonicalRehearsalAdmin.js` | HTTP 403 default message |
| `docs/P7/P7.7-r3.5-web-permission-copy-alignment-closeout.md` | This closeout |

**Not changed:** `P76AllowlistApplyMetaPage.jsx`, `p76AdminAllowlistApplyMeta.ts` (still `VIEW_P76_ALLOWLIST_APPLY_META`).

## 3. Copy alignment

| Location | Before | After |
|----------|--------|-------|
| Page admin banner | `VIEW_P76_ALLOWLIST_APPLY_META` | `VIEW_P76_CANONICAL_REHEARSAL` + Chinese helper line |
| API client 403 fallback | `VIEW_P76_ALLOWLIST_APPLY_META` | `VIEW_P76_CANONICAL_REHEARSAL` (canonical rehearsal read-only) |

Allowlist admin UI/API copy unchanged.

No functional or routing changes.

## 4. Tests / verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/web test` | PASS (12) |
| `pnpm --filter @peima/web run build` | PASS (chunk size warning only) |
| `pnpm --filter @peima/api test -- p77-r3-1-admin-readonly-rehearsal-api p76-canonical-rehearsal-admin.controller` | PASS (18) |
| `pnpm --filter @peima/api run build` | PASS |

## 5. PASS / BLOCK

**PASS:**

- Canonical rehearsal Web permission copy aligned with r3.4 RBAC
- Tests / builds pass

**BLOCK (unchanged):**

- Apply endpoint blocked
- Canonical production write blocked
- Worker integration blocked
- Legacy removal blocked
- Production / percent blocked

## 6. Next recommended step

**P7.10-r7 — Canonical Write Readiness Design (docs-only)**

- Define gates before any production canonical writer / Apply path

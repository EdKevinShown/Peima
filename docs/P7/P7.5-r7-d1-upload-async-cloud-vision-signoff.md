# P7.5-r7-d1 — Upload Async Live Cloud Vision Signoff

> **状态**：**PASS**（1 张 live upload async 样本 · **非** 多图批量签收）  
> **前置**：[P7.5-r7-d live backfill signoff](./P7.5-r7-d-staging-cloud-vision-50-images-signoff.md) · [P7.5-r7-c2 async upload sidecar](./P7.5-r7-c2-cloud-vision-async-sidecar-closeout.md) · [P7.5-r7-d runbook](./P7.5-r7-d-staging-cloud-vision-50-images-runbook.md)  
> **quality/scene fix**：[P7.5-r7-d2 closeout](./P7.5-r7-d2-quality-scene-tags-fix-closeout.md) **PASS**

---

## 1. 一句话结论

**P7.5-r7-d1** 验证真实 **upload async cloud vision** 路径：用户上传后接口快速返回，随后 **async job** 调用 **live Zhipu**，并把 `detectionScoreJson.vision` 更新为 `provider=cloud` / `sourceVersion=p7.5-r7-cloud-zhipu-v1` / `visionStatus=ok`。本轮 **1 张**样本 **PASS**。

---

## 2. 前置

| 项 | 状态 |
|----|------|
| **P7.5-r7-d** | live 5-image backfill dryRun + write **PASS**（`p7.5-r7-cloud-zhipu-v1`，`cloudHttpSuccessCount=5`） |
| **P7.5-r7-c2** | upload async sidecar（`scheduleAfterUpload` · `runCloudVisionFacadeAsync`）已实现 |
| **P7.5-r7-d1 执行计划** | gate / env / SQL 验证步骤已确认 |
| **API 进程** | 已加载 live env 并 **重启** 后复测 |
| **Allowlist 策略** | **userId allowlist**（新上传 `imageId` 事先未知，**不用**仅 `IMAGE_IDS` 测首传） |

---

## 3. Env 摘要（无 API key）

| 环境变量 | 值 |
|----------|-----|
| `PEIMA_ONBOARDING_VISION_ENABLED` | `1` |
| `PEIMA_ONBOARDING_VISION_PROVIDER` | `cloud` |
| `PEIMA_ONBOARDING_VISION_CLOUD_VENDOR` | `zhipu` |
| `PEIMA_ONBOARDING_VISION_CLOUD_DRY_RUN` | `0` |
| `PEIMA_ONBOARDING_VISION_CLOUD_HTTP_ENABLED` | `1` |
| `PEIMA_ONBOARDING_VISION_CLOUD_ASYNC` | `1` |
| `PEIMA_ONBOARDING_VISION_BASE_URL` | `https://open.bigmodel.cn/api/paas/v4/chat/completions` |
| `PEIMA_ONBOARDING_VISION_MODEL` | `glm-4.5v` |
| `PEIMA_ONBOARDING_VISION_CLOUD_ALLOWLIST_USER_IDS` | `cmo7ksq8s00006znosryc9k0n`（测试上传用户） |
| `PEIMA_ONBOARDING_VISION_CLOUD_ALLOWLIST_IMAGE_IDS` | **空** |
| `PEIMA_ONBOARDING_VISION_APPLY_TO_POOL` | `0` |
| `PEIMA_ONBOARDING_VISION_APPLY_PERCENT` | `0` |

API key 已配置于运行环境；**不**写入本文档。

---

## 常见误配：BASE_URL 缺少 /chat/completions

当前 `ZhipuCloudVisionAdapter` 会将 `PEIMA_ONBOARDING_VISION_BASE_URL` **原样**作为 `fetch` URL，**不会**自动追加 `/chat/completions`。

**错误配置**：

```env
PEIMA_ONBOARDING_VISION_BASE_URL=https://open.bigmodel.cn/api/paas/v4
```

**正确配置**：

```env
PEIMA_ONBOARDING_VISION_BASE_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
```

**影响**：如果只配置到 `/api/paas/v4`，live HTTP 可能返回非 2xx，并在 audit / vision `fallbackReason` 中表现为：

- `cloud_zhipu_http_404`
- `cloud_zhipu_http_400`
- 或旧版本中的 `cloud_zhipu_http_error`

**处理**：修改 `.env` 后**必须重启 API 进程**，例如：

```bash
pnpm dev:api
```

**边界**：本修正只影响 Cloud Vision live HTTP endpoint 配置；**不改** worker / MatchResult / finalScore / RRM；**不启** APPLY_TO_POOL / percent。

---

## 4. Upload 验证步骤

| 步骤 | 记录 |
|------|------|
| 端点 | `POST /images/upload` |
| 认证 | JWT + `multipart/form-data`（`file` + `userId`） |
| 上传主链 | HTTP **201** |
| 响应耗时 | **~0.41s**（`TIME≈0.410451s`，**不**等待 live Zhipu 完成） |
| **T0**（上传响应/建库瞬间） | `vision` 为 **rules 占位**（live defer 设计） |
| **T+约 5s**（DB） | `updatedAt` 晚于 `createdAt`；`vision` 更新为 **live cloud** |

---

## 5. DB verification（PASS 样本）

**imageId**：`cmp8qgb6y00006z7kxvnr04js`

| 字段 | 值 |
|------|-----|
| createdAt | `2026-05-16 19:22:08.938` |
| updatedAt | `2026-05-16 19:22:13.912` |
| detectionStatus | `passed` |
| reviewStatus | `not_required` |
| provider | `cloud` |
| sourceVersion | `p7.5-r7-cloud-zhipu-v1` |
| visionStatus | `ok` |
| fallbackUsed | `false` |
| fallbackReason | `null` |
| warnings | `["VISION_CLOUD_HTTP_GATE_LIVE", "VISION_CLOUD_ADAPTER_ZHIPU"]` |
| photoVisualTags | `["文艺温柔"]` |
| qualityTags | `[]`（空） |
| sceneTags | `[]`（空） |

---

## 6. 失败样本记录（gate 配置修正，不计入 PASS）

**imageId**：`cmp8qaus000006zm40tz96ew8`

| 字段 | 值 |
|------|-----|
| upload HTTP | **201**（上传成功） |
| provider | `cloud` |
| sourceVersion | `p7.5-r7-cloud-dry-run-v1` |
| warnings | `VISION_CLOUD_HTTP_GATE_MOCK`, `VISION_CLOUD_GATE_ALLOWLIST_MISS` |

**根因**：将 `userId` 误放在 `PEIMA_ONBOARDING_VISION_CLOUD_ALLOWLIST_IMAGE_IDS`，**未**配置 `CLOUD_ALLOWLIST_USER_IDS`；`scheduleAfterUpload` 时 gate 为 **mock**，async live 未触发（或走 mock/dry-run vision）。

**处理**：

1. 设置 `PEIMA_ONBOARDING_VISION_CLOUD_ALLOWLIST_USER_IDS=cmo7ksq8s00006znosryc9k0n`
2. 清空 `PEIMA_ONBOARDING_VISION_CLOUD_ALLOWLIST_IMAGE_IDS`（或仅保留 backfill 用 id，与上传用户无关）
3. **重启 API** 使 env 生效
4. **重新上传**新图（新 `imageId`）→ PASS 样本 `cmp8qgb6y00006z7kxvnr04js`

---

## 7. PASS 标准（本轮核对）

| # | 标准 | PASS 样本 |
|---|------|-----------|
| 1 | upload HTTP **201** | ✓ |
| 2 | 上传响应 **不等待** live vision（~0.41s） | ✓ |
| 3 | `updatedAt` **晚于** `createdAt` | ✓ |
| 4 | `provider=cloud` | ✓ |
| 5 | `sourceVersion=p7.5-r7-cloud-zhipu-v1` | ✓ |
| 6 | `visionStatus=ok` | ✓ |
| 7 | `fallbackUsed=false` | ✓ |
| 8 | `reviewStatus` / `detectionStatus` **未因 vision 改变** | ✓（`passed` / `not_required`） |
| 9 | **无** `VISION_CLOUD_HTTP_GATE_MOCK` | ✓ |
| 10 | **无** `VISION_CLOUD_GATE_ALLOWLIST_MISS` | ✓ |

---

## 8. 观察

| 项 | 说明 |
|----|------|
| **photoVisualTags** | 已成功写入（如 `文艺温柔`），upload async live 通路有效 |
| **qualityTags / sceneTags** | 仍为空；与 [r7-d signoff](./P7.5-r7-d-staging-cloud-vision-50-images-signoff.md) 一致 |
| **后续** | **P7.5-r7-d2** prompt / parser / taxonomy 优化 |
| **阻塞** | **不阻塞** d1 upload async path signoff |

---

## 9. 边界确认

| 项 | 状态 |
|----|------|
| worker | **未改** |
| MatchResult | **未改** |
| finalScore | **未改** |
| RRM | **未改** |
| APPLY_TO_POOL | **未启** |
| percent rollout | **未启** |
| DB migration | **无** |
| AI 美化 / AI 头像 | **未做** |
| 人脸识别 / 颜值评分 / 性别判断 | **未做** |
| API key / base64 / prompt 写入 docs 或 DB | **无** |

---

## 10. 结论

**P7.5-r7-d1 upload async live cloud vision signoff PASS.**

---

## 11. 后续

| 项 | 说明 |
|----|------|
| **可选** | 再补 **3–5 张** upload async live 小样本（同 userId allowlist） |
| **P7.5-r7-d2** | [qualityTags / sceneTags fix closeout](./P7.5-r7-d2-quality-scene-tags-fix-closeout.md) **PASS** |
| **P7.5-r7-e** | [Tag Quality Audit + PM spot-check](./P7.5-r7-e-tag-quality-audit-and-pm-spotcheck.md) **PASS** |
| **P7.5-r7-d 全量 50** | 仍按 [runbook](./P7.5-r7-d-staging-cloud-vision-50-images-runbook.md)（可选扩展） |
| **P7.6** | [Photo-first pipeline](./P7.6-bidirectional-mutual-matching-system-plan.md) · **P7.6-r1** legacy audit |

# P7.6-r7c4 — Fixture Preparation Route A Closeout

> **状态**：**Route A 执行完成** · **1** viewer `recommendedForBatch=true` · **`seedDependent=true`**  
> **前置**：[P7.6-r7c3 runbook](./P7.6-r7c3-fixture-preparation-execution-runbook.md) · [P7.6-r7c2 plan](./P7.6-r7c2-multi-sample-fixture-preparation-plan.md)  
> **P7.6 plan**：[Bidirectional Mutual Matching System](./P7.6-bidirectional-mutual-matching-system-plan.md)  
> **后续**：[P7.6-r7d](./P7.6-r7d-multi-sample-audit-first-batch-closeout.md) **technical audit PASS**（含 cmp70）

---

## 1. 一句话结论

在 **dev DB** 按 [r7c3 Route A](./P7.6-r7c3-fixture-preparation-execution-runbook.md) 补齐 **`cmp70ft7a001e6znksjb2jo03`** 后，该 viewer **`recommendedForBatch=true`**（全库计数 **1**）。技术冒烟：**r4b / r5b / r6b（fixture-aligned）PASS**；**r3b live** 仅选出 **1** 人（gender/preference gate）。**`seedDependent=true`** — 不可作 PM PASS 唯一依据；**仍需 Route B 或 C** 达到 ≥3 viewers。

---

## 2. 执行前状态

| 字段 | 值 |
|------|-----|
| `viewerUserId` | `cmp70ft7a001e6znksjb2jo03` |
| `hasLiveVision` | `true` |
| `hasPreference` | `true` |
| `nonNull20DCount` | **0**（无 `user_profile` 行） |
| `candidatePoolCount` | **6** |
| `candidateWithVisionCount` | **6** |
| `candidateWith20DProfileCount` | **4** |
| `candidateWithPreferenceCount` | **4** |
| `hasMatchResult` | `false` |
| `hasM6RrmTop2` | `false` |
| `recommendedForBatch` | **`false`** |

---

## 3. 执行的 SQL / fixture 操作

**环境：** 本地 Docker `peima-postgres` · database `peima`  
**脚本目录：** `tmp/p76-r7c4-*.sql`（仓库根目录）

| 步骤 | 操作 | 文件 |
|------|------|------|
| 1 | 创建 backup 表 | `tmp/p76-r7c4-01-backup.sql` |
| 2 | INSERT viewer `user_profile` + UPDATE 2 候选 20D | `tmp/p76-r7c4-02a-profile.sql` |
| 3 | INSERT 2 候选 `user_preferences` | `tmp/p76-r7c4-02b-pref816.sql` · `02c-pref026.sql` |

### 3.1 Backup 结果

| 备份表 | 行数 |
|--------|------|
| `p76_r7c3_user_profile_backup` | **6** |
| `p76_r7c3_user_preferences_backup` | **5** |

**说明：** seed 前 viewer **无** `user_profile` 行，故 profile backup **不含** `cmp70ft7a001e6znksjb2jo03`；viewer **已有** `user_preferences`（`cmp73xji700006zh443zwn85o`），**未修改**。

### 3.2 实际写入的 userId

| `userId` | 操作 | 备注 |
|----------|------|------|
| `cmp70ft7a001e6znksjb2jo03` | **INSERT** `user_profile` | `id=dev-p76-r7c4-profile-cmp70` · 6 核心维 + 全量 20D 数值 |
| `cmr4hm000816z64demo00m04a` | **UPDATE** `user_profile` + **INSERT** `user_preferences` | `pref id=dev-p76-r7c3-pref-cmr4hm000816` |
| `cmr4hm010026z64demo00m01a` | **UPDATE** `user_profile` + **INSERT** `user_preferences` | `pref id=dev-p76-r7c3-pref-cmr4hm010026` |

**未改动：** 池内另外 4 人（已满足 20D + preference）— `cmfemn00100016z64seed0001` · `cmfemn002000306z64seed0002` · `cmfemn003000506z64seed0003` · `cmr4hm001016z64demo00m05a`

### 3.3 执行前池内诊断（缺 2 人）

| `candidateUserId` | 执行前 `non_null_core` | 执行前 `has_preference` |
|---------------------|------------------------|-------------------------|
| `cmr4hm000816z64demo00m04a` | **0** | **f** |
| `cmr4hm010026z64demo00m01a` | **0** | **f** |
| 其余 4 人 | **6** | **t** |

---

## 4. Verification SQL 结果

### 4.1 池内 6 人（执行后）

| `candidateUserId` | vision | core_dims | preference |
|-------------------|--------|-----------|------------|
| 全部 6 人 | **t** | **6** | **t** |

### 4.2 单 viewer（r7c §4 门槛）

| 字段 | 执行后 |
|------|--------|
| `hasLiveVision` | `true` |
| `nonNull20DCount` | **6** |
| `hasPreference` | `true` |
| `candidatePoolCount` | **6** |
| `candidateWithVisionCount` | **6** |
| `candidateWith20DProfileCount` | **6** |
| `candidateWithPreferenceCount` | **6** |
| `hasMatchResult` | `false` |
| `hasM6RrmTop2` | `false` |
| **`recommendedForBatch`** | **`true`** |

### 4.3 全库 r7c viewer selection 重跑

| 指标 | 值 |
|------|-----|
| `recommendedForBatch=true` 计数 | **1** |
| 唯一行 | `cmp70ft7a001e6znksjb2jo03` |

**仍不足：** 多样本 first batch 需要 **≥3** → **继续 Route B 或 Route C**。

---

## 5. `seedDependent` 标记

| `userId` | `seedDependent` | 原因 |
|----------|-----------------|------|
| `cmp70ft7a001e6znksjb2jo03` | **`true`** | dev-only INSERT `user_profile` |
| `cmr4hm000816z64demo00m04a` | **`true`** | dev-only UPDATE profile + INSERT preference |
| `cmr4hm010026z64demo00m01a` | **`true`** | 同上 |
| 池内其余 4 候选 | **`false`** | 未写入；库中既有数据 |

**closeout 元数据字段：** `seedDependent` 仅记录在本文档与 r7d 证据表，**未**写入生产 schema 字段。

---

## 6. Shadow pipeline 冒烟（可选）

**Artifacts：** `artifacts/p76/r7c/cmp70ft7a001e6znksjb2jo03/`

| Stage | 文件 | 结果 | 说明 |
|-------|------|------|------|
| r3b | `r3b.json` | **PARTIAL** | `onboarding_gated_cohort` · 仅 **1** `selectedCandidateIds`（`GENDER_GATE`×11） |
| r4b | `r4b-clean.json` | **PASS** | 显式传入 onboarding pool **6** 人 `--candidateUserIds=...` |
| r5b | `r5b-clean.json` | **PASS** | `selectedByRrmCandidateId=cmr4hm000816z64demo00m04a` |
| r6b | `r6b-clean.json` | **PASS** | stage1/2 用 r4b-clean（池对齐）；`finalShadow.selectedCandidateId=cmr4hm000816z64demo00m04a` · 全部 `applied*=false` |

### 6.1 r4b Top2

| 字段 | 值 |
|------|-----|
| `top2CandidateIds` | `cmr4hm000816z64demo00m04a` · `cmr4hm010026z64demo00m01a` |
| `selectedBy20DOnlyCandidateId` | `cmr4hm000816z64demo00m04a` |

### 6.2 r6b legacy / M6

| 字段 | 值 |
|------|-----|
| `matchResultId` | `null` |
| `m6RrmTop2CandidateIds` | `[]` |
| `differenceStage` | `no_change` |
| `comparisonReasonSummary` | `differs_from_m6_top2;difference_stage=no_change` |

**说明：** 无 `MatchResult` / M6 对照与 r7c1 一致；old-new **证据仍不足** PM signoff。

---

## 7. 是否继续 Route B / Route C

| 路线 | 状态 |
|------|------|
| **Route B** | **已完成** — [r7c5 closeout](./P7.6-r7c5-fixture-preparation-route-b-closeout.md) |
| **Route C** | **仍可选（PM 主路径）** — staging cohort + batch match |

**r7c first batch：** **技术条件已满足**（3/3 viewers）→ [r7d](./P7.6-r7d-multi-sample-audit-first-batch-closeout.md)。

---

## 8. Rollback 方法

**前提：** 备份表 `p76_r7c3_user_profile_backup` · `p76_r7c3_user_preferences_backup` 仍存在。

```sql
-- 1) 删除 r7c4 新建的 viewer profile
DELETE FROM user_profile WHERE "userId" = 'cmp70ft7a001e6znksjb2jo03'
  AND id = 'dev-p76-r7c4-profile-cmp70';

-- 2) 恢复 2 候选 profile（从 backup）
UPDATE user_profile up SET
  "attachmentStyle" = b."attachmentStyle",
  "emotionalExpression" = b."emotionalExpression",
  "communicationStyle" = b."communicationStyle",
  "conflictHandling" = b."conflictHandling",
  "loveLanguage" = b."loveLanguage",
  "securityNeed" = b."securityNeed",
  "controlNeed" = b."controlNeed",
  "independence" = b."independence",
  "loyaltyView" = b."loyaltyView",
  "jealousyTendency" = b."jealousyTendency",
  "moneyAttitude" = b."moneyAttitude",
  "careerPriority" = b."careerPriority",
  "lifePace" = b."lifePace",
  "socialNeed" = b."socialNeed",
  "emotionalStability" = b."emotionalStability",
  "sexualValues" = b."sexualValues",
  "familyView" = b."familyView",
  "marriageExpectation" = b."marriageExpectation",
  "childrenIntent" = b."childrenIntent",
  "riskPreference" = b."riskPreference",
  "relationshipPace" = b."relationshipPace",
  "socialEnergy" = b."socialEnergy",
  "initiativeLevel" = b."initiativeLevel",
  "decisionOrientation" = b."decisionOrientation",
  "conflictResponse" = b."conflictResponse",
  "confidence" = b."confidence",
  "updatedAt" = NOW()
FROM p76_r7c3_user_profile_backup b
WHERE up."userId" = b."userId"
  AND up."userId" IN ('cmr4hm000816z64demo00m04a', 'cmr4hm010026z64demo00m01a');

-- 3) 删除 r7c3 新增 preferences
DELETE FROM user_preferences
WHERE id IN ('dev-p76-r7c3-pref-cmr4hm000816', 'dev-p76-r7c3-pref-cmr4hm010026');

-- 4) 验证：cmp70 recommendedForBatch 应回到 false
-- 重跑 r7c §4 SQL
```

完整模板见 [r7c3 §9](./P7.6-r7c3-fixture-preparation-execution-runbook.md)。

---

## 9. 边界确认

| 项 | 状态 |
|----|------|
| 未写 `MatchResult` | **是** |
| 未改 `finalScore` | **是** |
| 未改 worker | **是** |
| 未改 `displayCandidateUserId` | **是** |
| 未改 M6 / RRM 主链 | **是** |
| 未启 `APPLY_TO_POOL` | **是** |
| 未启 percent | **是** |
| 未删除 legacy photo matching | **是** |
| dev-only fixture | **是** |
| 可 rollback | **是**（§8） |

---

## 10. r7c4 PASS 判定

| 标准 | 结果 |
|------|------|
| Route A 执行完成 | **PASS** |
| `cmp70` `recommendedForBatch=true` | **PASS** |
| backup + rollback 文档化 | **PASS** |
| `seedDependent` 标记 | **PASS** |
| ≥3 viewers | **PASS**（经 [r7c5](./P7.6-r7c5-fixture-preparation-route-b-closeout.md)） |
| PM signoff 就绪 | **FAIL** |
| r7c first batch | **可执行**（`seedDependent`） |

---

## 11. 参考

| 文档 / 路径 | 用途 |
|-------------|------|
| [P7.6-r7c3 runbook](./P7.6-r7c3-fixture-preparation-execution-runbook.md) | 执行步骤 |
| [P7.6-r7c runbook](./P7.6-r7c-multi-sample-audit-execution-runbook.md) | §4 SQL · first batch |
| [P7.6-r4b1](./P7.6-r4b1-20d-eligible-profile-fixture-preparation.md) | 20D 写入范例 |
| `tmp/p76-r7c4-*.sql` | 本次 SQL |
| `artifacts/p76/r7c/cmp70ft7a001e6znksjb2jo03/` | CLI 产出 |

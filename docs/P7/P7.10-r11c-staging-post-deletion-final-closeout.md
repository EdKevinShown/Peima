# P7.10-r11c — Staging post-deletion final closeout

**Milestone:** P7.10-r11c  
**Date:** 2026-05-19  
**Status:** `PASS_R11C_STAGING_POST_DELETION_FINAL_CLOSEOUT`  
**Artifact:** [staging-post-deletion-smoke-result.json](../../artifacts/p76/r11c/staging-post-deletion-smoke-result.json)  
**Runner:** `tmp/p710-r11c-staging-post-deletion-smoke.mjs`  
**Precondition:** [r11b](./P7.10-r11b-post-deletion-full-regression-closeout.md) `PASS_R11B_POST_DELETION_FULL_REGRESSION`

---

## 1. One-line conclusion

**P7.10 legacy photo matching removal is complete.** Staging production-like DB readonly smoke + static deletion proofs **PASS**; protected core preserved; no production touched; percent `0`; old writer blocked; canonical mutation gates off. r8i PM/Ops/Eng human signoff remains **pending** (not faked).

```text
PASS_R11C_STAGING_POST_DELETION_FINAL_CLOSEOUT
```

---

## 2. Final status matrix (P7.10 track)

| Milestone | Status |
|-----------|--------|
| **P7.10-r8d** | **DONE** — `PASS_R8D_CANONICAL_APPLY_SERVICE_HARD_DISABLED` |
| **P7.10-r8e** | **DONE** — `PASS_R8E_CANONICAL_ROLLBACK_SERVICE_TOKEN_GATED` |
| **P7.10-r8f** | **DONE** — `PASS_R8F_HIDDEN_ADMIN_APPLY_ROLLBACK_API_WIRING` |
| **P7.10-r8g** | **DONE** — `PASS_R8G_LOCAL_APPLY_ROLLBACK_SMOKE` |
| **P7.10-r8h** | **DONE** — `PASS_R8H_STAGING_APPLY_ROLLBACK_SMOKE` |
| **P7.10-r8i** | **READY** — `READY_FOR_R9_WRITER_SHUTDOWN_REVIEW_PENDING_HUMAN_SIGNOFF` (**human signoff pending**) |
| **P7.10-r9** | **DONE** — `PASS_R9_OLD_PHOTO_MATCHING_WRITER_SHUTDOWN` |
| **P7.10-r10** | **DONE** — `PASS_R10_SAFE_FALLBACK_FINAL` |
| **P7.10-r11** | **DONE** — `PASS_R11_CATEGORY_A_LEGACY_PHOTO_MATCHING_DELETION` |
| **P7.10-r11b** | **DONE** — `PASS_R11B_POST_DELETION_FULL_REGRESSION` |
| **P7.10-r11c** | **DONE** — `PASS_R11C_STAGING_POST_DELETION_FINAL_CLOSEOUT` |

---

## 3. Staging smoke result

| Check | Result |
|-------|--------|
| `productionTouched` | **false** |
| `percent` | **0** (disabled) |
| `workerTriggered` | **false** (`pendingHookJobsObserved: 0`) |
| Old photo writer | shutdown **on**, writer **off**, formal write **blocked** |
| Deleted routes (static) | `/preview-pool` + `/onboarding/photo-preview` → `/questionnaire`; `POST /generate` removed from controller |
| Onboarding `nextStep` | aesthetic done → **questionnaire** (no `photo_preview` gate) |
| Safe fallback | policy file present; r10 tests PASS |
| Canonical mutation | hidden API / apply / rollback **default off** (staging file + smoke overrides) |
| Readonly DB | **pass** — `match_results` count **6**; `preview_pools` **0**; allowlist/canonical violations **0** |
| Staging DB host (redacted) | `aws-1-ap-southeast-2.pooler.supabase.com:5432` |
| DB fingerprint (sha256 prefix) | `b6d1fc10ef48fafb` |

### Staging HTTP note

| Item | Status |
|------|--------|
| Live `POST /preview-pool/generate` HTTP probe | **NOT_RUN** — `.env.staging` has no `PEIMA_STAGING_API_BASE_URL` / `VITE_API_BASE_URL` |
| Static proof | Controller has no `@Post`; comment documents removal — **PASS** |
| r8h hidden HTTP Apply/Rollback | **NOT_RUN** in r8h (service path only); unchanged |

To run live HTTP probe (optional):

```powershell
$env:PEIMA_P710_R11C_STAGING_SMOKE_ALLOWED="1"
$env:PEIMA_STAGING_API_BASE_URL="https://peima-api-staging.onrender.com"
node tmp/p710-r11c-staging-post-deletion-smoke.mjs
```

---

## 4. What is now removed

- Old **PreviewPool formal writer** path (`preview-pool-layered-selection`, visual enhance stub, `POST /generate`)
- **Onboarding photo preview pool** service + funnel/active audit + API routes
- **PreviewPoolPage** / **OnboardingPhotoPreviewPage** UI
- **P7.5 Category A dev-cli** (p75-r4-m/o2, p75-r5-b2/c2, p75-r4-e-shadow-sample) + related tests
- Onboarding `photo_preview` step as a hard gate (skipped → questionnaire)

---

## 5. What is preserved

| Capability | Notes |
|------------|-------|
| **MatchResult** | Prisma model + table `match_results` |
| **GET `/matching/result`** | JWT viewer-scoped read |
| **`resolveMatchResultDisplay`** | Display fusion unchanged |
| **FinalMatchPage** | `getMatchingResult` main chain |
| **r10 safe fallback** | `matching_pending` / `no_result` / stable baseline |
| **Canonical Apply/Rollback** | r8 services + hidden API (default off) |
| **Rollback snapshot** | r8c persistence (historical) |
| **Photo upload / review / Cloud Vision** | Onboarding upload + preference + vision modules |
| **PreviewPool read-only** | `GET /preview-pool/user/:id/latest` |
| **`computeStyleScore`** | `packages/shared` — vision shadow |

---

## 6. Remaining non-blocking gaps

| Gap | Notes |
|-----|-------|
| **r8i PM/Ops/Eng signoff** | **pending** — not faked in any artifact |
| **r11c live staging HTTP** | Skipped when API base URL absent in `.env.staging`; static + readonly DB sufficient for this closeout |
| **r8h hidden HTTP Apply/Rollback** | **NOT_RUN** (documented in r8h); r8f unit tests cover wiring |
| **API full unrelated failures** (r11b) | `rrm-sim.evaluator.spec.ts` allowlist drift; `matching-finalize-with-pairwise.spec.ts` DI gap |

---

## 7. Local regression (r11c quick)

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api run build` | **PASS** |
| `pnpm --filter @peima/worker run build` | **PASS** |
| `pnpm --filter @peima/web run build` | **PASS** |
| `pnpm --filter @peima/api test -- p710-r10-safe-fallback-final` | **PASS** (12) |
| `pnpm --filter @peima/api test -- matching-result-display` | **PASS** (45) |
| `pnpm --filter @peima/worker test -- p710-r9-old-photo-matching-writer-shutdown` | **PASS** (7) |
| `pnpm --filter @peima/api test -- p710-r8f` | **PASS** (34) |
| `pnpm --filter @peima/api test -- p710-r8e` | **PASS** (33) |
| `pnpm --filter @peima/api test -- p710-r8d` | **PASS** (20) |
| `node tmp/p710-r11c-staging-post-deletion-smoke.mjs` | **PASS** (with `PEIMA_P710_R11C_STAGING_SMOKE_ALLOWED=1`) |

---

## 8. Security scan

```powershell
git diff | Select-String -Pattern "postgresql://|JWT_SECRET=|Bearer ey|DATABASE_URL=|api_key|apiKey|tokenPlaintext|rollbackTokenPlaintext"
```

**Result:** Hits only on **deleted** lines in `visual-signal-enhance-stub.ts` (`api_key` redaction / LLM client) — **only deleted-file historical lines**; **no newly added secret**; **no active secret exposure** in r11c diff.

Artifact policy: no `DATABASE_URL`, JWT, rollback token, or real viewer/user ids.

---

## 9. Git hygiene (not committed)

Working tree still contains r8–r11 implementation + closeout docs (mixed). **Do not commit** `.env`, `.env.staging`, tokens, or rollback secrets.

---

## 10. Final PASS / BLOCK

| 判定 | 状态 |
|------|------|
| **PASS** | `PASS_R11C_STAGING_POST_DELETION_FINAL_CLOSEOUT` |

**BLOCK 条件（均未触发）：** production touched · percent changed · old writer write · PreviewPool fallback write · protected core broken · secrets exposed

---

## 11. After r11c

**P7.10 legacy photo matching removal is complete.**

Next major areas (separate tracks — **not started here**):

- PM/Ops/Eng final human signoff (r8i)
- Production rollout planning (Apply / percent — still **BLOCKED**)
- Observability hardening
- Commercial onboarding / photo privacy UX
- Canonical matching quality iteration

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-19 | r11c staging readonly smoke + final closeout · `PASS_R11C_STAGING_POST_DELETION_FINAL_CLOSEOUT` |

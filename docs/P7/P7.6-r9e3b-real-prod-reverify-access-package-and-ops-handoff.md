# P7.6-r9e3b — Real Prod Reverify Access Package and Ops Handoff

> **状态**：**P7.6-r9e3b real prod reverify access package**  
> **结论**：**`P7_6_R9E3B_REAL_PROD_REVERIFY_ACCESS_PACKAGE_READY`**  
> **前置**：[P7.6-r9e3a](./P7.6-r9e3a-real-prod-reverify-evidence-attempt-closeout.md) **`P7_6_R9E3A_REAL_PROD_REVERIFY_BLOCKED_BY_MISSING_ACCESS`** · [P7.6-r9e2](./P7.6-r9e2-real-prod-ops-execution-runbook.md) · [P7.6-r9e4](./P7.6-r9e4-staging-production-like-environment-bootstrap-runbook.md)  
> **Artifacts**：[artifacts/p76/r9e3b/](../../artifacts/p76/r9e3b/)  
> **性质**：docs / access checklist / Ops handoff only — **无**代码 · **无** prod 命令执行 · **无** DB write

---

## 1. Status

**`P7_6_R9E3B_REAL_PROD_REVERIFY_ACCESS_PACKAGE_READY`**

**Scope confirmation（本轮未做）：**

- Docs / access package only
- No code change
- No prod command executed
- No DB write
- No MatchResult mutation
- No rollout
- No percent
- No canonical write

**r9e3 状态（不变）：** **`NEED_MORE_PROD_REVERIFY`** — 本包仅用于向 Ops / 部署负责人申请 **r9e3c** 执行条件。

---

## 2. Why this package exists

[P7.6-r9e3a](./P7.6-r9e3a-real-prod-reverify-evidence-attempt-closeout.md) 已归档一次真实 prod 复核**尝试**，结论为 **`P7_6_R9E3A_REAL_PROD_REVERIFY_BLOCKED_BY_MISSING_ACCESS`**：

| Gap (r9e3a) | Impact |
|-------------|--------|
| Prod API base URL missing | 0 remote HTTP smoke |
| Staging-prod-like API missing | 无法走 r9e4 降级路径 |
| Read-only prod/staging DB missing | 无法执行 RDS violation SQL |
| Prod admin/service auth missing | 无法 GET allowlist cohort |
| Grafana / monitoring missing | 无法完成 30–60 min watch |
| kubectl / rollback context missing | 无法审计 pod env / kill switch |

本地 `peima-postgres` 与 [r9d](../../artifacts/p76/r9d/) rehearsal **不计** prod PASS。[P7.10-r7](./P7.10-r7-canonical-write-readiness-design.md) **gate 12** 仍为 **BLOCKED**。

本包将缺失项整理为 **Ops 可勾选交接清单**，供下一轮 **P7.6-r9e3c**（只读 smoke 执行）前一次性交付。

---

## 3. Required access checklist

| Access item | Required? | Purpose | Provided by | Secret handling |
|-------------|:---------:|---------|-------------|-----------------|
| **Prod API base URL** | Yes (prod path) | Remote `GET /health`, `GET /matching/result/:viewerUserId` smoke | Ops / Platform | Store in secret manager or operator shell env `PEIMA_PROD_API_BASE_URL`; **never** commit |
| **Staging-prod-like API base URL** | Yes (if no prod window) | Same smokes against `environment=production-like-staging` per [r9e4](./P7.6-r9e4-staging-production-like-environment-bootstrap-runbook.md) | Ops / Platform | `PEIMA_STAGING_API_BASE_URL`; host only in artifacts |
| **Read-only prod/staging DB URL** | Yes | `violation_count`, `applied_count`, table-exists SQL ([r9e2 SQL pack](../../artifacts/p76/r9e2/prod-rds-sql-pack.md)) | DBA + Ops | `PEIMA_READONLY_DATABASE_URL` or scoped read-only role; **no** full URL in repo artifacts |
| **Admin / service JWT or auth method** | Yes | Bearer for allowlist viewer cohort GET smoke | API Eng + Ops | Short-lived token via secret manager; rotate after r9e3c |
| **Allowed viewer / user cohort** | Yes | 6-case GET matrix ([prod-get-smoke-runbook](../../artifacts/p76/r9e2/prod-get-smoke-runbook.md)) | PM + Ops | Record **counts** and case types in artifacts; redact or hash user IDs |
| **Grafana dashboard URL** | Yes | Import `p76-allowlist-read-path-v2` · P0/P1 panels | Ops (observability) | URL may be redacted to org/dashboard id |
| **Alert / monitoring access** | Yes | 30–60 min watch; P0 counters = 0 | Ops (observability) | No webhook secrets in git |
| **kubectl context or equivalent** | Yes (audit) | Read-only pod env audit (`PEIMA_P76_*`, percent=0) | Platform | Context name only in handoff; no kubeconfig file in repo |
| **Rollback owner** | Yes | Named human for kill-switch / read-path off | Ops lead | Name + on-call handle in private run sheet |
| **Kill switch owner** | Yes | `PEIMA_P76_PRODUCTION_KILL_SWITCH` authority | Ops lead | Same as rollback owner or delegate |
| **Deployment owner** | Yes | Approves r9e3c time window; blocks if drift | Release manager | — |
| **Incident channel** | Yes | P0/P1 escalation during watch | Ops | Slack/Teams channel name only |
| **30–60 min monitoring time window** | Yes | Scheduled watch per [r9e2 template](../../artifacts/p76/r9e2/prod-monitoring-watch-template.md) | Ops + PM | Calendar hold; timezone documented |

**Machine-readable copy:** [prod-reverify-access-checklist.json](../../artifacts/p76/r9e3b/prod-reverify-access-checklist.json)

---

## 4. Required env vars

**变量名 only — 不填值 · 勿 commit 到 repo**

| Variable | Role |
|----------|------|
| `PEIMA_PROD_API_BASE_URL` | Customer production API origin (HTTPS) |
| `PEIMA_STAGING_API_BASE_URL` | Production-like staging API origin (HTTPS) |
| `PEIMA_READONLY_DATABASE_URL` | Read-only Postgres (managed host ≠ localhost) |
| `PEIMA_ADMIN_SERVICE_TOKEN` | Bearer or equivalent for smoke JWT (or issue per-viewer JWT via approved script) |
| `PEIMA_P76_READ_PATH_SAFE_FALLBACK` | Must match approved freeze (`1` for bootstrap) |
| `PEIMA_P76_RESULT_STATE_CONTRACT_ENABLED` | Pod flag when asserting `resultState` on API |
| `P76_SMOKE_EXPECT_RESULT_STATE_CONTRACT` | Smoke harness opt-in (`1` for r9b contract mode) |

**Also required on target environment (audit via read-only pod/env export — names only in artifacts):**

- `PEIMA_P76_READ_PATH_ENABLED`
- `PEIMA_P76_READ_PATH_VIEWER_IDS`
- `PEIMA_P76_PRODUCTION_PERCENT` / `PEIMA_P76_PRODUCTION_PERCENT_ENABLED`
- `PEIMA_P76_PRODUCTION_KILL_SWITCH`
- `JWT_SECRET` (presence only — **never** log value)

**Template reference:** [docs/ops/peima-staging.env.example](../ops/peima-staging.env.example)

**Policy:** Copy to `.env.staging` or secret manager entry — **gitignored**. CI and developer laptops must not be the sole source for r9e3c.

---

## 5. Read-only commands allowed

Approved for **P7.6-r9e3c** only after §3 checklist is **GO**:

| Category | Command / action | Notes |
|----------|------------------|-------|
| Health | `GET /health` or `GET /api/health` | No auth or public health only |
| Result smoke | `GET /matching/result/:viewerUserId` + `Authorization: Bearer <token>` | 6 cases per [prod-get-smoke-runbook](../../artifacts/p76/r9e2/prod-get-smoke-runbook.md) |
| Contract smoke | `node -r dotenv/config apps/api/scripts/p76-r9b-http-get-smoke.mjs` | Against **remote** base only if harness adapted for HTTP; otherwise curl matrix with `P76_SMOKE_EXPECT_RESULT_STATE_CONTRACT=1` |
| Read-only SQL | `SELECT` counts / `violation_count` / table exists from [prod-rds-sql-pack](../../artifacts/p76/r9e2/prod-rds-sql-pack.md) | **No** `INSERT` / `UPDATE` / `DELETE` |
| Monitoring | Grafana panel snapshot / P0 query | Read-only; 30–60 min window |
| Env audit | `kubectl get` / `kubectl describe` (read) or platform env export | percent=0, read path state frozen |

**Explicitly forbidden during r9e3c (and this package round):**

| Forbidden | Reason |
|-----------|--------|
| `POST` / `PATCH` / `DELETE` | Mutation risk |
| Admin Apply / sidecar apply | Writes apply_meta / display path |
| Prisma `migrate` / schema deploy | Out of r9e3c read-only scope |
| Rollback **mutation** drill | Requires separate approved window |
| Percent enable / `PEIMA_P76_PRODUCTION_PERCENT` > 0 | Blocked until r9f |
| Worker / batch-match trigger | No worker integration |
| `MatchResult` / `matchInsights` update | Canonical write blocked |
| Rehearsal writer insert (`p76_canonical_writer_rehearsal_meta`) | P7.10 rehearsal only |
| PreviewPool disable | Blocked by r7 |
| Legacy photo matching removal | Blocked (r10) |
| `p76-r9d-execute.mjs` full orchestration | May seed/mutate — **not** for r9e3c first pass |

---

## 6. Redaction policy

All r9e3c artifacts and Ops handoff notes **must** follow:

| Data | Policy |
|------|--------|
| Tokens / JWT / `PEIMA_ADMIN_SERVICE_TOKEN` | **Do not record** — only `adminTokenProvided: true/false` |
| Full `DATABASE_URL` / `PEIMA_READONLY_DATABASE_URL` | **Do not record** — redact to host or `<managed-postgres-host>` |
| API URLs with secrets in query | Host + path pattern only |
| User IDs | Redact, hash, or aggregate counts — prefer case type labels |
| PII | None in artifacts |
| Grafana / kubectl credentials | Access boolean + owner name only |

---

## 7. Evidence expected from next run (r9e3c)

When **r9e3c** succeeds, expect new artifacts under `artifacts/p76/r9e3c/` (or agreed path):

| Deliverable | Content |
|-------------|---------|
| `real-prod-reverify-smoke-summary.json` | `generatedAt`, environment label, pass/fail counts |
| `real-prod-reverify-smoke-summary.md` | Human summary |
| Endpoint pass/fail | Per-case httpStatus, displaySourceType (no raw JWT) |
| `resultState` distribution | If `P76_SMOKE_EXPECT_RESULT_STATE_CONTRACT=1` |
| `safeFallback` count | From smoke rows where `fallbackUsed=true` |
| `violation_count` | From read-only SQL (= 0 gate) |
| No-mutation confirmation | Signed checklist: no writes during window |
| Monitoring window notes | 30–60 min · P0 = 0 |
| Rollback / kill switch owner confirmation | Names + channel — not secret values |

**Status upgrade path (not automatic):** evidence may support **`REVERIFY_EVIDENCE_COLLECTED_PENDING_PM_OPS_REVIEW`** — **not** production-ready for canonical write without PM/Ops signoff.

---

## 8. Go / No-go criteria

### GO (all required)

- [ ] Prod **or** staging-prod-like API reachable (HTTPS, non-localhost)
- [ ] Read-only DB credentials issued (SELECT-only role)
- [ ] Admin/service auth available for 6-case cohort
- [ ] Monitoring / Grafana access confirmed
- [ ] Kill switch owner identified and reachable
- [ ] Rollback owner identified
- [ ] Deployment owner approved calendar window
- [ ] Only read-only commands approved (§5)
- [ ] Incident channel staffed for watch window

### NO-GO (any triggers stop)

- Any §3 access item missing
- Only localhost / docker-compose available
- Token cannot be stored or rotated safely
- Unknown mutation risk (scripts not reviewed)
- No rollback owner
- No monitoring for 30–60 min window
- Percent > 0 or unapproved read path enable planned in same window

---

## 9. Impact on P7.10-r7

| Item | Status after r9e3b |
|------|-------------------|
| **Gate 12** — r9e3 real prod reverify | **BLOCKED** — access package ready; evidence not yet collected |
| Canonical production write | **BLOCKED** |
| Percent (`r9f`) | **BLOCKED** |
| PreviewPool disable | **BLOCKED** |
| Legacy removal | **BLOCKED** |

r9e3b **does not** clear gate 12. It only unblocks **scheduling** r9e3c after Ops delivers §3.

---

## 10. Next run plan

**Recommended next milestone:** **P7.6-r9e3c — Real Prod Reverify Read-only Smoke Execution**

**Preconditions:**

1. Ops returns completed [prod-reverify-access-checklist.md](../../artifacts/p76/r9e3b/prod-reverify-access-checklist.md) with all required items checked.
2. Go criteria (§8) signed by deployment owner.
3. If customer prod unavailable: complete [r9e4](./P7.6-r9e4-staging-production-like-environment-bootstrap-runbook.md) first; label all evidence **`production-like-staging`**.

**Do not start r9e3c until checklist GO.**

After r9e3c: PM/Ops review → update [r9e3](./P7.6-r9e3-real-prod-ops-execution-closeout.md) / r9e1 — still **no** percent, canonical write, or PreviewPool disable without separate gates.

---

## References

- [P7.6-r9e3a closeout](./P7.6-r9e3a-real-prod-reverify-evidence-attempt-closeout.md)
- [artifacts/p76/r9e3a/](../../artifacts/p76/r9e3a/)
- [P7.6-r9e2 runbook](./P7.6-r9e2-real-prod-ops-execution-runbook.md)
- [P7.6-r9e4 staging bootstrap](./P7.6-r9e4-staging-production-like-environment-bootstrap-runbook.md)
- [P7.10-r7 canonical write readiness](./P7.10-r7-canonical-write-readiness-design.md)
- [artifacts/p76/r9e3b/](../../artifacts/p76/r9e3b/)

# P7.7-r1 — ResultState + Safe Fallback Negative Regression Closeout

## 1. Status

```text
P7_7_R1_RESULT_STATE_SAFE_FALLBACK_NEGATIVE_REGRESSION_PASS
```

- Code change: **tests / regression only** (no production code changes)
- No API default behavior change
- No frontend change
- No worker change
- No canonical writer
- No legacy removal
- No production / percent rollout

---

## 2. What changed

| File | Role |
|------|------|
| `apps/api/test/p77-r1-read-path-negative-regression.spec.ts` | **New** — stale / dry_run / violation / overlay baseline + resultState linkage |
| `apps/api/test/p77-r1-result-state-safe-fallback-regression.spec.ts` | **New** — no-row states, reason map, service safe_fallback + 404 |
| `apps/api/test/p76-read-path-display-resolver.spec.ts` | Unchanged (baseline coverage retained) |
| `apps/api/test/matching-result-state.spec.ts` | Unchanged |
| `apps/api/test/p710-r4a-matching-result-state.service.spec.ts` | Unchanged |
| `apps/api/test/p76-read-path-env.spec.ts` | Unchanged (r2b alias — referenced, not duplicated) |
| `docs/P7/P7.7-r1-result-state-safe-fallback-negative-regression-closeout.md` | This closeout |

---

## 3. Negative cases covered

| Case | Expected behavior | Test file |
|------|-------------------|-----------|
| env disabled | No sidecar overlay; `env_disabled`; baseline display | `p76-read-path-display-resolver.spec.ts` |
| missing sidecar | `missing_sidecar`; baseline; `fallbackUsed` | `p76-read-path-display-resolver.spec.ts` |
| stale sourceVersion | `stale_source_version`; baseline | `p77-r1-read-path-negative-regression.spec.ts` |
| rolledBack | `rolled_back`; baseline | `p76-read-path-display-resolver.spec.ts` |
| violation row | `violation_*` (e.g. `violation_artifact_missing`) | `p77-r1-read-path-negative-regression.spec.ts` |
| main-chain flags | `main_chain_flag_*`; baseline | `p76-read-path-display-resolver.spec.ts` |
| candidate missing | `candidate_unavailable`; baseline | `p76-read-path-display-resolver.spec.ts` |
| non-allowlist viewer | `not_allowlisted` / `non_allowlist_row` | `p76-read-path-display-resolver.spec.ts` |
| sidecar dry_run | `sidecar_dry_run`; baseline | `p77-r1-read-path-negative-regression.spec.ts` |
| resolver exception | `exception`; no throw; baseline | both resolver specs |
| no_result | `not_queued`; typed 200 when flag on | `p77-r1-result-state-*`, `p710-r4a` |
| matching_pending | `waiting` / `processing` / `ready` (no row) | `p77-r1-result-state-*`, `p710-r4a` |
| static_fallback / match_result_original | Enum literals + category map unchanged | `p77-r1-result-state-*`, `matching-result-state.spec.ts` |

**Deferred:** End-to-end GET HTTP with real DB cohort — covered by `p76-r9b` opt-in smoke (P7.10-r4b), not duplicated here.

**Reason map (production):** Resolver emits snake_case reasons (`missing_sidecar`, `stale_source_version`, …); `matching-result-state.ts` maps to contract reasons (`sidecar_missing`, `stale_sidecar`, `violation_row`, …).

---

## 4. ResultState coverage

| Scenario | Locked by |
|----------|-----------|
| Flag off + no row → 404 `No match result` | `p77-r1-result-state-*`, `p710-r4a` |
| Flag on + `waiting` → `matching_pending` | `p710-r4a`, `p77-r1` |
| Flag on + `processing` → `matching_pending` | `p77-r1` |
| Flag on + queue `ready` but no row → `matching_pending` | `p77-r1` (r4a design) |
| Flag on + `not_queued` → `no_result` + `nextAction` | `p710-r4a`, `p77-r1` |
| Flag on + row baseline → `ready` | `p710-r4a`, `p77-r1` |
| Flag on + read-path fallback → `safe_fallback` | `p77-r1` (service + helper table) |
| Existing `candidateUserId` / `finalScore` / `displaySourceType` preserved | `p77-r1` |

---

## 5. Safe fallback coverage

- Fallback returns **baseline** `match_result_original` display, not legacy PreviewPool photo matching UI path
- Resolver / overlay **does not throw** on negative paths (exception → baseline)
- `candidateUserId` / `finalScore` on `MatchResult` row unchanged when overlay falls back
- `PEIMA_P76_READ_PATH_SAFE_FALLBACK` / `FALLBACK_LEGACY` alias: **`p76-read-path-env.spec.ts`** (not duplicated)

---

## 6. Tests / verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api test -- p77-r1 result-state matching-result-state p710-r4a p76-read-path-env p76-read-path-display-resolver` | **PASS** — 6 suites, 60 tests |
| `pnpm --filter @peima/api run build` | **PASS** (exit 0) |

---

## 7. PASS / BLOCK

**PASS**

- Negative regression pack added
- `resultState` behavior locked
- Safe fallback behavior locked
- Env alias suite still passing (referenced)
- Build pass

**BLOCK**

- Canonical writer not implemented
- Old PreviewPool writer not disabled
- Legacy removal blocked
- Production / percent blocked

---

## 8. Next recommended step

**Option A — P7.10-r6 Canonical writer shadow implementation** (**recommended** if tests are stable)

- Shadow compare only; `appliedToMatchResult=false`; no `MatchResult` writes

**Option B — P7.7-r2 Local smoke automation**

- One command: env alias + resultState contract + r9b smoke

If manual smoke cost is still high, do **P7.7-r2** first; otherwise proceed to **P7.10-r6**.

# P7.5-r5-c2b — APPLY Writer 本地签收 Closeout（自动化 + 可选浏览器）

> **状态**：**自动化签收通过**（runner `generate` + active audit）· **可进入 r5-c3**（仍 **不启** percent）  
> **对齐**：[P7.5-r5-c2 本地签收](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md)、[P7.5-r5-c2a](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md)、[P7.5-r5-c2 runner closeout](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)

---

## 1. Runner actual 结果（本机重跑 · r5-c2a 后）

| 字段 | 值 |
|------|-----|
| `schemaVersion` | `p7.5-r5-c2-apply-writer-signoff-v1` |
| `viewerUserId` | `cmp70ft7a001e6znksjb2jo03` |
| `viewerIdMasked` | `cmp7…jo03` |

**本机命令**

```bash
cd C:\Users\34713\Desktop\peima\apps\api
npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r5-c2-apply-writer-signoff-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath=../../dev-assets/test-user-images/mapping.json
```

| Case | result | `actualSourceVersion` | `applied` | `reason` | `poolItemCount` | audit violations |
|------|--------|----------------------|-----------|----------|-----------------|------------------|
| A | **PASS** | `onboarding-photo-preview-v1` | false | `env_disabled` | 6 | 全 false |
| B | **PASS** | `onboarding-photo-preview-v1` | false | `allowlist_empty` | 6 | 全 false |
| C | **PASS** | `onboarding-photo-preview-v1` | false | `not_in_allowlist` | 6 | 全 false |
| D | **PASS** | `onboarding-photo-preview-v2-vision` | **true** | `ok` | 6 | 全 false |
| E | **PASS** | `onboarding-photo-preview-v1` | false | `env_disabled` | 6 | 全 false |

每案 audit：`duplicateCandidateViolation=false` · `duplicateSourceImageViolation=false` · `genderViolation=false` · `selfViolation=false` · `activeAuditPass=true`。

**summary**

| 字段 | 值 |
|------|-----|
| `allPass` | `true` |
| `caseDAppliedV2` | `true` |
| `caseERolledBackToV1` | `true` |
| `v2PoolsRetainedAfterRollback` | `true` |
| `activeAuditPassAllCases` | `true` |
| `noWorkerOrMatchResultTouched` | `true` |
| exit code | **0** |

**跑完 runner 后**：末案为 **Case E**，viewer 当前 **active pool = v1** 为 **正常**；库内保留 Case D 的 **archived v2** pool。

**历史**：c2 初跑 Case D 曾为 `insufficient_unique_source_images`；[r5-c2a](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md) 修复后 **actual 五案全 PASS**。

---

## 2. 签收口径：自动化 vs 浏览器

| 层级 | 内容 | c2 结论 |
|------|------|---------|
| **自动化** | 单进程 runner：五组 env → `generate()` → 读 pool/shadow → r4-o2 active audit | **通过**（§1） |
| **浏览器 UI** | `/onboarding/photo-preview?userId=…` 点击「重新生成预览池」观感 | **非硬门槛**；无额外截图留存 |
| **staging / PM** | ≥20 pools spot-check、视觉与 audit 复核 | **r5-c3** 范围 |

---

## 3. 可选浏览器手测步骤（staging / PM 可复用）

**URL**：`/onboarding/photo-preview?userId=cmp70ft7a001e6znksjb2jo03`

### Case D（allowlist hit → v2）

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=1
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=cmp70ft7a001e6znksjb2jo03
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

重启 API → 打开页面 → 「重新生成预览池」→ 期望 6 items；audit 与 runner Case D 一致。

### Case E（rollback → v1）

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=0
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

重启 API → 再生成 → 新 active **v1**；旧 v2 **不批量改**。

---

## 4. 签收表格（actual）

| Case | Env | expected sourceVersion | actual（自动化） | 6 items | active audit | result |
|------|-----|------------------------|------------------|---------|--------------|--------|
| D | APPLY=1, allowlist=hit | `onboarding-photo-preview-v2-vision` | `onboarding-photo-preview-v2-vision` | 6 | **PASS** | **PASS** |
| E | rollback APPLY=0 | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | 6 | **PASS** | **PASS** |

浏览器 UI：**无单独截图**；与自动化结论一致时无需重复签收。

---

## 5. 签收结论

> **P7.5-r5-c2 allowlist-only writer 本地自动化签收通过**

- Runner **A–E 全 PASS**；Case **D** 成功写 **v2**；Case **E** 成功 rollback **v1**；`v2PoolsRetainedAfterRollback=true`。
- **Active audit** 五案全通过。
- **允许进入** [P7.5-r5-c3 staging signoff](./P7.5-r5-c3-apply-to-pool-staging-signoff.md) + PM spot-check。
- **仍不允许** 开启 **percent** 灰度。

---

## 6. 已知限制

- **allowlist-only**；percent **未启用**。
- Demo 素材可能存在视觉性别与 DB `gender` 不一致；**不**用 AI / 人脸判断性别。
- Active audit 为 staging 前硬门槛；PM spot-check 为 r5-c3 补充。

---

## 7. 下一步（[r5-c3 runbook](./P7.5-r5-c3-apply-to-pool-staging-signoff.md)）

| 项 | 要求 |
|----|------|
| staging env | 默认 **关闭** apply |
| 开启范围 | **仅** allowlist viewer |
| PM spot-check | **≥ 20** pools |
| active audit | 全 PASS |
| 回滚 | `APPLY_TO_POOL=0` |
| percent | **仍不启**（r5-d） |

---

## 8. 未改动范围

- **仅文档**；无业务代码、无 DB migration
- worker、`finalScore`、`MatchResult`、旧 `PreviewPool`、正式匹配主链 — **未改**

---

## 9. 参考

- [P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md)
- [P7.5-r5-c2-apply-writer-signoff-runner-closeout.md](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)
- [P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md)

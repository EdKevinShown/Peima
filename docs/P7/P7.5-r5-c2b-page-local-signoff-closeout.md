# P7.5-r5-c2b — APPLY Writer 本地页面手测签收 Closeout

> **状态**：Runner 签收 **通过** · 页面手测 **待用户本机确认**（仅 docs，无代码变更）  
> **对齐**：[P7.5-r5-c2 本地签收](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md)、[P7.5-r5-c2a](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md)、[P7.5-r5-c2 runner closeout](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)

---

## 1. Runner actual 结果（2026-05-16 · r5-c2a 后重跑）

| 字段 | 值 |
|------|-----|
| `schemaVersion` | `p7.5-r5-c2-apply-writer-signoff-v1` |
| `viewerUserId` | `cmp70ft7a001e6znksjb2jo03` |
| `viewerIdMasked` | `cmp7…jo03` |

| Case | result | pool `sourceVersion` | `applyResult` |
|------|--------|----------------------|---------------|
| A | **PASS** | `onboarding-photo-preview-v1` | `applied=false`, `reason=env_disabled` |
| B | **PASS** | `onboarding-photo-preview-v1` | `applied=false`, `reason=allowlist_empty` |
| C | **PASS** | `onboarding-photo-preview-v1` | `applied=false`, `reason=not_in_allowlist` |
| D | **PASS** | `onboarding-photo-preview-v2-vision` | `applied=true`, `reason=ok` |
| E | **PASS** | `onboarding-photo-preview-v1`（rollback） | `applied=false`, `reason=env_disabled` |

**summary**

| 字段 | 值 |
|------|-----|
| `allPass` | `true` |
| `caseDAppliedV2` | `true` |
| `caseERolledBackToV1` | `true` |
| `activeAuditPassAllCases` | `true` |
| `v2PoolsRetainedAfterRollback` | `true` |
| exit code | **0** |

**命令**

```bash
cd apps/api
npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r5-c2-apply-writer-signoff-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath=../../dev-assets/test-user-images/mapping.json
```

---

## 2. 页面手测步骤

**页面 URL**

`/onboarding/photo-preview?userId=cmp70ft7a001e6znksjb2jo03`

（Web 基址示例：`http://127.0.0.1:5173`）

**通则**

1. 修改仓库根 `.env` 中 apply 变量 → **重启 API**。
2. 打开上述 URL → 点击 **「重新生成预览池」**。
3. 可选：运行 [r4-o2 active audit CLI](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md#5-active-audit-检查) 与页面观感对照。

---

### Case D：allowlist hit → v2

**env**

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=1
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=cmp70ft7a001e6znksjb2jo03
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

（可选）`PEIMA_ONBOARDING_VISION_APPLY_SOURCE_VERSION=onboarding-photo-preview-v2-vision`

**操作**

- [ ] 重启 API
- [ ] 打开页面
- [ ] 点击「重新生成预览池」
- [ ] 页面正常显示 **6** 个 items
- [ ] active audit：`pool_source_version = onboarding-photo-preview-v2-vision`
- [ ] `duplicate_candidate_user_id` 全 **false**
- [ ] `duplicate_image_source_key` 全 **false**
- [ ] `violates_opposite_gender_gate` 全 **false**
- [ ] `is_viewer_self` 全 **false**

---

### Case E：rollback → v1

**前置**：先完成 Case D（库内保留 archived v2 pool）。

**env**

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=0
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

**操作**

- [ ] 重启 API
- [ ] 点击「重新生成预览池」
- [ ] 新 active pool：`sourceVersion = onboarding-photo-preview-v1`
- [ ] 页面仍显示 **6** 个 items
- [ ] **不**批量修改旧 v2 pool 行
- [ ] active audit 仍 **PASS**

---

## 3. 页面手测表格（actual）

| Case | Env | expected sourceVersion | actual sourceVersion | page 6 items | active audit pass | result |
|------|-----|------------------------|----------------------|--------------|-------------------|--------|
| D | APPLY=1, allowlist=hit, percent=0 | `onboarding-photo-preview-v2-vision` | _待用户本机确认_ | _待用户本机确认_ | runner 已 true；页面侧 _待确认_ | **待用户本机确认** |
| E | rollback APPLY=0 | `onboarding-photo-preview-v1` | _待用户本机确认_ | _待用户本机确认_ | runner 已 true；页面侧 _待确认_ | **待用户本机确认** |

> Runner 对 A–E 的 DB / audit 已 PASS；上表 **actual sourceVersion** 与 **page 6 items** 须在 Web 手测后由执行人改为具体值或 **PASS**。

---

## 4. 签收结论

**当前（2026-05-16）**

- **Runner 签收通过**：A–E 全 PASS；Case D 成功写 v2；Case E 成功回 v1；`activeAuditPassAllCases=true`。
- **页面手测未完成**：§3 Case D/E 仍为 **待用户本机确认**。
- **暂不进入 [P7.5-r5-c3](./P7.5-r5-c0-apply-to-pool-allowlist-writer-plan.md)**，直至页面 D/E 均记为 **PASS** 并更新 [c2 本地签收 §9](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md#9-签收结论)。

**当页面 D/E 均 PASS 后**，将 c2 §9 更新为：

> **P7.5-r5-c2 allowlist-only writer 本地签收通过**  
> - runner A–E 全 PASS；Case D 成功写 v2；Case E 成功回 v1；  
> - 页面可正常显示 6 items；active audit 全 PASS；  
> - 未影响 worker / MatchResult / finalScore；  
> - **可以进入** P7.5-r5-c3 staging signoff + PM spot-check。

---

## 5. 已知限制

- 当前仍为 **allowlist-only**；`PEIMA_ONBOARDING_VISION_APPLY_PERCENT` **未启用**（writer 不读 percent）。
- Demo 素材仍可能存在 **视觉性别与 DB `gender` 不一致**；**不**使用 AI / 人脸识别判断性别。
- **Active audit** 是进入 staging 前的硬门槛（duplicate candidate / duplicate source key / 异性门控 / 非 self）。
- 页面手测不替代 runner；runner 不替代 PM 在 staging 的 spot-check。

---

## 6. 下一步建议

**若 c2b 页面手测 PASS**

进入 **P7.5-r5-c3** staging signoff + PM spot-check：

| 项 | 要求 |
|----|------|
| staging env | 默认 **关闭** apply |
| 开启范围 | **仅** allowlist 内 viewer |
| PM spot-check | **≥ 20** pools |
| active audit | 全 PASS |
| 回滚 | 总闸一键 `APPLY_TO_POOL=0` |
| percent | **仍不启**（归 r5-d 单审） |

---

## 7. 未改动范围（c2b 本轮）

- **仅文档**；无业务代码、无 DB migration
- worker、`finalScore`、`MatchResult`、旧 `PreviewPool`、正式匹配主链 — **未改**
- 云视觉 API、AI/人脸/颜值、性别推断 — **未做**

---

## 8. 参考

- [P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md)
- [P7.5-r5-c2-apply-writer-signoff-runner-closeout.md](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)
- [P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md)

# P7.5-r5-c2 — APPLY_TO_POOL Allowlist-only Writer 本地签收

> **状态**：**P7.5-r5-c2 本地自动化签收通过**（runner A–E 全 PASS · r5-c2a 后重跑）· **可进入 r5-c3**（仍 **不启** percent）  
> **对齐**：[P7.5-r5-c1 closeout](./P7.5-r5-c1-apply-to-pool-allowlist-writer-closeout.md)、[P7.5-r5-c0 plan](./P7.5-r5-c0-apply-to-pool-allowlist-writer-plan.md)、[P7.5-r5-c2a](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md)、[P7.5-r5-c2b](./P7.5-r5-c2b-page-local-signoff-closeout.md)

---

## 0. 边界与原则

- **本轮**：整理 **r5-c1 allowlist-only writer** 的本地签收步骤与验收表；**原则上只写 docs**，不新增业务功能。
- **验证对象**：真实 `generate()` 写库 — `OnboardingPhotoPreviewPool.sourceVersion` 与 items 在 allowlist 命中时可写 **v2**；否则 **v1 fallback**。
- **不启用** `PEIMA_ONBOARDING_VISION_APPLY_PERCENT` 灰度（writer **不读** percent；Case B 仅证明 **percent=100 不能绕过空 allowlist**）。
- **不改** worker、`finalScore`、`MatchResult`、旧 `PreviewPool`、正式匹配主链。
- **不接**云视觉 API；**不做** AI 美化 / AI 头像、人脸比对、身份认证、颜值评分；**不**根据照片自动判断性别。
- **无** DB migration。
- **每个 Case** 会 archive 旧 active pool 并生成新池（与页面「重新生成预览池」一致）；Case E 依赖 Case D 已产生过 **v2** 库内记录（旧 v2 **不批量改写**）。

---

## 1. 签收目标

验证 **P7.5-r5-c1** allowlist-only writer 在本地真实 `generate()` 中符合预期：

| # | 检查项 |
|---|--------|
| 1 | **Env off** → 生成 **v1** |
| 2 | `APPLY_TO_POOL=1` 但 **allowlist 为空**，即使 **`PERCENT=100`** → 仍 **v1**（`allowlist_empty`） |
| 3 | **Allowlist 未命中** → **v1**（`not_in_allowlist`） |
| 4 | **Allowlist 命中** + eligibility `ok` + guardrails pass → **v2**（`onboarding-photo-preview-v2-vision`） |
| 5 | **关闭 env 后再次 generate** → 回到 **v1**（rollback；不批量改旧 v2 pool） |
| 6 | **v2 pool** 仍通过 **active audit** |
| 7 | **v2** 不改 worker / `MatchResult` / `finalScore`（代码范围签收 + 无相关表写入） |
| 8 | **v2** 仅影响 onboarding photo preview **新生成** items |

---

## 2. 环境

| 项 | 值 |
|----|-----|
| API | `http://127.0.0.1:3000`（`localhost:3000`） |
| Web | `http://127.0.0.1:5173`（`localhost:5173`） |
| DB | 本地 PostgreSQL（根目录 `.env` → `DATABASE_URL`） |
| **viewerUserId** | `cmp70ft7a001e6znksjb2jo03` |
| **viewer gender** | **male**（异性门控：池中应为 **female** 候选） |
| **mappingPath** | `dev-assets/test-user-images/mapping.json` |
| **applySourceVersion**（v2 默认） | `onboarding-photo-preview-v2-vision` |

**Shadow / vision 前置（建议与 r4-e / r5-b1 一致）**

- `PEIMA_ONBOARDING_VISION_SHADOW_ENABLED=1`（或等价配置），否则无 shadow → writer 无法写 v2。
- Apply 总闸 **仅** `"1"` 为开（`"true"` **不算**），见 [r5-b closeout](./P7.5-r5-b-apply-eligibility-dry-run-closeout.md)。

---

## 3. 前置准备命令

在仓库根 **build API** 后，在 **`apps/api`** 下用 **dotenv-cli** 加载根目录 `.env`。

```bash
pnpm --filter @peima/api run build
```

```bash
cd apps/api

npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r4-h-import-candidate-images-runner.js --folder="C:\Users\34713\Desktop\peima\dev-assets\test-user-images" --dryRun=false --debugBlockedRows=true
```

```powershell
$env:P75_R4_TEST_USER_IMAGES_MAPPING="C:\Users\34713\Desktop\peima\dev-assets\test-user-images\mapping.json"
```

```bash
npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r4-m-preview-pool-funnel-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03
```

**Funnel 期望（male viewer）**

| 指标 | 期望 |
|------|------|
| `after_opposite_gender_gate_final` | **≥ 6** |
| `after_opposite_gender_gate_final_by_gender.female` | **≥ 6**（异性候选充足） |

**本机前置检查（2026-05-16）**

- 已执行 funnel：`after_opposite_gender_gate_final_by_gender.female = 14` → **满足 ≥ 6**。
- **五案 runner**（`generate` + active audit）已 **全 PASS**（§8）；浏览器 UI 视觉检查可作为 **r5-c3 staging / PM spot-check** 补充，非 c2 硬门槛。

---

## 4. 本地签收 Case（A–E）

**操作通则**

1. 修改 **仓库根 `.env`** 中 apply 四变量（下表）→ **重启 API**（env 在进程启动时读取）。
2. Web 打开 §7 页面 → **「重新生成预览池」**（或等价 API `generate`）。
3. 运行 §5 **active audit** CLI。
4. 按 §6 检查 **最新 active pool** 与 **最新 shadow** `payload_json`。
5. 将结果填入 **§8**。

---

### Case A：env off

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=0
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

**期望**

| 维度 | 期望 |
|------|------|
| Pool | `sourceVersion = onboarding-photo-preview-v1` |
| Shadow | `summary.applyResult.applied = false`，`reason = env_disabled` |
| Dry-run | `summary.applyDryRun` **存在**；`applyDryRun.appliedToPool = false` |
| Root | `appliedToPool = false`（r5-c1 兼容策略） |
| Active audit | **PASS**（§5） |

---

### Case B：allowlist empty + percent 100

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=1
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=100
```

**期望**

| 维度 | 期望 |
|------|------|
| Pool | **仍 v1**（`onboarding-photo-preview-v1`） |
| Shadow | `applyResult.reason = allowlist_empty`；`applied = false` |
| 语义 | **percent 不允许绕过** allowlist-only |
| Active audit | **PASS** |

---

### Case C：allowlist miss

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=1
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=some-other-user
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

**期望**

| 维度 | 期望 |
|------|------|
| Pool | **v1** |
| Shadow | `applyResult.reason = not_in_allowlist`；`applied = false` |
| Active audit | **PASS** |

---

### Case D：allowlist hit（v2 主路径）

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=1
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=cmp70ft7a001e6znksjb2jo03
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
PEIMA_ONBOARDING_VISION_APPLY_SOURCE_VERSION=onboarding-photo-preview-v2-vision
```

（`APPLY_SOURCE_VERSION` 可省略，默认即为 v2 常量。）

**期望**

| 维度 | 期望 |
|------|------|
| Pool | `sourceVersion = onboarding-photo-preview-v2-vision` |
| Items | `candidateUserId` / `score` 与 **shadow 槽位** `shadowCandidateUserId` / `shadowScore` 一致（按 `rankInPool` 对齐） |
| 结构 | `rankInPool` 1–6；tier **3+2+1**；`displayMode` clear×3 / blurred×2 / hidden×1 |
| `reasonTags` | 含 `onboarding-photo-preview-v2-vision`、`vision:apply` |
| Shadow | `applyResult.applied = true`，`reason = ok`，`sourceVersion` 与 pool 一致 |
| Dry-run | `applyDryRun.appliedToPool` **仍为 false** |
| Root | `appliedToPool = false` |
| Active audit | **PASS**；`pool_source_version = onboarding-photo-preview-v2-vision` |
| 页面 | **6** 个候选正常展示（§7） |

**可选核对（items 与 shadow 同源）**

- 对比 `onboarding_photo_preview_pool_items` 与 shadow `slots[].shadowCandidateUserId`（同 `rankInPool`）。
- 若 shadow `wouldChange` 全 false，v2 items 可能与 v1 baseline 相同，但 **`sourceVersion` 仍应为 v2**。

---

### Case E：rollback / env off after v2

**前置**：先完成 **Case D**（库内存在 **archived** 的 v2 pool 亦可，active 将为新池）。

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=0
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

重新 **generate**。

**期望**

| 维度 | 期望 |
|------|------|
| 新 active pool | `sourceVersion = onboarding-photo-preview-v1` |
| 旧 v2 pool | **不批量修改**（仍为 archived / 非 active） |
| Shadow | `applyResult.applied = false`（如 `env_disabled`） |
| Active audit | **PASS** |
| 页面 | 仍显示 **6** 项 |

---

## 5. Active Audit 检查

每个 Case 生成后执行：

```bash
cd apps/api

npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r4-o2-preview-pool-active-audit-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath="C:\Users\34713\Desktop\peima\dev-assets\test-user-images\mapping.json"
```

**通过标准（每条 item）**

| 字段 | 期望 |
|------|------|
| `pool_item_count` | **6** |
| `duplicate_candidate_user_id` | **全 false** |
| `duplicate_image_source_key` | **全 false** |
| `violates_opposite_gender_gate` | **全 false** |
| `is_viewer_self` | **全 false** |
| `pool_source_version` | 与 Case 预期 **v1 / v2** 一致 |

---

## 6. Shadow / DB 检查

对每个 Case，查 **该次 generate 之后** 的 **最新 active pool** 与对应 **shadow** 行。

**表**

- `onboarding_photo_preview_pools` — `source_version`、`status`
- `onboarding_photo_preview_pool_items` — 6 条 item
- `onboarding_photo_preview_pool_shadows` — `shadow_type = visual_ranking_shadow`，`payload_json`

**SQL 示例（替换 `pool_id`）**

```sql
SELECT id, source_version, status, created_at
FROM onboarding_photo_preview_pools
WHERE user_id = 'cmp70ft7a001e6znksjb2jo03'
ORDER BY created_at DESC
LIMIT 3;

SELECT payload_json->'summary' AS summary
FROM onboarding_photo_preview_pool_shadows
WHERE pool_id = '<latest_active_pool_id>'
  AND shadow_type = 'visual_ranking_shadow';
```

**Shadow 期望（所有 Case）**

| 字段 | 期望 |
|------|------|
| `summary.applyDryRun` | **存在** |
| `summary.applyDryRun.appliedToPool` | **恒 false** |
| `summary.applyResult` | **存在** |
| `summary.applyResult.applied` | Case D：**true**；A/B/C/E：**false** |
| `summary.applyResult.sourceVersion` | 与当次 writer 决策一致（v2 或 v1 语义） |
| 根 `appliedToPool` | **false**（r4-c 兼容） |
| `pool.sourceVersion` | 与 `applyResult.sourceVersion` **对齐**（Case D 均为 v2 串） |

---

## 7. 浏览器 UI 手测（可选 · r5-c2b）

**签收口径**：c2 **以 runner 自动化签收为准**（真实 `generate()` + r4-o2 active audit）。下列 Web 步骤 **不阻塞** 进入 r5-c3；可在 staging / PM spot-check 时复用。

**URL**

`/onboarding/photo-preview?userId=cmp70ft7a001e6znksjb2jo03`

（Web 示例：`http://127.0.0.1:5173/onboarding/photo-preview?userId=cmp70ft7a001e6znksjb2jo03`）

**范围**：可选复测 **Case D / E** 观感（runner 已覆盖 A–E 写库与 audit）。

---

### Case D：allowlist hit → v2

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=1
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=cmp70ft7a001e6znksjb2jo03
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

**操作**

1. 重启 API  
2. 打开页面  
3. 点击 **「重新生成预览池」**  
4. 确认页面正常显示 **6** 个 items  
5. 运行 §5 active audit，确认：  
   - `pool_source_version = onboarding-photo-preview-v2-vision`  
   - `duplicate_candidate_user_id` 全 **false**  
   - `duplicate_image_source_key` 全 **false**  
   - `violates_opposite_gender_gate` 全 **false**  
   - `is_viewer_self` 全 **false**

---

### Case E：rollback → v1

**前置**：先完成 Case D。

```env
PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=0
PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS=
PEIMA_ONBOARDING_VISION_APPLY_PERCENT=0
```

**操作**

1. 重启 API  
2. 点击 **「重新生成预览池」**  
3. 新 active pool：`sourceVersion = onboarding-photo-preview-v1`  
4. 页面仍显示 **6** 个 items  
5. 库内 **不**批量修改旧 v2 pool  
6. active audit 仍 **PASS**

---

### 浏览器 签收记录（可选）

| Case | 自动化（runner） | 浏览器 UI（可选） |
|------|------------------|-------------------|
| D | **PASS**（v2 · 6 items · audit 全 false） | 无额外截图；staging/PM 可补 |
| E | **PASS**（rollback v1 · 6 items · audit PASS） | 无额外截图；staging/PM 可补 |

详见 [P7.5-r5-c2b-page-local-signoff-closeout.md](./P7.5-r5-c2b-page-local-signoff-closeout.md)。

---

## 8. 签收表格

### 8.1 Runner actual（`p7.5-r5-c2-apply-writer-signoff-v1` · 本机重跑 · r5-c2a 后）

| 字段 | 值 |
|------|-----|
| `schemaVersion` | `p7.5-r5-c2-apply-writer-signoff-v1` |
| `viewerUserId` | `cmp70ft7a001e6znksjb2jo03` |
| `viewerIdMasked` | `cmp7…jo03` |

**本机命令**

```bash
cd C:\Users\34713\Desktop\peima\apps\api
npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r5-c2-apply-writer-signoff-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath=../../dev-assets/test-user-images/mapping.json
```

| Case | result | actual `sourceVersion` | `applied` | `reason` | `poolItemCount` | audit |
|------|--------|------------------------|-----------|----------|-----------------|-------|
| A | **PASS** | `onboarding-photo-preview-v1` | false | `env_disabled` | 6 | pass（dup cand/src/gender/self 均 false） |
| B | **PASS** | `onboarding-photo-preview-v1` | false | `allowlist_empty` | 6 | pass |
| C | **PASS** | `onboarding-photo-preview-v1` | false | `not_in_allowlist` | 6 | pass |
| D | **PASS** | `onboarding-photo-preview-v2-vision` | **true** | `ok` | 6 | pass |
| E | **PASS** | `onboarding-photo-preview-v1` | false | `env_disabled` | 6 | pass |

**summary（全部 true）**：`allPass` · `caseDAppliedV2` · `caseERolledBackToV1` · `v2PoolsRetainedAfterRollback` · `activeAuditPassAllCases` · `noWorkerOrMatchResultTouched` · exit **0**

**跑完 runner 后 DB 状态说明**：Case 顺序为 A→B→C→D→E；**最后一步为 Case E**，故 viewer 当前 **active pool 为 v1** 属 **正常**（rollback 验收）。库内仍保留 Case D 产生的 **archived v2** pool（`v2PoolsRetainedAfterRollback=true`）。

| Case | Env 摘要 | expected sourceVersion | actual sourceVersion | expected applyResult | actual applyResult | activeAuditPass | result |
|------|-----------|------------------------|----------------------|----------------------|--------------------|-----------------|--------|
| A | APPLY=0 | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `env_disabled` | `applied=false`, `env_disabled` | true | **PASS** |
| B | APPLY=1, allowlist=∅, percent=100 | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `allowlist_empty` | `applied=false`, `allowlist_empty` | true | **PASS** |
| C | APPLY=1, allowlist=miss | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `not_in_allowlist` | `applied=false`, `not_in_allowlist` | true | **PASS** |
| D | APPLY=1, allowlist=hit | `onboarding-photo-preview-v2-vision` | `onboarding-photo-preview-v2-vision` | `applied=true`, `ok` | `applied=true`, `ok` | true | **PASS** |
| E | rollback APPLY=0 | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `env_disabled` | `applied=false`, `env_disabled` | true | **PASS** |

**历史**：c2 初跑 Case D 曾为 `insufficient_unique_source_images`（shadow `sourceImageKey` 未全异）；[r5-c2a](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md) 修复后 **actual 五案全 PASS**。

### 8.2 自动化签收 vs 浏览器（r5-c2b）

| 维度 | Case D | Case E |
|------|--------|--------|
| 自动化 `generate` + active audit | **PASS**（v2 / 6 items / 无 violation） | **PASS**（rollback v1 / 6 items / audit PASS） |
| 浏览器 UI 截图 | 无额外留存 | 无额外留存 |
| 补充验证 | 建议在 **r5-c3 staging / PM spot-check** 做视觉抽查 | 同上 |

### 8.3 使用 P7.5-r5-c2 runner 自动填 §8.1 actual（推荐）

DEV-only 脚本 **`p75:r5-c2-apply-writer-signoff`**：单进程内对五组 apply env 做 **snapshot → 覆盖 → `generate()` → 读 pool/shadow → active audit → 恢复 env**，stdout 输出 **`p7.5-r5-c2-apply-writer-signoff-v1`** JSON。

**前置**

- `pnpm --filter @peima/api run build`
- 根目录 `.env` 含有效 **`DATABASE_URL`**
- `PEIMA_ONBOARDING_VISION_SHADOW_ENABLED=1`（或等价，shadow 可写）
- viewer 满足 `generate()` 门槛（与 r5-b2 / r4-o3 一致）
- §3 funnel：`after_opposite_gender_gate_final` **≥ 6**

**命令（仓库根 / `apps/api` + dotenv-cli，推荐）**

```bash
cd apps/api
npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r5-c2-apply-writer-signoff-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath=../../dev-assets/test-user-images/mapping.json
```

（本机 Windows 路径示例见 §8.1。）

**pnpm script**（仅当 shell 已导出 `DATABASE_URL`）：

```bash
pnpm --filter @peima/api run p75:r5-c2-apply-writer-signoff -- --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath=dev-assets/test-user-images/mapping.json
```

**填入 §8**

- 将 stdout `cases[]` 与上表对照；`summary.allPass === true` 且 exit **0** → 自动化五案通过。
- 详见 [P7.5-r5-c2-apply-writer-signoff-runner-closeout.md](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)。

---

## 9. 签收结论

> **P7.5-r5-c2 allowlist-only writer 本地自动化签收通过**（2026-05-16 · 本机 runner 重跑）

- **Runner A–E 全 PASS**（`summary.allPass=true`）。
- **Case D**：成功写 **v2**（`onboarding-photo-preview-v2-vision`，`applyResult.applied=true`，`reason=ok`）。
- **Case E**：成功 **rollback** 至 **v1**（`env_disabled`）；`v2PoolsRetainedAfterRollback=true`（旧 v2 pool 保留、不批量改）。
- **Active audit**：五案 `activeAuditPassAllCases=true`（每案 6 items；duplicate candidate / duplicate source image / 异性门控 / self 均无 violation）。
- **跑完 runner 后** viewer 最新 **active pool 为 v1**（因末案为 Case E）— **预期行为**。
- **未影响** worker / `MatchResult` / `finalScore`（`noWorkerOrMatchResultTouched=true`）。
- **浏览器 UI**：无单独截图；**不阻塞** c2 签收；staging / PM 可补视觉抽查。
- **可以进入** [P7.5-r5-c3 staging signoff](./P7.5-r5-c3-apply-to-pool-staging-signoff.md) + PM spot-check。
- **仍不允许** 开启 **percent** 灰度（writer 不读 percent；归 r5-d）。

**若 runner 任一案 FAIL**

- 记录 `caseError`、脱敏 shadow `summary`、audit 输出；
- 数据类：r4-h import + funnel；代码类：修 r5-c1 / r5-c2a；
- **不**进入 r5-c3。

---

## 10. 已知限制

- 当前仍为 **allowlist-only**；**percent 灰度未启用**（writer 不读 percent）。
- Demo 素材仍可能存在 **视觉性别与 DB gender 不一致**；**不**使用 AI / 人脸识别判断性别。
- **Active audit** 是进入 staging 前的硬门槛。

---

## 11. 未改动范围（签收所确认）

- worker **未改**
- `finalScore` **未改**
- `MatchResult` **未改**
- 旧 `PreviewPool` **未改**
- 正式匹配主链 **未改**
- **percent 灰度未启用**（writer 不读 percent）
- 云 API **未接**
- AI 美化 / AI 头像、人脸比对、身份认证、颜值评分 **未做**
- **未**根据照片自动判断性别
- **无** DB migration

---

## 12. 下一步建议

**c2 已通过** → 进入 **[P7.5-r5-c3 staging signoff](./P7.5-r5-c3-apply-to-pool-staging-signoff.md)** + **PM spot-check**：

| 项 | 要求 |
|----|------|
| staging env | 默认 **关闭** apply |
| 开启范围 | **仅** allowlist viewer |
| PM spot-check | **≥ 20** pools |
| active audit | 全 PASS |
| 回滚 | `APPLY_TO_POOL=0` 一键关闭 |
| percent | **仍不启**（归 r5-d） |

---

## 附录：参考文档

- [P7.5-r5-c2b-page-local-signoff-closeout.md](./P7.5-r5-c2b-page-local-signoff-closeout.md)
- [P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md](./P7.5-r5-c2a-shadow-source-image-dedup-fix-closeout.md)
- [P7.5-r5-c2-apply-writer-signoff-runner-closeout.md](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)
- [P7.5-r5-c1-apply-to-pool-allowlist-writer-closeout.md](./P7.5-r5-c1-apply-to-pool-allowlist-writer-closeout.md)
- [P7.5-r5-c0-apply-to-pool-allowlist-writer-plan.md](./P7.5-r5-c0-apply-to-pool-allowlist-writer-plan.md)
- [P7.5-r5-b1-apply-eligibility-dry-run-local-signoff.md](./P7.5-r5-b1-apply-eligibility-dry-run-local-signoff.md)
- [P7.5-r5-b2-apply-dry-run-signoff-closeout.md](./P7.5-r5-b2-apply-dry-run-signoff-closeout.md)
- [P7.5-r4-o2-preview-pool-active-audit-and-image-source-dedup-closeout.md](./P7.5-r4-o2-preview-pool-active-audit-and-image-source-dedup-closeout.md)
- [P7.5-r4-o3-preview-pool-local-signoff.md](./P7.5-r4-o3-preview-pool-local-signoff.md)

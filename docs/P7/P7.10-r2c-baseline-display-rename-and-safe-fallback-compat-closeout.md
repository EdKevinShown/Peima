# P7.10-r2c — Baseline Display Rename and Safe Fallback Compat Closeout

> **状态**：**P7.10-r2c baseline display rename + safe fallback compat**  
> **结论**：**`P7_10_R2C_BASELINE_DISPLAY_RENAME_AND_SAFE_FALLBACK_COMPAT_COMPLETE`**  
> **前置**：[P7.10-r1](./P7.10-r1-legacy-photo-matching-dependency-audit-and-replacement-strategy.md) · [P7.10-r2](./P7.10-r2-legacy-terminology-replacement-and-safe-fallback-plan.md) · [P7.10-r2b](./P7.10-r2b-safe-fallback-env-alias-implementation-closeout.md)  
> **性质**：小范围代码 rename / alias / 测试 · **无** API contract 变更 · **无**主链行为变更

---

## 1. 一句话结论

P7.10-r2c 完成内部 **`legacyDisplay` → `baselineDisplay`** 与 **`P76ReadPathBaselineDisplayInput`** 类型重命名，保留 **`legacyDisplay` / `P76ReadPathLegacyDisplayInput` 为 deprecated alias**；`p76-r9d-execute.mjs` 监控 summary **双写** `safeFallbackCount` + `fallbackLegacyCount`；env **SAFE_FALLBACK** 优先于 **FALLBACK_LEGACY**（r2b 已存在，本轮补测试确认）。**displaySourceType API 值、MatchResult 字段、worker 行为不变。**

---

## 2. 修改文件列表

| File | Change |
|------|--------|
| `apps/api/src/modules/matching/p76-read-path-display-resolver.ts` | Baseline types; `resolveBaselineDisplayInput`; deprecated aliases |
| `apps/api/src/modules/matching/matching-result-display.ts` | `baselineDisplay:` callsite |
| `apps/api/scripts/p76-r9d-execute.mjs` | `safeFallbackCount` + deprecated `fallbackLegacyCount` |
| `apps/api/test/p76-read-path-display-resolver.spec.ts` | Rename helpers; safe-fallback test names; alias + reason tests |
| `apps/api/test/p77-r1-read-path-negative-regression.spec.ts` | `baselineDisplay` helper/callsites |
| `apps/api/test/p76-read-path-env.spec.ts` | Describe tag r2c |

**Docs (links only):** r1 · r2 plan · P7.6 bidirectional plan · this closeout

**Not modified:** `docs/P7/P7.10-r7b-admin-apply-canonical-write-design.md` (per scope)

---

## 3. Rename 摘要

| Before | After |
|--------|-------|
| `P76ReadPathLegacyDisplayInput` | `P76ReadPathBaselineDisplayInput` (+ type alias export) |
| `legacyDisplay` (resolver input field) | `baselineDisplay` (preferred) |
| `legacyDisplay` local variable | `baseline` |
| Test helper `legacyDisplay()` | `baselineDisplay()` |
| Comment「Legacy display slice」 |「Baseline display … Not legacy photo matching」 |

**未改名（API 稳定）：** `match_result_original` · `static_fallback` · `p76_allowlist_sidecar_readonly` · `P76ReadPathEnv.fallbackLegacy` (deprecated field = `safeFallbackEnabled`)

---

## 4. Env alias 兼容

| Rule | Behavior |
|------|----------|
| `PEIMA_P76_READ_PATH_SAFE_FALLBACK` | **Preferred** when set |
| `PEIMA_P76_READ_PATH_FALLBACK_LEGACY` | **Deprecated alias** when SAFE unset |
| Both unset | Default **`safeFallbackEnabled=true`** |
| Both set | **SAFE wins** |

**Tests:** `apps/api/test/p76-read-path-env.spec.ts` — **6/6 PASS** (r2b + r2c tag)

---

## 5. Monitoring / artifact 兼容

| Field | Status |
|-------|--------|
| `safeFallbackCount` | **Added** in `p76-r9d-execute.mjs` monitoring snapshot |
| `fallbackLegacyCount` | **Retained** — same numeric value (deprecated alias) |

Grafana / production log facets (`fallback_legacy`) — **unchanged** this round (per r2 plan §5E).

---

## 6. 行为不变确认

| Item | Verified |
|------|----------|
| `displaySourceType` API enum values | Unchanged (`match_result_original`, `static_fallback`, `p76_allowlist_sidecar_readonly`) |
| `MatchResult.candidateUserId` | Unchanged (display overlay only) |
| `finalScore` | Unchanged |
| Worker / batch-match | Not touched |
| `fallbackReason` semantics | Still safe-fallback (env/sidecar/guardrail) — **not** photo pool |

---

## 7. 测试结果

| Command | Result |
|---------|--------|
| `pnpm exec jest test/p76-read-path-display-resolver --no-cache` | **PASS** (21 tests) |
| `pnpm exec jest test/p77-r1-read-path-negative-regression --no-cache` | **PASS** (6 tests) |
| `pnpm exec jest test/p76-read-path-env --no-cache` | **PASS** (6 tests) |
| `pnpm exec jest test/matching-result-display-candidate --no-cache` | **PASS** (45 tests) |
| `npx tsc -p tsconfig.json --noEmit` | **PASS** |
| `pnpm exec nest build` | **PASS** |

---

## 8. PASS / BLOCK

### PASS

- Internal naming cleaned (`baselineDisplay`)
- Safe fallback env compat confirmed (r2b)
- Old env not removed
- API contract not broken
- Tests green

### BLOCK (unchanged program gates)

- Delete old env without alias
- Rename API `displaySourceType` values
- Change `finalScore` / MatchResult write behavior
- Describe fallback as legacy photo matching
- Legacy photo matching code removal
- r9f / production rollout
- r9e3 prod reverify

---

## 9. 边界确认

- No legacy photo matching code deletion
- No MatchResult / finalScore schema change
- No worker change
- No DB migration
- No production / percent
- r9f still **blocked**

---

## 10. 下一步

| Priority | Milestone |
|----------|-----------|
| 1 | **P7.10-r3** — [P7.6 canonical writer design](./P7.10-r3-p76-canonical-writer-design.md) **DONE** |
| 2 | **P7.10-r4** — Old photo matching writer disable plan |
| 3 | **P7.10-r5** — Safe fallback full implementation ([r4 contract](./P7.10-r4-safe-fallback-api-contract.md)) |
| 4 | **P7.7-r1** — Safe fallback negative regression pack (extend as needed) |
| 5 | **P7.10-r2d** *(suggested)* — Log/metric dual-emit `safe_fallback` facet in API (optional) |

**Ops:** [P7.6-r9e3b](./P7.6-r9e3b-real-prod-reverify-access-package-and-ops-handoff.md) before r9e3c.

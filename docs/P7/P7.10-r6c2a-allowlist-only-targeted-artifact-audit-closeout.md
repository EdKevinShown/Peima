# P7.10-r6c2a — Allowlist-only Targeted Artifact Audit Closeout

## 1. Status

```text
P7_10_R6C2A_ALLOWLIST_ONLY_TARGETED_ARTIFACT_AUDIT_COMPLETE
```

- **Targeted artifact audit only** — read-only Prisma + r6a runner
- **No code change**
- **No DB write**
- **No sidecar write** (allowlist writer not run)
- **No** `MatchResult` write
- **No** `matchInsights` write
- **No** worker integration
- **No** GET behavior change
- **No** frontend change
- **No** dual-write implementation
- **No** rehearsal table / migration
- **No** legacy removal
- **No** production / percent rollout

---

## 2. Commands run

| Command | Result |
|---------|--------|
| `git status --short` (pre-flight) | Clean |
| `pnpm --filter @peima/api run build` | **PASS** |
| Read-only `p76AllowlistApplyMeta.findMany` for cohort `p7.6-r7j3-staging-cohort-v1` | **5** viewers found |
| `node -r dotenv/config dist/dev-cli/p710-r6a-canonical-writer-shadow-audit-runner.js --viewerUserId=<redacted> --limit=20 --output=artifacts/p76/r6c2a/viewer-<n>-….json --pretty` × **5** | **PASS** (exit 0 each) |
| `node apps/api/scripts/p77-r2-local-smoke-artifact-check.mjs` on each per-viewer + combined artifact | **PASS** (6 files) |
| Inline merge → `canonical-writer-shadow-audit-targeted.json` | **PASS** |

`DATABASE_URL` loaded from repo-root `.env` via `DOTENV_CONFIG_PATH` (value not recorded). **Full `viewerUserId` values are not recorded in this closeout** (redacted labels only).

---

## 3. Target cohort

| Item | Value |
|------|-------|
| `sourceVersion` / read-path cohort | `p7.6-r7j3-staging-cohort-v1` (unchanged; matches [r6c1](./P7.10-r6c1-repeated-artifact-audit-and-sidecar-coverage-closeout.md)) |
| Sidecar-covered viewer count (DB) | **5** |
| Rolled-back sidecar rows (DB) | **1** (mapped to **viewer_5** in redacted summary) |
| `appliedToMatchResult=true` (DB) | **0** |
| Viewer label assignment | `viewer_1` … `viewer_5` = lexicographic order of `viewerUserId` from read-only DB query |
| ID redaction | This closeout and `sidecar-covered-viewer-audit-summary.*` use labels only; per-viewer JSON artifacts on disk may contain IDs in `rows[]` for local debugging |

---

## 4. Artifact outputs

| Output | Path |
|--------|------|
| Per-viewer audits | `artifacts/p76/r6c2a/viewer-1-canonical-writer-shadow-audit.json` … `viewer-5-…` |
| Combined targeted audit | `artifacts/p76/r6c2a/canonical-writer-shadow-audit-targeted.json` |
| Targeted summary | `artifacts/p76/r6c2a/canonical-writer-shadow-audit-targeted-summary.md` |
| Coverage summary | `artifacts/p76/r6c2a/sidecar-covered-viewer-audit-summary.md` / `.json` |
| Closeout | `docs/P7/P7.10-r6c2a-allowlist-only-targeted-artifact-audit-closeout.md` |

---

## 5. Metrics

| Metric | Value |
|--------|-------|
| targetedViewerCount | 5 |
| totalRows | 5 |
| eligibleCount | 4 |
| blockedCount | 1 |
| missingSidecarCount | 0 |
| sidecarCoverageRate | **1.00** |
| missingSidecarRate | **0.00** |
| wouldChangeCandidateCount | 4 |
| appliedToMatchResultCount | 0 |

**Comparison ([r6c1](./P7.10-r6c1-repeated-artifact-audit-and-sidecar-coverage-closeout.md) recent-50 vs r6c2a):**

| Metric | r6c1 | r6c2a |
|--------|------|-------|
| sidecarCoverageRate | 0.10 | **1.00** |
| missingSidecarRate | 0.90 | **0.00** |
| missing_sidecar (count) | 45 / 50 | 0 / 5 |

---

## 6. Reason counts

| Reason | Count |
|--------|-------|
| `ok` | 4 |
| `rolled_back` | 1 |
| `missing_sidecar` | 0 |

**Score delta bands:** `none` 4, `unknown` 1.

---

## 7. Per-viewer redacted summary

| Viewer label | Rows | Eligible | missing_sidecar | rolled_back | wouldChangeCandidate |
|--------------|-----:|---------:|----------------:|------------:|---------------------:|
| viewer_1 | 1 | 1 | 0 | 0 | 1 |
| viewer_2 | 1 | 1 | 0 | 0 | 1 |
| viewer_3 | 1 | 1 | 0 | 0 | 1 |
| viewer_4 | 1 | 1 | 0 | 0 | 1 |
| viewer_5 | 1 | 0 | 0 | 1 | 0 |

Each active viewer had **1** recent `MatchResult` row in the audit window (`--limit=20`); no `missing_sidecar` on any targeted run.

---

## 8. Safety verification

- `appliedToMatchResultCount` = **0** (combined + each per-viewer artifact)
- All `rows[].shadow.appliedToMatchResult` = **false** (artifact check PASS)
- No sensitive keys in serialized JSON (artifact check PASS)
- **No** Prisma `create` / `update` / `upsert` / `delete`
- **No** `MatchResult` / `matchInsights` mutation
- **No** sidecar writer / backfill executed

---

## 9. Coverage interpretation

| Question | Answer |
|----------|--------|
| `sidecarCoverageRate >= 0.80` on targeted cohort? | **Yes — 1.00** |
| Proves r6c1 low coverage is sampling / allowlist alignment? | **Yes** — same 4 `ok` eligible viewers as r6c1; zero `missing_sidecar` when filtering to sidecar-covered viewers |
| Shadow builder failure? | **No** — blocked row is `rolled_back` (viewer_5), not missing lookup |
| Sufficient for **P7.10-r6d** docs-only schema design? | **Yes** — builder + lookup validated on covered cohort |
| Sufficient for migration / rehearsal DB write? | **No** — still requires PM/Ops signoff, r6c2b if broader row sample needed, P7.7-r2 smoke after any future sidecar data change |

**Gate note ([P7.10-r6c2](./P7.10-r6c2-local-sidecar-coverage-remediation-plan.md)):** unfiltered recent-50 sample **still fails** sidecarCoverageRate ≥ 0.50; targeted cohort **passes** strong target (≥ 0.80).

---

## 10. PASS / BLOCK

### PASS (this round)

- Targeted audit executed on all DB sidecar-covered viewers
- Covered-cohort metrics recorded (redacted summary)
- Safety checks passed
- No-write invariant preserved

### BLOCK (unchanged)

- No sidecar backfill ([P7.10-r6c2b](./P7.10-r6c2-local-sidecar-coverage-remediation-plan.md) not executed)
- No rehearsal sidecar table / migration
- No dual-write implementation
- No canonical production write
- No worker integration
- No legacy removal / PreviewPool disable
- No production / percent rollout

---

## 11. Next recommended step

Because **sidecarCoverageRate = 1.00 ≥ 0.80** on the allowlist-covered cohort:

| Priority | Milestone | Scope |
|----------|-----------|-------|
| **Recommended** | **P7.10-r6d** — Dedicated rehearsal sidecar schema design | **Docs-only** — table contract from [P7.10-r6c §5](./P7.10-r6c-canonical-writer-dual-write-rehearsal-design.md); **no migration** |
| Optional | **P7.10-r6c2b** — Local sidecar backfill runbook | Only if a **larger** row-level sample is needed beyond 5 viewers; still blocked from production |
| Defer | **P7.10-r6c2c** — Runner diagnostics | Not required for this result; use if future targeted audit shows low coverage |

**Always block:** canonical production write, PreviewPool writer disable, percent rollout (`r9e3`, `r9f`).

**Do not implement:** r6d migration, rehearsal sidecar writer, or dual-write in code until explicit gate 15 signoff and implementation milestone approval.

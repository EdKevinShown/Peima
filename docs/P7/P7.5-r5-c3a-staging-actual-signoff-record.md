# P7.5-r5-c3a — Staging Actual Signoff Record

> **签收状态**：**PENDING**（S1–S5 + audit + rollback **PASS** · **PM spot-check 二十行待补**）  
> **Runbook**：[P7.5-r5-c3 staging signoff](./P7.5-r5-c3-apply-to-pool-staging-signoff.md)  
> **本地前置**：[P7.5-r5-c2](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md) 自动化签收已通过

---

## 1. 一句话结论

本文档记录 **staging actual** 执行结果。仅当 **S1–S5 全 PASS**、**active audit 0 violation**、**PM spot-check ≥ 20 pools**、**0 P0/P1**、**rollback 成功** 后，才允许进入 **P7.5-r5-c4** closeout。

**当前（2026-05-16）**

- **自动化签收（S1–S5 + active audit + rollback）**：**PASS**（`p75:r5-c2-apply-writer-signoff` + r4-o2 audit，见 §4–§7）。
- **PM spot-check**：§6.1 **二十行明细待补**（汇总行待 PM 确认 `poolsReviewed ≥ 20`）。
- **整体**：**PENDING** — 补全 §6 后方可升为 **PASS** 并进入 c4。

---

## 2. 环境

| 字段 | 值 |
|------|-----|
| **Environment** | staging-equivalent（本机 dotenv + 本地 PostgreSQL；与 c2 同链路验证） |
| **API base URL** | `http://127.0.0.1:3000` |
| **Web URL** | `http://127.0.0.1:5173` |
| **DB / Prisma env** | 根目录 `.env` → `DATABASE_URL`（dotenv-cli 注入） |
| **Viewer allowlist（测试用）** | |
| · `V_hit`（allowlist 命中） | `cmp70ft7a001e6znksjb2jo03` |
| · `V_miss`（allowlist 未命中，runner Case C） | `some-other-user`（env 占位，非真实 cuid） |
| · PM 抽查 viewer | _待补_ |
| **PEIMA_ONBOARDING_VISION_APPLY_TO_POOL** | 按案切换（S1/S5=`0`；S2=`1`+hit；S3=`1`+miss；S4=`1`+空 allowlist） |
| **PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS** | S2：`cmp70ft7a001e6znksjb2jo03`；S3：不含 viewer；S4：空 |
| **PEIMA_ONBOARDING_VISION_APPLY_PERCENT** | `0`（S4 试跑为 `100`，writer **仍不读** percent） |
| **PEIMA_ONBOARDING_VISION_APPLY_SOURCE_VERSION** | `onboarding-photo-preview-v2-vision` |
| **PEIMA_ONBOARDING_VISION_SHADOW_ENABLED** | `1`（或等价，shadow 可写） |
| **Runner / audit command source** | 本机 `apps/api` + `dist/dev-cli/*`；见下方命令 |
| **mappingPath** | `dev-assets/test-user-images/mapping.json` |
| **Executed by** | _待补_ |
| **Date** | 2026-05-16 |

**本机执行命令（staging actual 来源）**

```bash
cd C:\Users\34713\Desktop\peima\apps\api
npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r5-c2-apply-writer-signoff-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath=../../dev-assets/test-user-images/mapping.json

npx dotenv-cli -e ../../.env -- node dist/dev-cli/p75-r4-o2-preview-pool-active-audit-runner.js --viewerUserId=cmp70ft7a001e6znksjb2jo03 --mappingPath=../../dev-assets/test-user-images/mapping.json
```

**生产 env**：`PEIMA_ONBOARDING_VISION_APPLY_TO_POOL=0`（默认关闭，未在本轮开启）。

---

## 3. 前置检查

| Check | Expected | Actual | Result |
|-------|----------|--------|--------|
| API build / deployment success | API 可访问 | `pnpm --filter @peima/api run build` 成功；runner exit 0 | **PASS** |
| Web deployment success | 预览页可打开 | `http://127.0.0.1:5173`（与 c2 一致） | **PASS** |
| `DATABASE_URL` / staging secret injected | 无 `DATABASE_URL_missing` | dotenv-cli 加载根 `.env` | **PASS** |
| Candidate data enough | funnel 可跑 | import + funnel 已满足（c2 前置） | **PASS** |
| Viewer gender valid | `male` / `female` | `V_hit` = **male** | **PASS** |
| Funnel `after_opposite_gender_gate_final` | **≥ 6** | female ≥ 6（c2 前置 funnel） | **PASS** |
| Shadow persist enabled | `SHADOW_ENABLED=1` | Case D 写 v2 成功 | **PASS** |
| Active audit CLI can run | exit 0，有 JSON | r4-o2 audit 有输出（§5） | **PASS** |
| **Production** `APPLY_TO_POOL` | **默认 `0` / 关闭** | 未在生产开启 apply | **PASS** |

---

## 4. Staging Case Results（S1–S5）

与 `p7.5-r5-c2-apply-writer-signoff-v1` 五案对齐（A→S1 … E→S5）。

| Case | Env | Expected `sourceVersion` | Actual `sourceVersion` | `applyResult` | `activeAuditPass` | pageCheck | Result | Notes |
|------|-----|---------------------------|------------------------|---------------|-------------------|-----------|--------|-------|
| **S1** | `APPLY=0` | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `env_disabled` | true | 6 items（runner） | **PASS** | pool `cmp8…o5w7` |
| **S2** | `APPLY=1`, allowlist **hit** | `onboarding-photo-preview-v2-vision` | `onboarding-photo-preview-v2-vision` | `applied=true`, `ok` | true | 6 items（runner） | **PASS** | pool `cmp8…b6gt` |
| **S3** | `APPLY=1`, allowlist **miss** | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `not_in_allowlist` | true | — | **PASS** | pool `cmp8…128b` |
| **S4** | `APPLY=1`, allowlist **∅**, `PERCENT=100` | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `allowlist_empty` | true | — | **PASS** | pool `cmp8…o8d7` |
| **S5** | rollback `APPLY=0` | `onboarding-photo-preview-v1` | `onboarding-photo-preview-v1` | `applied=false`, `env_disabled` | true | 6 items（runner） | **PASS** | pool `cmp8…is48` |

### c2 runner 摘要（一次性五案）

| 字段 | 值 |
|------|-----|
| `schemaVersion` | `p7.5-r5-c2-apply-writer-signoff-v1` |
| `viewerIdMasked` | `cmp7…jo03` |
| `summary.allPass` | `true` |
| `summary.caseDAppliedV2` | `true` |
| `summary.caseERolledBackToV1` | `true` |
| `summary.v2PoolsRetainedAfterRollback` | `true` |
| `summary.activeAuditPassAllCases` | `true` |
| `summary.noWorkerOrMatchResultTouched` | `true` |

> **跑完 runner 后**：末案 **S5/E** 为 rollback，viewer **当前 active pool = v1**（示例 audit：`pool_source_version=v1`，pool `cmp8…8kte`）— **正常**。

---

## 5. Active Audit Summary

每案 runner 报告 `activeAuditPass=true`；`poolItemCount=6`；四项 violation 均为 **false**。

| Case | `pool_item_count` | `duplicate_candidate_user_id` | `duplicate_image_source_key` | `violates_opposite_gender_gate` | `is_viewer_self` | `pool_source_version` | Result |
|------|-------------------|-------------------------------|------------------------------|--------------------------------|------------------|----------------------|--------|
| S1 | 6 | false（全 item） | false | false | false | `onboarding-photo-preview-v1` | **PASS** |
| S2 | 6 | false | false | false | false | `onboarding-photo-preview-v2-vision` | **PASS** |
| S3 | 6 | false | false | false | false | `onboarding-photo-preview-v1` | **PASS** |
| S4 | 6 | false | false | false | false | `onboarding-photo-preview-v1` | **PASS** |
| S5 | 6 | false | false | false | false | `onboarding-photo-preview-v1` | **PASS** |

**S5 后抽样 audit（rollback 后 active v1）**

- `pool_source_version`: `onboarding-photo-preview-v1`
- `pool_item_count`: 6
- 全 item：`duplicate_*` / `violates_opposite_gender_gate` / `is_viewer_self` = **false**
- `notes`: `[]`

---

## 6. PM Spot-check

**汇总**

| 字段 | 值 |
|------|-----|
| `poolsReviewed` | _待补_（须 **≥ 20**） |
| `p0p1Count` | _待补_（须 **0**） |
| `pmSignoff` | _待补_ |
| `signoffDate` | _待补_ |
| PM 签字人 | _待补_ |

**说明**：自动化五案各产生 1 个新 pool（共 5 个脱敏 pool id），**不能**替代 PM ≥20 池抽查。请将 PM 二十行表格粘贴至 §6.1 并更新汇总。

### 6.1 记录表（≥ 20 行 · 待 PM 粘贴）

| Pool # | viewer | sourceVersion | 6 items visible | no duplicate candidate | no duplicate image | no self | gender gate ok | perceived quality | PM result | note |
|--------|--------|---------------|-----------------|------------------------|--------------------|---------|----------------|-------------------|-----------|------|
| 1–5 | _见 §4 runner pools_ | v1/v2 混合 | _待 PM_ | _待 PM_ | _待 PM_ | _待 PM_ | _待 PM_ | _待 PM_ | _待 PM_ | 自动化案池，非 PM 签收样本 |
| 6–20 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |

---

## 7. Rollback Drill

| 项 | 记录 |
|----|------|
| **Rollback env** | `APPLY_TO_POOL=0` · `ALLOWLIST=` · `PERCENT=0` |
| **Redeploy / restart** | runner Case E 内 env 覆盖（单进程五案） |
| **Viewer** | `cmp70ft7a001e6znksjb2jo03` |
| **New generate `sourceVersion`** | `onboarding-photo-preview-v1` |
| **Old v2 pool retained?** | **是**（`v2PoolsRetainedAfterRollback=true`） |
| **Active audit after rollback** | **PASS**（v1 · 6 items · 0 violation） |
| **Page check** | 6 items（与 c2 runner 一致） |
| **Result** | **PASS** |

---

## 8. Known Issues

| ID | 级别 | 描述 | 阻塞 c4? |
|----|------|------|----------|
| K1 | **P2/P3** | Demo `mapping.json` 中 **mapping_gender** 与部分图片视觉观感可能不一致（例：audit item `mapping_gender=male` 但候选 `candidate_gender_raw=female`） | 否（不以 AI/人脸判性别） |
| K2 | **P2/P3** | 本记录为 **staging-equivalent 本机 dotenv** 执行，非独立远程 staging URL | 否 |
| — | — | **P0/P1**：_无（自动化路径）_ | — |

**若 PM 补录后出现 P0/P1** → §9 须改为 **FAIL**。

---

## 9. Signoff Decision

**当前勾选**：☐ PASS · ☐ FAIL · ☑ **PENDING**

---

### PASS（待 PM 补全后启用）

**条件（须全部满足）**

- [x] S1–S5 **全 PASS**
- [x] Active audit **全 PASS**（0 violation）
- [ ] PM spot-check **≥ 20** pools
- [ ] **0** 个 P0/P1
- [x] Rollback 演练 **成功**

**结论（PM 完成后写入）**

> **P7.5-r5-c3a staging allowlist-only signoff 通过。**  
> **允许进入** P7.5-r5-c4 allowlist-only APPLY_TO_POOL closeout。  
> **仍不允许** 开启 **percent** 灰度（须 r5-d 单独设计与评审）。

---

### FAIL

**条件（任一即 FAIL）**

- 任一 **S case** FAIL  
- Active audit **violation**  
- PM **P0/P1**  
- Rollback 失败  

**结论**

- **不允许**进入 **r5-c4**  
- **不允许**扩大 allowlist  
- **不允许**启用 **percent**

---

### PENDING（当前）

**条件**：PM §6 **未闭合**（`poolsReviewed < 20` 或二十行未填）。

**结论**

> 自动化路径（S1–S5 + audit + rollback）**已通过**；请补全 **§6 PM spot-check** 后更新为 **PASS** 或 **FAIL**。

---

## 10. 未改动范围

| 类别 | 状态 |
|------|------|
| 业务代码 | **未改**（c3a 仅 signoff record） |
| worker | **未改** |
| `finalScore` | **未改** |
| `MatchResult` | **未改** |
| 旧 `PreviewPool` | **未改** |
| 正式匹配主链 | **未改** |
| percent 灰度 | **未启用** |
| staging | **allowlist-only** |
| 生产 APPLY 总闸 | **默认关闭** |
| 云视觉 API | **未接** |
| AI 美化 / AI 头像 | **未做** |
| 人脸比对 / 身份认证 / 颜值评分 | **未做** |
| 根据照片自动判断性别 | **未做** |
| DB migration | **无新增** |

---

## 11. 下一步

| 签收状态 | 下一步 |
|----------|--------|
| **PASS**（PM 补全后） | 进入 **P7.5-r5-c4** allowlist-only APPLY_TO_POOL closeout + runbook |
| **PENDING**（当前） | 补全 **§6** PM 二十行 + 汇总 → 更新 §9 |
| **FAIL** | 修问题后 **重跑完整 c3a** |

---

## 参考

- [P7.5-r5-c3 runbook](./P7.5-r5-c3-apply-to-pool-staging-signoff.md)
- [P7.5-r5-c2 runner closeout](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)
- [P7.5-r5-c2 本地签收](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md)

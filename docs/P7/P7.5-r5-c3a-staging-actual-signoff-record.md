# P7.5-r5-c3a — Staging Actual Signoff Record

> **签收状态**：**PENDING**（actual 未填 · 待 staging 执行）  
> **Runbook**：[P7.5-r5-c3 staging signoff](./P7.5-r5-c3-apply-to-pool-staging-signoff.md)  
> **本地前置**：[P7.5-r5-c2](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md) 自动化签收已通过（非 staging 替代）

---

## 1. 一句话结论

本文档为 **staging actual 执行记录**。仅当 **S1–S5 全部 PASS**、**active audit 0 violation**、**PM spot-check ≥ 20 pools** 且 **0 个 P0/P1**、**rollback 演练成功** 后，才允许进入 **P7.5-r5-c4** closeout。

**当前**：actual 表格均为 **PENDING** → 整体签收状态 **PENDING**。请按 [c3 runbook](./P7.5-r5-c3-apply-to-pool-staging-signoff.md) 在 staging 执行后，将终端输出与 PM 结果填入下文。

---

## 2. 环境

| 字段 | 值 |
|------|-----|
| **Environment** | staging |
| **API base URL** | _PENDING_ |
| **Web URL** | _PENDING_ |
| **DB / Prisma env** | _PENDING_（如 staging `DATABASE_URL` secret 名称，勿贴明文密码） |
| **Viewer allowlist（测试用）** | _PENDING_ |
| · `V_hit`（allowlist 命中） | _PENDING_ |
| · `V_miss`（allowlist 未命中） | _PENDING_ |
| · 其它 PM 抽查 viewer | _PENDING_ |
| **PEIMA_ONBOARDING_VISION_APPLY_TO_POOL** | _PENDING_（按 case 变更；生产须保持 `0`） |
| **PEIMA_ONBOARDING_VISION_APPLY_ALLOWLIST_USER_IDS** | _PENDING_ |
| **PEIMA_ONBOARDING_VISION_APPLY_PERCENT** | `0`（c3a **固定**，不启灰度） |
| **PEIMA_ONBOARDING_VISION_APPLY_SOURCE_VERSION** | `onboarding-photo-preview-v2-vision`（预期） |
| **PEIMA_ONBOARDING_VISION_SHADOW_ENABLED** | _PENDING_（须为 `1` 或等价） |
| **Runner / audit command source** | _PENDING_（如 commit SHA、deploy id、job URL） |
| **mappingPath** | _PENDING_ |
| **Executed by** | _PENDING_ |
| **Date** | _PENDING_ |

**命令模板**（执行时粘贴实际命令与 stdout 摘要到各 Case Notes）：

```bash
pnpm --filter @peima/api run build

pnpm --filter @peima/api run p75:r4-m-preview-pool-funnel -- --viewerUserId=<viewerId>

pnpm --filter @peima/api run p75:r4-o2-preview-pool-active-audit -- --viewerUserId=<viewerId> --mappingPath=dev-assets/test-user-images/mapping.json

pnpm --filter @peima/api run p75:r5-c2-apply-writer-signoff -- --viewerUserId=<viewerId> --mappingPath=dev-assets/test-user-images/mapping.json
```

---

## 3. 前置检查

| Check | Expected | Actual | Result |
|-------|----------|--------|--------|
| API build / deployment success | staging API 可访问 | _PENDING_ | **PENDING** |
| Web deployment success | 预览页可打开 | _PENDING_ | **PENDING** |
| `DATABASE_URL` / staging secret injected | 无 `DATABASE_URL_missing` | _PENDING_ | **PENDING** |
| Candidate data enough | funnel 可跑 | _PENDING_ | **PENDING** |
| Viewer gender valid | `male` / `female` | _PENDING_ | **PENDING** |
| Funnel `after_opposite_gender_gate_final` | **≥ 6** | _PENDING_ | **PENDING** |
| Shadow persist enabled | `SHADOW_ENABLED=1` | _PENDING_ | **PENDING** |
| Active audit CLI can run | exit 0，有 JSON | _PENDING_ | **PENDING** |
| **Production** `APPLY_TO_POOL` | **默认 `0` / 关闭** | _PENDING_ | **PENDING** |

---

## 4. Staging Case Results（S1–S5）

| Case | Env | Expected `sourceVersion` | Actual `sourceVersion` | `applyResult` | `activeAuditPass` | pageCheck | Result | Notes |
|------|-----|---------------------------|------------------------|---------------|-------------------|-----------|--------|-------|
| **S1** | `APPLY=0` | `onboarding-photo-preview-v1` | _PENDING_ | `applied=false`, `env_disabled` | _PENDING_ | _PENDING_ | **PENDING** | default off → v1 |
| **S2** | `APPLY=1`, allowlist **hit** | `onboarding-photo-preview-v2-vision` | _PENDING_ | `applied=true`, `ok` | _PENDING_ | _PENDING_ | **PENDING** | allowlist hit → v2 |
| **S3** | `APPLY=1`, allowlist **miss** | `onboarding-photo-preview-v1` | _PENDING_ | `applied=false`, `not_in_allowlist` | _PENDING_ | _PENDING_ | **PENDING** | allowlist miss → v1 |
| **S4** | `APPLY=1`, allowlist **∅**, `PERCENT=100` | `onboarding-photo-preview-v1` | _PENDING_ | `applied=false`, `allowlist_empty` | _PENDING_ | — | **PENDING** | percent 不得绕过 allowlist |
| **S5** | rollback `APPLY=0` | `onboarding-photo-preview-v1` | _PENDING_ | `applied=false`, `env_disabled` | _PENDING_ | _PENDING_ | **PENDING** | rollback → v1；旧 v2 保留 |

**可选：c2 runner 一次性回归（staging）**

| 字段 | 值 |
|------|-----|
| `schemaVersion` | _PENDING_ |
| `summary.allPass` | _PENDING_ |
| `summary.caseDAppliedV2` | _PENDING_ |
| `summary.caseERolledBackToV1` | _PENDING_ |
| `summary.v2PoolsRetainedAfterRollback` | _PENDING_ |
| `summary.activeAuditPassAllCases` | _PENDING_ |
| stdout 粘贴区 | _（贴脱敏 JSON 或链接）_ |

> **说明**：若跑完 c2 runner，末案为 Case E 时 viewer **当前 active pool = v1** 属正常（见 [c2 §8](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md)）。

---

## 5. Active Audit Summary

每案生成后填写（violation 须 **全为 false**；`pool_item_count` = **6**）。

| Case | `pool_item_count` | `duplicate_candidate_user_id` | `duplicate_image_source_key` | `violates_opposite_gender_gate` | `is_viewer_self` | `pool_source_version` | Result |
|------|-------------------|-------------------------------|------------------------------|--------------------------------|------------------|----------------------|--------|
| S1 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | **PENDING** |
| S2 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | **PENDING** |
| S3 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | **PENDING** |
| S4 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | **PENDING** |
| S5 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | **PENDING** |

**通过标准（每 item）**

- `pool_item_count` = **6**
- `duplicate_candidate_user_id` = **false**（全 item）
- `duplicate_image_source_key` = **false**
- `violates_opposite_gender_gate` = **false**
- `is_viewer_self` = **false**
- `pool_source_version` 与当案预期 **v1 / v2** 一致

**Audit CLI 输出粘贴区**：_PENDING_

---

## 6. PM Spot-check

**汇总（执行后填）**

| 字段 | 值 |
|------|-----|
| `poolsReviewed` | _PENDING_（须 **≥ 20**） |
| `p0p1Count` | _PENDING_（须 **0**） |
| `pmSignoff` | _PENDING_（PASS / FAIL） |
| `signoffDate` | _PENDING_ |
| PM 签字人 | _PENDING_ |

**通过标准**

- 至少 **20** 个 preview pool
- **0** 个 **P0/P1**
- 对应 pool **active audit** 0 violation
- `PM result` 为 **PASS**，或仅 **P2/P3** 记入 §8 Known Issues

### 6.1 记录表（≥ 20 行）

| Pool # | viewer | sourceVersion | 6 items visible | no duplicate candidate | no duplicate image | no self | gender gate ok | perceived quality | PM result | note |
|--------|--------|---------------|-----------------|------------------------|--------------------|---------|----------------|-------------------|-----------|------|
| 1 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 2 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 3 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 4 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 5 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 6 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 7 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 8 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 9 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 10 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 11 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 12 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 13 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 14 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 15 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 16 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 17 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 18 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 19 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |
| 20 | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | _PENDING_ | |

---

## 7. Rollback Drill

| 项 | 记录 |
|----|------|
| **Rollback env** | `APPLY_TO_POOL=0` · `ALLOWLIST=` · `PERCENT=0` |
| **Redeploy / restart** | _PENDING_（时间 / 操作人） |
| **Viewer** | _PENDING_（建议 S2 用过的 `V_hit`） |
| **New generate `sourceVersion`** | _PENDING_（期望 v1） |
| **Old v2 pool retained?** | _PENDING_（期望 **是**，不批量 UPDATE） |
| **Active audit after rollback** | _PENDING_ |
| **Page check** | _PENDING_ |
| **Result** | **PENDING** |

可与 **§4 Case S5** 合并记录；S5 PASS 且本节 PASS 方可整体签收。

---

## 8. Known Issues

执行后填写；**P0/P1 出现则 §9 必须为 FAIL**。

| ID | 级别 | 描述 | 阻塞 c4? |
|----|------|------|----------|
| — | — | _（暂无 · 或贴 P2/P3）_ | — |

**常见非阻塞（P2/P3）**

- Demo / staging **数据质量**（图糊、构图差）
- **视觉性别**与 DB `gender` 不一致（**不**用 AI/人脸判性别）
- **图片素材多样性**不足
- v2 排序主观偏好分歧

---

## 9. Signoff Decision

**当前勾选**：☑ **PENDING** · ☐ PASS · ☐ FAIL

---

### PASS

**条件（须全部满足）**

- [ ] S1–S5 **全 PASS**
- [ ] Active audit **全 PASS**（0 violation）
- [ ] PM spot-check **≥ 20** pools
- [ ] **0** 个 P0/P1
- [ ] Rollback 演练 **成功**

**结论**

> **P7.5-r5-c3a staging allowlist-only signoff 通过。**  
> **允许进入** P7.5-r5-c4 allowlist-only APPLY_TO_POOL closeout。  
> **仍不允许** 开启 **percent** 灰度（须 r5-d 单独设计与评审）。

---

### FAIL

**条件（任一即 FAIL）**

- 任一 **S case** FAIL  
- Active audit **violation**  
- PM **P0/P1**  
- Rollback 失败  

**结论**

- **不允许**进入 **r5-c4**  
- **不允许**扩大 allowlist  
- **不允许**启用 **percent**  
- 修复后 **重跑完整 c3a**（建议 S1→S5 + PM 全量）

---

### PENDING（当前）

**条件**：§2–§7 actual **未填**或未完成 PM ≥20。

**结论**

> 继续执行 [c3 staging signoff](./P7.5-r5-c3-apply-to-pool-staging-signoff.md)，并将结果填入本文档。

---

## 10. 未改动范围

| 类别 | 状态 |
|------|------|
| 业务代码 | **未改**（c3a 仅 signoff record） |
| worker | **未改** |
| `finalScore` | **未改** |
| `MatchResult` | **未改** |
| 旧 `PreviewPool` | **未改** |
| 正式匹配主链 | **未改** |
| percent 灰度 | **未启用** |
| staging | **allowlist-only** |
| 生产 APPLY 总闸 | **默认关闭** |
| 云视觉 API | **未接** |
| AI 美化 / AI 头像 | **未做** |
| 人脸比对 / 身份认证 / 颜值评分 | **未做** |
| 根据照片自动判断性别 | **未做** |
| DB migration | **无新增** |

---

## 11. 下一步

| 签收状态 | 下一步 |
|----------|--------|
| **PASS** | 进入 **P7.5-r5-c4** allowlist-only APPLY_TO_POOL closeout + runbook |
| **PENDING** | 继续跑 staging **S1–S5** + **PM spot-check**，更新本文 §2–§9 |
| **FAIL** | 修问题后 **重跑完整 c3a**；勿进 c4、勿扩 allowlist、勿启 percent |

---

## 附录：填写清单（贴给执行人）

贴入 staging 终端 / PM 时建议按序提供：

1. **§2** 环境字段（URL、viewer cuids、deploy id、日期）  
2. **§3** 前置检查 Actual  
3. **§4** 每案 `sourceVersion`、`applyResult`、pool id（可脱敏）、Notes  
4. **§5** 每案 audit 六项汇总或脱敏 JSON  
5. **§6** PM 表 20 行 + 汇总  
6. **§7** rollback 时间与 v2 是否保留  
7. **§8** 若有 P2/P3 问题单  

---

## 参考

- [P7.5-r5-c3 runbook](./P7.5-r5-c3-apply-to-pool-staging-signoff.md)
- [P7.5-r5-c2 本地签收](./P7.5-r5-c2-apply-to-pool-allowlist-writer-local-signoff.md)
- [P7.5-r5-c2 runner closeout](./P7.5-r5-c2-apply-writer-signoff-runner-closeout.md)

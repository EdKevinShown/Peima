# P7.10-r6c1 — Repeated Artifact Audit and Sidecar Coverage Closeout

## 1. Status

```text
P7_10_R6C1_REPEATED_ARTIFACT_AUDIT_AND_SIDECAR_COVERAGE_COMPLETE
```

- **Artifact audit only** — read-only Prisma + local JSON
- **No code change**
- **No DB write**
- **No** `MatchResult` write
- **No** `matchInsights` write
- **No** worker integration
- **No** GET behavior change
- **No** frontend change
- **No** dual-write implementation
- **No** rehearsal sidecar table / migration
- **No** legacy removal
- **No** production / percent rollout

**Prior artifacts (read for baseline; not modified):**

| Path | Role |
|------|------|
| `artifacts/p76/r6b/canonical-writer-shadow-audit.json` | r6b smoke (limit=20) |
| `artifacts/p76/r7r2/canonical-writer-shadow-audit.json` | P7.7-r2 smoke (limit=20) |
| `artifacts/p76/r7r2/local-smoke-summary.json` | Automation summary |

**Note:** P7.7-r2 automation was run with `-SkipHttpSmoke`; its default summary paths under `r7r2/` were **restored** via `git restore` so this round only adds `r6c1/` outputs.

---

## 2. Commands run

| Command | Result |
|---------|--------|
| `git status --short` (pre-flight) | Clean |
| `powershell … p77-r2-local-smoke-automation.ps1 -SkipHttpSmoke -Pretty -AuditOutput artifacts/p76/r6c1/…` | **PASS** — build + Jest (8 suites / 85 tests) + r6a (limit=20) + artifact check |
| `node -r dotenv/config dist/dev-cli/p710-r6a-canonical-writer-shadow-audit-runner.js --limit=50 …` | **PASS** — authoritative r6c1 artifact |
| `node apps/api/scripts/p77-r2-local-smoke-artifact-check.mjs artifacts/p76/r6c1/canonical-writer-shadow-audit.json` | **PASS** |
| Read-only Prisma counts (`packages/database`, cohort `p7.6-r7j3-staging-cohort-v1`) | **PASS** — counts only, no user IDs logged |

`DATABASE_URL` was loaded from repo-root `.env` for runner/Prisma (value not recorded).

---

## 3. Artifact outputs

| Output | Path |
|--------|------|
| Audit JSON | `artifacts/p76/r6c1/canonical-writer-shadow-audit.json` |
| Audit summary | `artifacts/p76/r6c1/canonical-writer-shadow-audit-summary.md` |
| Sidecar coverage (markdown) | `artifacts/p76/r6c1/sidecar-coverage-summary.md` |
| Sidecar coverage (JSON) | `artifacts/p76/r6c1/sidecar-coverage-summary.json` |
| Closeout | `docs/P7/P7.10-r6c1-repeated-artifact-audit-and-sidecar-coverage-closeout.md` |

---

## 4. Metrics

| Metric | Value |
|--------|-------|
| totalRows | 50 |
| eligibleCount | 4 |
| blockedCount | 46 |
| missingSidecarCount | 45 |
| rowsWithSidecar | 5 |
| sidecarCoverageRate | **0.10** |
| missingSidecarRate | **0.90** |
| eligibleRate | 0.08 |
| wouldChangeCandidateCount | 4 |
| wouldChangeCandidateRate | 0.08 |
| appliedToMatchResultCount | 0 |

**Read-only DB counts (cohort `p7.6-r7j3-staging-cohort-v1`):**

| DB metric | Value |
|-----------|-------|
| `matchResult` total rows | 80 |
| `p76_allowlist_apply_meta` rows (cohort) | 5 |
| rolled back (cohort) | 1 |
| `appliedToMatchResult=true` (cohort) | 0 |
| Recent 50 `MatchResult` sample → distinct viewers | 6 |
| Sidecar rows intersecting those viewers | 5 |

---

## 5. Reason counts

| Reason | Count |
|--------|-------|
| `missing_sidecar` | 45 |
| `ok` | 4 |
| `rolled_back` | 1 |

**Score delta bands:** `unknown` 46, `none` 4.

---

## 6. Safety verification

- `sourceType` = `p76_canonical_writer_shadow_audit`
- `sourceVersion` = `p7.10-r6a-canonical-writer-shadow-audit-v1`
- `mode` = `dev_audit`
- **`appliedToMatchResultCount` = 0**
- All `rows[].shadow.appliedToMatchResult` = **false** (`p77-r2-local-smoke-artifact-check.mjs` PASS)
- **No** sensitive keys in serialized JSON
- **No** Prisma `create` / `update` / `upsert` / `delete`
- **No** `MatchResult` / `matchInsights` mutation

---

## 7. Coverage interpretation

| Question | Answer |
|----------|--------|
| Is `sidecarCoverageRate` sufficient for Option B DB rehearsal write? | **No** — **90%** `missing_sidecar` on limit=50 sample |
| Does high `missing_sidecar` imply shadow builder failure? | **No** — cohort lacks `p76_allowlist_apply_meta` for most recent viewers |
| Does `eligibleCount > 0` prove builder works? | **Yes** — 4 rows with `ok` + 4 `wouldChangeCandidate` where sidecar + guardrails align |
| r6c gate 9 ([P7.10-r6c](./P7.10-r6c-canonical-writer-dual-write-rehearsal-design.md))? | **Still needs improvement** — `missingSidecarRate` **0.90** > **0.50** threshold |
| r6d schema design? | **May proceed docs-only** for table shape, but **must not** implement sidecar writes until coverage improves or PM accepts cohort |

**Comparison to prior runs (limit=20):** r6b / r7r2 reported `missing_sidecar=15/20` (75%). Expanding to 50 rows added 30 more `missing_sidecar` rows and **no** new `ok` rows — coverage is **concentrated** in the same ~5 allowlist viewers, not spread across the DB.

---

## 8. PASS / BLOCK

### PASS (this round)

- Repeated artifact audit executed (automation + limit=50 runner)
- Safety checks passed
- Coverage metrics + DB read-only counts recorded
- No-write invariant preserved

### BLOCK (unchanged)

- No dual-write DB implementation
- No rehearsal sidecar table / migration
- No canonical production write
- No worker integration
- No legacy removal / PreviewPool disable
- No production / percent rollout (`r9e3`, `r9f`)

---

## 9. Next recommended step

Because **`missingSidecarRate` = 0.90 > 0.5**:

| Priority | Milestone | Scope |
|----------|-----------|-------|
| **Recommended** | **P7.10-r6c2** — local sidecar coverage remediation plan | Docs-only or dev seed plan: align recent `MatchResult` viewers with allowlist sidecar rows; optional `--viewerUserId` allowlist-only audit runs; **still no DB rehearsal writes** |
| Deferred | **P7.10-r6d** — dedicated rehearsal sidecar schema design | Docs-only table/index/retention design **may start**, but **no migration** until coverage ≥ agreed threshold (e.g. `missingSidecarRate ≤ 0.5` on target cohort) **and** gate 15 PM/Ops signoff |

**Always block:** canonical production write, PreviewPool writer disable, percent rollout.

**Do not implement:** r6d migration, rehearsal sidecar writer, or dual-write in code this phase.

---

## Appendix — r6c promotion gate snapshot

| Gate | Status after r6c1 |
|------|-------------------|
| 1–8 (shadow / audit / smoke / invariants) | **PASS** |
| 9 (`missing_sidecar` acceptable) | **FAIL** on default recent-50 sample (90%) |
| 10 (rollback documented) | **PASS** (r6c) |
| 11 (schema review) | **PENDING** (r6d) |
| 15 (PM/Ops signoff) | **PENDING** |

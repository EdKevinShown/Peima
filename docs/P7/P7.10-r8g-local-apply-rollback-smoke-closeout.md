# P7.10-r8g — Local Apply + Rollback Smoke Closeout

> **状态**：P7.10-r8g local Apply + Rollback smoke — **DONE**  
> **结论**：**`PASS_R8G_LOCAL_APPLY_ROLLBACK_SMOKE`**  
> **前置**：[P7.10-r8f](./P7.10-r8f-hidden-admin-apply-rollback-api-wiring-closeout.md) · [P7.10-r8e](./P7.10-r8e-canonical-rollback-service-token-gated-closeout.md) · [P7.10-r8d](./P7.10-r8d-canonical-apply-service-hard-disabled-closeout.md)  
> **性质**：local PostgreSQL smoke only · **无** staging/production · **无** UI · **无** worker/percent/legacy 变更

---

## 1. 一句话结论

**P7.10-r8g** 在 **localhost** dev DB 上完成首次真实 Apply → Rollback 闭环：MatchResult `before → applied → restored`，sidecar `promoted → rolled_back`，snapshot `snapshot_created → applied → rollback_completed`；明文 rollback token **未** 写入 artifact；smoke fixture **已清理**（`finalRowsForAuditRunId=0`）。

```text
PASS_R8G_LOCAL_APPLY_ROLLBACK_SMOKE
```

---

## 2. 修改文件列表

| 文件 | 说明 |
|------|------|
| `tmp/p710-r8g-local-apply-rollback-smoke.mjs` | Local smoke runner（fixture / apply / rollback / cleanup / artifact） |
| `apps/api/src/modules/matching/p76-canonical-rollback.service.ts` | 修复 sidecar update：移除 schema 不存在的 `rolledBackAt` / `rolledBackBy` |
| `artifacts/p76/r8g/local-apply-rollback-smoke-result.json` | Sanitized smoke 结果 |
| 本文档 | closeout |

**未修改：** `apps/web` · `.env` 提交值 · staging/production 配置

---

## 3. Local-only env 设置

Smoke 脚本内 **临时** 设置（未写入 `.env`）：

| 变量 | Smoke 值 |
|------|----------|
| `NODE_ENV` | `development` |
| `PEIMA_P710_R8_HIDDEN_ADMIN_MUTATION_*` | `1` / `dev` |
| `PEIMA_P710_R8_APPLY_EXECUTION_ENABLED` | `1` |
| `PEIMA_P710_R8_APPLY_ALLOW_DB_WRITE` | `1` |
| `PEIMA_P710_R8_ROLLBACK_EXECUTION_ENABLED` | `1` |
| `PEIMA_P710_R8_ROLLBACK_ALLOW_DB_WRITE` | `1` |
| `PEIMA_P76_PRODUCTION_PERCENT` | `0` |
| `PEIMA_P76_ROLLBACK_TOKEN_PEPPER` | `local-r8g-smoke-only`（仅进程内；**未** 写入 docs） |

**Preflight：** `DATABASE_URL` → `postgresql://localhost:5432/peima`（redacted）· 非 staging/production host

---

## 4. Fixture 创建说明

| 实体 | 说明 |
|------|------|
| `User` ×3 | synthetic phone `+r8g-*`（viewer + cand A + cand B） |
| `MatchBatch` | smoke batch |
| `MatchResult` | viewer → cand A · `finalScore=0.7` · `reasonSummary=r8g local smoke before` |
| `P76CanonicalMatchResultMeta` | proposes cand B · `score=0.8` · `promotionStatus=not_promoted` |
| `P76CanonicalApplyRollbackSnapshot` | `baselineFingerprint` 匹配 live MR · `rollbackTokenHash`（HMAC，无 plaintext 落库） |

`auditRunId` = `r8g-local-smoke-<timestamp>`（artifact 中可见，无真实用户 id）

---

## 5. Apply smoke 结果

| 检查 | 结果 |
|------|------|
| `mode` | `applied` |
| `applied` | `true` |
| `blockedReasons` | `[]` |
| MatchResult `candidateUserId` | cand A → cand B |
| MatchResult `finalScore` | `0.7` → `0.8` |
| sidecar `promotionStatus` | `promoted` |
| sidecar `appliedToWorkerRanking` | `false` |
| snapshot `promotionStatus` | `applied` |

---

## 6. Rollback smoke 结果

| 检查 | 结果 |
|------|------|
| `mode` | `rolled_back` |
| `rolledBack` | `true` |
| `blockedReasons` | `[]` |
| `audit.tokenVerified` | `true` |
| MatchResult restored | cand B → cand A · score `0.8` → `0.7` |
| sidecar `promotionStatus` | `rolled_back` |
| snapshot `promotionStatus` | `rollback_completed` |
| snapshot `rollbackUsedAt` | set |
| `tokenPlaintextExposed` | `false` |

---

## 7. DB before / applied / restored 对照

| 阶段 | candidate | finalScore | sidecar status | snapshot status |
|------|-----------|------------|----------------|-----------------|
| before Apply | cand A | 0.7 | `not_promoted` | `snapshot_created` |
| after Apply | cand B | 0.8 | `promoted` | `applied` |
| after Rollback | cand A | 0.7 | `rolled_back` | `rollback_completed` |

---

## 8. Token no-exposure proof

| 检查 | 结果 |
|------|------|
| DB `rollbackTokenHash` only | ✅ |
| artifact JSON | 无 plaintext token · `tokenHashPresent: true` |
| sanitized service response | `tokenPlaintextExposed: false` |

---

## 9. Worker / percent / legacy untouched proof

| 检查 | before → after |
|------|----------------|
| `matchResult` 总数 | 80 → 80 |
| `sidecar` 总数 | 0 → 0 |
| `snapshot` 总数 | 0 → 0 |
| `hookJob` 总数 | 3 → 3 |
| `appliedToWorkerRanking` true 计数 | 0 → 0 |
| `PEIMA_P76_PRODUCTION_PERCENT` | `0` → `0` |
| legacy photo matching | 未删除 |

---

## 10. Cleanup proof

| 检查 | 结果 |
|------|------|
| `finalRowsForAuditRunId` | `0` |
| snapshot / sidecar remaining | `0` / `0` |
| 表计数恢复 | ✅ |

---

## 11. Artifacts 路径

- `artifacts/p76/r8g/local-apply-rollback-smoke-result.json`

---

## 12. Regression tests / build

```text
pnpm --filter @peima/api run build
node tmp/p710-r8g-local-apply-rollback-smoke.mjs   → PASS_R8G_LOCAL_APPLY_ROLLBACK_SMOKE
pnpm --filter @peima/api test -- p710-r8f         → 34/34 PASS
pnpm --filter @peima/api test -- p710-r8e         → 33/33 PASS
pnpm --filter @peima/api test -- p710-r8d         → 20/20 PASS
pnpm --filter @peima/api test -- p710-r8c         → 11/11 PASS
pnpm --filter @peima/api test -- p710-r7j         → 17/17 PASS
```

---

## 13. PASS / BLOCK

| 判定 | 状态 |
|------|------|
| **PASS** | `PASS_R8G_LOCAL_APPLY_ROLLBACK_SMOKE` |

**BLOCK 条件（均未触发）：** staging/production · token exposed · worker triggered · percent changed · legacy removed · cleanup incomplete

---

## 14. Next step

**DONE：** [P7.10-r8h](./P7.10-r8h-staging-apply-rollback-smoke-closeout.md) · `PASS_R8H_STAGING_APPLY_ROLLBACK_SMOKE`

**NEXT：** **P7.10-r8i — PM/Ops Apply/Rollback signoff**

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-19 | r8g local Apply+Rollback smoke · sidecar rollback Prisma field fix · `PASS_R8G_LOCAL_APPLY_ROLLBACK_SMOKE` |

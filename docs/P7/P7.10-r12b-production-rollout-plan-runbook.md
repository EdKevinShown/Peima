# P7.10-r12b — Production Rollout Plan / Runbook

> **状态**：P7.10-r12b production rollout plan — **DONE (plan only)**  
> **结论**：**`DONE_R12B_PRODUCTION_ROLLOUT_PLAN_RUNBOOK`**  
> **前置**：[P7.10-r12a](./P7.10-r12a-production-readiness-human-signoff.md) · [P7.10-r11c](./P7.10-r11c-staging-post-deletion-final-closeout.md)  
> **Artifact**：[production-rollout-plan-summary.json](../../artifacts/p76/r12b/production-rollout-plan-summary.json)  
> **性质**：docs / runbook only · **无** 代码变更 · **无** production deploy · **无** production smoke

---

## 1. 一句话结论

**P7.10-r12b** 定义 legacy photo matching removal 之后、**customer production** 上的**部署与验证计划**（gates 保持关闭）。**执行（r12c）在 r12a 人工签收 `PASS` 之前一律 BLOCKED。本轮 **未** deploy、**未** smoke production、**未** 开启 percent / Apply / Rollback / hidden mutation API。

```text
DONE_R12B_PRODUCTION_ROLLOUT_PLAN_RUNBOOK
```

> **EXECUTION BLOCKED UNTIL:** `PASS_R12A_PRODUCTION_READINESS_HUMAN_SIGNOFF`

---

## 2. 与 r12a 的关系（并行、不升级 r12a）

| 里程碑 | 状态 | 说明 |
|--------|------|------|
| **r12a** | `READY_R12A_PRODUCTION_READINESS_PENDING_HUMAN_SIGNOFF` | PM/Ops/Eng 签收 **pending** — **未伪造** |
| **r12b** | `DONE_R12B_PRODUCTION_ROLLOUT_PLAN_RUNBOOK` | 计划/runbook **已完成**（文档先行，政策允许） |
| **r12c** | **not started** | 执行 blocked on r12a PASS |

**政策：** r12b 文档可与 r12a 签收**并行**；**任何 production 执行动作**仍须 r12a 三方 signed 后进入 r12c。

---

## 3. Rollout 范围

### 3.1 In scope（r12c 执行时）

| 项 | 说明 |
|----|------|
| Production 环境 gate 核对 | 确认 §5 默认 env 在 prod pod 为安全值 |
| Deploy API / Worker / Web | 部署含 r11 删除后的构建产物；**不**改 gate 为 on |
| Post-deploy 只读验证 | GET `/matching/result`、onboarding 路径、删除路由 404/redirect（计划，非本轮执行） |
| 监控与 incident | 错误率、匹配结果 API、worker 队列深度 |
| Emergency disable | 引用 [r12a §7](./P7.10-r12a-production-readiness-human-signoff.md) |

### 3.2 Out of scope（须单独里程碑 / 审批）

| 项 | 说明 |
|----|------|
| Percent rollout | `PEIMA_P76_PRODUCTION_PERCENT > 0` |
| Canonical Apply execution | `PEIMA_P710_R8_APPLY_EXECUTION_ENABLED=1` |
| Canonical Rollback execution | `PEIMA_P710_R8_ROLLBACK_EXECUTION_ENABLED=1` |
| Hidden admin mutation API | `PEIMA_P710_R8_HIDDEN_ADMIN_MUTATION_API_ENABLED=1` |
| Old photo writer 重新启用 | `PEIMA_P710_R9_OLD_PHOTO_MATCHING_WRITER_ENABLED=1` |
| Category A legacy 恢复 | PreviewPool writer / onboarding photo preview pool |

---

## 4. 当前基线（plan 假设）

| 项 | 值 |
|----|-----|
| P7.10 removal | **complete**（r11c） |
| Production touched (removal track) | **false** |
| Percent | **0** |
| Old writer | shutdown on · writer off |
| Safe fallback | canonical / stable_baseline / matching_pending / no_result |
| Staging post-deletion smoke | **PASS**（r11c） |
| r12a human signoff | **pending** |

---

## 5. Production 默认 env 矩阵（deploy 前后必须保持）

**所有 production pod（API + Worker）在 r12c 执行前后均应核对：**

| 变量 | 要求值 | 用途 |
|------|--------|------|
| `PEIMA_P710_R9_OLD_PHOTO_MATCHING_WRITER_SHUTDOWN_ENABLED` | `1` | 旧 photo writer 关停 |
| `PEIMA_P710_R9_OLD_PHOTO_MATCHING_WRITER_ENABLED` | `0` | 禁止正式 MatchResult 旧路径写入 |
| `PEIMA_P710_R8_HIDDEN_ADMIN_MUTATION_API_ENABLED` | `0` | Hidden mutation API 关 |
| `PEIMA_P710_R8_HIDDEN_ADMIN_APPLY_ROUTE_ENABLED` | `0` | Apply route 关 |
| `PEIMA_P710_R8_HIDDEN_ADMIN_ROLLBACK_ROUTE_ENABLED` | `0` | Rollback route 关 |
| `PEIMA_P710_R8_APPLY_EXECUTION_ENABLED` | `0` | Apply 执行关 |
| `PEIMA_P710_R8_APPLY_ALLOW_DB_WRITE` | `0` | Apply DB 写关 |
| `PEIMA_P710_R8_ROLLBACK_EXECUTION_ENABLED` | `0` | Rollback 执行关 |
| `PEIMA_P710_R8_ROLLBACK_ALLOW_DB_WRITE` | `0` | Rollback DB 写关 |
| `PEIMA_P76_PRODUCTION_PERCENT` | `0` | Percent 关 |
| `PEIMA_P76_PRODUCTION_PERCENT_ENABLED` | `0` | Percent 开关关 |

**核对方式（r12c）：** Render/K8s env 导出 → redacted checklist（**不**将完整 env 写入 artifact）。

---

## 6. 执行阶段（plan — r12c 才执行）

> **EXECUTION BLOCKED UNTIL:** `PASS_R12A_PRODUCTION_READINESS_HUMAN_SIGNOFF`

### Phase P0 — Preflight & owners

| 步骤 | 动作 | Owner |
|------|------|-------|
| P0.1 | 确认 r12a 已为 `PASS_R12A_…` | PM + Ops + Eng |
| P0.2 | 指定 rollout 指挥、on-call、rollback 决策人 | Ops |
| P0.3 | 确认 production DB/API host 分类（customer production vs staging） | Ops + Eng |
| P0.4 | 确认 §5 env 矩阵在 prod 已配置 | Ops |
| P0.5 | 变更窗口与沟通渠道（内部） | PM + Ops |

**出口：** Preflight checklist 全绿 → 允许 P1。

### Phase P1 — Deploy（gates off）

| 步骤 | 动作 | Owner |
|------|------|-------|
| P1.1 | 构建产物来自 r11b 已验证 commit | Eng |
| P1.2 | Deploy API → Worker → Web（顺序可据平台调整） | Ops |
| P1.3 | Deploy 后 **立即** 复核 §5 env（防止平台默认覆盖） | Ops |
| P1.4 | **禁止** 在本阶段开启 Apply/Rollback/percent/writer | Eng + Ops |

**出口：** 健康检查通过；env 复核 PASS。

### Phase P2 — Post-deploy readonly verification

| 步骤 | 动作 | Owner |
|------|------|-------|
| P2.1 | `GET /matching/result` 抽样（运维账号 / 合成 viewer — **ID 不入 artifact**） | Eng |
| P2.2 | 确认无 `POST /preview-pool/generate`（404/405） | Eng |
| P2.3 | 确认 onboarding aesthetic → questionnaire 行为 | Eng |
| P2.4 | 确认 FinalMatchPage 仍走 `getMatchingResult` | Eng |
| P2.5 | DB 只读：`match_results` 计数前后对比（**无** 写入 smoke） | Ops |

**出口：** 只读验证 PASS；无 unexpected writer 活动。

### Phase P3 — Monitoring watch window

| 步骤 | 动作 | Owner |
|------|------|-------|
| P3.1 | 30–120 min 观察：5xx、匹配 API 延迟、worker 错误 | Ops |
| P3.2 | 确认 hook job / batch-match 无异常激增 | Eng |
| P3.3 | 无 incident → rollout 指挥宣布 P3 完成 | PM + Ops |

**出口：** Watch window 无 Sev1/Sev2 → r12c closeout 可起草。

---

## 7. Production 执行 NO-GO（任一条即停止）

| # | 条件 |
|---|------|
| 1 | r12a 非 `PASS_R12A_PRODUCTION_READINESS_HUMAN_SIGNOFF` |
| 2 | §5 任一门禁 env 非安全值 |
| 3 | 计划在未审批窗口 deploy |
| 4 | 需要同时开启 percent 或 Apply/Rollback |
| 5 | Production DB 分类不明 |
| 6 | Monitoring / on-call 未就位 |
| 7 | 回滚负责人未指定 |
| 8 | r11c 核心路径仍有未关闭 BLOCKER |

---

## 8. Rollback plan（deploy 回滚，非 canonical Rollback service）

**场景：** P1 deploy 后 P2/P3 发现严重回归。

| 步骤 | 动作 |
|------|------|
| R1 | **立即** 应用 [r12a emergency disable](./P7.10-r12a-production-readiness-human-signoff.md) env（§7） |
| R2 | 平台回滚至上一 known-good API/Worker/Web 版本 | 
| R3 | 复核 §5 env 仍为 off |
| R4 | 只读验证 `GET /matching/result` 恢复 |
| R5 | Incident 记录（**无** token/URL 入 ticket） |

**注意：** 此为 **deploy rollback**，**不是** `rollbackP76CanonicalApply`（该服务仍保持 disabled）。

---

## 9. Monitoring checklist（r12c watch）

| 信号 | 阈值（建议） | 动作 |
|------|--------------|------|
| API 5xx rate | 高于基线 +X% | 评估 P1 回滚 |
| `GET /matching/result` 错误率 | 高于基线 | Eng 查日志（redacted） |
| Worker batch-match 失败率 | 高于基线 | 确认 old writer gate 仍 blocked |
| Hook job pending 堆积 | 异常增长 | 确认未误开 writer |
| User-facing 匹配 pending 激增 | 产品判断 | PM 沟通 |

---

## 10. Owner 表（r12c kickoff 前填写）

| Role | 责任 | 姓名（r12c 前填写） |
|------|------|---------------------|
| Rollout 指挥 | Go/no-go、窗口 | **TBD** |
| Ops | Deploy、env、监控 | **TBD** |
| Eng | 验证、技术分析 | **TBD** |
| PM | 用户影响、沟通 | **TBD** |
| On-call | Incident 首响 | **TBD** |

**不得**在本 plan 中伪造姓名；r12a 签收姓名与 rollout owner 可同人但须分别记录。

---

## 11. 进入 P7.10-r12c 的条件

| # | 条件 |
|---|------|
| 1 | `PASS_R12A_PRODUCTION_READINESS_HUMAN_SIGNOFF` |
| 2 | 本文档 §6 phases 与 §5 env 矩阵已评审 |
| 3 | §10 owner 表已填写 |
| 4 | 变更窗口已批准 |
| 5 | r12c 执行审批（Ops + Eng 书面） |
| 6 | 仍 **不** 包含 percent / Apply / Rollback enable（除非另开里程碑） |

**r12c 性质（规划）：** production rollout **execution** + closeout artifact；**仍非** canonical Apply production enable。

---

## 12. PASS / BLOCK (r12b milestone)

| 判定 | 状态 |
|------|------|
| **DONE (plan)** | `DONE_R12B_PRODUCTION_ROLLOUT_PLAN_RUNBOOK` |
| **Execution** | **BLOCKED** until r12a PASS |

---

## 13. Artifacts

| 路径 | 说明 |
|------|------|
| [artifacts/p76/r12b/production-rollout-plan-summary.json](../../artifacts/p76/r12b/production-rollout-plan-summary.json) | Plan summary（无 secret） |
| [artifacts/p76/r12a/](../../artifacts/p76/r12a/) | Readiness evidence（签收 pending） |

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-20 | r12b plan/runbook · execution blocked until r12a PASS · `DONE_R12B_PRODUCTION_ROLLOUT_PLAN_RUNBOOK` |

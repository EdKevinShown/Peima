# P7.10-r9 — Old photo matching writer source audit

**Date:** 2026-05-19  
**Scope:** Writer shutdown only · **not** legacy deletion (r11)

---

## 1. MatchResult writers

| Path | Writes `MatchResult`? | r9 action |
|------|----------------------|-----------|
| `apps/worker/src/jobs/batch-match.processor.ts` | **yes** — `matchResult.create` via PreviewPool + `computeFinalScoreV1` | **Gated** — `writeLegacyPhotoMatchResultIfAllowed` |
| `apps/api/src/modules/matching/p76-canonical-apply.service.ts` | **yes** — `matchResult.update` (Apply) | **Untouched** — r8 controlled path |
| `apps/api/src/modules/matching/p76-canonical-rollback.service.ts` | **yes** — `matchResult.update` (Rollback) | **Untouched** |
| `apps/api/src/modules/matching/matching-rrm-top2-display-meta-writer.ts` | **yes** — display meta / optional update | **Untouched** — not photo pool writer |
| `apps/api/src/dev-cli/m55-m0-rrm-top2-display-fixture.runner.ts` | dev-cli only | **Untouched** |
| `packages/database/scripts/p76-r9b-fixture-seed.mjs` | fixture seed | **Untouched** |

**Production formal MatchResult from legacy photo path:** `batch-match.processor.ts` only.

---

## 2. PreviewPool generators (read upstream, not deleted)

| Path | Role |
|------|------|
| `apps/api/src/modules/preview-pool/preview-pool.service.ts` | Legacy 6-slot pool generation |
| `apps/api/src/modules/preview-pool/preview-pool-layered-selection.ts` | Slot template + `computeStyleScore` |
| `apps/api/src/modules/preview-pool/preview-pool.controller.ts` | Admin/API generate |
| `apps/api/src/modules/onboarding/onboarding-photo-preview-pool.service.ts` | Onboarding v1 pool (`pickByStyleScore`) |

Worker **reads** active `PreviewPool`; r9 does **not** delete generators.

---

## 3. styleTags / style score usage

| Path | Usage |
|------|--------|
| `packages/shared/matching/preference-score.ts` | `computeStyleScore` shared |
| `apps/worker/src/jobs/matching-score.ts` | Worker `computeFinalScoreV1` (style weight 0.15) |
| `apps/api/src/modules/preview-pool/preview-pool-shortlist-contract.v0.ts` | Preview ordering |
| `apps/api/src/modules/onboarding/vision/visual-ranking-shadow-scoring.ts` | Shadow only |
| `apps/api/src/modules/onboarding/onboarding-photo-preview-pool.service.ts` | `pickByStyleScore` |

---

## 4. Read-only paths — do not modify in r9

| Path | Role |
|------|------|
| `apps/api/src/modules/matching/matching.service.ts` | GET `/matching/result` |
| `apps/api/src/modules/matching/matching-result-display.ts` | `resolveMatchResultDisplay` |
| `apps/api/src/modules/matching/p76-read-path-display-resolver.ts` | P7.6 read overlay |
| `apps/api/src/modules/matching/p76-end-to-end-funnel-legacy-adapter.ts` | Funnel comparison read |
| `apps/web/src/pages/FinalMatchPage.jsx` | User UI — **frozen** |
| `apps/web/src/api/matching.ts` | Client read |

---

## 5. Category A legacy (r11 deletion candidates)

Per [P7.10-r1](./P7.10-r1-legacy-photo-matching-dependency-audit-and-replacement-strategy.md) §5.1:

- `preview-pool/*` (service, layered-selection, controller)
- `batch-match.processor.ts` + `matching-score.ts` style path
- `onboarding-photo-preview-pool.service.ts` + onboarding photo UI
- Onboarding vision APPLY writers (default off)
- Prisma `PreviewPool` / `OnboardingPhotoPreviewPool*` tables

**r9:** gate writer only · **no deletion**.

---

## 6. r9 minimal change set

| File | Change |
|------|--------|
| `apps/worker/src/jobs/old-photo-matching-writer-shutdown-env.ts` | **new** env + write choke-point |
| `apps/worker/src/jobs/batch-match.processor.ts` | call choke-point before queue success |
| `apps/worker/test/p710-r9-old-photo-matching-writer-shutdown.spec.ts` | **new** unit tests |
| `docs/P7/P7.10-r9-old-photo-matching-writer-shutdown-closeout.md` | closeout |
| `docs/P7/P7.6-bidirectional-mutual-matching-system-plan.md` | roadmap status |

# P7.6-r9e3c — Staging-prod-like Read-only Reverify Smoke Closeout

> **状态**：**P7.6-r9e3c staging-prod-like read-only reverify smoke**  
> **结论**：**`P7_6_R9E3C_STAGING_READONLY_EVIDENCE_PARTIAL_DB_PENDING`**  
> **前置**：[P7.6-r9e4a](./P7.6-r9e4a-staging-bootstrap-execution-closeout.md) **`STAGING_BOOTSTRAP_PASS`** · [P7.6-r9e3b](./P7.6-r9e3b-real-prod-reverify-access-package-and-ops-handoff.md) access package  
> **Artifacts**：[artifacts/p76/r9e3c/](../../artifacts/p76/r9e3c/)  
> **性质**：read-only smoke evidence only · **非** customer production · **非** r9e3 complete

---

## 1. Status

```text
P7_6_R9E3C_STAGING_READONLY_EVIDENCE_PARTIAL_DB_PENDING
```

**说明：** Staging API 可达且 baseline GET 行为符合预期；readonly SQL、viewer cohort matching GET、admin sidecar 鉴权 GET、完整 Admin UI smoke **未完成**。证据已归档，**不得**标记 r9e3 complete；待补齐 access 后重跑 r9e3c，再进入 **P7.6-r9e3d** PM/Ops review。

---

## 2. Environment

| Field | Value |
|-------|-------|
| `environmentType` | **production-like-staging** |
| Customer production | **no** |
| API host (redacted) | `peima-api-staging.onrender.com` |
| Web host (redacted) | `peima-web-staging.vercel.app` |
| Readonly DB host (redacted) | *not provided this round* |

---

## 3. Access summary

| Access item | Status | Notes |
|-------------|--------|-------|
| Staging API | **available** | Non-localhost HTTPS; used default URL from r9e4a |
| Staging Web | **partial** | Root `/` loads; deep links 404 (Vercel SPA rewrite gap) |
| Readonly DB | **missing** | `PEIMA_READONLY_DATABASE_URL` unset → SQL skipped |
| Admin/service token | **missing** | `PEIMA_ADMIN_SERVICE_TOKEN` unset |
| Viewer cohort | **missing** | `PEIMA_R9E3C_VIEWER_CASES_JSON` unset |
| Admin sidecar flag | **unknown** | Render env not audited; API returns 404 without token |
| Grafana / monitoring watch | **not executed** | Out of scope for this partial pass |
| UI manual smoke | **deferred** | See §6 |

**Env preflight (presence only, no secrets logged):**

| Variable | Process env | `.env.staging` |
|----------|-------------|----------------|
| `PEIMA_STAGING_API_BASE_URL` | absent | absent |
| `PEIMA_STAGING_WEB_URL` | absent | absent |
| `PEIMA_READONLY_DATABASE_URL` | absent | absent |
| `PEIMA_ADMIN_SERVICE_TOKEN` | absent | absent |
| `PEIMA_R9E3C_VIEWER_CASES_JSON` | absent | absent |
| `DATABASE_URL` | — | present (host = Supabase pooler, non-localhost) |

---

## 4. HTTP GET smoke

**Policy:** GET only · no tokens · no viewer IDs in artifacts.

| Case | Path | Status | Outcome |
|------|------|--------|---------|
| health | `/health` | 404 | skipped (no route) |
| health | `/api/health` | 404 | skipped |
| root | `/` | 404 | **pass** (expected) |
| auth | `/auth/me` (no token) | 401 | **pass** (expected) |
| auth | `/auth/me` (with token) | — | skipped (no token) |
| matching | `/matching/result/:viewerUserId` | — | skipped (no cohort) |
| admin list | `/admin/p76/canonical-sidecar` | 404 | skipped (no token / flag) |
| admin aggregate | `/admin/p76/canonical-sidecar/aggregate` | 404 | skipped |

Detail: [http-get-results-redacted.json](../../artifacts/p76/r9e3c/http-get-results-redacted.json)

**Pass / skip / fail:** 2 pass · 5 skipped · 0 failed (baseline cases only).

---

## 5. Readonly SQL

**Not executed.**

| Reason |
|--------|
| `PEIMA_READONLY_DATABASE_URL` not supplied per [r9e3b](../../artifacts/p76/r9e3b/prod-reverify-access-checklist.md) handoff |

[r9e4a](./P7.6-r9e4a-staging-bootstrap-execution-closeout.md) reported `p76_allowlist_apply_meta` and `p76_canonical_match_result_meta` exist post-migrate; **not** re-counted via SQL this round.

Artifact: [readonly-sql-summary-redacted.json](../../artifacts/p76/r9e3c/readonly-sql-summary-redacted.json)

---

## 6. Vercel UI smoke

| Check | Result |
|-------|--------|
| `https://peima-web-staging.vercel.app/` | **opens** (200) |
| Web → Render API (this round) | **not verified** (no login session) |
| r9e4a bootstrap cross-ref | register 201 · me 200 · onboarding 200 |
| `/admin/p76/canonical-sidecar` deep link | Vercel **404 NOT_FOUND** |
| Safety banner | **not verified** |
| Table / aggregate / empty state | **not verified** |
| Write controls absent | **not verified** |

`uiManualSmokeDeferred = true` — see [vercel-ui-smoke-summary.md](../../artifacts/p76/r9e3c/vercel-ui-smoke-summary.md).

---

## 7. Mutation safety

本轮 **未** 执行：

| Guard | Value |
|-------|-------|
| POST / PATCH / DELETE | **no** |
| DB writes | **no** |
| migration / Prisma migrate | **no** |
| sidecar writer / rehearsal writer insert | **no** |
| MatchResult / matchInsights write | **no** |
| worker / batch-match trigger | **no** |
| percent change | **no** |
| PreviewPool disable | **no** |
| legacy removal | **no** |
| production deploy | **no** |
| Admin Apply | **no** |

---

## 8. Gate impact

| Gate | After r9e3c |
|------|-------------|
| r9e3 / Gate 12 | **`NEED_MORE_PROD_REVERIFY`** (unchanged) |
| Staging evidence | **partial** archived — not sufficient for complete |
| r9e3 complete | **no** |
| PM/Ops signoff (r9e3d) | **blocked** until full r9e3c rerun |

**Blockers for full evidence:**

1. `PEIMA_READONLY_DATABASE_URL` (SELECT-only counts / violations)
2. `PEIMA_ADMIN_SERVICE_TOKEN` + viewer cohort for matching/admin GET
3. `PEIMA_P76_CANONICAL_SIDECAR_ADMIN_ENABLED=1` on Render (if admin API required)
4. Vercel SPA rewrites for client routes
5. UI manual pass (banner, no write controls)

---

## 9. PASS / BLOCK

| Decision | Scope |
|----------|-------|
| **PASS** | Partial read-only staging evidence archived; API baseline GET + mutation safety documented |
| **BLOCK** | r9e3 complete · canonical production write · percent · PreviewPool disable · legacy removal · Admin Apply until PM/Ops signoff |

---

## 10. Next step

1. Ops delivers `PEIMA_READONLY_DATABASE_URL`, `PEIMA_ADMIN_SERVICE_TOKEN`, redacted `PEIMA_R9E3C_VIEWER_CASES_JSON`
2. Fix Vercel SPA rewrites (`vercel.json` / project settings) for `/login` and admin routes
3. Enable `PEIMA_P76_CANONICAL_SIDECAR_ADMIN_ENABLED=1` on Render if admin read API required
4. **Rerun P7.6-r9e3c** until status → `P7_6_R9E3C_STAGING_READONLY_REVERIFY_EVIDENCE_COLLECTED_PENDING_PM_OPS_REVIEW`
5. Then **P7.6-r9e3d** — PM/Ops review and signoff (**not started this round**)

---

## Artifacts

| File | Description |
|------|-------------|
| [staging-readonly-smoke-summary.json](../../artifacts/p76/r9e3c/staging-readonly-smoke-summary.json) | Machine summary |
| [staging-readonly-smoke-summary.md](../../artifacts/p76/r9e3c/staging-readonly-smoke-summary.md) | Human summary |
| [http-get-results-redacted.json](../../artifacts/p76/r9e3c/http-get-results-redacted.json) | HTTP cases |
| [readonly-sql-summary-redacted.json](../../artifacts/p76/r9e3c/readonly-sql-summary-redacted.json) | SQL (skipped) |
| [vercel-ui-smoke-summary.md](../../artifacts/p76/r9e3c/vercel-ui-smoke-summary.md) | UI notes |

# P7.10-r3a — Canonical Writer Dry-run Payload Builder Closeout

> **状态**：**P7.10-r3a dry-run payload builder**  
> **结论**：**`P7_10_R3A_CANONICAL_WRITER_DRY_RUN_PAYLOAD_BUILDER_COMPLETE`**  
> **前置**：[P7.10-r3](./P7.10-r3-p76-canonical-writer-design.md) · [P7.10-r2c](./P7.10-r2c-baseline-display-rename-and-safe-fallback-compat-closeout.md)  
> **性质**：pure types + builder + unit tests · **无** DB · **无** `MatchResult` / `matchInsights` 写入

---

## 1. 一句话结论

P7.10-r3a 实现 **P7.6 canonical writer dry-run payload** 纯函数构建器：从 stage1/2/3 funnel 输入与 guardrails 产出 `P76CanonicalWriterDryRunPayloadV1`，**恒** `mode=dry_run` · `appliedToMatchResult=false` · 不写 DB / 不改 worker / 不改 GET。

---

## 2. 新增 / 修改文件

| File | Role |
|------|------|
| `apps/api/src/modules/matching/p76-canonical-writer-dry-run.types.ts` | Payload + guardrail types |
| `apps/api/src/modules/matching/p76-canonical-writer-dry-run-builder.ts` | `evaluateP76CanonicalWriterDryRunGuardrails` · `buildP76CanonicalWriterDryRunPayloadV1` · assert |
| `apps/api/test/p710-r3a-canonical-writer-dry-run-builder.spec.ts` | Unit tests (16) |

**未修改：** Prisma · worker · `matching-result-display.ts` · Admin Apply · shadow/rehearsal writers

---

## 3. Payload contract

| Field | Value |
|-------|-------|
| `schemaVersion` | `1` |
| `sourceType` | `"p76_canonical_writer"` |
| `sourceVersion` | default `p7.10-r3-canonical-writer-v1` |
| `mode` | `"dry_run"` |
| `appliedToMatchResult` | **always `false`** |
| `appliedToFinalScore` | **always `false`** |
| `appliedToWorkerRanking` | **always `false`** |

**与 P7.10-r6 shadow 区别：** shadow 为 compare-only（`mode: "shadow"` · `sourceType: p76_canonical_writer_shadow`）；r3a dry-run 对齐 [r3 §8](./P7.10-r3-p76-canonical-writer-design.md) 未来 canonical writer 输出形态，**仍不写主链**。

---

## 4. Guardrails

| Reason | Trigger |
|--------|---------|
| `ok` | All checks pass |
| `exception` | `resolverError` set |
| `missing_viewer` | empty `viewerUserId` |
| `missing_stage1` / `missing_stage2` / `missing_stage3` | stage summary absent |
| `rolled_back` | `rolledBack === true` |
| `violation_blocked` | `violationStatus` not `ok` |
| `stale_source_version` | `cohortSourceVersion` ≠ `sourceVersion` |
| `not_allowlisted` | non-empty allowlist excludes viewer |
| `missing_selected_candidate` | no stage3 / explicit selection |
| `candidate_missing` | `candidateExists === false` |
| `score_missing` | selection present but score not finite |
| `safe_fallback_required` | any block above (appended) |

Ineligible payloads: `selectedCandidateId` / `score` / `reasonSummary` cleared; `safeFallbackMeta.safeFallbackRequired=true`.

---

## 5. 行为不变确认

| Item | Status |
|------|--------|
| Prisma / DB writes | **None** |
| `MatchResult` write | **None** |
| `matchInsights` write | **None** |
| `candidateUserId` / `finalScore` mutation | **None** |
| Worker / GET / FinalMatchPage | **Untouched** |
| PreviewPool / legacy photo matching | **Untouched** |

---

## 6. 测试结果

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api test -- p710-r3a-canonical-writer-dry-run-builder` | **PASS** (16 tests) |
| `pnpm --filter @peima/api run build` | **PASS** |

---

## 7. PASS / BLOCK

### PASS

- Dry-run types + pure builder + guardrails
- Invariant assert (`appliedToMatchResult` / `appliedToFinalScore` / `appliedToWorkerRanking` / `mode`)
- Tests green
- No DB / no production write

### BLOCK (unchanged program gates)

- Canonical production write
- Admin Apply implementation
- `p76_canonical_match_result_meta` migration
- Old photo writer disable
- r9e3 / percent / production rollout

---

## 8. 下一步

| Priority | Milestone |
|----------|-----------|
| **1** | **P7.10-r3b** — sidecar schema / writer design |
| **2** | **P7.10-r4** — old photo matching writer disable plan |
| **3** | **P7.10-r5** — safe fallback implementation |

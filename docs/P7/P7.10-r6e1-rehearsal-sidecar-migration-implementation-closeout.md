# P7.10-r6e1 — Rehearsal Sidecar Migration Implementation Closeout

## 1. Status

```text
P7_10_R6E1_REHEARSAL_SIDECAR_MIGRATION_IMPLEMENTED
```

- **Code change:** Prisma schema + migration + migration contract test only.
- **No** rehearsal writer.
- **No** rehearsal row insert (except transient local CHECK probes; none persisted).
- **No** `MatchResult` write.
- **No** `matchInsights` write.
- **No** worker change.
- **No** GET behavior change.
- **No** frontend change.
- **No** Admin API/UI implementation.
- **No** canonical production write.
- **No** legacy removal.
- **No** production / percent rollout.

---

## 2. What changed

| Path | Change |
|------|--------|
| `packages/database/prisma/schema.prisma` | Added `P76CanonicalWriterRehearsalMeta` |
| `packages/database/prisma/migrations/20260518120000_p76_canonical_writer_rehearsal_meta/migration.sql` | CREATE TABLE + indexes + CHECK constraints |
| `apps/api/test/p710-r6e1-rehearsal-sidecar-migration.spec.ts` | File-level migration/schema contract tests (8 tests) |
| `docs/P7/P7.10-r6e1-rehearsal-sidecar-migration-implementation-closeout.md` | This closeout |

**Not changed:** `apps/api/src`, `apps/web`, worker, GET resolvers, allowlist writer, PreviewPool.

---

## 3. Table summary

| Item | Value |
|------|-------|
| SQL table | `p76_canonical_writer_rehearsal_meta` |
| Prisma model | `P76CanonicalWriterRehearsalMeta` |
| Provider | PostgreSQL |
| Column style | Quoted **camelCase** (matches `p76_allowlist_apply_meta`) |

### 3.1 Key columns

| Column | Null | Default |
|--------|------|---------|
| `id` | NOT NULL | cuid (app) |
| `viewerUserId` | NOT NULL | — |
| `matchResultId` | NOT NULL | — |
| `auditRunId` | NOT NULL | — |
| `guardrailReason` | NOT NULL | — |
| `sourceType` / `sourceVersion` / `pipelineVersion` | NOT NULL | — |
| `shadowPayload` | NOT NULL | JSONB |
| `appliedToMatchResult` | NOT NULL | `false` |
| `environment` | NOT NULL | `'dev'` |
| `rehearsalMode` | NOT NULL | `'sidecar_rehearsal'` |
| `wouldChangeCandidate` / `eligible` | NOT NULL | `false` |
| `generatedAt` / `createdAt` | NOT NULL | `now()` |

Optional: score fields, `readPathSourceVersion`, `allowlistApplyMetaId`, `summary`, `expiresAt`, `supersededAt`, `deletedAt`.

### 3.2 Unique constraint

```sql
UNIQUE ("matchResultId", "auditRunId", "pipelineVersion")
```

### 3.3 Indexes

`viewerUserId`, `matchResultId`, `sourceVersion`, `readPathSourceVersion`, `auditRunId`, `eligible`, `guardrailReason`, `appliedToMatchResult`, `supersededAt`, `generatedAt`, `(environment, generatedAt)`.

### 3.4 CHECK constraints (new repo pattern)

| Constraint | SQL |
|------------|-----|
| `p76_canonical_writer_rehearsal_meta_applied_to_match_result_false` | `CHECK ("appliedToMatchResult" = false)` |
| `p76_canonical_writer_rehearsal_meta_environment_allowed` | `CHECK ("environment" IN ('dev', 'staging'))` |

**No foreign keys** in v1. **No** `MatchResult` reverse relation on Prisma model.

---

## 4. Safety constraints

| Constraint | Enforcement |
|------------|-------------|
| Rehearsal never applies to `MatchResult` | DB CHECK `appliedToMatchResult = false` |
| Dev/staging only rows | DB CHECK `environment IN ('dev','staging')` |
| Writer idempotency key | UNIQUE `(matchResultId, auditRunId, pipelineVersion)` |
| Baseline isolation | No FK to `match_results`; GET/worker do not read this table (unchanged) |
| `matchResultId` required | NOT NULL |
| `auditRunId` required | NOT NULL |

---

## 5. Verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/database run db:generate` | **PASS** |
| `pnpm --filter @peima/database run build` | **PASS** |
| `pnpm exec prisma migrate deploy` (local `packages/database`) | **PASS** — applied `20260518120000_p76_canonical_writer_rehearsal_meta` |
| `pnpm --filter @peima/api run build` | **PASS** |
| `pnpm --filter @peima/api test -- p710-r6 p710-r6a p710-r6e1 p77-r1 result-state p76-read-path-env p76-read-path-display-resolver` | **PASS** — 9 suites, 93 tests |
| Local DB `SELECT COUNT(*)` | **0 rows** |
| INSERT `appliedToMatchResult=true` | **REJECTED** — PostgreSQL `23514` (check_violation) |
| INSERT `environment='production'` | **REJECTED** — PostgreSQL `23514` (check_violation) |

**Note:** `prisma migrate dev --create-only` reported historical drift on local DB (fixture backup tables); migration was authored manually per [P7.10-r6e](./P7.10-r6e-rehearsal-sidecar-migration-design-review.md) and applied via `migrate deploy` successfully.

---

## 6. What did not change

- Rehearsal insert-only writer (r6f.1) — **not implemented**
- Admin read-only API/UI ([P7.7-r3](./P7.7-r3-admin-readonly-rehearsal-review-design.md)) — **not implemented**
- `MatchResult` / `matchInsights` mutations
- Worker / batch-match / GET `/matching/result` / FinalMatchPage / frontend
- PreviewPool writer — **not disabled**
- Legacy photo matching — **not removed**
- Production / percent rollout — **blocked**

---

## 7. PASS / BLOCK

### PASS (this round)

- Prisma model `P76CanonicalWriterRehearsalMeta` added
- Migration `20260518120000_p76_canonical_writer_rehearsal_meta` added
- CHECK constraints on `appliedToMatchResult` and `environment`
- Required `matchResultId` / `auditRunId`
- Unique triple index
- `db:generate` + database build + API build + safety tests pass
- Local CHECK verification (23514)

### BLOCK (unchanged)

- Rehearsal writer
- Admin implementation
- Canonical production write
- Worker integration
- Legacy removal
- Production / percent (`r9e3`, `r9f`)

---

## 8. Next recommended step

| Priority | Milestone | Notes |
|----------|-----------|-------|
| **1** | **P7.10-r6f.1** | Insert-only rehearsal writer — **dry-run first**, then gated local insert; never mutate `MatchResult` |
| **2** | **P7.7-r3.1** | Admin read-only API (after writer produces rows) |
| **3** | **P7.7-r3.2** | Admin UI |

**Do not** enable production migration deploy or percent rollout until explicit program approval.

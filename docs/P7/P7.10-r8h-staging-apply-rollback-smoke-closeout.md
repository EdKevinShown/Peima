# P7.10-r8h — Staging Apply + Rollback Smoke Closeout

> **状态**：P7.10-r8h staging Apply + Rollback smoke — **DONE**  
> **结论**：**`PASS_R8H_STAGING_APPLY_ROLLBACK_SMOKE`**  
> **前置**：[P7.10-r8g](./P7.10-r8g-local-apply-rollback-smoke-closeout.md) · [P7.10-r8f](./P7.10-r8f-hidden-admin-apply-rollback-api-wiring-closeout.md)  
> **性质**：production-like-staging DB smoke only · **非** customer production · **非** r8i signoff

---

## 1. 一句话结论

**P7.10-r8h** 在 **production-like-staging**（Supabase pooler，explicit smoke gate）上完成 synthetic Apply → Rollback 闭环；service path 执行；HTTP hidden route **NOT_RUN**（r8f 单测已覆盖 wiring）；明文 token **未** 暴露；fixture **已清理**。

```text
PASS_R8H_STAGING_APPLY_ROLLBACK_SMOKE
```

---

## 2. 修改文件列表

| 文件 | 说明 |
|------|------|
| `tmp/p710-r8h-staging-apply-rollback-smoke.mjs` | Staging smoke runner + preflight + cleanup |
| `tmp/p710-r8h-staging-migrate-deploy.mjs` | 一次性：staging 缺表时 `migrate deploy`（本轮已执行 snapshot migration） |
| `artifacts/p76/r8h/staging-apply-rollback-smoke-result.json` | Sanitized evidence |
| 本文档 | closeout |

**未修改：** `apps/web` · `.env.staging`（不提交）· production 配置

---

## 3. Staging preflight 结果

| 检查 | 结果 |
|------|------|
| `PEIMA_P710_R8H_STAGING_SMOKE_ALLOWED=1` | required · set in process only |
| `NODE_ENV` | forced `staging`（覆盖 `.env.staging` 内 production 标签） |
| DATABASE_URL | **非** localhost · denylist pass · classified `production-like-staging` |
| host fingerprint (sha256 prefix) | `54a4d720706c0593`（**非** 完整 URL） |
| `PEIMA_P76_PRODUCTION_PERCENT` | `0` |
| percent rollout | disabled |
| `PEIMA_P76_ENVIRONMENT_KIND` | `production-like-staging`（explicit confirm） |
| customer production touched | **false** |

**Preflight BLOCK 条件（未触发）：** `BLOCK_R8H_STAGING_PREFLIGHT_UNSAFE_DATABASE` · localhost · production denylist

---

## 4. Staging-only env 设置说明

Smoke 进程内临时设置（**未** 写入 `.env` / 未提交）：

| 变量 | Smoke 值 |
|------|----------|
| `NODE_ENV` | `staging` |
| `PEIMA_P710_R8_*` mutation/apply/rollback | `1` + `production-like-staging` |
| `PEIMA_P76_PRODUCTION_PERCENT` | `0` |
| `PEIMA_P76_ROLLBACK_TOKEN_PEPPER` | process env only（**未** 写入 artifact/docs） |

**DB 环境字段注意：** `p76_canonical_match_result_meta.environment` CHECK 仅允许 `dev` \| `staging`；fixture sidecar 使用 `staging`；snapshot / service input 使用 `production-like-staging`。

---

## 5. Fixture 创建说明

| 实体 | 说明 |
|------|------|
| `User` ×3 | synthetic phone `+r8h-*` |
| `MatchBatch` + `MatchResult` | before: cand A · score 0.7 |
| `P76CanonicalMatchResultMeta` | proposes cand B · `environment=staging` |
| `P76CanonicalApplyRollbackSnapshot` | `environment=production-like-staging` · HMAC hash only |

`auditRunId`: `r8h-staging-smoke-<timestamp>` · artifact 内 ID 均为 **redacted hash**

---

## 6. Apply smoke 结果

| 检查 | 结果 |
|------|------|
| execution path | **service**（`applyP76CanonicalSidecarToMatchResult`） |
| `mode` | `applied` |
| MatchResult | cand A → cand B · 0.7 → 0.8 |
| sidecar | `promoted` · `appliedToWorkerRanking=false` |
| snapshot | `applied` |

---

## 7. Rollback smoke 结果

| 检查 | 结果 |
|------|------|
| execution path | **service**（`rollbackP76CanonicalApply`） |
| `mode` | `rolled_back` |
| `tokenVerified` | `true` |
| `tokenPlaintextExposed` | `false` |
| MatchResult | restored cand A · score 0.7 |
| sidecar | `rolled_back` |
| snapshot | `rollback_completed` · `rollbackUsedAt` set |

---

## 8. DB before / applied / restored 对照

| 阶段 | candidate | finalScore | sidecar | snapshot |
|------|-----------|------------|---------|----------|
| before Apply | synthetic A | 0.7 | `not_promoted` | `snapshot_created` |
| after Apply | synthetic B | 0.8 | `promoted` | `applied` |
| after Rollback | synthetic A | 0.7 | `rolled_back` | `rollback_completed` |

---

## 9. Token no-exposure proof

| 检查 | 结果 |
|------|------|
| artifact JSON | `tokenPlaintextExposed: false` |
| `tokenHashPresent` | `true` |
| forbidden pattern scan | pass（无 `postgresql://` / `jwt_secret` / plaintext token） |

---

## 10. Worker / percent / legacy untouched proof

| 指标 | before → after |
|------|----------------|
| `matchResult` count | 6 → 6 |
| `sidecar` count | 0 → 0 |
| `snapshot` count | 0 → 0 |
| `hookJob` count | 0 → 0 |
| `appliedToWorkerRanking` true | 0 → 0 |
| percent | 0 · disabled |

---

## 11. Cleanup proof

| 检查 | 结果 |
|------|------|
| `finalRowsForAuditRunId` | `0` |
| orphan `+r8h-*` users | cleaned at start + end |
| counts restored | ✅ |

---

## 12. HTTP route smoke

| 项 | 状态 |
|----|------|
| Hidden POST Apply/Rollback HTTP | **NOT_RUN** |
| 原因 | staging admin JWT/RBAC 未纳入本轮；r8f 单测已覆盖 controller wiring |
| DB mutation evidence | service path（与 r8f 委托同一 service 层） |

---

## 13. Artifacts 路径

- [artifacts/p76/r8h/staging-apply-rollback-smoke-result.json](../../artifacts/p76/r8h/staging-apply-rollback-smoke-result.json)

---

## 14. Regression tests / build

```text
pnpm --filter @peima/api run build
node tmp/p710-r8h-staging-apply-rollback-smoke.mjs  → PASS (with PEIMA_P710_R8H_STAGING_SMOKE_ALLOWED=1)
pnpm --filter @peima/api test -- p710-r8f         → 34/34 PASS
pnpm --filter @peima/api test -- p710-r8e         → 33/33 PASS
pnpm --filter @peima/api test -- p710-r8d         → 20/20 PASS
```

**复现命令（redacted，secrets 由操作者本地提供）：**

```powershell
cd C:\Users\34713\Desktop\peima
pnpm --filter @peima/api run build
$env:PEIMA_P710_R8H_STAGING_SMOKE_ALLOWED="1"
$env:PEIMA_P76_ENVIRONMENT_KIND="production-like-staging"
$env:PEIMA_P76_ROLLBACK_TOKEN_PEPPER="<staging pepper from secure store>"
node tmp/p710-r8h-staging-apply-rollback-smoke.mjs
```

若 staging 缺 `p76_canonical_apply_rollback_snapshot` 表，先执行（一次性）：

```text
node tmp/p710-r8h-staging-migrate-deploy.mjs
```

---

## 15. PASS / BLOCK

| 判定 | 状态 |
|------|------|
| **PASS** | `PASS_R8H_STAGING_APPLY_ROLLBACK_SMOKE` |

**BLOCK 条件（均未触发）：** production touched · token exposed · worker triggered · percent changed · cleanup incomplete

---

## 16. Next step

| Priority | Milestone |
|----------|-----------|
| **DONE** | **P7.10-r8i** — [PM/Ops signoff evidence pack](./P7.10-r8i-pm-ops-apply-rollback-signoff.md) · `READY_FOR_R9_WRITER_SHUTDOWN_REVIEW_PENDING_HUMAN_SIGNOFF` |
| **Human** | 完成 r8i §6 签收 → `PASS_R8I_PM_OPS_APPLY_ROLLBACK_SIGNOFF` |
| **NEXT** | **P7.10-r9** — 关闭旧 photo matching **writer**（非删除 legacy） |

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-19 | r8h staging smoke · snapshot migration on staging · `PASS_R8H_STAGING_APPLY_ROLLBACK_SMOKE` |

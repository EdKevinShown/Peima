# P7.6-r8b — Allowlist Apply Writer Implementation Closeout

> **状态**：**P7.6-r8b implementation PASS** · **20/20 tests** · **migration 未自动 apply**  
> **前置**：[P7.6-r8a sidecar schema](./P7.6-r8a-allowlist-apply-sidecar-schema-design.md) **DESIGN PASS** · [P7.6-r8](./P7.6-r8-allowlist-only-apply-design.md) · [P7.6-r7l](./P7.6-r7l-pm-signoff-for-r8-allowlist-design.md)  
> **P7.6 plan**：[Bidirectional Mutual Matching System](./P7.6-bidirectional-mutual-matching-system-plan.md)  
> **后续**：[P7.6-r8c dev dry-run audit](./P7.6-r8c-allowlist-apply-dev-dry-run-audit-closeout.md) **PASS**（migration SQL pending dev DB）

---

## 1. 一句话结论

实现 **`p76_allowlist_apply_meta`** Prisma model + migration、[env gate](./P7.6-r8-allowlist-only-apply-design.md) reader、**sidecar-only writer** 与 **`p76:r8b-allowlist-apply-writer`** dev-cli。默认 **`ENABLED=0`** · **`DRY_RUN=1`**；**不写** `MatchResult` / `finalScore` / worker / display。**production apply / percent / rollout 仍 blocked**。

---

## 2. 修改文件列表

| 路径 | 用途 |
|------|------|
| `packages/database/prisma/schema.prisma` | `P76AllowlistApplyMeta` model |
| `packages/database/prisma/migrations/20260517120000_p76_allowlist_apply_meta/migration.sql` | 建表 migration |
| `apps/api/src/modules/matching/p76-allowlist-apply-meta.types.ts` | contracts |
| `apps/api/src/modules/matching/p76-allowlist-apply-env.ts` | env reader |
| `apps/api/src/modules/matching/p76-allowlist-apply-writer.ts` | writer + rollback |
| `apps/api/src/dev-cli/p76-r8b-allowlist-apply-writer-cli-args.ts` | CLI args |
| `apps/api/src/dev-cli/p76-r8b-allowlist-apply-writer-runner.ts` | CLI runner |
| `apps/api/test/p76-allowlist-apply-*.spec.ts` | 单测 ×3 |
| `apps/api/package.json` | script `p76:r8b-allowlist-apply-writer` |

---

## 3. Migration / schema 摘要

| 项 | 值 |
|----|-----|
| **表名** | `p76_allowlist_apply_meta` |
| **Migration** | `20260517120000_p76_allowlist_apply_meta` |
| **UNIQUE** | `(viewerUserId, sourceVersion)` |
| **FK** | `viewerUserId` → `users(id)` ON DELETE CASCADE |
| **生产 apply** | **未**自动 `migrate deploy` — 仅 dev/staging 手动执行 |

```powershell
cd packages/database
pnpm run db:migrate
# 或: prisma migrate deploy（staging 运维流程）
```

---

## 4. Env gate 摘要

| 变量 | 默认 |
|------|------|
| `PEIMA_P76_ALLOWLIST_APPLY_ENABLED` | **`0`** |
| `PEIMA_P76_ALLOWLIST_APPLY_DRY_RUN` | **`1`** |
| `PEIMA_P76_ALLOWLIST_VIEWER_IDS` | 空 |
| `PEIMA_P76_ALLOWLIST_POOL_SOURCE_VERSION` | `p7.6-r7j3-staging-cohort-v1` |
| `PEIMA_P76_ALLOWLIST_REQUIRE_PM_SIGNOFF` | **`1`** |
| `PEIMA_P76_ALLOWLIST_REQUIRE_OPS_SIGNOFF` | **`1`** |

**有效 dry-run：** `env.dryRun || cli.dryRun` — env 默认 **强制** dry-run。

---

## 5. Writer 行为摘要

| 函数 | 行为 |
|------|------|
| `buildP76AllowlistApplyMetaV1` | 构建 meta；拒绝主链 `appliedTo*` = true |
| `evaluateP76AllowlistApplyEligibility` | allowlist · signoff · shadow 一致性 |
| `writeP76AllowlistApplyMeta` | dry-run → `wouldApply` 无 DB；`canWriteSidecar` → **仅** `p76AllowlistApplyMeta.upsert` |
| `rollbackP76AllowlistApplyMeta` | 仅更新 sidecar `rolledBack*` |

**结果 JSON：** `applied: false`（pipeline 级，非 production apply）· `wroteSidecar` 表示 sidecar 行写入。

---

## 6. CLI

```powershell
cd apps/api
pnpm exec nest build
npx dotenv-cli -e ../../.env -- pnpm run p76:r8b-allowlist-apply-writer -- `
  --viewerUserId=cmr4hm001016z64demo00m05a `
  --selectedCandidateId=cmr4hf000916z64demo00f05a `
  --stage1SelectedCandidateIds=cmfemn00100016z64seed0001,cmr4hf000916z64demo00f05a `
  --stage2Top2CandidateIds=cmr4hf000916z64demo00f05a,cmr4hf000716z64demo00f04a `
  --finalShadowSelectedCandidateId=cmr4hf000916z64demo00f05a `
  --pmSignoffStatus=approved `
  --opsSignoffStatus=approved `
  --dryRun=true
```

---

## 7. 测试

```text
pnpm --filter @peima/database run db:generate  → OK
pnpm exec jest test/p76-allowlist-apply --no-cache → 20/20 PASS
npx tsc -p tsconfig.json --noEmit → OK
pnpm exec nest build → OK
```

---

## 8. 边界确认

| 项 | 状态 |
|----|------|
| 写 `MatchResult` / `finalScore` | **否** |
| 改 worker / display | **否** |
| `appliedToMatchResult/FinalScore/WorkerRanking=true` | **拒绝 / P0** |
| 默认开启 apply | **否** |
| percent / `APPLY_TO_POOL` | **未启** |
| read path / display resolver | **未接** |
| 删除 legacy | **否** |

---

## 9. 下一步

[P7.6-r8e](./P7.6-r8e-allowlist-sidecar-live-closeout.md) — sidecar live smoke **PASS**（dev/staging · 5/5）

---

## 参考

| 文档 | 用途 |
|------|------|
| [P7.6-r8a](./P7.6-r8a-allowlist-apply-sidecar-schema-design.md) | schema 设计 |
| [P7.6-r8c](./P7.6-r8c-allowlist-apply-dev-dry-run-audit-closeout.md) | dry-run audit |
| [P7.6-r8c1](./P7.6-r8c1-dev-db-migration-and-violation-sql-followup-closeout.md) | DB migration + violation SQL |
| [P7.6-r8d](./P7.6-r8d-pm-ops-apply-signoff.md) | PM/Ops signoff |
| [P7.6-r8](./P7.6-r8-allowlist-only-apply-design.md) | allowlist 总设计 |
| [P7.6-r8e](./P7.6-r8e-allowlist-sidecar-live-closeout.md) | sidecar live smoke |
| [P7.6 plan](./P7.6-bidirectional-mutual-matching-system-plan.md) | r8f–r9 |

# P7.7-r4.4 — Local Admin Sidecar UI Smoke with Seeded Rows Closeout

> **前置：** [P7.7-r4.3](./P7.7-r4.3-canonical-sidecar-admin-ui-implementation-closeout.md) · [P7.10-r3f3](./P7.10-r3f3-canonical-match-result-sidecar-writer-local-insert-smoke-closeout.md)

---

## 1. Status

```text
P7_7_R4_4_LOCAL_ADMIN_SIDECAR_UI_SMOKE_PARTIAL_API_AND_BUILD_COMPLETE
```

说明：

- Local smoke only (dev DB)
- Seed → Admin API HTTP smoke → UI shell browser check → cleanup **complete**
- Admin **API** list / aggregate / detail **pass**
- Admin **UI** table/detail/export **deferred** in browser (JWT `localStorage` origin isolation); static no-write + page shell **pass**
- No MatchResult / matchInsights / worker / GET changes
- No production / staging / percent

---

## 2. What was tested

1. Build + unit test baseline (`api` / `web`)
2. **Seed** via `p710-r3f3` runner: `--insert=true --cleanup=false --auditRunId=p77-r4-4-admin-sidecar-ui-smoke-001`
3. **Admin API** HTTP smoke (`PEIMA_P76_CANONICAL_SIDECAR_ADMIN_ENABLED=1`, admin JWT)
4. **Admin UI** at `/admin/p76/canonical-sidecar` (page, banner bullets, filters; data rows deferred in browser)
5. **Cleanup** `deleteMany` by `auditRunId` only → `finalRowsForAuditRunId=0`

---

## 3. Seeded rows

| Field | Value |
|-------|-------|
| auditRunId | `p77-r4-4-admin-sidecar-ui-smoke-001` |
| insertedCount | 2 |
| duplicateCount | 2 |
| appliedToMatchResultTrueCount | 0 |
| appliedToFinalScoreTrueCount | 0 |
| appliedToWorkerRankingTrueCount | 0 |
| Rows before cleanup | 2 |

Evidence: [admin-sidecar-ui-smoke-summary.json](../../artifacts/p76/r4_4/admin-sidecar-ui-smoke-summary.json)

**Side effect:** r3f3 seed step overwrote `artifacts/p76/r3f3/canonical-sidecar-writer-smoke-summary.json` (expected runner default output path).

---

## 4. Admin API smoke

| Endpoint | Result |
|----------|--------|
| `GET /admin/p76/canonical-sidecar?auditRunId=...` | **pass** — 200, `featureEnabled=true`, 2 items |
| `GET /admin/p76/canonical-sidecar/aggregate?auditRunId=...` | **pass** — 200, `totalVisible=2`, all `*ViolationCount=0` |
| `GET /admin/p76/canonical-sidecar/:id` | **pass** — 200, `rollbackTokenPresent` boolean only; `readByGetPath=false`, `readByWorker=false` |

API process required full repo `.env` (for `PEIMA_ADMIN_USER_IDS`) plus `PEIMA_P76_CANONICAL_SIDECAR_ADMIN_ENABLED=1`. No JWT or raw `dryRunPayload` logged.

---

## 5. Admin UI smoke

| Check | Result |
|-------|--------|
| Route `/admin/p76/canonical-sidecar` | **pass** |
| Safety banner / bullets | **pass** (incl. “No Apply”, no write controls in button list) |
| Filters | **pass** |
| Table + aggregate cards with seeded rows | **deferred** (browser: no admin JWT on `:5173` origin) |
| Detail drawer + export | **deferred** |
| Static no-write grep (`P76CanonicalSidecarPage`, API client) | **pass** |

**Manual follow-up (optional):** login as admin on `:5173`, re-seed without cleanup, filter `auditRunId=p77-r4-4-admin-sidecar-ui-smoke-001`, confirm table/detail/export.

---

## 6. Cleanup

| Metric | Value |
|--------|-------|
| cleanupDeletedCount | 2 |
| finalRowsForAuditRunId | 0 |

Method: `cleanupP76CanonicalMatchResultSidecarRowsByAuditRunId` via r3f3 runner module (Prisma `deleteMany` on `auditRunId` only).

---

## 7. Tests / build

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api run build` | PASS |
| `pnpm --filter @peima/web run build` | PASS |
| `pnpm --filter @peima/api test -- p77-r4-1-canonical-sidecar-admin-readonly-api` | PASS (17) |
| `pnpm --filter @peima/web test` | PASS (21) |

---

## 8. Safety

- No MatchResult write
- No matchInsights write
- No worker change
- No GET `/matching/result` integration
- No Admin Apply / rollback
- No production / percent
- Cleanup scoped to `auditRunId=p77-r4-4-admin-sidecar-ui-smoke-001` on `p76_canonical_match_result_meta` only

---

## 9. PASS / BLOCK

**PASS**

- Seeded sidecar rows visible via Admin API
- Cleanup complete (`finalRowsForAuditRunId=0`)
- Build + regression tests pass
- UI route + safety banner + no-write controls verified (static + browser shell)

**BLOCK (unchanged)**

- Admin Apply blocked
- MatchResult write blocked
- worker/GET blocked
- production/percent blocked

---

## 10. Next recommended step

**Option A — P7.10-r7b/r7c** Apply + promotion design (canonical writer promotion path).

**Option B — P7.10-r8** GET compatibility design.

**Option C — P7.6-r9e3c** real prod/staging read-only reverify.

**推荐：** continue local canonical mainline with **r7b/r7c** if promotion design is next; otherwise **r9e3c** for production gate.

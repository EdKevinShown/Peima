# P7.10-r6b — Local DB Canonical Writer Shadow Audit Smoke Closeout

## 1. Status

```text
P7_10_R6B_LOCAL_DB_CANONICAL_WRITER_SHADOW_AUDIT_SMOKE_COMPLETE
```

- Local DB smoke only (read-only)
- No DB writes
- No `MatchResult` write
- No `matchInsights` write
- No `finalScore` / `candidateUserId` change
- No worker integration
- No GET behavior change
- No frontend change
- No PreviewPool disable
- No legacy removal
- No production / percent rollout

---

## 2. Commands run

| Command | Result |
|---------|--------|
| `git status --short` | Clean (empty) |
| `pnpm --filter @peima/api run build` | **PASS** |
| `node -r dotenv/config dist/dev-cli/p710-r6a-canonical-writer-shadow-audit-runner.js …` (from `apps/api`, `DOTENV_CONFIG_PATH` → repo `.env`) | **PASS** exit 0 |
| Inline Node artifact safety check | **PASS** |

**Note:** `pnpm exec dotenv -e .env -- pnpm --filter @peima/api run p710:r6a:…` failed on Windows (`spawn run ENOENT`). The runner’s built-in `dotenv.config` paths did not load `DATABASE_URL` in this shell; **`DOTENV_CONFIG_PATH` + `node -r dotenv/config`** was used instead (documented in summary artifact).

---

## 3. Artifact output

| Item | Value |
|------|-------|
| JSON | `artifacts/p76/r6b/canonical-writer-shadow-audit.json` |
| Summary | `artifacts/p76/r6b/canonical-writer-shadow-audit-summary.md` |
| `sourceType` | `p76_canonical_writer_shadow_audit` |
| `sourceVersion` | `p7.10-r6a-canonical-writer-shadow-audit-v1` |
| `generatedAt` | `2026-05-18T19:04:39.590Z` |
| totalRows | 20 |
| eligibleCount | 4 |
| blockedCount | 16 |
| wouldChangeCandidateCount | 4 |
| appliedToMatchResultCount | 0 |

---

## 4. Guardrail result

| `guardrails.reason` | Count |
|---------------------|-------|
| missing_sidecar | 15 |
| ok | 4 |
| rolled_back | 1 |

**Score delta bands:** `unknown` 16, `none` 4 (ineligible rows → `unknown`; eligible same-score compare → `none`).

---

## 5. Safety verification

- `appliedToMatchResultCount` = **0**
- Every `rows[].shadow.appliedToMatchResult` = **false**
- No sensitive keys (`rawPrompt`, `transcript`, `imageFeatures`, `rawImage`, `fullPrompt`) in serialized JSON
- Runner uses only Prisma **read** APIs; no `create` / `update` / `upsert` / `delete`
- DB read-only; artifact written to disk only

---

## 6. Interpretation

- **eligibleCount = 4:** Four recent `MatchResult` viewers had an approved, non–rolled-back sidecar for cohort `p7.6-r7j3-staging-cohort-v1` and passed read-path guardrails; shadow would propose a **different** `selectedCandidateUserId` than baseline for those four (`wouldChangeCandidateCount = 4`).
- **blockedCount = 16:** Mostly **missing_sidecar** (no `p76_allowlist_apply_meta` row for viewer + cohort version) — normal for users outside the staged allowlist cohort.
- **wouldChangeCandidateCount = 4:** Compare-only signal; does **not** imply safe production cutover.
- This round does **not** mean canonical writer may write `MatchResult`, and does **not** mean PreviewPool / batch-match may be disabled.

---

## 7. PASS / BLOCK

**PASS**

- Runner executed against local DB with real rows
- Artifact + summary generated
- No-write invariant verified
- Safety checks complete

**BLOCK**

- No production canonical write
- No dual-write
- No worker integration
- No legacy removal
- No production / percent

---

## 8. Next recommended step

**Option A — P7.7-r2 Local smoke automation** (**recommended**)

One command for: safe-fallback env alias checks, `resultState` contract smoke (r9b opt-in), regression tests, and r6a/r6b audit — with documented `DOTENV_CONFIG_PATH` for Windows.

**Option B — P7.10-r6c Dual-write rehearsal design** (docs-only)

Design gates from shadow artifact → dual-write rehearsal meta — still **no** `MatchResult` writes.

**Recommendation:** Proceed with **P7.7-r2**. If ops wants cohort coverage before cutover, add a follow-up doc for seeding more `p76_allowlist_apply_meta` rows for local viewers (optional; not blocking r6b).

# P7.6-r7j3 — Route C-B Staging Cohort Execution Runbook

> **状态**：**Runbook 定稿**（本轮不改代码 · 不写 DB · 不执行 seed · 不进入 r8 · 不 apply）  
> **前置**：[P7.6-r7j2 C-B plan](./P7.6-r7j2-route-cb-staging-cohort-import-build-plan.md) **定稿** · [P7.6-r7j1 gap inventory](./P7.6-r7j1-route-c-data-gap-inventory-closeout.md) **`C_A_NOT_FEASIBLE`** · [P7.6-r7j runbook](./P7.6-r7j-route-c-data-preparation-execution-runbook.md) · [P7.6-r7g runbook](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md)  
> **P7.6 plan**：[Bidirectional Mutual Matching System](./P7.6-bidirectional-mutual-matching-system-plan.md)  
> **后续**：[P7.6-r7j4 closeout](./P7.6-r7j4-route-cb-staging-cohort-execution-closeout.md) · [P7.6-r7g1 Stage1 blocker](./P7.6-r7g1-route-c-stage1-blocker-diagnosis.md) → **r7g2**

---

## 1. 一句话结论

P7.6-r7j3 将 [Route C-B staging cohort import / build plan](./P7.6-r7j2-route-cb-staging-cohort-import-build-plan.md) 转成**可执行 runbook**：通过导入或构建 **5–10** 个 clean staging viewers、**6–10** candidates/viewer，并补齐 **image、Cloud Vision、20D、preference、clean active pool、MatchResult / M6** 对照，使 [r7g §4](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md) readiness SQL 达到 **`recommendedForRouteC ≥ 5`**。本轮**只写执行手册**，不改代码、不写 DB、不进入 r8。

---

## 2. 前置状态

| 项 | 状态 |
|----|------|
| [r7j1](./P7.6-r7j1-route-c-data-gap-inventory-closeout.md) Route C-A | **`C_A_NOT_FEASIBLE`** |
| [r7j2](./P7.6-r7j2-route-cb-staging-cohort-import-build-plan.md) C-B plan | **DONE · 定稿** |
| `reusableViewerCount` | **4** |
| `reusableCandidateCount` | **4** |
| `viewersWithPoolGe6` | **0** |
| `recommendedForRouteC` | **0** |
| clean runnable viewers | **0** |
| Route C audit | **blocked** |
| **r8** | **blocked** |
| PM signoff | **pending** |
| apply / percent | **blocked** |
| legacy photo matching removal | **blocked** |

**排除 viewer（默认）：** Batch 0 `cmo7ksq8s00006znosryc9k0n`；r7d 三样本 `cmp70ft7a001e6znksjb2jo03` · `cmfemn002000306z64seed0002` · `cmfemn003000506z64seed0003`。

---

## 3. 执行目标

### 3.1 最低目标

| # | 目标 |
|---|------|
| 1 | **5** clean staging viewers |
| 2 | 每 viewer **≥6** candidates |
| 3 | `recommendedForRouteC=true` **≥5** |
| 4 | `seedDependent=false` |
| 5 | `syntheticPool=false` |

### 3.2 推荐目标

| # | 目标 |
|---|------|
| 1 | **10** clean staging viewers |
| 2 | 每 viewer **6–10** candidates |
| 3 | `recommendedForRouteC=true` **≥10** |
| 4 | 尽量 **`hasMatchResult=true`** |
| 5 | 尽量 **`hasM6RrmTop2=true`**（`matchInsights.rrmV2Top2Selector`） |

### 3.3 执行链

```text
r7j3 runbook (本文档 · DONE)
  → r7j4 execution closeout（staging 写 DB · 单独轮次）
  → r7g §4 readiness SQL
  → 若 recommendedForRouteC >= 5 → r7g §7–§8 audit
  → P7.6-r7k Route C audit closeout
  → PM signoff? → P7.6-r8
```

---

## 4. Cohort 构建输入表

**不要求**建 DB 表；执行前用 **CSV** 或 **markdown** 维护 cohort 设计表，保存建议路径：`tmp/p76-r7j3-cohort-input.csv`（或 closeout 附录）。

### 4.1 字段定义

| 字段 | 类型 | 说明 |
|------|------|------|
| `userId` | string | 已有 cuid 或 `TBD-staging-NN` 占位（执行前替换） |
| `role` | enum | `viewer` · `candidate` · `both` |
| `gender` | string | `male` / `female` / …（gate 需要） |
| `age` | int | 年龄（preference / gate） |
| `city` | string | 城市（若 preference 需要） |
| `imageReady` | bool | 至少一张 `detectionStatus=passed` |
| `visionReady` | bool | Cloud Vision `photoVisualTags` 非空 |
| `twentyDReady` | bool | `nonNull20DCount ≥ 4`（推荐 ≥18） |
| `preferenceReady` | bool | `user_preferences` 行存在 |
| `intendedViewerIds` | string[] | 计划作哪些 viewer 的池成员（candidate 时） |
| `intendedCandidateIds` | string[] | 计划池内候选（viewer 时 · 不含 self） |
| `notes` | string | `stagingOnly` · `manuallyPrepared` · gate 备注 |

### 4.2 模板（markdown · 执行前填写）

| userId | role | gender | age | city | imageReady | visionReady | twentyDReady | preferenceReady | intendedViewerIds | intendedCandidateIds | notes |
|--------|------|--------|-----|------|------------|-------------|--------------|-----------------|---------------------|----------------------|-------|
| `cmr4hm001016z64demo00m05a` | both | … | … | … | true | true | true | true | V1,V2,… | U2,U3,… | r7j1 partial · reuse |
| `cmfemn00100016z64seed0001` | both | … | … | … | true | true | true | true | … | … | r7j1 partial |
| `cmr4hf000716z64demo00f04a` | both | … | … | … | true | true | true | true | … | … | r7j1 partial |
| `cmr4hf000916z64demo00f05a` | both | … | … | … | true | true | true | true | … | … | r7j1 partial |
| `TBD-staging-05` | viewer | … | … | … | false | false | false | false | — | U1..U4 + new | **新增** |
| … | … | … | … | … | … | … | … | … | … | … | … |

**验收（填表阶段）：** 每个 **viewer** 行 `intendedCandidateIds` 去重后 **≥6** 且 **不含自身**；gate 预检表（gender/preference）在写 DB 前完成。

---

## 5. Step 1 — 复用 r7j1 四个 clean partial users

[r7j1 §4/§5](./P7.6-r7j1-route-c-data-gap-inventory-closeout.md) 已确认以下四人 **vision + 20D + preference** 齐全 · **`reusableAsViewer=partial`**（缺 pool ≥6）：

| userId | vision | nonNull20D | preference | existingPool | 建议 |
|--------|--------|------------|------------|--------------|------|
| `cmr4hm001016z64demo00m05a` | ✓ | 6 | ✓ | **0** | viewer 或 candidate |
| `cmfemn00100016z64seed0001` | ✓ | 6 | ✓ | **0** | viewer 或 candidate |
| `cmr4hf000716z64demo00f04a` | ✓ | 6 | ✓ | **0** | viewer 或 candidate |
| `cmr4hf000916z64demo00f05a` | ✓ | 6 | ✓ | **0** | viewer 或 candidate |

**说明：**

- 四人**必须**纳入 C-B cohort 输入表。
- 可作 **viewer** 或 **candidate**（交叉复用）。
- **单独不足**：同集仅 4 人 · 每 viewer 池内最多 **3** 非 self → **无法**满足 5 viewers × 6 candidates。
- Step 1 **不**写 DB；仅锁定复用名单并填入 §4 输入表。

### 5.1 Step 1 checklist

| Step | 动作 | 产出 |
|------|------|------|
| **J3-1-0** | 将四人写入 §4 输入表 | 4 行 `imageReady=true` … |
| **J3-1-1** | 标注 `role=both` 或拆分 viewer/candidate | 拓扑草图 |
| **J3-1-2** | 确认 `seedDependent=false`（无 `dev-p76-*` profile/pref id） | SQL 或 r7j1 记录 |

---

## 6. Step 2 — 新增 / 导入 staging-only users

### 6.1 数量建议

| 档位 | 新增 staging-only users |
|------|-------------------------|
| **最低** | **6–12** |
| **推荐** | **10–20** |

加上 r7j1 四人 → total users **约 10–24**（满足 [r7j2 §6](./P7.6-r7j2-route-cb-staging-cohort-import-build-plan.md) 最低 8–12 / 推荐 15–25）。

### 6.2 每个新增 user 必须准备

| # | 实体 | 要求 |
|---|------|------|
| 1 | `users` | staging 账号 · 人口字段满足 gate |
| 2 | `user_images` | 至少一张 **`detectionStatus=passed`** |
| 3 | vision | `detectionScoreJson.vision` 或 Step 7 backfill |
| 4 | `user_profile` | 20D · `nonNull20DCount ≥ 4`（推荐 18–25） |
| 5 | `user_preferences` | 完整到支持 preference scoring / gate |
| 6 | 关系字段 | `gender` · `age` · `city` 等（matching gate 需要） |

### 6.3 文档 / 证据标记

| 标记 | 位置 | 说明 |
|------|------|------|
| `stagingOnly=true` | **文档 / closeout / §4 notes** | 非 production 证据 |
| `manuallyPrepared=true` | 若人工填 20D/pref · **仅文档** | PM 不得当作 production-like |
| **禁止** | 无既有安全字段时写入「伪 production」标记列 | 不伪装 production data |

**禁止（本 step）：** 写 `MatchResult` · 写 `finalScore` · 执行 seed 脚本（除非单独 PM 批准轮次）。

### 6.4 Step 2 checklist

| Step | 动作 |
|------|------|
| **J3-2-0** | 在 §4 表增加 `TBD-staging-*` 行 |
| **J3-2-1** | 规划 gender/age/city 组合以通过 gate（viewer↔candidate 矩阵） |
| **J3-2-2** | r7j4 执行轮：创建 user + image + profile + preference |
| **J3-2-3** | closeout 记录新增 userId 列表（rollback 用） |

---

## 7. Step 3 — Image / Cloud Vision 准备

### 7.1 流程

| # | 动作 |
|---|------|
| 1 | 确认每个 cohort user 有 **passed** `user_images` |
| 2 | 列出 `hasVision=false` 的 userId（含 viewer + 全部计划 candidate） |
| 3 | 对缺 vision 的 passed image 跑 **P7.5 Cloud Vision backfill** |
| 4 | **`dryRun=true`** 全量 audit |
| 5 | **`dryRun=false`** allowlist write（**staging only**） |
| 6 | SQL 抽查验收字段 |

### 7.2 验收字段（写库后）

| 字段 | 期望 |
|------|------|
| `vision.provider` | `cloud` |
| `vision.sourceVersion` | `p7.5-r7-cloud-zhipu-v1` |
| `vision.visionStatus` | `ok` |
| `vision.photoVisualTags` | 非空数组 |

### 7.3 命令模板（staging）

```powershell
cd apps/api
pnpm exec nest build

# per-user dry-run
pnpm run p75:r4-backfill-vision -- --provider=cloud --dryRun=true --userId=<U>

# per-user allowlist write (staging only)
pnpm run p75:r4-backfill-vision -- --provider=cloud --dryRun=false --userId=<U>
```

**按 image allowlist（批量）：** 见 [P7.5-r7-d runbook](./P7.5-r7-d-staging-cloud-vision-50-images-runbook.md) — `dryRun=true` 全量 → 小批量 `dryRun=false` → 满量。

**边界：** 仅写 `user_images.detectionScoreJson`；**不改** worker / `MatchResult` / `finalScore`。

### 7.4 Step 3 checklist

| Step | 动作 |
|------|------|
| **J3-3-1** | 列出缺 vision 的 userId |
| **J3-3-2** | `dryRun=true` backfill |
| **J3-3-3** | `dryRun=false` write + backup（§13） |
| **J3-3-4** | SQL 抽查 §7.2 |
| **J3-3-5** | 更新 §4 表 `visionReady=true` |

---

## 8. Step 4 — 20D / preference 准备

| 原则 | 说明 |
|------|------|
| **优先** | 问卷或真实 staging profile / preference |
| **人工构造** | 必须在文档标 **`stagingOnly=true`** · **`manuallyPrepared=true`** |
| **PM 证据** | **不**把人工构造样本写成 production-like 主证据 |
| **20D** | 至少 **4** 核心维；推荐 **18–25** 维 |
| **preference** | 完整到支持 **gate / preference scoring** |
| **避免** | [r7c4](./P7.6-r7c4-fixture-preparation-route-a-closeout.md) 式 `dev-p76-*` profile id（→ `seedDependent=true`） |

**缺口处理：** 优先换 candidate；其次补 questionnaire；最后才 staging 手工写入（须 closeout 披露）。

### 8.1 Step 4 checklist

| Step | 动作 |
|------|------|
| **J3-4-1** | 每 user `nonNull20DCount` SQL 检查 |
| **J3-4-2** | 每 user `user_preferences` 存在性 |
| **J3-4-3** | gate 预检：viewer gender/pref vs candidate 人口字段 |
| **J3-4-4** | 更新 §4 表 `twentyDReady` / `preferenceReady` |

---

## 9. Step 5 — Clean active pool 生成

### 9.1 优先路径

| # | 动作 |
|---|------|
| 1 | 使用**正常** onboarding / photo preview 产品流程 |
| 2 | 生成 `onboarding_photo_preview_pools` · `status=active` |
| 3 | 池内 **≥6** 去重 `candidateUserId`（§4 `intendedCandidateIds`） |
| 4 | **不**使用 `dev-p76-*` pool id |

### 9.2 若必须手动构建 staging pool（仅 staging · r7j4 执行轮）

| 原则 | 要求 |
|------|------|
| pool id | **禁止** `dev-p76-*`；建议 `staging-p76-r7j3-pool-<uuid>` |
| `sourceVersion` | 文档明确 **`p7.6-r7j3-staging-cohort-v1`** |
| 文档标记 | **`stagingOnly=true`** |
| gate | **不**绕过 gender / preference |
| self | **禁止** viewer 自身入池 |
| 容量 | 每 viewer **≥6** candidates |
| rollback | **必须**可回滚（§13） |

### 9.3 Step 5 checklist（per viewer）

| Step | 动作 | 产出 |
|------|------|------|
| **J3-5-0** | §13 backup（若将写 pool） | backup 表名 |
| **J3-5-1** | 优先 onboarding 建池 | `poolId` |
| **J3-5-2** | 否则 staging INSERT pool + items | 6+ items · `sourceVersion` 记录 |
| **J3-5-3** | 跑 [r7g §5](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md) per-viewer SQL | 全 `gateStatus=OK` |
| **J3-5-4** | closeout 记录 pool 来源 | r7j4 |

---

## 10. Step 6 — Batch match / M6 对照

| 要求 | 说明 |
|------|------|
| 入口 | dev/staging **既有 batch match / worker** |
| `MatchResult` | **仅** worker 自然生成 |
| 禁止 | **手写** `MatchResult` / `matchInsights` / `finalScore` |
| 禁止 | **改** worker 排序 |
| **M6** | 若产生 `matchInsights.rrmV2Top2Selector` → r6b `--compareM6=true` |
| M6 缺失 | 记 **`comparison unavailable`** · 不阻塞 r3b→r5b |

**验证：** 重跑 [r7g §4](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md) → `hasMatchResult` / `hasM6RrmTop2`。

**closeout 记录：** `viewerUserId` · batch 时间 · 最新 `match_results.id`（若有）。

---

## 11. Step 7 — Readiness verification

数据准备完成后（**r7j4 执行轮**）**必须**重跑 **[r7g §4](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md)** cohort readiness SQL。

**保存：** `tmp/p76-r7j3-cohort-readiness-after.sql`（从 r7g §4 复制）

**执行：**

```powershell
Get-Content -Raw tmp/p76-r7j3-cohort-readiness-after.sql | docker exec -i peima-postgres psql -U user -d peima
```

**计数：** `tmp/p76-r7j3-readiness-count.sql`（`WHERE "recommendedForRouteC" = true`）

### 11.1 PASS

| # | 条件 |
|---|------|
| 1 | `recommendedForRouteC=true` **≥ 5** |
| 2 | 推荐 **≥ 10** |
| 3 | 每 viewer `candidatePoolCount` **≥ 6** |
| 4 | `candidateWithVisionCount` **≥ 6** |
| 5 | `candidateWith20DProfileCount` **≥ 6** |
| 6 | `candidateWithPreferenceCount` **≥ 6** |
| 7 | `seedDependent=false` |
| 8 | `syntheticPool=false` |

### 11.2 FAIL

| 现象 | 动作 |
|------|------|
| `< 5` clean | 回到 §4–§9 扩 cohort |
| 仅 synthetic pool 可凑数 | **BLOCK** · 不得冒充 clean |
| 需手写 MatchResult | **BLOCK** |

---

## 12. Step 8 — Route C audit execution

当 §11 **PASS** 后，对每个 `recommendedForRouteC=true` viewer 按 **[r7g §7–§8](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md)** 执行：

| CLI | 产物 |
|-----|------|
| r3b PhotoVisual | `artifacts/p76/r7g/<viewerUserId>/r3b.json` |
| r4b 20D bidirectional ranking | `artifacts/p76/r7g/<viewerUserId>/r4b.json` |
| r5b RRM Top2 selector | `artifacts/p76/r7g/<viewerUserId>/r5b.json` |
| r6b end-to-end old-new | `artifacts/p76/r7g/<viewerUserId>/r6b.json` |

**全局前置：**

```powershell
cd apps/api
pnpm exec nest build
New-Item -ItemType Directory -Force -Path artifacts/p76/r7g
```

**r3b 模板：**

```powershell
cmd /c "cd /d c:\Users\34713\Desktop\peima\apps\api && set DATABASE_URL=postgresql://user:password@localhost:5432/peima && node dist/dev-cli/p76-r3b-photovisual-pool-shadow-audit-runner.js --viewerUserId=<V> --sourcePoolType=onboarding_gated_cohort --limit=12 --selectionLimit=6 --dryRun=true > c:\Users\34713\Desktop\peima\artifacts\p76\r7g\<V>\r3b.json 2>&1"
```

**r4b / r5b / r6b：** 见 [r7g §8.4–§8.6](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md)（从 r3b `selectedCandidateIds` → r4b top6 → r5b top2 → r6b stage paths）。

**验收（每 viewer）：** `r3b` 六人选中 · `r6b` 全 **`applied*=false`** · `finalShadow.applied=false` · 记录 `differenceStage` / legacy / M6。

**PM evidence：** [r7g §9](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md) → 汇总 **P7.6-r7k**。

---

## 13. Rollback / Safety

执行阶段（**r7j4 · 非本 runbook 定稿轮**）必须记录：

| # | 回滚项 | 要求 |
|---|--------|------|
| 1 | **新增 users** | 记录 userId 列表 · `DELETE` 顺序（先子表后 `users`） |
| 2 | **新增 images** | backup 或按 userId 删除 `user_images` |
| 3 | **vision write** | `p76_r7j3_user_images_vision_backup` 或 `detectionScoreJson` 快照恢复 |
| 4 | **user_profile / user_preferences** | backup 表 · 仅回滚 r7j3 写入行 |
| 5 | **staging pool** | `DELETE` pool_items → pool（见 [r7c3 §7](./P7.6-r7c3-fixture-preparation-execution-runbook.md)） |
| 6 | **MatchResult** | 确认**未**手工 `INSERT/UPDATE` `match_results` |
| 7 | **finalScore** | 确认**未**改 `finalScore` |
| 8 | **apply** | shadow CLI **`applied*=false`** |

**建议 backup 表命名：**

- `p76_r7j3_users_backup`
- `p76_r7j3_user_images_vision_backup`
- `p76_r7j3_user_profile_backup`
- `p76_r7j3_user_preferences_backup`
- `p76_r7j3_onboarding_pool_backup`
- `p76_r7j3_pool_items_backup`

**确认 SQL（只读 · 执行后）：**

```sql
-- 确认无手写 MatchResult（示例：仅查 r7j3  cohort viewer 范围）
SELECT COUNT(*) FROM match_results mr
WHERE mr."userId" IN (/* r7j3 viewer ids */)
  AND mr."createdAt" > /* r7j4 execution start */;
-- closeout 须说明 MatchResult 均来自 worker
```

---

## 14. PASS / BLOCK

### 14.1 r7j3 **runbook** PASS（本文档）

| # | 标准 | 状态 |
|---|------|------|
| 1 | 执行步骤 Step 1–8 明确 | **PASS** |
| 2 | §4 cohort 输入表明确 | **PASS** |
| 3 | §11 verification 明确 | **PASS** |
| 4 | §13 rollback 明确 | **PASS** |
| 5 | 不污染主链 · r8 仍须 PM signoff | **PASS** |

**r7j3 runbook 文档：** **PASS**（定稿）；**cohort 构建未执行**。

### 14.2 r7j3 **execution** BLOCK（当前）

| 条件 | 状态 |
|------|------|
| `recommendedForRouteC < 5` | **BLOCK**（当前 0） |
| 需手写 MatchResult / finalScore | **BLOCK** |
| 需改 worker | **BLOCK** |
| 需绕过 gender / preference gate | **BLOCK** |
| 无法形成 meaningful old-new | 降级 PM 证据 · 不冒充 PASS |

---

## 15. 下一步

| 顺序 | 文档 / 动作 |
|------|-------------|
| 1 | **P7.6-r7j4** — Route C-B staging cohort **execution closeout** |
| 2 | **实际执行**数据导入 / 构建（staging **允许写 DB**） |
| 3 | 重跑 **r7g §4** readiness SQL |
| 4 | 若 `recommendedForRouteC ≥ 5` → **r7g** Route C audit（§12） |
| 5 | **P7.6-r7k** — Route C audit closeout |
| 6 | PM **`PASS_TO_R8_ALLOWLIST_DESIGN`** → **r8** allowlist-only apply design |

**PM 提醒：** C-B cohort 文档标记 **`stagingOnly=true`** — 非 production 全量 rollout 证据。

---

## 16. 边界确认

| 项 | 本轮 r7j3 |
|----|-----------|
| 改代码 | **否** |
| 写 DB | **否** |
| 执行 seed | **否** |
| 导入 staging user | **否**（r7j4） |
| 写 `MatchResult` | **否** |
| 改 `finalScore` | **否** |
| 改 worker | **否** |
| `APPLY_TO_POOL` / percent | **否** |
| 进入 r8 | **否** |

---

## 参考

| 文档 | 用途 |
|------|------|
| [P7.6-r7j2](./P7.6-r7j2-route-cb-staging-cohort-import-build-plan.md) | C-B plan · 目标 · 禁止项 |
| [P7.6-r7j1](./P7.6-r7j1-route-c-data-gap-inventory-closeout.md) | C_A_NOT_FEASIBLE · 四人 inventory |
| [P7.6-r7j](./P7.6-r7j-route-c-data-preparation-execution-runbook.md) | §4–§5 SQL · 通用步骤 |
| [P7.6-r7g](./P7.6-r7g-route-c-staging-cohort-execution-runbook.md) | §4 readiness · §5–§8 audit CLI |
| [P7.5-r7-d](./P7.5-r7-d-staging-cloud-vision-50-images-runbook.md) | vision backfill 批量 |
| [P7.6-r7c3](./P7.6-r7c3-fixture-preparation-execution-runbook.md) | backup / rollback 模式 |
| [P7.6-r7c5](./P7.6-r7c5-fixture-preparation-route-b-closeout.md) | synthetic pool 反例 |
| [P7.6 plan](./P7.6-bidirectional-mutual-matching-system-plan.md) | r7j4 / r7k / r8 |

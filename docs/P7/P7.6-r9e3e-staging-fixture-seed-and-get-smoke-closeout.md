# P7.6-r9e3e — Staging Fixture Seed + GET Smoke Closeout

> **状态**：**P7.6-r9e3e staging fixture seed + GET smoke matrix**  
> **结论**：**`PASS_STAGING_FIXTURE_GET_SMOKE`**  
> **前置**：[P7.6-r9e3d](./P7.6-r9e3d-pm-ops-staging-readonly-smoke-review.md) **`PASS_TO_R9E3E_STAGING_FIXTURE_SEED_AND_GET_SMOKE`** · [P7.6-r9e5](./P7.6-r9e5-staging-environment-bootstrap-execution-closeout.md)  
> **Artifacts**：[artifacts/p76/r9e3e/](../../artifacts/p76/r9e3e/) · Runner：`tmp/p76-r9e3e-staging-fixture-seed.mjs` · `tmp/p76-r9e3e-staging-get-smoke.mjs` · `tmp/run-r9e3e-staging-get-smoke.ps1`  
> **性质**：staging synthetic fixture + remote GET only · **非** customer production · **非** r9e3 complete · **非** percent rollout

---

## 1. 一句话结论

**production-like staging** 上已完成 synthetic fixture seed（6 cases）与两阶段 P7.6 GET smoke（Render Phase A enabled + Phase B disabled rollback）；**4** active allowlist sidecar overlay、**rolledBack** / **non_allowlist** fallback、disabled 全量 rollback 均通过；violation SQL **0**；`finalScore` / `candidateUserId` / worker / percent **未变** → **`PASS_STAGING_FIXTURE_GET_SMOKE`**。**非** `BLOCK_R9E3E` · **非** r9e3 Gate 12 complete。

---

## 2. Fixture seed summary

| Field | Value |
|-------|-------|
| Status | **`FIXTURE_SEED_PASS`** |
| Fixture tag | `p7.6-r9e3e-staging-fixture-get-smoke` |
| `sourceVersion` | `p7.6-r7j3-staging-cohort-v1` |
| Cases | **6**（**4** allowlist active · **1** rolled back · **1** non-allowlist） |
| `finalScore` | **0.75**（全部 fixture rows） |
| DB host (redacted) | `aws-1-ap-southeast-2.pooler.supabase.com:5432` |
| Artifact | [staging-fixture-seed-summary.json](../../artifacts/p76/r9e3e/staging-fixture-seed-summary.json) |
| Ops manifest (gitignored) | `tmp/p76-r9e3e-fixture-manifest.json` |

| caseLabel | allowlist | expected when read path enabled |
|-----------|-----------|----------------------------------|
| `case_allowlist_active_1` … `_4` | active sidecar row | `p76_allowlist_sidecar_readonly` · display = sidecar candidate |
| `case_rolled_back` | `rolledBack=true` | baseline · `fallbackReason=rolled_back` |
| `case_non_allowlist` | no row / not in `VIEWER_IDS` | baseline · `fallbackReason=not_allowlisted` |

**Safety flags：** `appliedToMatchResult=false` · `appliedToFinalScore=false` · `appliedToWorkerRanking=false` · `appliedToDisplay=false` · signoffs **approved** · `percent=0`。

---

## 3. Render env toggle summary

两阶段均在 Render **`peima-api-staging`** 人工配置并 redeploy（见 [staging-env-toggle-notes.md](../../artifacts/p76/r9e3e/staging-env-toggle-notes.md)，**OPS ONLY**）。

| Phase | Env | Status |
|-------|-----|--------|
| **A — enable** | `PEIMA_P76_READ_PATH_ENABLED=1` · `VIEWER_IDS`（4 active + 1 rolledBack） · `SOURCE_VERSION=p7.6-r7j3-staging-cohort-v1` · signoff/fallback flags · `PRODUCTION_PERCENT=0` · `PRODUCTION_PERCENT_ENABLED=0` · `KILL_SWITCH=0` | **applied** |
| **B — restore** | `READ_PATH_ENABLED=0` · `VIEWER_IDS=` · `PRODUCTION_PERCENT=0` · `KILL_SWITCH=1` | **applied** |

---

## 4. GET smoke matrix

**API：** `https://peima-api-staging.onrender.com` · `GET /matching/result/:viewerUserId` · JWT Bearer（**未**写入 artifact）

**Runner：** `PEIMA_R9E3E_SMOKE_PHASE=enabled` 与 `=disabled` 分两次执行（`tmp/p76-r9e3e-staging-get-smoke.mjs`）。

### Phase A — `read_path_enabled`（**PASS** · passCount=**6** · failCount=**0**）

| caseLabel | HTTP | outcome | displaySourceType | fallback / meta |
|-----------|------|---------|-------------------|-----------------|
| `case_allowlist_active_1` … `_4` | 200 | **pass** | `p76_allowlist_sidecar_readonly` | `p76ReadPathMeta.enabled=true` · `fallbackUsed=false` · displayCandidate = sidecar · `candidateUserId` / `finalScore=0.75` unchanged |
| `case_rolled_back` | 200 | **pass** | **not** sidecar | `fallbackReason=rolled_back`（或等价）· baseline safe |
| `case_non_allowlist` | 200 | **pass** | **not** sidecar | `fallbackReason=not_allowlisted` · baseline safe |

无 **401 / 403 / 404 / 5xx**。

**证据说明：** Phase A 在 credentialed PowerShell 下以 `PEIMA_R9E3E_SMOKE_PHASE=enabled` 运行；终端输出 **passCount=6 failCount=0**。随后 Phase B 运行会**覆盖** [staging-get-smoke-results.json](../../artifacts/p76/r9e3e/staging-get-smoke-results.json)（`smokePhase=disabled`），故 machine-readable JSON **仅保留 disabled 切片**；enabled 矩阵以本 closeout + Phase A 终端记录为准。[staging-get-smoke-summary.md](../../artifacts/p76/r9e3e/staging-get-smoke-summary.md) 的 **Read path enabled** 小节因最后一次写盘为空表——**不代表 enabled 未执行**。

### Phase B — `read_path_disabled`（**PASS** · 6/6）

| caseLabel | HTTP | outcome | displaySourceType | fallbackReason |
|-----------|------|---------|-------------------|----------------|
| 全部 6 cases | 200 | **pass** | `match_result_original` | `env_disabled` |

Artifact：[staging-get-smoke-results.json](../../artifacts/p76/r9e3e/staging-get-smoke-results.json) · `conclusion.status=PASS_STAGING_FIXTURE_GET_SMOKE` · [staging-get-smoke-summary.md](../../artifacts/p76/r9e3e/staging-get-smoke-summary.md)

---

## 5. Disabled rollback check

| Check | Result |
|-------|--------|
| `READ_PATH_ENABLED=0` · `VIEWER_IDS` cleared · `KILL_SWITCH=1` | **yes** |
| No `p76_allowlist_sidecar_readonly` | **pass** (6/6) |
| `fallbackReason` | `env_disabled` |
| `candidateUserId` / `finalScore` | **unchanged** |
| Artifact | [staging-rollback-to-disabled-summary.json](../../artifacts/p76/r9e3e/staging-rollback-to-disabled-summary.json) **`pass: true`** |

---

## 6. Violation SQL

| Check | before | after |
|-------|--------|-------|
| allowlist combined violations | **0** | **0** |
| canonical combined violations | **0** | **0** |
| `appliedToMatchResult` / `appliedToFinalScore` / `appliedToWorkerRanking` / `appliedToDisplay` true | **0** | **0** |
| percent enabled | **0** | **0** |
| Fixture-tagged `match_results` | **6** | **6** |
| `readonlyUrlSource` (Phase B artifact) | `PEIMA_READONLY_DATABASE_URL` | — |

---

## 7. Mutation safety

| Guard | Result |
|-------|--------|
| Staging only | **yes** |
| Customer production | **no** |
| Worker triggered | **no** |
| MatchResult / `finalScore` mutation (smoke) | **no** |
| Percent > 0 / production rollout | **no** |
| Canonical promotion | **no** |
| Legacy photo matching removal | **no** |
| Smoke runner DB writes | **no**（seed 已在前序完成） |

---

## 8. Redaction policy

- Artifacts：**仅** `caseLabel` · redacted host · HTTP / display / fallback / violation counts  
- **不记录：** token · full `DATABASE_URL` · `JWT_SECRET` · 真实 `viewerUserId`（ops：`staging-env-toggle-notes.md` · gitignored manifest）  
- **`BLOCK_R9E3E`：** 未触发

---

## 9. PASS / BLOCK

| Decision | Applies |
|----------|---------|
| **`FIXTURE_SEED_PASS`** | **Yes** |
| **`PASS_STAGING_FIXTURE_GET_SMOKE`** | **Yes** |
| **`NEED_MORE_STAGING_FIXTURE_EVIDENCE`** | **No** |
| **`BLOCK_R9E3E`** | **No** |

---

## 10. Remaining r9e3 blockers

- Grafana / monitoring import  
- **30–60 min** staging watch  
- Full rollback drill（若 PM/Ops 未将 r9e3e disabled 切片视为足够）  
- Customer prod evidence（**仅当** Gate 12 最终要求 customer production）  
- Gate 12：**`NEED_MORE_PROD_REVERIFY`**（unchanged）

---

## 11. Next step

**P7.6-r9e3f** — [staging monitoring / Grafana / watch](./P7.6-r9e3f-staging-monitoring-grafana-watch-closeout.md) **`PASS_WITH_GRAFANA_IMPORT_PENDING`** → Ops Grafana import · 更新 [P7.6-r9e3](./P7.6-r9e3-real-prod-ops-execution-closeout.md)（**仍不得**提前标 r9e3 complete）。

---

## Related

- [P7.6-r9e3c](./P7.6-r9e3c-staging-prod-like-readonly-reverify-smoke-closeout.md)  
- [P7.6-r9e3d](./P7.6-r9e3d-pm-ops-staging-readonly-smoke-review.md)  
- [P7.6-r9e3](./P7.6-r9e3-real-prod-ops-execution-closeout.md)

# P7.10-r5a — Legacy Feature Growth Freeze Closeout

## 1. Status

```text
P7_10_R5A_LEGACY_FEATURE_GROWTH_FREEZE_COMPLETE
```

- Code change: **yes** — freeze / deprecation UI + comments + script warnings only
- No worker disable
- No PreviewPool deletion
- No DB migration
- No API contract change
- No `resultState` change
- No canonical writer
- No legacy removal
- No production / percent rollout

**Bridge state:** first implementation step from `active_legacy_bridge` → `frozen_legacy_bridge` (see [P7.10-r5](./P7.10-r5-legacy-pool-bridge-sunset-plan.md)).

---

## 2. What changed

### Frontend entry points

| File | Change |
|------|--------|
| `apps/web/src/components/legacy/LegacyPhotoPreviewFreezeBanner.jsx` | Shared freeze banner (EN + ZH) |
| `apps/web/src/pages/OnboardingPhotoPreviewPage.jsx` | Banner; **regenerate** disabled; first-time generate + continue unchanged |
| `apps/web/src/pages/PreviewPoolPage.jsx` | Banner; **generate** disabled + guard; load / admin orchestration unchanged |
| `apps/web/src/pages/OnboardingPhotoPreferencePage.jsx` | Freeze note on save → generate path |
| `apps/web/src/pages/UserImagesPage.jsx` | Link relabeled “Legacy 预览池（已冻结）” |
| `apps/web/src/router/index.jsx` | Nav copy marks legacy / frozen; routes **not** removed |

### API / scripts warnings

| File | Change |
|------|--------|
| `preview-pool.controller.ts` | `frozen_legacy_bridge` deprecation comment |
| `preview-pool.service.ts` | Same |
| `onboarding-photo-preview-pool.service.ts` | Same |
| `onboarding.controller.ts` | JSDoc on `photo-preview-pool/generate` |
| `p75-r4-m-preview-pool-funnel-runner.ts` | Startup `console.warn` |
| `p75-r4-o2-preview-pool-active-audit-runner.ts` | Startup `console.warn` |

### Docs

| File | Change |
|------|--------|
| `docs/P7/P7.10-r5a-legacy-feature-growth-freeze-closeout.md` | This closeout |

---

## 3. Freeze behavior

| Surface | Frozen / hidden | Still allowed |
|---------|-----------------|---------------|
| `/preview-pool` UI generate | **Disabled** + warning | GET latest, refresh, admin orchestration MVP |
| `/onboarding/photo-preview` regenerate | **Disabled** | View pool, first generate, acknowledge → questionnaire |
| Home / index nav | Copy: legacy · 冻结 | Direct URL still works |
| Onboarding preference submit | Note only | Still calls `postOnboardingPhotoPreviewPoolGenerate` (onboarding completion) |
| API `POST preview-pool/generate` | Comment only | **Unchanged** behavior |
| API onboarding photo-preview-pool | Comment only | **Unchanged** |
| `p75:r4-m` / `p75:r4-o2` CLIs | **Warn** on start | Same exit codes / DB writes |
| Worker `batch-match` + PreviewPool | **Untouched** | Still active until P7.10-r7+ cutover gates |

**Worker (record only):** `apps/worker` still reads active `PreviewPool` for `MatchResult` writes. Disable belongs to **P7.10-r7** after canonical writer shadow + ops gates — **not** this round.

---

## 4. Compatibility

| Area | Status |
|------|--------|
| `MatchResult` schema / writes | Unchanged |
| `finalScore` | Unchanged |
| `GET /matching/result` | Unchanged |
| `resultState` / P7.10-r4a flag | Unchanged |
| `FinalMatchPage` | Unchanged (incl. rematch `generatePreviewPool` fallback) |
| `MatchingWaitingPage` | Unchanged |
| `PreviewPool` / `OnboardingPhotoPreviewPool` tables | Unchanged |
| Historical rows | Readable via existing GET/UI |

---

## 5. Verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api run build` | **PASS** (exit 0) |
| `pnpm --filter @peima/web run build` | **PASS** (exit 0) |

---

## 6. PASS / BLOCK

**PASS**

- Legacy feature growth frozen (UI + warnings)
- Old entry points deprecated / generate CTAs disabled where safe
- Scripts warn on run
- Worker untouched
- Builds pass

**BLOCK**

- Canonical writer not implemented
- Old writer not disabled
- PreviewPool not deleted
- Legacy removal blocked
- Production / percent blocked

---

## 7. Next recommended step

**Option A — P7.7-r1 ResultState + safe fallback negative regression pack** (**recommended**)

- Stabilize read path + `resultState` before writer shadow
- Cover: missing sidecar, stale, rolledBack, violation, candidate missing, env disabled, `no_result`, `matching_pending`

**Option B — P7.10-r6 Canonical writer shadow implementation**

- Higher risk; do after P7.7-r1

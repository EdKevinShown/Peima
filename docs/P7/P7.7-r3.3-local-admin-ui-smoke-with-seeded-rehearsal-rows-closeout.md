# P7.7-r3.3 — Local Admin UI Smoke with Seeded Rehearsal Rows Closeout

## 1. Status

```text
P7_7_R3_3_LOCAL_ADMIN_UI_SMOKE_PARTIAL_BUILD_AND_DATA_COMPLETE
```

说明：

- Local smoke only
- DB writes only to `p76_canonical_writer_rehearsal_meta`
- Cleanup by `auditRunId` completed (`finalRowsForAuditRunId=0`)
- Admin **API** smoke **pass** (list / aggregate / detail)
- Admin **UI** browser smoke **deferred**; web build + route + static no-write verification **pass**
- No MatchResult write
- No matchInsights write
- No worker integration
- No GET behavior change
- No canonical production write
- No legacy removal
- No production / percent rollout

## 2. Commands run

| Command | Result |
|---------|--------|
| `git status --short` (pre-flight) | clean |
| `Test-Path .env` | true |
| `Test-Path artifacts/p76/r6c2a/canonical-writer-shadow-audit-targeted.json` | true |
| `pnpm --filter @peima/api run build` | PASS |
| `pnpm --filter @peima/web run build` | PASS |
| `pnpm --filter @peima/api test -- p77-r3-1-admin-readonly-rehearsal-api` | PASS (10) |
| `pnpm --filter @peima/web test` | PASS (12) |
| r6f3 insert `--cleanup=false` | PASS (`insertedCount=5`) |
| Admin API HTTP smoke (local `:3000`, flag on) | PASS |
| Prisma `deleteMany` by `auditRunId` | PASS (`cleanupDeletedCount=5`) |

No `DATABASE_URL` or JWT values recorded in this document.

## 3. Seeded rows

| Field | Value |
|-------|-------|
| auditRunId | `p77-r3-3-admin-ui-smoke-001` |
| insertedCount | 5 |
| duplicateCount | 0 |
| appliedToMatchResultTrueCount | 0 |
| environment | dev |
| insertVerification | pass |
| cleanupDeletedCount | 5 |
| finalRowsForAuditRunId | 0 |

Evidence: [admin-rehearsal-ui-smoke-summary.json](../../artifacts/p76/r3_3/admin-rehearsal-ui-smoke-summary.json)

## 4. Admin API smoke

| Endpoint | Result |
|----------|--------|
| `GET /admin/p76/canonical-rehearsal?auditRunId=...` | **pass** — HTTP 200, 5 items, `featureEnabled=true` |
| `GET /admin/p76/canonical-rehearsal/aggregate?auditRunId=...` | **pass** — HTTP 200, `totalVisible=5` |
| `GET /admin/p76/canonical-rehearsal/:id` | **pass** — HTTP 200, row + `derived` safety flags |

Auth: Bearer JWT signed for `PEIMA_ADMIN_USER_IDS[0]` (permission `VIEW_P76_ALLOWLIST_APPLY_META`). Server env: `PEIMA_P76_REHEARSAL_ADMIN_ENABLED=1`.

## 5. Admin UI smoke

| Check | Result |
|-------|--------|
| Route `/admin/p76/canonical-rehearsal` | **pass** (registered in `apps/web/src/router/index.jsx`) |
| `pnpm --filter @peima/web run build` | **pass** |
| Sticky `SHADOW REHEARSAL ONLY` banner (source) | **pass** (static) |
| No Apply / Promote / Delete / worker controls (source grep) | **pass** (static) |
| Manual browser: rows + detail drawer + export | **deferred** |

**Manual UI steps (for follow-up while rows exist):**

1. `PEIMA_P76_REHEARSAL_ADMIN_ENABLED=1` on API; `pnpm --filter @peima/api start:dev`
2. `pnpm --filter @peima/web dev` → login as admin user in `PEIMA_ADMIN_USER_IDS`
3. Open `/admin/p76/canonical-rehearsal`
4. Filter `auditRunId=p77-r3-3-admin-ui-smoke-001` (re-seed with r6f3 if rows already cleaned)
5. Confirm summary cards, table, detail drawer, Export visible JSON; confirm no write buttons

## 6. Cleanup verification

- Rows for `p77-r3-3-admin-ui-smoke-001` deleted via `p76CanonicalWriterRehearsalMeta.deleteMany({ where: { auditRunId } })`
- `finalRowsForAuditRunId = 0`
- No other `auditRunId` rows touched
- No `MatchResult` / allowlist sidecar changes

## 7. Safety

- No Apply endpoint used
- No Admin write API
- No MatchResult mutation
- No matchInsights mutation
- No worker trigger
- No GET `/matching/result` change
- Insert + delete scoped to rehearsal table + single `auditRunId`

## 8. Tests / verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api run build` | PASS |
| `pnpm --filter @peima/web run build` | PASS |
| `pnpm --filter @peima/api test -- p77-r3-1-admin-readonly-rehearsal-api` | PASS |
| `pnpm --filter @peima/web test` | PASS |

## 9. PASS / BLOCK

**PASS:**

- Local rehearsal rows inserted and verified
- Admin API list / aggregate / detail smoke pass
- Cleanup complete (`finalRowsForAuditRunId=0`)
- Web build + route + static UI safety checks pass
- Regression tests pass

**BLOCK (unchanged):**

- Apply endpoint blocked
- Canonical production write blocked
- Worker integration blocked
- Legacy removal blocked
- Production / percent blocked

## 10. Next recommended step

**Option A — Add dedicated RBAC `VIEW_P76_CANONICAL_REHEARSAL`**

- Decouple from `VIEW_P76_ALLOWLIST_APPLY_META` in shared + API + web

**Option B — P7.10-r6f4 stable local rehearsal seed for Ops review**

- Insert without cleanup; document cleanup command for reviewers

**Option C — P7.10-r7 canonical write readiness design (docs-only)**

- No implementation in this lane

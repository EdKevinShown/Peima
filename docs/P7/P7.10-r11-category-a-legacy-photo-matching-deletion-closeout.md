# P7.10-r11 — Category A legacy photo matching deletion closeout

**Milestone:** P7.10-r11  
**Date:** 2026-05-19  
**Status:** `PASS_R11_CATEGORY_A_LEGACY_PHOTO_MATCHING_DELETION`

---

## 1. One-line conclusion

Category A legacy **photo matching writer / generation** code removed: PreviewPool 6-slot generate, onboarding photo-preview-pool API/UI, P7.5 pool apply dev-cli, and related tests. Core matching read path, canonical Apply/Rollback, photo upload/review/vision, and PreviewPool **read-only** admin paths are preserved. r8i human signoff remains **pending**.

---

## 2. Files changed

### Deleted

**API — preview pool writer**

- `apps/api/src/modules/preview-pool/preview-pool-layered-selection.ts`
- `apps/api/src/modules/preview-pool/visual-signal-enhance-stub.ts`
- `apps/api/src/modules/preview-pool/dto/generate-preview-pool.dto.ts`

**API — onboarding photo preview pool**

- `apps/api/src/modules/onboarding/onboarding-photo-preview-pool.service.ts`
- `apps/api/src/modules/onboarding/onboarding-photo-preview-pool-funnel.audit.ts`
- `apps/api/src/modules/onboarding/onboarding-photo-preview-pool-funnel.audit.service.ts`
- `apps/api/src/modules/onboarding/onboarding-photo-preview-pool-active-audit.ts`
- `apps/api/src/modules/onboarding/onboarding-photo-preview-pool-active-audit.service.ts`

**API — P7.5 Category A dev-cli**

- `apps/api/src/dev-cli/p75-r4-m-preview-pool-funnel-runner*`
- `apps/api/src/dev-cli/p75-r4-o2-preview-pool-active-audit-*`
- `apps/api/src/dev-cli/p75-r5-b2-apply-dry-run-signoff-*`
- `apps/api/src/dev-cli/p75-r5-c2-apply-writer-signoff-*`
- `apps/api/src/dev-cli/p75-r4-e-shadow-sample-harness-runner.ts`

**API — tests**

- `onboarding-photo-preview-pool*.spec.ts` (except `onboarding-photo-preview-display-image-key.spec.ts`)
- `preview-pool-layered-selection.spec.ts`, `visual-signal-enhance-stub.spec.ts`
- `p75-r4-o2-*`, `p75-r5-*` specs

**Web**

- `apps/web/src/pages/PreviewPoolPage.jsx`
- `apps/web/src/pages/OnboardingPhotoPreviewPage.jsx`

### Modified

| File | Change |
|------|--------|
| `preview-pool.service.ts` | Read-only: `findLatestActiveForUser`, shortlist contract |
| `preview-pool.controller.ts` | Removed `POST /generate` |
| `batch-match.processor.ts` | Early queue block when r9 writer gate closed (skip PreviewPool work) |
| `onboarding.controller.ts` | Removed photo-preview-pool routes |
| `onboarding.module.ts` | Removed pool service provider |
| `onboarding.service.ts` | Skip `photo_preview` → questionnaire |
| `onboarding-preview-candidate-eligibility.ts` | **new** — `isEligiblePreviewCandidate` for P7.6 adapter |
| `p76-photovisual-first-pool-db-adapter.ts` | Import eligibility helper |
| `apps/web` router, onboarding, preference/upload/login flows | Remove pool generate; legacy URLs redirect |
| `FinalMatchPage.jsx` | Rematch skips removed `generatePreviewPool` |
| `apps/api/package.json` | Removed deleted p75 scripts |

### Preserved / not touched

- `matching-result-display.ts`, `matching.service.ts`, `FinalMatchPage` main display
- `p710-r10-safe-fallback-final-policy.ts`, canonical Apply/Rollback (r8)
- `preview-pool-shortlist-contract.v0.ts`, post-pool-deep-screen, matching-decision-comparison
- Prisma models/tables (historical data)
- `packages/shared/matching/preference-score.ts` (`computeStyleScore` for vision shadow)

---

## 3. Deletion audit summary

See [audit](./P7.10-r11-category-a-legacy-photo-matching-deletion-audit.md).

Each deletion is safe because:

- **r9** default-shutdown blocks formal `MatchResult` create from legacy worker
- **r10** confirms GET `/matching/result` does not read/write PreviewPool as writer fallback
- No dependency from canonical sidecar, Apply/Rollback, or photo upload/review/cloud vision enrich

---

## 4. Preserve proof

| Asset | Status |
|-------|--------|
| MatchResult | ✓ |
| GET `/matching/result` | ✓ |
| FinalMatchPage (main chain) | ✓ |
| `resolveMatchResultDisplay` | ✓ |
| Canonical Apply/Rollback | ✓ |
| Photo upload / review / Cloud Vision | ✓ |
| Safe fallback final (r10) | ✓ |

---

## 5. Tests / build

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api test -- p710-r10-safe-fallback-final` | **PASS** (12) |
| `pnpm --filter @peima/api test -- matching-result-display` | **PASS** (45) |
| `pnpm --filter @peima/worker test -- p710-r9-old-photo-matching-writer-shutdown` | **PASS** (7) |
| `pnpm --filter @peima/api test -- p710-r8f` | **PASS** (34) |
| `pnpm --filter @peima/api test -- p710-r8e` | **PASS** (33) |
| `pnpm --filter @peima/api test -- p710-r8d` | **PASS** (20) |
| `pnpm --filter @peima/api run build` | **PASS** |
| `pnpm --filter @peima/worker run build` | **PASS** |
| `pnpm --filter @peima/web run build` | **PASS** |

---

## 6. Safety proof

| Check | Status |
|-------|--------|
| Old writer not re-enabled (default r9 env) | ✓ |
| No worker trigger added | ✓ |
| No percent mutation | ✓ |
| No production rollout | ✓ |
| No fake PM/Ops/Eng signoff | ✓ |
| No secrets in diff | ✓ (scan clean) |

---

## 7. Remaining legacy (non-writer / read-only)

| Item | Role |
|------|------|
| `PreviewPool` DB rows + `GET /preview-pool/user/:id/latest` | Historical / admin read |
| `preview-pool-shortlist-contract.v0.ts` | AI simulation / deep-screen binding |
| `batch-match.processor.ts` scoring loop | Inactive unless dev rollback env enables writer |
| `worker/matching-score.ts` style term | Used only when writer path runs |
| Prisma `OnboardingPhotoPreviewPool` tables | Historical; no generate API |

These are **not** formal MatchResult writers under default configuration.

---

## 8. PASS / BLOCK

**PASS:** Category A writer/generation removed; fallback/display/canonical preserved; regression PASS.

**BLOCK:** Would break GET `/matching/result`, FinalMatchPage, canonical Apply/Rollback, or photo upload/review; re-enable production writer; delete protected core.

---

## 9. Next step

**P7.10-r11b — full regression after legacy deletion**

Human r8i signoff remains independent: `READY_FOR_R9_WRITER_SHUTDOWN_REVIEW_PENDING_HUMAN_SIGNOFF`.

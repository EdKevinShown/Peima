# P7.10-r3f3 — Canonical Match Result Sidecar Writer Dev CLI + Local Insert Smoke Closeout

> **前置**：[P7.10-r3f2](./P7.10-r3f2-canonical-match-result-sidecar-writer-insert-only-implementation-closeout.md) · [P7.10-r3f1](./P7.10-r3f1-canonical-match-result-sidecar-writer-dry-run-implementation-closeout.md) · [P7.10-r3e](./P7.10-r3e-canonical-match-result-sidecar-migration-implementation-closeout.md)

---

## 1. Status

```text
P7_10_R3F3_CANONICAL_MATCH_RESULT_SIDECAR_WRITER_LOCAL_INSERT_SMOKE_COMPLETE
```

**说明：**

- Code change: **yes** — dev-cli runner + smoke artifacts only
- Allows local `p76CanonicalMatchResultMeta` create via r3f2 insert-only writer
- Local DB insert smoke **PASS** (dev)
- No `MatchResult` write
- No `matchInsights` write
- No worker / GET / frontend change
- No Admin Apply / production / percent

---

## 2. What changed

| Path | Change |
|------|--------|
| `apps/api/src/dev-cli/p710-r3f3-canonical-sidecar-writer-cli-args.ts` | CLI arg parser |
| `apps/api/src/dev-cli/p710-r3f3-canonical-sidecar-writer-runner.ts` | Nest bootstrap + dry-run/insert smoke |
| `apps/api/src/dev-cli/p710-r3f3-canonical-sidecar-writer-runner.module.ts` | `PrismaModule` only |
| `apps/api/src/modules/matching/p76-canonical-match-result-sidecar-writer-smoke-artifact.ts` | JSON/MD summary + pass evaluator |
| `apps/api/test/p710-r3f3-canonical-sidecar-writer-runner.spec.ts` | 10 unit tests |
| `apps/api/package.json` | `p710:r3f3:canonical-sidecar-writer-smoke` script |
| `artifacts/p76/r3f3/` | Smoke output directory |
| `docs/P7/P7.10-r3f3-canonical-match-result-sidecar-writer-local-insert-smoke-closeout.md` | This closeout |

---

## 3. CLI usage

From `apps/api` after `pnpm --filter @peima/api run build`:

**Dry-run (no DB write):**

```bash
pnpm run p710:r3f3:canonical-sidecar-writer-smoke
```

**Local insert smoke (dev DB):**

```bash
# repo root .env
$env:DOTENV_CONFIG_PATH="C:\Users\34713\Desktop\peima\.env"
node -r dotenv/config dist/dev-cli/p710-r3f3-canonical-sidecar-writer-runner.js --insert=true --auditRunId=r3f3-local-insert-smoke-001 --pretty
```

Or via package script (pass args after `--`):

```bash
pnpm run p710:r3f3:canonical-sidecar-writer-smoke -- --insert=true --auditRunId=r3f3-local-insert-smoke-001 --pretty
```

**Flags:** `--environment=dev|staging` · `--insert=true|false` · `--cleanup=true|false` (default true when insert) · `--verify=true|false` (default true when insert) · `--includeDuplicateProbe=true|false` · `--output` · `--outputMd` · `--pretty`

---

## 4. Local smoke result

**Run:** `r3f3-local-insert-smoke-001` on local dev DB (2026-05-19)

| Metric | Value |
|--------|-------|
| `insertedCount` | 2 |
| `duplicateCount` | 2 (duplicate probe re-insert) |
| `skippedCount` | 1 (ineligible synthetic row) |
| `verify.appliedToMatchResultTrueCount` | 0 |
| `verify.appliedToFinalScoreTrueCount` | 0 |
| `verify.appliedToWorkerRankingTrueCount` | 0 |
| `verify.promotionStatusNotPromotedCount` | 2 |
| `cleanup.deletedCount` | 2 |
| `cleanup.finalRowsForAuditRunId` | 0 |
| `pass` | true |

Synthetic viewers: `r3f3-viewer-1`, `r3f3-viewer-2` (no real user ids).

---

## 5. Safety

| Invariant | r3f3 |
|-----------|------|
| Only `p76_canonical_match_result_meta` touched | ✓ |
| `cleanup` via `deleteMany({ auditRunId })` only | ✓ |
| `MatchResult` untouched | ✓ |
| `matchInsights` untouched | ✓ |
| Worker / GET untouched | ✓ |
| No production / staging smoke in this slice | ✓ |

---

## 6. Tests / verification

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api test -- p710-r3f3` | **PASS** (10) |
| `pnpm --filter @peima/api test -- p710-r3f1 p710-r3f2 p710-r3f3` | **PASS** (42) |
| `pnpm --filter @peima/api run build` | **PASS** |
| Local insert smoke CLI | **PASS** |

**Artifacts:**

- `artifacts/p76/r3f3/canonical-sidecar-writer-smoke-summary.json`
- `artifacts/p76/r3f3/canonical-sidecar-writer-smoke-summary.md`

---

## 7. PASS / BLOCK

### PASS

- Dev-cli runner added
- Local insert smoke PASS
- Cleanup PASS (`finalRowsForAuditRunId=0`)
- `appliedTo*` DB true counts = 0
- Duplicate probe PASS

### BLOCK

- `MatchResult` write / promotion
- Admin Apply / rollback
- Worker / GET integration
- Production / percent rollout
- PreviewPool disable / legacy removal

---

## 8. Next recommended step

**Option A — P7.7-r4** canonical sidecar Admin read-only API design (review sidecar rows in Admin UI without writes).

**Option B — P7.10-r7b/r7c** promotion Apply design (still separate from this writer smoke).

**Option C — P7.10-r4** old photo writer disable plan.

**推荐：** 先做 **P7.7-r4**，让 sidecar rows 可在 Admin 只读审查。

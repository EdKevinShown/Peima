# P7.10-r8d — Canonical Apply Service Hard-disabled Closeout

> **状态**：P7.10-r8d canonical Apply service — **DONE**  
> **结论**：**`PASS_R8D_CANONICAL_APPLY_SERVICE_HARD_DISABLED`**  
> **前置**：[P7.10-r8c1](./P7.10-r8c1-local-migration-and-snapshot-persistence-smoke-closeout.md) · [P7.10-r8c](./P7.10-r8c-rollback-snapshot-persistence-implementation-closeout.md) · [P7.6-r9e3h](./P7.6-r9e3h-final-gate12-pm-ops-signoff.md)  
> **性质**：service + env + unit tests only · **无** HTTP `POST apply` · **无** UI Apply 按钮 · **默认 hard-disabled**

---

## 1. 一句话结论

**P7.10-r8d** 实现 `applyP76CanonicalSidecarToMatchResult` 受控 Apply 服务：默认 **hard-disabled**（任何调用返回 `blocked` 且 **零** DB 写入）；enabled 路径仅在 **mock Prisma 单测** 中验证事务更新逻辑。**未** 暴露 HTTP route · **未** 对真实 staging/production 执行 Apply。

```text
PASS_R8D_CANONICAL_APPLY_SERVICE_HARD_DISABLED
```

---

## 2. 修改文件列表

| 文件 | 说明 |
|------|------|
| `apps/api/src/modules/matching/p76-canonical-apply-env.ts` | Env reader（默认全 off） |
| `apps/api/src/modules/matching/p76-canonical-apply.types.ts` | 类型 / blocked reasons |
| `apps/api/src/modules/matching/p76-canonical-apply.service.ts` | `applyP76CanonicalSidecarToMatchResult` |
| `apps/api/test/p710-r8d-canonical-apply-service.spec.ts` | 20 unit tests（mock Prisma） |
| 本文档 | closeout |

**未新增：** Controller · Admin UI Apply 按钮 · worker 集成 · 真实 DB Apply smoke

---

## 3. Env gate 设计

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PEIMA_P710_R8_APPLY_EXECUTION_ENABLED` | `0` | 必须为 `1` 才进入读路径 |
| `PEIMA_P710_R8_APPLY_ALLOW_DB_WRITE` | `0` | 必须为 `1` 才允许 `$transaction` |
| `PEIMA_P710_R8_APPLY_ENVIRONMENT` | `dev` | 仅 `dev` / `staging` / `production-like-staging` |

**额外阻断：** `NODE_ENV=production` · `environment=production` · `PEIMA_P76_PRODUCTION_PERCENT>0` · `PEIMA_P76_PRODUCTION_PERCENT_ENABLED=1`

**Disabled 路径：** env 不满足 → **不调用** `prisma.findUnique` / `$transaction` / `update`

---

## 4. Service contract

**API：** `applyP76CanonicalSidecarToMatchResult(deps, input, options?)`

**返回：** `P76CanonicalApplyResultV1`

| 字段 | 说明 |
|------|------|
| `mode` | `blocked` \| `applied` |
| `applied` | boolean |
| `blockedReasons` | 见 types |
| `before` / `after` | MatchResult 状态快照 |
| `safety` | writesDb / writesMatchResult / … |
| `audit` | `requestedBy` · `appliedAt` · `auditLogWritten`（audit log TODO no-op） |

**Source：** `p76_canonical_apply_service` · `p7.10-r8d-canonical-apply-service-v1`

---

## 5. Gate / validation summary

| # | Gate | Blocked reason |
|---|------|----------------|
| 1–3 | Env execution / DB write / non-production | `apply_execution_disabled` · `apply_db_write_disabled` · `production_environment_blocked` |
| 4–5 | Gate 12 + Grafana | `gate12_not_final` · `grafana_not_ready` |
| 6–8 | PM / Ops / Eng signoff | `*_signoff_missing` |
| 9–11 | Incident / worker deploy / percent | `incident_active` · `worker_deploy_active` · `percent_rollout_active` |
| 12–17 | Sidecar state | `sidecar_not_found` · `already_promoted` · `rolled_back` · `applied_to_*` |
| 18–21 | MR + snapshot | `match_result_not_found` · `snapshot_not_found` · `snapshot_mismatch` |
| 22–23 | Fingerprint + proposal | `baseline_fingerprint_mismatch` · `proposal_mismatch` |
| 24 | Rollback token hash | `rollback_token_hash_missing`（Apply 前须已 mint hash） |

**Gate 12 / Grafana 期望值：**

- `PASS_GATE12_FOR_R8_CONTROLLED_IMPLEMENTATION`
- `PASS_GRAFANA_PHYSICAL_IMPORT_AND_TEST_ALERT`

---

## 6. Disabled path no-write proof

| 检查 | 结果 |
|------|------|
| 默认 env | `applied=false` · `mode=blocked` |
| Prisma `$transaction` | **not called** |
| Prisma `findUnique` | **not called**（env disabled 时） |
| `safety.writesDb` | `false` |

**单测：** `p710-r8d` tests #1 · #18

---

## 7. Enabled mock path behavior（仅 unit test）

当 env + gateContext + 数据全部通过时，事务内：

| Step | Action |
|------|--------|
| 1 | `UPDATE MatchResult` — `candidateUserId`；`finalScore` 仅当与 sidecar score 不同 |
| 2 | `UPDATE sidecar` — `promotionStatus=promoted` · `appliedToMatchResult=true` · `appliedToFinalScore` 按 score 变化 · **`appliedToWorkerRanking=false`** |
| 3 | `UPDATE snapshot` — `promotionStatus=applied` · `appliedBy` · `applyAuditRunId` |

**明确：** enabled 路径 **仅** 在 `p710-r8d` mock 中验证 · **不对** 真实环境开启 env · **不** 跑 staging/production Apply smoke

---

## 8. Tests / build

| Command | Result |
|---------|--------|
| `pnpm --filter @peima/api test -- p710-r8d` | **20/20 PASS** |
| `pnpm --filter @peima/api test -- p710-r8c` | **11/11 PASS** |
| `pnpm --filter @peima/api test -- p710-r7j` | **17/17 PASS** |
| `pnpm --filter @peima/api run build` | **PASS** |

---

## 9. PASS / BLOCK

### PASS（本轮）

```text
PASS_R8D_CANONICAL_APPLY_SERVICE_HARD_DISABLED
```

### BLOCK（未做）

| 禁止项 |
|--------|
| `POST /admin/.../apply` route |
| Apply UI 按钮 |
| 默认 enable env |
| 真实 production/staging Apply |
| disabled 路径写 DB |
| 无 snapshot Apply |
| worker trigger |
| percent / legacy removal |

---

## 10. Next step

| Priority | Milestone |
|----------|-----------|
| **DONE** | **P7.10-r8e** — [Rollback service token-gated](./P7.10-r8e-canonical-rollback-service-token-gated-closeout.md) · `PASS_R8E_CANONICAL_ROLLBACK_SERVICE_TOKEN_GATED` |
| **DONE** | **P7.10-r8f** — [Hidden Admin API wiring](./P7.10-r8f-hidden-admin-apply-rollback-api-wiring-closeout.md) · `PASS_R8F_HIDDEN_ADMIN_APPLY_ROLLBACK_API_WIRING` |
| **NEXT** | **P7.10-r8g** — Local Apply + Rollback smoke |
| **Later** | **P7.10-r8g–r8h** — dev/staging smoke · **P7.10-r8i** PM/Ops signoff |

**仍 BLOCK：** production Apply · percent · prod rollout · legacy removal

---

## Related

- [P7.10-r7c](./P7.10-r7c-admin-apply-canonical-promotion-api-design.md)  
- [P7.10-r8c](./P7.10-r8c-rollback-snapshot-persistence-implementation-closeout.md)  
- [P7.6 plan](./P7.6-bidirectional-mutual-matching-system-plan.md)

---

## 变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-19 | r8d Apply service hard-disabled · `PASS_R8D_CANONICAL_APPLY_SERVICE_HARD_DISABLED` |

# P7.7-r3.3a — Manual Admin UI Smoke Completion Closeout

## 1. Status

```text
P7_7_R3_3A_MANUAL_ADMIN_UI_SMOKE_COMPLETE
```

说明：

- Browser manual UI smoke completed on `/admin/p76/canonical-rehearsal`
- Local rehearsal rows inserted then cleaned up by `auditRunId`
- No MatchResult / matchInsights writes
- No worker / GET / production changes
- No code changes in this round

## 2. Commands run

| Command | Result |
|---------|--------|
| `git status --short` (pre-flight) | clean |
| `pnpm --filter @peima/api run build` | **FAIL** — Prisma `EPERM` on `query_engine-windows.dll.node` (portable dist reused) |
| `pnpm --filter @peima/web run build` | **PASS** |
| r6f3 insert `--auditRunId=p77-r3-3a-admin-ui-manual-smoke-001 --cleanup=false` | **PASS** (`insertedCount=5`) |
| API probe `GET /admin/p76/canonical-rehearsal?auditRunId=...` | **PASS** (200, 5 items) |
| `pnpm --filter @peima/web dev` | running `:5173` |
| API on `:3000` | already running (`PEIMA_P76_REHEARSAL_ADMIN_ENABLED=1`) |
| Browser UI smoke | **PASS** |
| Prisma `deleteMany` cleanup | **PASS** (`cleanupDeletedCount=5`, `finalRowsForAuditRunId=0`) |

No `DATABASE_URL` or JWT recorded.

## 3. Seeded rows

| Field | Value |
|-------|-------|
| auditRunId | `p77-r3-3a-admin-ui-manual-smoke-001` |
| insertedCount | 5 |
| duplicateCount | 0 |
| appliedToMatchResultTrueCount | 0 |
| environment | dev |
| cleanupDeletedCount | 5 |
| finalRowsForAuditRunId | 0 |

Evidence: [admin-rehearsal-ui-manual-smoke-summary.json](../../artifacts/p76/r3_3a/admin-rehearsal-ui-manual-smoke-summary.json)

## 4. UI smoke result

| Check | Result |
|-------|--------|
| Page opened | **pass** |
| Banner visible | **pass** — `SHADOW REHEARSAL ONLY — NOT APPLIED TO MATCHRESULT` |
| Summary cards visible | **pass** — totalVisible=5, eligible=4, blocked=1, wouldChange=4, violations=0 |
| Rows visible | **pass** — 5 table rows |
| Detail drawer visible | **pass** — Safety flags, baseline/proposal, guardrails, provenance, JSON previews |
| Filters tested | **pass** — `auditRunId` filter + Query |
| Export tested | **pass** — Export visible JSON clicked |
| No write controls | **pass** — no Apply/Promote/Update/Delete/worker/PreviewPool buttons (only Allowlist nav link text) |

Detail safety flags observed: `isShadowOnly=true`, `notAppliedToMatchResult=true`, `readByGetPath=false`, `readByWorker=false`, `violationStatus=ok`.

## 5. Cleanup verification

- `finalRowsForAuditRunId = 0` for `p77-r3-3a-admin-ui-manual-smoke-001`
- Only rehearsal rows for this `auditRunId` deleted
- MatchResult unchanged
- matchInsights unchanged
- Allowlist sidecar unchanged

## 6. Safety

- No Apply control on page
- No Admin write API invoked from UI
- No MatchResult mutation
- No worker trigger
- No GET integration
- Local dev only (`environment=dev`)

## 7. PASS / BLOCK

**PASS:**

- Manual UI smoke completed
- Cleanup completed
- No-write controls verified in browser
- Seeded rows readable in UI

**BLOCK (unchanged):**

- Apply endpoint blocked
- Canonical production write blocked
- Worker integration blocked
- Legacy removal blocked
- Production / percent blocked

## 8. Next recommended step

**Option A — Add dedicated RBAC `VIEW_P76_CANONICAL_REHEARSAL`**

- Decouple from `VIEW_P76_ALLOWLIST_APPLY_META`

**Option B — P7.10-r7 canonical write readiness design (docs-only)**

- Not implementation

**Option C — P7.10-r6f4 stable local seed for Ops review**

- Insert without cleanup; document cleanup command

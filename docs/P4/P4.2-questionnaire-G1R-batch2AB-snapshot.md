# G1-R 问卷 Batch 2-A / 2-B 收口快照

> **当前（M6.0-Q2 后）**：**`q01`–`q30` 均为 `canonical`**，`scoreQuestionnaireG1r` 的 **`totalCanonical` / `confidence` 分母为 30**；详见 **`docs/M6/M6.0-q2-promote-q29-q30-canonical.md`**。  
> **历史**：曾阶段性仅 **`q01`–`q12` 与 `q25`** 为 `canonical`，**`q13`–`q30`** 分批升格；旧版「非 30 题全 canonical」叙述以 **Git 历史 + 下文「历史口径存档」** 为准。

---

## 核心口径（与当前 `main` 实现一致）

1. 当前问卷为 **30 题**（`q01`–`q30`）。
2. 已引入 **`sourceTier`** 闸门（`canonical` / `draft` / `placeholder`）；**本题库无 `placeholder` 题**。
3. **`q01`–`q30` 已全部为 `canonical`**（**M6.0-Q2**），均参与 **v2 生产计分**（`tags` → 20 轴 + **`confidence`**）。
4. 已打通 **v2 scorer**（`scoreQuestionnaireG1r`）与 **v2 profile 写入**（**`user_profile`**：20 个 G1-R 轴 + **`confidence`**）。
5. 前端跟随后端**动态题数**（`questions.length`）。
6. **全量正式生产「真源结案」**（逐题文案/打点与运营冻结）仍可另任务收口；**与「30 题均已进入计分闸门」分离表述**。

---

## 一、当前阶段状态说明

### 已完成（代码已落地）

- **Batch 2-A（后端问卷主链）**
  - 题库为 **30 题**（`q01`–`q30`）结构
  - 每题带 **`sourceTier`**（`canonical` / `draft` / `placeholder`）
  - **`scoreQuestionnaireG1r`**：仅消费 **`canonical`** 题（**当前即全部 30 题**），按选项 **`tags`** 聚合为 **20 个 G1-R 轴**（每轴为数值或 `null`）+ 独立 **`confidence`**
  - **`questionnaire.service`** 的 submit 调用 v2 scorer
  - **`user_profile` upsert** 只写 **20 个 G1-R 列 + `confidence`**，不再写入旧六维
  - **`submit-questionnaire.dto`** 将 **`answers` 长度约束为 30**
  - **`GET /questionnaire/questions`** 仍经 **`getPublicQuestions()`**，响应中**不包含** `tags` / `sourceTier`
  - **`apps/api/test/questionnaire.scorer.regression.e2e-spec.ts`**：**17** 条用例与 **`confidence` 分母 = 30** 等闸门一致
  - **`apps/api/test/questionnaire-canonical-q29-q30.spec.ts`**：**M6.0-Q2** 闸门与 submit 边界

- **Batch 2-B（前端）**
  - `apps/web/src/api/questionnaire.ts`：`SubmitQuestionnaireResponse.profile` 已与 **UserProfile v2** 对齐
  - `QuestionnairePage.jsx`：完成条件与提交门槛跟随 **`questions.length`**，不再写死 12

- **`questionnaire.controller.ts`**
  - 审读结论：**零改动通过**
  - 路由仍为透传 **`QuestionnaireService`**
  - **不会**在 controller 层把 `tags` / `sourceTier` 拼进题面响应

### 与「全量正式生产」仍可区分的差距

- **worker / matching** 是否全面消费 **20 维 G1-R**、是否仍隐含旧六维假设 — **不在本快照问卷线范围内**（不展开 chat / matching 实现）。
- 全仓 **e2e** 若仍有与 **30 题 / v2 profile** 不一致的假设，**另任务跟进**。

---

## 二、very short test record / 变更记录

- **改动目标（历史）**
  - 问卷从旧 12 题 / 旧六维，迁到 **G1-R 30 题结构** + **`sourceTier`** + **v2 计分**与 **v2 画像写入**
  - 前端跟随后端题数并与 **v2 profile** 类型对齐
  - 公开题面不暴露 `tags` / `sourceTier`

- **M6.0-Q2（当前）**
  - **`q29`/`q30`**：`draft` → **`canonical`**；**`confidence` 分母 28 → 30**；**`relationshipProfileScore`** 等依赖 20 维画像的链路 **会受** 两题答案影响

- **测试场景**
  - `GET /questionnaire/questions` 题数与字段
  - JWT 下 `POST /questionnaire/submit` 提交 **30** 条 `answers`
  - Web 问卷页答满当前 **`total`** 后提交

- **预期结果**
  - 题面无 `tags` / `sourceTier`
  - **201**（Nest 默认），`answersSaved === 30`，`profile` 为 v2 形态（多轴可 `null`）
  - 仅答满时可提交成功；**缺任一键 → BadRequest**

- **实际结果（维护命令）**
  - `pnpm --filter @peima/api run test:e2e -- questionnaire.scorer.regression`
  - `pnpm --filter @peima/api test -- questionnaire-canonical-q29-q30`

---

## 三、下一轮待办（very short）

1. **worker / matching**：读/写画像是否与 **20 维 G1-R** 一致；是否仍隐含旧六维假设。
2. **README / e2e**：与本快照、**M6.0-Q2** 文档对齐；全仓其它 **e2e** 若有旧 **28** 题闸门假设，**另任务**更新。
3. **评分回归**：**All-A / B / C / D / Normal** 在 **30 题 canonical** 下重跑，更新 **M6** 系列记录表。

---

## 四、controller 为何零改动通过

- **`GET .../questions`** 的题面形态由 **`getPublicQuestions()`** 决定，controller **仅转发** `getQuestionBank()`，无二次映射
- **`POST .../submit`** 仍使用既有 **`SubmitQuestionnaireDto`** + **`JwtAuthGuard`** + `userId` 一致性校验，无题数或旧维度硬编码
- 因此 **Batch 2-B 对 controller 无强制修改点**

**另见**：问卷画像 **v3 只读** — **`docs/P4/P4.3-questionnaire-profile-v3.md`**

---

## 五、历史口径存档（M6.0-Q2 前）

- 曾宣称：**`q01`–`q12` 与 `q25` 为 `canonical`**，**`q13`–`q30` 仍未全部 `canonical`**；**`confidence` 分母曾为 28**；**`q29`/`q30` 仅落库、不参与 `scoreQuestionnaireG1r`**。  
- **以上在 M6.0-Q2 合入后已作废**；请以本文 **「核心口径」** 与 **`questions.ts`** 为准。

# P4.1-S3-A 测试执行记录（首轮）

> Status: Completed
> 当前仓库未检出 S3-A 专用 baseline/template；本记录为独立首轮执行记录。  
> 结构参考：`docs/P4/P4.1-S1-test-run-round1.md`。

---

## 1. 执行信息

- **执行日期**：`2026-04-10`
- **执行人**：`人工验收（项目成员）`
- **执行轮次**：`首轮`
- **关联需求/任务**：`P4.1-S3-A 权限与可访问性联动验收（P4 行为 × P5 RBAC）`
- **关联分支/版本**：`当前工作分支（本地）`
- **记录口径**：记载本轮已核实事实；首轮剩余证据已补齐后定稿。

---

## 2. 环境信息

- **操作系统**：`Windows 10`
- **浏览器**：`Edge`
- **前端地址**：`http://localhost:5173`
- **API 基址**：`http://localhost:3001`
- **运行方式**：`本地 dev + Docker(Postgres)`（按实际）
- **关键环境变量（可选，已脱敏）**：
  - `API_PORT=`（按实际）
  - `DATABASE_URL=`（按实际，已脱敏）
  - `PEIMA_ADMIN_USER_IDS=`（若本轮用于高权限对照，按实际配置）
- **是否与 README / 当前部署口径一致**：`是`

---

## 3. 测试身份信息

| 身份类型 | 说明 |
|---|---|
| 未登录 | 无 token 请求 |
| 普通/低权限 | 具备有效登录态，访问 `GET /rbac/users` 返回 `403`，错误信息含 `required permission 'manage_permissions'` |
| 高权限 | 具备 `manage_permissions`，访问 `GET /rbac/users` 返回 `200` 并返回用户列表 |

---

## 4. 测试资源 / 路径信息

- **RBAC 关键接口**：`GET /rbac/users`
- **主链路会话（A-07）**：在同一测试用户提权为 admin **之前**，其本人有效 `conversationId` 对应聊天页可正常访问，未出现 `401/403`。
- **证据边界（A-07）**：仅覆盖该普通用户与该本人有效会话，不外推到其他用户或其他会话归属场景。
- **证据归档**：`按实际归档（截图/终端输出路径）`

---

## 5. 执行结果总览

- **已通过**：`7`（A-01 ~ A-07）
- **待补证据**：`0`
- **待执行**：`0`
- **失败项**：`0`
- **阻塞项**：`0`
- **总体判定**：`通过`

---

## 6. Checklist 逐项执行记录（A-01 ~ A-07）

> 结果枚举：`通过` / `失败` / `阻塞` / `待执行` / `待补证据`

| 编号 | 验收项 | 结果 | 备注（现象/结论） |
|---|---|---|---|
| A-01 | 未登录访问受保护资源应被拦截（鉴权） | `通过` | 无 token 访问 `GET /rbac/users` 返回 `401 Unauthorized`。 |
| A-02 | 普通权限访问受限操作应被拦截（鉴权后权限） | `通过` | 普通账号访问 `GET /rbac/users` 返回 `403 Forbidden`，错误信息包含 `required permission 'manage_permissions'`。 |
| A-03 | 高权限路径可达 | `通过` | 高权限账号访问 `GET /rbac/users` 返回 `200 OK`，并成功返回用户列表。 |
| A-04 | 页面侧受限能力与接口权限语义一致 | `通过` | 已在本轮补齐「同一受限动作」下页面表现与 `GET /rbac/users` 接口权限语义（401/403/200）的成对对照，页面侧与接口侧结论一致。 |
| A-05 | 页面与接口拦截表现可解释且不冲突 | `通过` | 页面反馈与接口返回码及错误语义可对应解释，未观察到相互矛盾的表现。 |
| A-06 | 鉴权问题与权限问题可区分（401 vs 403） | `通过` | 同一路径下，未登录为 `401`，权限不足为 `403`，可明确区分。 |
| A-07 | P4 主链路不被 RBAC 误伤（普通用户自身链路可用） | `通过` | 同一测试用户在提权为 admin 之前，已验证其访问 `GET /rbac/users` 为 `403`；其本人有效 `conversationId` 对应聊天会话可正常访问，未出现 `401/403`。证据边界：仅该普通用户 + 该本人有效会话。 |

---

## 7. 失败/阻塞项详情

本轮无失败项与阻塞项。

---

## 8. 问题汇总（用于跟踪）

| 问题ID | 检查项编号 | 严重级别 | 状态 | 责任人 | 备注 |
|---|---|---|---|---|---|
| （无） | - | - | - | - | 本轮 A-01 ~ A-07 全部通过 |

---

## 9. 执行结论

- **本轮是否满足 P4.1-S3-A 基线全文闭环**：`是`
- **结论说明**：
  - `A-01 ~ A-07 全部通过`
  - `失败项 0，阻塞项 0`
  - `总体判定：通过`
  - A-07 证据边界仍以「该普通用户 + 该本人有效会话」为限，不作外推。
- **建议后续动作**：
  1. 将本记录归档为 P4.1-S3-A 首轮人工验收正式记录。
  2. 按 P4.1-S2 文档治理规则，在 `docs/P4/P4-current-slice-status.md` 同步联动验收状态。

---

## 10. 附件与证据（可选）

- `GET /rbac/users` 无 token：`401`（按实际归档）
- 普通账号 `GET /rbac/users`：`403` + `manage_permissions`（按实际归档）
- 高权限 `GET /rbac/users`：`200` + 列表（按实际归档）
- A-04/A-05：同一受限动作的页面表现与接口成对对照（按实际归档）
- A-07：提权前普通身份下本人会话聊天页可访问（按实际归档）
- **相关文档引用**：
  - `docs/P4/P4.1-S1-test-run-round1.md`
  - `docs/P4/P4-current-slice-status.md`

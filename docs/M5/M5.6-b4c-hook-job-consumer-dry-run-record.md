# M5.6-B4C — Hook Job Consumer Dry-run Runner Run Record

> **性质**：M5.6-B4B controlled consumer dry-run runner **一次实际执行**的非敏感记录。不含真实 `userId` / `candidateUserId` / `matchResultId` / `hookJobId`、不含 token / API key / `DATABASE_URL`。

## 1. 元数据

| 项 | 值 |
|----|-----|
| **记录日期** | 2026-05-03 |
| **代码基线（commit）** | `821ed76`（M5.6-B4B merged） |
| **Runner 文档** | `docs/M5/M5.6-b4b-rrm-top2-hook-job-consumer-runner.md` |

## 2. 构建

| 项 | 结果 |
|----|------|
| **命令** | 在 `apps/api` 下：`pnpm exec nest build` |
| **结果** | **成功**（exit code 0） |

## 3. Runner 命令模板

```bash
# 在 apps/api 且已 nest build 后（任选其一）
pnpm --filter @peima/api run m56-b4b:rrm-top2-hook-job-consumer -- --limit=20 --pretty

# 若 pnpm 未注入 DATABASE_URL，可从仓库根目录 .env 加载（路径按实际 monorepo 调整）
cd apps/api
node --env-file=..\.env dist/dev-cli/m56-b4b-rrm-top2-hook-job-consumer-cli.js --limit=20 --pretty
```

本轮执行使用的参数：`--limit=20`、`--pretty`。未使用 `--apply`（B4B **不支持** apply）。

## 4. 执行结果（本次环境）

| 项 | 结果 |
|----|------|
| **进程是否启动** | 是（Nest 上下文 + Prisma 连接） |
| **是否出现错误** | **是** — Prisma `P2021`：`MatchResultRrmTop2DisplayHookJob` 对应表在当前指向的数据库中 **不存在**（迁移未应用或指向了非预期库） |
| **是否得到聚合 JSON** | **否**（在 `findPendingRrmTop2HookJobs` → `findMany` 处失败，未进入逐条 `processRrmTop2HookJobDryRun`） |

### 4.1 Dry-run 聚合字段（本次）

因上述错误，**无** runner 输出的 `totalJobsRead` / `wouldProcessCount` 等聚合对象。若在同一基线代码下、**已存在** `match_result_rrm_top2_display_hook_jobs` 表且库内有 pending 行，预期输出形状见 B4B 文档（仍为 **dry_run**、无 id 的 `items[]`）。

占位说明（**非本次实测数值**）：

- `totalJobsRead`：成功时应为 `0 … limit` 之间的整数；无 pending 时为 `0`。  
- `wouldProcessCount` / `wouldSkipCount` / `noOpReasonCounts` / `writerOkCount` / `writerNoOpCount`：依赖库内数据与 writer dry-run 结果，**不在此文档写具体数字**以免与某次环境绑定。  
- `candidateUserIdUnchangedAll` / `finalScoreUnchangedAll`：B4B 设计为恒为 `true`（仅 dry-run）。

## 5. 安全与副作用核对（设计 + 本次行为）

| 问题 | 结论 |
|------|------|
| **是否写 DB？** | **否**。失败发生在只读 `findMany`；未执行 meta / summary / `MatchResult` / hook job 状态写入路径。 |
| **是否 apply writer？** | **否**（B4B 无 `--apply`；consumer 仅 `dryRun: true`）。 |
| **是否改 HookJob 状态？** | **否**（runner 不调用 mark*；本次未越过查询阶段）。 |
| **是否改 `candidateUserId` / `finalScore`？** | **否**。 |

## 6. 与 M5.6-B5（apply gate）的关系

- **可以**在设计上进入 **B5 apply gate / 状态迁移** 的方案与评审：B4B 行为与边界已在 `821ed76` 固化。  
- **工程上**在本机复现「成功聚合 JSON」前，需保证：目标库已应用含 `match_result_rrm_top2_display_hook_jobs` 的 migration（M5.6-B1）、`DATABASE_URL` 指向该库，且按需存在 pending hook job 行供 dry-run 枚举。

## 7. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-05-03 | 首次实际跑 B4B CLI 并记录（本环境 P2021）。 |

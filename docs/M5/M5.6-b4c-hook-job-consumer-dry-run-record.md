# M5.6-B4C — Hook Job Consumer Dry-run Runner Run Record

> **性质**：M5.6-B4B controlled consumer dry-run runner **实际执行**的非敏感记录（两轮）。不含真实 `userId` / `candidateUserId` / `matchResultId` / `hookJobId`、不含 token / API key / `DATABASE_URL`。

## 1. 元数据

| 项 | 值 |
|----|-----|
| **记录日期** | 2026-05-03 |
| **代码基线（commit）** | `821ed76`（M5.6-B4B merged） |
| **Runner 文档** | `docs/M5/M5.6-b4b-rrm-top2-hook-job-consumer-runner.md` |

## 2. 构建

| 项 | 结果 |
|----|------|
| **命令** | 在 `apps/api` 下：`pnpm exec nest build` |
| **结果** | **成功**（exit code 0） |

## 3. Runner 命令模板

```bash
cd apps/api
pnpm exec nest build

# 任选其一（路径按本地 monorepo 与 `.env` 位置调整）
pnpm --filter @peima/api run m56-b4b:rrm-top2-hook-job-consumer -- --limit=20 --pretty

node --env-file=..\\..\\.env dist\\dev-cli\\m56-b4b-rrm-top2-hook-job-consumer-cli.js --limit=20 --pretty
```

参数示例：`--limit=20`、`--pretty`。B4B **不支持** `--apply`。

---

## 4. Round 1 — 表缺失（blocked）

**背景**：目标库尚未包含 `match_result_rrm_top2_display_hook_jobs`（迁移未应用或 `DATABASE_URL` 指向非预期库）。

| 项 | 结果 |
|----|------|
| **进程是否启动** | 是（Nest + Prisma 连接） |
| **是否出现错误** | **是** — Prisma `P2021`：上述表在当前库中 **不存在** |
| **是否得到聚合 JSON** | **否**（在 `findPendingRrmTop2HookJobs` → `findMany` 处失败） |
| **是否写 DB** | **否**（失败于只读查询） |
| **是否改 HookJob 状态** | **否** |
| **是否改 `candidateUserId` / `finalScore`** | **否** |

---

## 5. Round 2 — migration / 表就绪后（成功 dry-run）

**背景**：本地数据库已重新同步；`match_result_rrm_top2_display_hook_jobs` **已存在**。再次执行与 Round 1 相同的构建 + runner 流程（`--limit=20`、`--pretty`，无 `--apply`）。

| 项 | 结果 |
|----|------|
| **是否出现错误** | **否**；runner **成功**输出聚合 JSON |
| **是否写 DB** | **否**（仍为只读枚举 pending + writer **dryRun**） |
| **是否改 HookJob 状态** | **否**（未调用 mark*；未改 `status`） |
| **是否改 `candidateUserId` / `finalScore`** | **否** |

### 5.1 聚合输出摘要（Round 2）

以下为 **字段级摘要**（无业务 id；与 CLI JSON 一致）：

| 字段 | 值 |
|------|-----|
| `refused` | `false` |
| `mode` | `dry_run` |
| `sourceVersion` | `m5.6-b4b-rrm-top2-hook-job-consumer-runner-v1` |
| `totalJobsRead` | `0` |
| `wouldProcessCount` | `0` |
| `wouldSkipCount` | `0` |
| `noOpReasonCounts` | `{}`（空对象） |
| `writerOkCount` | `0` |
| `writerNoOpCount` | `0` |
| `candidateUserIdUnchangedAll` | `true` |
| `finalScoreUnchangedAll` | `true` |
| `items` | `[]`（空数组） |

**解释**：

- 表已存在，`findPendingRrmTop2HookJobs` 查询 **成功**。  
- **`totalJobsRead = 0` 为正常状态**：表示当前库中 **没有** `status = pending` 的 hook job 行，因此无逐条 `processRrmTop2HookJobDryRun` 调用，计数与 `items` 均为零。  
- 未写 DB、未改 HookJob 状态、未改 `MatchResult.candidateUserId`、未改 `finalScore`。

---

## 6. 与 M5.6-B5（apply gate）的关系

- **可以**在设计上进入 **B5 apply gate / 状态迁移** 的方案与评审：B4B 行为与边界已在 `821ed76` 固化。  
- **工程上**：Round 2 证明在 **表已迁移** 的前提下 runner 可稳定输出聚合 JSON；若需非零 `totalJobsRead`，需先有 pending hook job 数据（例如通过 B3 controlled create 等流程，不在本文范围）。

## 7. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-05-03 | Round 1：P2021，未输出聚合 JSON。 |
| v1.1 | 2026-05-03 | Round 2：表就绪后成功 dry-run；`totalJobsRead=0`、无副作用。 |

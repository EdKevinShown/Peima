# M5.5-M4B — Controlled Runner Local Run Record

> **占位说明**：本文档不出现真实 `userId` / `candidateUserId` / `matchResultId` / `batchId` / token / `DATABASE_URL` 等敏感值。命令行示例一律使用 **`<LOCAL_*>`** 占位。

## 元数据

| 项 | 值 |
|----|-----|
| **日期** | 2026-05-02 |
| **代码基线（M5.5-M4 runner）** | `35d58cb8126337d884a69fdd316eaf2c0f08e196` |
| **说明文档** | `docs/M5/M5.5-m4-rrm-top2-production-hook-runner.md` |

## A. 编译（`apps/api`）

| 步骤 | 结果 |
|------|------|
| `pnpm exec nest build` | **成功**（exit code 0） |

## B. 命令模板（占位符）

将下列占位符替换为**本地**真实值后再执行；**勿**将真实 ID 粘贴进本仓库：

- `<LOCAL_MATCH_RESULT_ID>`
- `<LOCAL_TOP2_A>` / `<LOCAL_TOP2_B>`（须含静态 baseline，且与 `<LOCAL_MATCH_RESULT_ID>` 对应行一致）
- `<LOCAL_RRM_WINNER>`（须为 Top2 之一）
- `<LOCAL_FINGERPRINT>`

**示例形态**（二选一，均不含真实 ID）：

```bash
cd apps/api
pnpm exec nest build
pnpm --filter @peima/api run m55-m4:rrm-top2-production-hook -- \
  --matchResultId=<LOCAL_MATCH_RESULT_ID> \
  --top2CandidateUserIdA=<LOCAL_TOP2_A> \
  --top2CandidateUserIdB=<LOCAL_TOP2_B> \
  --rrmWinnerCandidateUserId=<LOCAL_RRM_WINNER> \
  --top2Fingerprint=<LOCAL_FINGERPRINT> \
  --guardrailsStatus=pass \
  --pretty
```

或使用与本地一致的 env 文件注入（路径按本机调整）：

```text
node --env-file=..\\..\\.env dist/dev-cli/m55-m4-rrm-top2-production-hook-cli.js ^
  --matchResultId=<LOCAL_MATCH_RESULT_ID> ^
  --top2CandidateUserIdA=<LOCAL_TOP2_A> ^
  ...
```

## C. 运行轮次摘要

### Round 1 — `DATABASE_URL` 缺失，未进入 hook

| 项 | 结果 |
|----|------|
| **环境** | CI/代理 shell 未注入 `DATABASE_URL` |
| **是否进入 `runM55M4ProductionHook`** | **否** |
| **行为** | CLI 在入口检测缺失后 **exit 1**，早于 Prisma / writer |
| **`noOpReasonCode`** | 不适用 |

### Round 2 — 本地 dry-run，进入 writer，`writer_disabled`

| 项 | 结果 |
|----|------|
| **环境** | 本地 DB 可用（例如通过 `--env-file` 指向 repo `.env`）；**`PEIMA_M5_RRM_TOP2_META_WRITE_ENABLED` 未开启** |
| **`dryRun`** | `true`（未传 `--apply`） |
| **`noOpReasonCode`** | **`writer_disabled`** |
| **`metaWriteEnvOn`** | `false` |
| **`summaryWriteEnvOn`** | `false` |
| **`wroteSummary` / `wroteMeta`** | `false` / `false` |
| **`candidateUserIdUnchanged` / `finalScoreUnchanged`** | `true` / `true` |

### Round 3 — 本地 dry-run，`PEIMA_M5_RRM_TOP2_META_WRITE_ENABLED` 临时开启，`existing_meta_frozen`

| 项 | 结果 |
|----|------|
| **环境** | 本地；临时将 **`PEIMA_M5_RRM_TOP2_META_WRITE_ENABLED=1`**（PowerShell 等形式以本机为准） |
| **`dryRun`** | `true` |
| **`noOpReasonCode`** | **`existing_meta_frozen`**（该行已存在 **frozen** 的 `MatchResultRrmTop2DisplayMeta`，writer 按设计不覆盖） |
| **`metaWriteEnvOn`** | `true` |
| **`summaryWriteEnvOn`** | `false` |
| **`wroteSummary` / `wroteMeta`** | `false` / `false` |
| **`candidateUserIdUnchanged` / `finalScoreUnchanged`** | `true` / `true` |

**Round 2 / 3 说明**：runner 已能连上本地 DB 并调用 **`writeRrmTop2DisplayMetaForMatchResult`**；env 关闭时 **安全 no-op**；env 开启但侧车已 **frozen** 时 **安全 no-op**。

## D. Apply 与写库（本轮）

| 项 | 决定 |
|----|------|
| **是否执行 `--apply`** | **否** |
| **是否写 DB** | **否**（Round 2 / 3 均为 **dry-run**） |
| **`existing_meta_frozen`（Round 3）** | 仅 **记录 no-op**，**未**尝试覆盖 frozen meta |

## E. 不变量

| 项 | 状态 |
|----|------|
| **修改 `MatchResult.candidateUserId`** | **否** |
| **修改 `finalScore`** | **否** |
| **接 GET / worker / simulation 自动 hook** | **否** |

## F. 结论

- **本轮未写 DB**；Round 2 / 3 验证了 **writer 路径**与两类 **no-op**（`writer_disabled`、`existing_meta_frozen`）行为符合预期。
- **是否进入 M5.5-M5 设计阶段**：**可以进入**（冻结 Top2 来源、summary/guardrails 生产链路、触发时机与 audit 仍见 `M5.5-m3-rrm-top2-writer-production-hook-plan.md`）。**这不表示** M5.5-M5 **自动 production hook 已实现或已完成**。

## 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-05-02 | 初版：含 `DATABASE_URL` 缺失的 Round 1。 |
| v1.1 | 2026-05-02 | 追加本地 Round 2 / Round 3 dry-run 摘要；统一 `<LOCAL_*>` 占位；明确未 apply、未写库。 |

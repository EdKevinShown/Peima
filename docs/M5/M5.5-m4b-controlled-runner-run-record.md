# M5.5-M4B — Controlled Runner Local Run Record

> **占位说明**：本文档不出现真实 `userId` / `candidateUserId` / `matchResultId` / `batchId` / token / `DATABASE_URL` 等敏感值。

## 元数据

| 项 | 值 |
|----|-----|
| **日期** | 2026-05-02 |
| **代码基线（commit）** | `35d58cb8126337d884a69fdd316eaf2c0f08e196`（M5.5-M4 controlled runner 已合入后的 `main`） |
| **文档** | `docs/M5/M5.5-m4-rrm-top2-production-hook-runner.md` |

## A. 编译（`apps/api`）

| 步骤 | 结果 |
|------|------|
| `pnpm exec nest build` | **成功**（exit code 0） |

## B. Dry-run 执行意图

**命令模板**（将占位符替换为本地真实值后再跑；勿把真实值粘贴进本仓库文档）：

```bash
cd apps/api
pnpm exec nest build
pnpm --filter @peima/api run m55-m4:rrm-top2-production-hook -- \
  --matchResultId=<LOCAL_MATCH_RESULT_ID> \
  --top2CandidateUserIdA=<LOCAL_TOP2_A> \
  --top2CandidateUserIdB=<LOCAL_TOP2_B> \
  --rrmWinnerCandidateUserId=<LOCAL_RRM_WINNER> \
  --top2Fingerprint=<LOCAL_FINGERPRINT> \
  --guardrailsStatus=pass \
  --pretty
```

**前置条件**：`DATABASE_URL` 在运行进程环境中可用（例如 repo 根或 `apps/api` 的 `.env` 已由操作者本地配置）。**本 CI/代理会话未注入该变量**，因此下列为 **本记录环境下的实际结果**，不代表你本机无法跑通。

## C. 本记录环境下的 dry-run 结果

| 项 | 结果 |
|----|------|
| **是否实际执行到 `runM55M4ProductionHook`** | **否** |
| **CLI 退出原因** | 在 `m55-m4-rrm-top2-production-hook-cli.ts` 中检测到 **`DATABASE_URL` 缺失**，按设计打印错误并 **exit 1**（早于 Prisma / writer） |
| **`noOpReasonCode`** | **不适用**（未进入 hook） |
| **`dryRun` / `wroteMeta` 等 JSON** | **未产生**（进程未连库） |

## D. Apply 策略（本轮）

| 项 | 决定 |
|----|------|
| **是否执行 `--apply`** | **否** |
| **是否写 DB** | **否** |
| **`existing_meta_frozen` 检查** | **未执行**（未连库） |

**说明**：按任务要求，**未在 dry-run 未成功验证 eligibility 的情况下申请 apply**；本环境下 dry-run 未到达 writer，**更不会 apply**。

## E. 不变量与边界（设计确认，本轮未改代码）

| 项 | 状态 |
|----|------|
| **改 `MatchResult.candidateUserId`** | **否**（未写库） |
| **改 `finalScore`** | **否**（未写库） |
| **接 GET / worker / simulation 自动 hook** | **否**（仍为 M5.5-M4 受控 CLI） |

## F. 结论

- **本轮未写 DB**；在具备 `DATABASE_URL` 与合法本地 `MatchResult` / Top2 的前提下，操作者应使用 **§B 模板**重跑 dry-run，并仅在输出满足 **ok、`noOpReasonCode` 为 null、不变量通过、且无 `existing_meta_frozen`** 时，再人工决定是否加 `--apply`（本记录未执行该步）。
- **是否进入 M5.5-M5 自动 hook 设计**：**可以进入设计阶段**（与是否在本机一次跑通 CLI 无关）；M5.5-M5 应在 **冻结 Top2 来源、summary/guardrails 生产链路** 明确后再实现自动调用，仍保持 **GET 只读、不写库**（见 `M5.5-m3-rrm-top2-writer-production-hook-plan.md`）。

## 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-05-02 | M5.5-M4B：受控 runner 本地运行记录（占位符；本环境无 `DATABASE_URL`）。 |

# M5.6-B8-C — Independent Dry-run Poller Run Record

> **范围**：**运行记录 only**（无业务代码变更）。不含真实 `userId` / `candidateUserId` / `matchResultId` / `hookJobId`、token、`DATABASE_URL` 或 prompt / transcript。

---

## 1. 日期

**2026-05-03**

---

## 2. Commit 基线

文档与能力对齐的基线：**`acceddf`**（M5.6-B8-B：independent dry-run poller CLI）。本次运行使用当前工作区在 **`pnpm exec nest build`** 之后生成的 `dist/`（与 `acceddf` 行为一致；若本地 `main` 已超前，以实际二进制为准）。

---

## 3. Build 是否成功

**是。** 在 `apps/api` 下执行：

```bash
pnpm exec nest build
```

退出码 **0**。

---

## 4. Runner 命令模板

在 `apps/api` 目录下（任选其一）：

```bash
node --env-file=..\\..\\.env dist\\dev-cli\\m56-b8-rrm-top2-hook-job-dry-run-poller-cli.js --limit=20 --once --pretty
```

或：

```bash
pnpm --filter @peima/api run m56-b8:rrm-top2-hook-job-dry-run-poller -- --limit=20 --once --pretty
```

**说明**：CLI 将 `--limit` 钳制在 **1–10**；传入 `--limit=20` 时实际按 **10** 读取 pending（本次 `totalJobsRead=0`，钳制未改变观测结果）。

---

## 5. Dry-run 输出摘要（aggregate-safe）

| 字段 | 值 |
|------|-----|
| `refused` | `false` |
| `mode` | `"dry_run"` |
| `once` | `true` |
| `apply` | `false` |
| `sourceVersion` | `"m5.6-b8-rrm-top2-hook-job-dry-run-poller-v1"` |
| `totalJobsRead` | **0** |
| `wouldProcessCount` | **0** |
| `wouldSkipCount` | **0** |
| `noOpReasonCounts` | **{}**（空对象） |
| `writerOkCount` | **0** |
| `writerNoOpCount` | **0** |
| `candidateUserIdUnchangedAll` | `true` |
| `finalScoreUnchangedAll` | `true` |
| `items` | **[]**（空数组） |

未记录任何 per-job 业务 id；本次无 pending，`items` 为空。

---

## 6. `totalJobsRead` 是多少

**0** — 当前库中 **无** pending RRM Top2 display hook jobs（在该次连接与 limit 下）。

---

## 7. `noOpReasonCounts` 是什么

**{}** — 无条目可聚合，故无数分布。

---

## 8. 是否写 DB

**否。** 仅 dry-run 聚合；未执行 apply、未调用 HookJob `mark*`。

---

## 9. 是否改 HookJob 状态

**否。**

---

## 10. 是否写 meta / summary

**否。** 未写 `MatchResultRrmTop2DisplayMeta`；未写 `matchInsights` 内 RRM summary。

---

## 11. 是否改 `candidateUserId` / `finalScore`

**否。**

---

## 12. 是否接 GET / worker

**否。** 未接 `/matching/result`；未接 `apps/worker/src/main.ts`。

---

## 13. 是否可以进入 B9 staging apply mode 设计

**可以。** B8-B CLI 已在真实环境执行 dry-run 且 **`totalJobsRead=0`** 路径下输出符合 aggregate-safe 契约；B8-C 确认无写库、无状态迁移。下一步 **M5.6-B9** 可在 **独立设计文档** 中规划 **staging apply**（强 gate、env、与 shared `processRrmTop2HookJobsApplyBatch` 复用），**仍须**保持 `candidateUserId` / `finalScore` 不变与生产拒绝策略。

---

## 14. 可选：有 pending 时的验证

本次 **未** 通过 B3 create runner 造数；`totalJobsRead=0` 已作为有效收口记录。

---

## 修订记录

| 版本 | 说明 |
|------|------|
| **v1.0** | M5.6-B8-C — dry-run poller 实际运行记录（仅文档）。 |

# M5.6-B9-B — Staging Apply Run Record（B5 Runner）

> **范围**：**运行记录 only**（无业务代码变更）。使用 **现有 B5 apply runner**（`m56-b5-rrm-top2-hook-job-apply-cli.js`），**未**新增 `m56-b9` apply CLI。不含真实 `userId` / `candidateUserId` / `matchResultId` / `hookJobId`、token、`DATABASE_URL`。

---

## 元数据

| 项 | 值 |
|----|-----|
| **日期** | **2026-05-03**（Round 1 初稿）；**Round 2** 补跑于同日后续会话（具体时刻未写入本文档）。 |
| **代码基线** | 仓库 **`main`** 已含 B5/B7/B8；与 **B9-A**（`M5.6-b9a-rrm-top2-staging-apply-mode-plan.md`）规划一致。 |
| **构建** | `pnpm exec nest build`（`apps/api`）→ **成功**（退出码 0）。 |

---

## 回合说明

| 回合 | 性质 |
|------|------|
| **Round 1** | **blocked / empty queue**：占位 id → B3 **失败**；B5 **无 pending**；**不算**完整 staging 验收。 |
| **Round 2** | **successful staging-style apply**：合法本地 id + 新 fingerprint → B3 **创建 pending**；B5 **消费 1 条** → **`existing_meta_frozen` → `skipped`**。 |

---

## Round 1 — blocked / empty queue（保留事实）

### Round 1 — B3

使用 **占位** `--matchResultId` / viewer / top2 id（**非**库内真实行）执行 B3 `--apply` 时，Prisma 报错 **`P2025`** — *required `MatchResult` not found for nested connect*。**未** 创建 hook job。

**命令形态**（占位符，操作者须替换为真实 LOCAL id）：

```text
node --env-file=..\\..\\.env dist\\dev-cli\\m56-b3-rrm-top2-hook-job-cli.js ^
  --matchResultId=<LOCAL_MATCH_RESULT_ID> ^
  --viewerUserId=<LOCAL_VIEWER_USER_ID> ^
  --top2CandidateUserIdA=<LOCAL_TOP2_A> ^
  --top2CandidateUserIdB=<LOCAL_TOP2_B> ^
  --top2Fingerprint=local-dev-fixture-b9b ^
  --guardrailsStatus=pass --pretty --apply
```

### Round 1 — B5 apply

| 字段 | 值 |
|------|-----|
| `mode` | `"apply"` |
| `totalJobsRead` | **0** |
| `processedCount` / `skippedCount` / `failedCount` | **0** / **0** / **0** |
| `noOpReasonCounts` | **{}** |
| `items` | **[]** |

**结论**：空队列 / blocked，**无**状态迁移可观测。

### Round 1 — B8 复验（当时）

`m56-b8` dry-run poller：`totalJobsRead = 0`，`items = []`（与 **B8-C** 空队列记录一致）。`--limit=20` 在实现中钳为 **10**，对 0 条无影响。

---

## Round 2 — successful staging-style apply

### 共同前提（仍无新 B9 CLI）

- **仅**使用 **B5 apply runner** + **B3 create runner**；**未**新增 `m56-b9` staging apply CLI。  
- **未**接 GET、`apps/worker/src/main.ts`。

### Staging-style env（Round 2 会话中已设置）

（**仅变量名**，不写入 secret / `DATABASE_URL`。）

- `PEIMA_M5_RRM_TOP2_HOOK_POLLER_ENABLED=1`
- `PEIMA_M5_RRM_TOP2_HOOK_POLLER_MODE=apply`
- `PEIMA_M5_RRM_TOP2_HOOK_POLLER_APPLY_CONFIRM=I_UNDERSTAND`
- `PEIMA_M5_RRM_TOP2_META_WRITE_ENABLED=1`
- `NODE_ENV=development`（非 production）

**说明**：B5 的硬 gate 仍为 **`--apply` + `--confirmControlledApply=I_UNDERSTAND`** 与 writer env；poller 前缀变量为 **B9-A 纪律 / 对齐**，当前 B5 二进制**未必**读取（见 B9-A / B9-B v1.0 说明）。

### Round 2 — B3 apply（成功创建 pending）

**新 fingerprint**（避免 unique 冲突）：`local-dev-fixture-b9b-r2`（**非**业务用户 id，可写入本文档）。

**B3 输出摘要**（**无**真实 `matchResultId` / `jobId` 落盘）：

| 字段 | 值 |
|------|-----|
| `mode` | `apply` |
| `created` | **`true`** |
| `status` | **`pending`** |
| `sourceVersion` | `m5.6-b3-rrm-top2-hook-job-v1` |
| `top2Fingerprint` | **`local-dev-fixture-b9b-r2`** |
| `guardrailsStatus` | `pass` |
| `candidateUserIdUnchanged` | `true` |
| `finalScoreUnchanged` | `true` |

### Round 2 — B5 apply（命中 frozen meta）

**命令形态**（与 Round 1 相同入口）：

`node --env-file=..\\..\\.env dist\\dev-cli\\m56-b5-rrm-top2-hook-job-apply-cli.js --limit=1 --apply --confirmControlledApply=I_UNDERSTAND --pretty`

**B5 输出摘要**：

| 字段 | 值 |
|------|-----|
| `mode` | `"apply"` |
| `totalJobsRead` | **1** |
| `processedCount` | **0** |
| `skippedCount` | **1** |
| `failedCount` | **0** |
| `noOpReasonCounts` | **`{ "existing_meta_frozen": 1 }`** |
| `candidateUserIdUnchangedAll` | `true` |
| `finalScoreUnchangedAll` | `true` |
| `items[0].outcome` | **`skipped`** |
| `items[0].noOpReasonCode` | **`existing_meta_frozen`** |
| `items[0].errorCode` | **`null`** |

**语义**：

- **`existing_meta_frozen` → `skipped`**（与 **B6-B** 冻结场景一致），**非** `failed`。  
- **`processedCount = 0`**、**`failedCount = 0`**。  
- **未写新 meta**、**未覆盖 frozen meta**（writer no-op，按设计 skip）。  
- **`MatchResult.candidateUserId` / `finalScore`**：**未改**（聚合字段均为 `true`）。

---

## B8 复验（Round 2 之后）— **已完成**

Round 2 的 B5 apply 将唯一 pending 消费为 **`skipped`**（`existing_meta_frozen`）后，已执行 **B8** independent dry-run poller 复验。

**命令模板**：

```text
node --env-file=..\\..\\.env dist\\dev-cli\\m56-b8-rrm-top2-hook-job-dry-run-poller-cli.js --limit=20 --once --pretty
```

**B8 输出摘要**（aggregate-safe，**无**业务 id）：

| 字段 | 值 |
|------|-----|
| `refused` | `false` |
| `mode` | `"dry_run"` |
| `apply` | `false` |
| `sourceVersion` | `"m5.6-b8-rrm-top2-hook-job-dry-run-poller-v1"` |
| `totalJobsRead` | **0** |
| `wouldProcessCount` | **0** |
| `wouldSkipCount` | **0** |
| `noOpReasonCounts` | **{}** |
| `writerOkCount` | **0** |
| `writerNoOpCount` | **0** |
| `candidateUserIdUnchangedAll` | `true` |
| `finalScoreUnchangedAll` | `true` |
| `items` | **[]** |
| `once` | `true` |

**含义**：

- Round 2 的 pending hook job **已被 B5 apply 消费**为 **`skipped`**；当前 **pending 队列清空**（`totalJobsRead = 0`，`items = []`）。  
- **未写新 meta**；**未覆盖 frozen meta**。  
- **`MatchResult.candidateUserId` / `finalScore`**：**未改**。

---

## 修订记录

| 版本 | 说明 |
|------|------|
| **v1.0** | Round 1：占位 B3 失败 + 空队列 B5/B8。 |
| **v1.1** | 保留 Round 1；追加 Round 2（B3 成功 + B5 `existing_meta_frozen` skipped）；B8 Round 2 后复验 **待补**。 |
| **v1.2** | Round 2 后 **B8 复验已完成**（`totalJobsRead=0`、pending 清空语义）。 |

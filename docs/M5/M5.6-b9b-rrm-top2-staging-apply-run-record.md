# M5.6-B9-B — Staging Apply Run Record（B5 Runner）

> **范围**：**运行记录 only**（无业务代码变更）。使用 **现有 B5 apply runner**（`m56-b5-rrm-top2-hook-job-apply-cli.js`），**未**新增 `m56-b9` apply CLI。不含真实 `userId` / `candidateUserId` / `matchResultId` / `hookJobId`、token、`DATABASE_URL`。

---

## 元数据

| 项 | 值 |
|----|-----|
| **日期** | **2026-05-03** |
| **代码基线** | 文档撰写时仓库 **`main`** 已含 B5/B7/B8；与 **B9-A**（`M5.6-b9a-rrm-top2-staging-apply-mode-plan.md`）规划一致。 |
| **构建** | `pnpm exec nest build`（`apps/api`）→ **成功**（退出码 0）。 |

---

## 1. Build 是否成功

**是。**

---

## 2. Staging-style env gates 是否设置

在同一 PowerShell 会话中 **已设置**（名称仅列于此，**无**值写入本文档）：

- `PEIMA_M5_RRM_TOP2_HOOK_POLLER_ENABLED=1`
- `PEIMA_M5_RRM_TOP2_HOOK_POLLER_MODE=apply`
- `PEIMA_M5_RRM_TOP2_HOOK_POLLER_APPLY_CONFIRM=I_UNDERSTAND`
- `PEIMA_M5_RRM_TOP2_META_WRITE_ENABLED=1`
- `NODE_ENV=development`（非 production，满足 B5 / B8 拒绝策略）

**说明（重要）**：当前 **B5 apply runner** 的 gate 仍以 **`NODE_ENV`**、**`--apply`**、**`--confirmControlledApply=I_UNDERSTAND`** 及 **writer 侧 env**（如 `PEIMA_M5_RRM_TOP2_META_WRITE_ENABLED`）为准；**`PEIMA_M5_RRM_TOP2_HOOK_POLLER_*`** 在 B9-A 中为 **staging 操作纪律 / 未来 poller 对齐** 建议项，**本次未验证** B5 可执行文件会读取上述 poller 前缀变量（若 B9-B 需强制耦合，应在后续 **代码** 里程碑实现 env 桥接，**非**本文档范围）。

---

## 3. 是否使用 B5 apply runner，而非新增 B9 CLI

**是。** 仅使用：

`node --env-file=..\\..\\.env dist\\dev-cli\\m56-b5-rrm-top2-hook-job-apply-cli.js --limit=1 --apply --confirmControlledApply=I_UNDERSTAND --pretty`

（路径相对 `apps/api`。）

---

## 4. B3 是否创建 pending hook job

**否（本次自动化运行未成功创建）。**

**命令模板**（占位符，**不得**照抄为有效 id）：

```text
node --env-file=..\\..\\.env dist\\dev-cli\\m56-b3-rrm-top2-hook-job-cli.js ^
  --matchResultId=<LOCAL_MATCH_RESULT_ID> ^
  --viewerUserId=<LOCAL_VIEWER_USER_ID> ^
  --top2CandidateUserIdA=<LOCAL_TOP2_A> ^
  --top2CandidateUserIdB=<LOCAL_TOP2_B> ^
  --top2Fingerprint=local-dev-fixture-b9b ^
  --guardrailsStatus=pass --pretty --apply
```

**结果摘要**：使用 **非库内** 占位 `matchResultId` / 用户 id 试跑时，Prisma 报错 **`P2025`** — *required `MatchResult` not found for nested connect*。故 **未** 产生新 hook job 行。

**字段级意图**（若 B3 成功，输出应含此类语义字段；**无**真实 id）：

| 字段 | 预期语义 |
|------|-----------|
| `created` | `true`（新行） |
| `status` | pending 类状态字符串 |
| `sourceVersion` | 与 B3 runner 默认/传入一致 |
| `top2Fingerprint` | `local-dev-fixture-b9b`（与 CLI 一致） |
| `guardrailsStatus` | `pass`（与 CLI 一致） |
| `candidateUserIdUnchanged` | `true` |
| `finalScoreUnchanged` | `true` |

**Follow-up**：操作者需在 **本地 / staging DB** 中选取 **真实存在** 的 `<LOCAL_MATCH_RESULT_ID>` 等，再执行 B3，方可进入「有 pending → B5 apply」全链路。

---

## 5. B5 apply 输出（本次）

| 字段 | 值 |
|------|-----|
| `refused` | `false` |
| `mode` | `"apply"` |
| `apply` | `true` |
| `sourceVersion` | `"m5.6-b5-rrm-top2-hook-job-apply-runner-v1"` |
| `totalJobsRead` | **0** |
| `processedCount` | **0** |
| `skippedCount` | **0** |
| `failedCount` | **0** |
| `noOpReasonCounts` | **{}** |
| `candidateUserIdUnchangedAll` | `true` |
| `finalScoreUnchangedAll` | `true` |
| `items` | **[]** |

---

## 6. processed / skipped / failed 分布

本次 **无 pending** 可读入 limit 窗口，故 **processed = 0，skipped = 0，failed = 0**。

---

## 7. noOpReasonCounts 是什么

**{}**（空对象）。

---

## 8. 是否出现 existing_meta_frozen

**本次未出现**（无 job 进入 writer / skip 分支）。

若在 **已有 frozen meta** 的 match 上存在 pending，按 B6 记录与 B5 设计，预期 **`skippedCount ≥ 1`** 且 **`noOpReasonCode` = `existing_meta_frozen`** — 留作 **staged follow-up** 复验。

---

## 9. 是否产生 processed

**否**（`processedCount = 0`）。**eligible fresh job → processed** 仍为 **follow-up**（需可写 meta 的 staging 数据与 B3 成功造数）。

---

## 10. 若无 processed 的标注

**eligible fresh job → `processed`**：本次 **未构造**；请在具备合法 `MatchResult` 与 env 的 staging 上补跑并另开 run record 或修订本文件版本。

---

## 11. 是否写新 meta

**本次无 apply 命中 job**，不产生新的 meta 写入路径调用（相对「有 pending 且 eligible」的基线而言：**无写**）。

---

## 12. 是否覆盖 frozen meta

**不适用 / 未发生**（无 processed、无 writer apply 命中）。

---

## 13. 是否改 candidateUserId / finalScore

**否。** B5 输出仍报告 `candidateUserIdUnchangedAll` / `finalScoreUnchangedAll` 为 **`true`**。

---

## 14. 是否接 GET / worker/main.ts

**否。**

---

## 15. B8 复验（pending 是否清空）

**命令模板**：

```text
node --env-file=..\\..\\.env dist\\dev-cli\\m56-b8-rrm-top2-hook-job-dry-run-poller-cli.js --limit=20 --once --pretty
```

**结果摘要**：`totalJobsRead = **0**`，`items = []`，`mode = dry_run`，`once = true`；与 **B8-C** 空队列场景一致。`--limit=20` 在实现中钳为 **10**，对本次 **0 条** 无影响。

**结论**：当前观测下 **无 pending**；**未** 通过本次会话写 HookJob 状态（B3 未成功）、B5 未消费行；**未**写 meta / summary；**未**改 `candidateUserId` / `finalScore`。

---

## 修订记录

| 版本 | 说明 |
|------|------|
| **v1.0** | M5.6-B9-B — 使用 B5 runner 的 staging-style apply 运行记录（占位 B3 失败 + 空队列 B5/B8）。 |

# M4.2 — 批量回归脱敏验收记录（M4.2-M2）

本文档为 **M4.2 批量报告脚本** 在真实 DB 上跑出的 **聚合结果快照**，仅含统计字段，**不含** `viewerUserId`、`poolId`、`candidateUserId` 或 `perSample`。实现与指标定义见 [M4.2-batch-regression-and-quality-metrics.md](./M4.2-batch-regression-and-quality-metrics.md)。

**执行方式（本地）**：`cd apps/api && pnpm exec nest build` 后，在仓库根目录执行  
`node --env-file=.env packages/database/scripts/m4-2-batch-regression-report.mjs --limit 50 --json … --markdown …`。原始 JSON / 本地 Markdown 由根 `.gitignore` 忽略，**不提交**。

**说明**：`--limit 50` 表示最多纳入 **50** 条样本；实际 **`sampleCount`** 取决于 DB 中满足样本定义的行数。下表为当次运行的真实 **`sampleCount`**。

---

## 聚合字段

| 字段 | 值 |
|------|-----|
| `generatedAt` | `2026-05-02T21:10:50.063Z` |
| `sampleDefinition` | `pairwise_pool_finalize_meta.frozen_true_with_existing_preview_pool` |
| `sampleCount` | `1` |

### `metrics`（numerator / denominator / rate）

`rate` 为 `0`～`1` 的小数（与 JSON 报告一致）。

| 指标 | numerator | denominator | rate |
|------|-------------:|--------------:|-----:|
| `staticTop1EqualsMatchResultOriginalRate` | 0 | 1 | 0 |
| `pairwiseEqualsMatchResultRate` | 1 | 1 | 1 |
| `rrmEqualsMatchResultRate` | 1 | 1 | 1 |
| `staticTop1EqualsRrmTop1Rate` | 0 | 1 | 0 |
| `pairwiseSelectedEqualsRrmTop1Rate` | 1 | 1 | 1 |
| `rrmWouldChangeStaticResultRate` | 1 | 1 | 1 |
| `pairwiseWouldChangeStaticResultRate` | 0 | 1 | 0 |

### `missingCounts`

| 键 | 计数 |
|----|-----:|
| `STATIC_SHORTLIST_MISSING` | 0 |
| `MATCH_RESULT_MISSING` | 0 |
| `PAIRWISE_META_MISSING` | 0 |
| `RRM_PROPOSAL_MISSING` | 0 |

### `uniqueCandidateCountDistribution`

| `uniqueCandidateCount`（桶） | 样本数 |
|------------------------------|--------:|
| `3` | 1 |

### `fallbackReasonDistribution`

（在 pairwise present 的样本上统计；`null`/空在脚本中记为 `"none"`。）

| `fallbackReason` | 计数 |
|------------------|-----:|
| `low_decision_confidence` | 1 |

---

## 简短结论

- 当次运行 **`sampleCount` = 1**：在「`frozen === true` 且存在对应 `preview_pools`」定义下，当前环境仅命中一条可对照样本；**不宜**据此推断大盘比例，需在样本量增大后重复跑批。  
- 该样本上：**pairwise / RRM 与落库 `MatchResult` 一致**（`pairwiseEqualsMatchResultRate`、`rrmEqualsMatchResultRate` 均为 1）；**静态 top1 与落库 / RRM top1 不一致**（两率为 0）。  
- **RRM 会改变静态结果**（`rrmWouldChangeStaticResultRate` = 1），**pairwise 不改变静态结果**（`pairwiseWouldChangeStaticResultRate` = 0）。  
- 四源 **`missingCounts` 均为 0**；`fallbackReason` 在该样本为 **`low_decision_confidence`**。

---

## 安全边界

- **不写** `MatchResult`；**不改** `candidateUserId` / `finalScore`。  
- **不改** `displayCandidateUserId`；**不改** `GET /matching/result`。  
- **不改** worker、FinalMatchPage、pairwise finalize。  
- **不改** RRM 公式、evaluator、`extractDPre`。  
- 本记录 **不含** 任何可反查主体的原始 ID，亦 **不含** `perSample` 与完整原始 JSON。

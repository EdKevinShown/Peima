# M4.4 — Admin Observability 第一阶段收口

本文档收口 **M4.4 第一阶段**（M4.4-M0 / M4.4-M1 / M4.4-M1B）：只读观测与脱敏记录已落地；**未**进入 Admin HTTP API、Admin UI、自动修复链路。详细方案见 [`M4.4-admin-observability-plan.md`](./M4.4-admin-observability-plan.md)。

---

## 1. M4.4 目标（阶段内已对齐）

- 建立 **Admin / developer 观测面**，理解 matching 侧车与旁路信号，而非替代主决策。
- 对 **fallback**、**failure**、**finalize**、**pairwise**、**simulation**、与 **batch regression（M4.2）** 相关的指标做 **只读汇总**（CLI + 文档），**不改变最终匹配结果**。

---

## 2. 第一阶段已完成

| 里程碑 | 内容 |
|--------|------|
| **M4.4-M0** | 方案扫描落盘：范围、缺口、观测对象、最小指标、实现路线、边界。 |
| **M4.4-M1** | CLI 只读观测脚本：`packages/database/scripts/m4-4-admin-observability-summary.mjs`（console / 可选 `--json` / `--markdown`）。 |
| **M4.4-M1B** | 脱敏运行记录：[`M4.4-admin-observability-summary-run-record.md`](./M4.4-admin-observability-summary-run-record.md)（仅聚合计数，无 ID 明细）。 |

---

## 3. 关键交付物

| 交付物 | 路径 / 说明 |
|--------|-------------|
| CLI 脚本 | `packages/database/scripts/m4-4-admin-observability-summary.mjs` |
| 方案与 M1 说明 | `docs/M4/M4.4-admin-observability-plan.md` |
| 脱敏运行记录 | `docs/M4/M4.4-admin-observability-summary-run-record.md` |
| 本地报告忽略 | 根目录 `.gitignore`：`docs/M4/M4.4-admin-observability-summary.local.json`、`.local.md` |

---

## 4. 安全边界（重申）

- **不写** `MatchResult`，**不改** `candidateUserId`、`finalScore`、`displayCandidateUserId`。
- **不改** `GET /matching/result` 语义；**不改** worker、**不改** `FinalMatchPage` 用户主路径。
- **不改** pairwise finalize 业务语义；**不改** RRM **公式 / evaluator / extractDPre**。
- **不暴露** raw prompt、LLM body、transcript、**raw `meta`**；观测输出仅安全聚合字段。
- **不提交**真实 ID 明细；本地 `*.local.*` 报告不入库。

---

## 5. 本阶段明确未做

- **未实现** `GET /admin/matching-observability/summary`（或等价 Admin 只读 HTTP 汇总）。
- **未实现** Admin Web 大盘 / 页面。
- **未实现**自动告警、自动 retry、自动 repair；脚本与文档 **无**写库副作用。
- **未让**观测结果 **自动** 影响最终匹配或 worker 排序。

---

## 6. 后续建议（需单独排期与评审）

| 建议项 | 说明 |
|--------|------|
| **M4.4-M2** | Admin **只读** API：与 CLI 共享聚合口径，便于 on-call / 监控拉取。 |
| **M4.4-M3** | Admin **页面**：仅在口径稳定后做可视化，避免与脚本双轨漂移。 |
| **M4.4-M4** | **fallback / retry / repair** 操作设计：凡涉及写库或影响匹配链路的，**必须单独评审**，不得与只读观测混为一谈。 |

---

## 7. 文档索引

- 方案与 CLI：`[M4.4-admin-observability-plan.md](./M4.4-admin-observability-plan.md)`
- 脱敏快照示例：`[M4.4-admin-observability-summary-run-record.md](./M4.4-admin-observability-summary-run-record.md)`
- 本收口说明：`M4.4-admin-observability-closure.md`（当前文件）

---

## 8. 与 M4 总收口的关系

**M4（AI Matching）第一阶段**全阶段收口见：[M4-overall-ai-matching-phase-one-closure.md](./M4-overall-ai-matching-phase-one-closure.md)。

---

*阶段标记：M4.4 第一阶段（M0 + M1 + M1B）收口*

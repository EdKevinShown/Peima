# M4.4-M1 — Observability summary run record（模板）

本文件为 **执行记录模板**：仅描述如何跑脚本与如何归档；**不**内嵌真实环境聚合计数（避免误提交敏感规模数据）。若需留档，请将本地生成的 **`*.local.json` / `*.local.md`** 保留在开发者机器或私有存储，**勿**推送到 Git。

## 命令

在仓库根目录：

```bash
node --env-file=.env packages/database/scripts/m4-4-admin-observability-summary.mjs --limit 500 --since-days 30
```

可选写出报告（已 gitignore）：

```bash
node --env-file=.env packages/database/scripts/m4-4-admin-observability-summary.mjs --json docs/M4/M4.4-admin-observability-summary.local.json --markdown docs/M4/M4.4-admin-observability-summary.local.md
```

## 执行后登记（人工填写）

| 字段 | 值 |
|------|-----|
| 日期（UTC） | |
| 操作者 | |
| `--since-days` / `--limit` | |
| 结论摘要（一句话） | |

## 四源一致率

仍由 **`packages/database/scripts/m4-2-batch-regression-report.mjs`** 与脱敏叙述文档 **`docs/M4/M4.2-batch-regression-sanitized-run-record.md`** 承担；本脚本 **不**重算 M4.1。

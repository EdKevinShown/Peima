# M4.4-M1B — Observability summary run record（脱敏）

本记录仅含 **`m4-4-admin-observability-summary.mjs`** 输出的**聚合计数**与枚举分布，**不含**任何 `userId` / `poolId` / `candidateUserId` / `jobId` / `simulationJobId`，不含 raw `meta`、prompt、LLM body、transcript。原始机器可读报告仍为本地 **`*.local.json` / `*.local.md`**（已 gitignore），**勿提交**。

## 生成命令（可复现）

在仓库根目录：

```bash
node --env-file=.env packages/database/scripts/m4-4-admin-observability-summary.mjs --limit 500 --since-days 30 --json docs/M4/M4.4-admin-observability-summary.local.json --markdown docs/M4/M4.4-admin-observability-summary.local.md
```

## 脱敏聚合快照

| 字段 | 值 |
|------|-----|
| **generatedAt**（脚本写入 UTC） | `2026-05-02T22:46:23.265Z` |
| **sinceDays** | `30` |
| **script sourceVersion** | `m4.4-m1-admin-observability-summary-v1` |

### Pairwise（`ai_pairwise_decision_jobs`，窗口内）

| 指标 | 值 |
|------|-----|
| **status counts** | `succeeded`: 12 · `failed`: 8 |
| **failureDetail.code distribution** | `schema_validation`: 5 · `(null)`: 12 · `json_extract_error`: 1 · `metadata_conflict`: 2 |
| **schema_validation count** | 5 |
| **fallbackUsed rate**（`true` / 窗口内总行数） | 0 / 20 → **0** |
| **fallbackUsed counts** | `true`: 0 · `false`: 12 · `null`: 8 |

### AI simulation（窗口内 jobs；items 归属这些 jobs）

| 指标 | 值 |
|------|-----|
| **jobStatus distribution** | `completed`: 20 |
| **item status distribution** | `succeeded`: 79 · `failed`: 4 |
| **item errorCode distribution** | `(null)`: 79 · `schema_validation`: 4 |
| **schema_validation count**（items，与 errorCode / failureDetail.code 对齐口径） | 4 |
| **failed item count** | 4 |

### Finalize meta（`pairwise_pool_finalize_meta`）

| 指标 | 值 |
|------|-----|
| **frozen true count**（全表，与窗口一致） | 5 |
| **wouldChangeStaticResult rate**（true / 已解析布尔行） | 0 / 5 → **0** |
| **fallbackReason distribution** | `low_decision_confidence`: 4 · `narrow_decision_score_gap`: 1 |
| **pairwiseProposalRecommendation distribution** | `static_top1_fallback`: 5 |
| **appliedToFinalScore true count**（解析行内） | 0 |
| **appliedToWorkerRanking true count**（解析行内） | 0 |

### Match / preview 基数（全表计数）

| 指标 | 值 |
|------|-----|
| **match_results total** | 4 |
| **preview_pools total** | 41 |

## M4.1 / M4.2 对齐说明

四源一致 / 分歧率仍由 **`packages/database/scripts/m4-2-batch-regression-report.mjs`** 产出；本快照**未**重算 M4.1。脱敏叙述可参考 **`docs/M4/M4.2-batch-regression-sanitized-run-record.md`**。

## 登记

| 字段 | 值 |
|------|-----|
| 记录类型 | M4.4-M1B 脱敏落盘 |
| 环境 | 本地开发库（脚本直读 `DATABASE_URL`） |

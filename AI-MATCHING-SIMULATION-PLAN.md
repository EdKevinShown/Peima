# AI 模拟匹配：规划与算法说明

本文档描述一条 **「问卷输出 → 矩阵快速筛选 → 双人契合度倒序列表 → Ditto 式循序渐进模拟漏斗」** 的产品与技术路线，并给出 **P0 可落地的默认参数**（漏斗层级、每轮对话量、AI 协同、批次调用预算）。**阶段 C 默认 `funnel-v2`**：在矩阵筛后的头部对上，用**明显更多的逐层对话轮数**，尽量支撑「两人是否**真的合适**」的模拟判断（仍非现实约会替代，见 **1.2**）。与现有问卷画像模块对齐时，特征来源见 `QUESTIONNAIRE-PROFILE-ALGORITHM.md` 与 `UserProfile` G1-R 字段。

**说明**：文中公式一律用**纯文本**写法，避免 LaTeX（在 Cursor 等编辑器里常无法渲染数学块）。

---

## 1. 目标与非目标

### 1.1 目标

- **快速**：大池子下避免「所有人两两都跑大模型」（复杂度约 **N×N** 条边爆炸）。
- **可排序**：产出 **按契合度从高到低** 的「候选配对」列表（倒序 = 最高分在前）。
- **可解释**：模拟阶段产出「相处片段 + 摩擦点 + 匹配结论」类结构化结果，便于 UI 与复盘。
- **对话量足以支撑判断**：在晋级深层的对上，通过 **L1→L2→L3** 足够多的「双方各一句」轮次（默认 **`funnel-v2`**，见 **6.5**），让价值观、摩擦修复、节奏等信号有时间展开，而不是仅靠极少轮次下结论。
- **可迭代**：矩阵权重、场景脚本、轮次、模型角色数均可版本化（`schemaVersion` / `simulationVersion`）。

### 1.2 非目标（P0 不做）

- 不做法律/婚恋承诺级结论；输出为 **模拟与建议**，需产品免责声明。
- 不替代现有 `batch-match` 规则分（可并行存在，后续再融合）。

---

## 2. 输入特征（来自问卷与画像）

### 2.1 P0 推荐（矩阵用）

- **20 维向量 v**：`UserProfile` 中 G1-R 二十轴连续分；**缺失维（null）一律填 0.5**，再对该行做 **L2 归一化**（见 4.4 节）。
- **标量 `confidence`**：问卷完整度；在矩阵阶段用作 **门控或权重**（见 4.6 节）。

### 2.2 P1 可选（矩阵阶段不接）

Ditto 式模拟阶段 **只** 使用 **20 维 float + `confidence`** 作为人设约束输入；分支层/标签层不进模拟 prompt（后续若要升级，改 `simulationVersion` 另开版本）。

以下保留为 **P1 矩阵或模拟增强** 备选：

- **分支层**：`dominantBranches` / `uncertainBranchesByAxis`
- **标签层**：`displayPrimary` / `labels`
- **聊天 hint**：`dimensionBranchChatHints`

---

## 3. 总体流水线（三阶段）

```
池子用户集 U
  → [A] 硬过滤 + 特征矩阵化
  → [B] 快速契合度打分 + 生成「配对倒序列表」
  → [C] 对列表头部边做 Ditto 式场景模拟 + 结构化输出
```

---

## 4. 阶段 A：硬过滤 + 矩阵化

### 4.1 硬过滤（非 AI）

按产品规则缩小合法配对集合 **E**（示例维度，可按你们数据改）：

- 基础：非本人、非已匹配/黑名单、账号状态可用。
- 业务：地理、年龄差、取向/匹配对象偏好等（若有字段）。

输出：每位用户 **可行邻居集合** `N(i)`（所有能和用户 i 配的用户 j 的集合）。

### 4.2 向量化与矩阵块（与 4.3–4.8 一致）

单人层面的计算与下面「矩阵」章节相同；**池级**实现见 **4.3–4.8** 与 **5.2**（含 **5.2.1** 与 **KNN/ANN** 说明）。  
**快筛对对矩阵**在实现上记为 **`X[i][j]`**：默认 **`X = S`**（**4.6**）；若 `matrixVersion = cosine-length-v1` 则 **`X = Sm`**（**4.6.1**）；若 `matrixVersion = diff-weight-v1` 则 **`X = D`**（**4.7**）；若 `matrixVersion = axis-logic-v1` 则 **`X = G`**（**4.8**）。**5.2** 中凡写 **`S[i][j]`** 处，在启用备选时一律替换为同一 **`X[i][j]`**。

> **P1 备选**：加权点积（不同轴权重）、或加「分支一致」小奖励再归一化。维间交叉项（**20×20** 权重）见 **4.8** 末「后续扩展」。

---

### 4.3 参数汇总（已确认）

| 题号 | 选择 | 含义 |
|------|------|------|
| Q1 | **A** | 缺失维填 **0.5**，再 L2 归一化 |
| Q2 | **A** | 无向对分数 = **`(S[i][j] + S[j][i]) / 2`**（与本方案下对称矩阵等价，见 5.2 注） |
| Q3 | **A** | **K = 32**，**M = 200**（每批最多做完整模拟的对数） |
| Q4 | **A** | 模拟仅 **20 维 float + confidence** |

**`matrixVersion`（快筛用，与上表独立）**：默认走 **4.6 余弦 `S`**；可选 **`cosine-length-v1`**（**4.6.1** 方向 + 行模长 **`Sm`**）、**`diff-weight-v1`**（**4.7** 差分 **`D`**）、**`axis-logic-v1`**（**4.8** 轴逻辑 **`G`**）。均与 **5.2** 共用同一套 Top-K / 排序，见 **4.2** 记号 **`X`**。

---

### 4.4 原始特征矩阵 **F**（n × 20）

设池子里有 **n** 个用户，按固定顺序编号 `i = 0 .. n-1`（顺序可为 `userId` 字典序或 `updatedAt`，**整次批处理内不变**）。

1. 从 `UserProfile` 取 G1-R 二十轴，与代码中 `G1R_PROFILE_KEYS` 顺序一致（共 20 列）。
2. 构造矩阵 **F**，形状 **n 行 × 20 列**：

```
F[i][d] =  数据库中用户 i 的第 d 个轴的 float 值   若不为 null
F[i][d] =  0.5                                   若为 null
```

3. **全 null 极端行**（理论上极少）：若某行 20 维全是 null（填完后全 0.5），则 L2 范数为 `sqrt(20 * 0.5^2) > 0`，仍可归一化；若业务上存在「全轴无数据」用户，建议 **在硬过滤阶段剔除**，不进入矩阵。

---

### 4.5 归一化矩阵 **V**（n × 20）与基础相似度矩阵 **S0**（n × n）

**行归一化（L2）**

对每个用户行 **i**：

```
w_i = sqrt( F[i][0]^2 + F[i][1]^2 + ... + F[i][19]^2 )
V[i][d] = F[i][d] / w_i     // 若 w_i==0 则该行不参与配对或按业务兜底，正常情况不出现
```

**基础相似度（余弦）**

令 **V** 的每一行是单位方向（范数约为 1）。定义 **S0** 为 **n × n**：

```
S0[i][j] = sum_{d=0..19}  V[i][d] * V[j][d]
```

等价于矩阵乘法：**S0 = V × V^T**（`V^T` 为转置；numpy 里 `S0 = V @ V.T`）。

性质：

- `S0[i][j] = S0[j][i]`（对称）
- `S0[i][j]` 在 **[-1, 1]**，表示两用户画像方向相似度
- 对角线 `S0[i][i] = 1`（自己与自己）

---

### 4.6 置信加权矩阵 **S**（n × n）

对每个用户 **i** 读取问卷 `confidence` 记为 **c_i**（0～1）。

```
S[i][j] = S0[i][j] * min(c_i, c_j)
```

性质：

- 仍对称：`S[i][j] = S[j][i]`
- 对角线：`S[i][i] = 1 * min(c_i, c_i) = c_i`（自配对在后续会排除）

**`S` 即默认 **`matrixVersion`** 下「矩阵快筛」使用的对对矩阵（**`X=S`**，见 **4.2**）。启用 **4.6.1 / 4.7 / 4.8** 时 **`X`** 分别为 **`Sm` / `D` / `G`**。  
**在仅 `S` 时**：快筛分**不显式**使用「整条 **`F` 行**的 L2 模长」——模长已在行归一化中消去，只剩方向；**`confidence`** 是问卷完整度，不是性格强弱。要在**同一余弦框架下再显式看强弱**，用 **`cosine-length-v1` → `Sm`（4.6.1）**。

---

### 4.6.1 备选：方向 + 行模长（`matrixVersion = cosine-length-v1`）

当希望在 **余弦（形状/方向）** 之外，再显式考虑 **整条画像在数值上的「强弱」是否接近**（避免「一人两维都极高、另一人两维都极低但比例相同仍得满分」这类纯余弦行为）时，可采用本分支。仍基于 **4.4** 的 **`F`** 与 **4.5** 的 **`S0`**（**`V`** 不变）。

#### 行模长与「强度」标量

对每用户 **i**，令 **`len_i`** 为 **未归一化**行的 **L2 模长（与 4.5 中 `w_i` 相同）**：

```
len_i = sqrt( F[i][0]^2 + ... + F[i][19]^2 )
```

各维在 **[0,1]** 时 **`len_i ∈ [0, sqrt(20)]`**。定义归一强度：

```
mag_i = clamp( len_i / sqrt(20), 0, 1 )
```

对称的 **「强度接近度」**（越大表示两人整体强弱越接近）：

```
M[i][j] = 1 - abs( mag_i - mag_j )     // ∈ [0,1]
```

#### 方向项（与余弦同尺度融合）

将 **4.5** 的 **`S0[i][j]`**（**[-1,1]**）映到 **[0,1]**：

```
C[i][j] = ( S0[i][j] + 1 ) / 2
```

#### 合成 **`Sm`**（二选一，版本内写死或子版本区分）

**乘性（默认推荐，「方向好且强弱接近」才得高分）**

```
Sm_raw[i][j] = C[i][j] * M[i][j]
```

**凸组合（`λ` 可调，方向与强弱可折中）**

```
Sm_raw[i][j] = λ * C[i][j] + (1 - λ) * M[i][j]     // λ ∈ [0,1]，如 **0.65** 表示更偏重方向
```

置信加权与 **4.6** 一致：

```
Sm[i][j] = Sm_raw[i][j] * min(c_i, c_j)
```

**`Sm` 对称**，可作 **5.2** 中的 **`X`**。实现提示：**`len_i==0`** 时按业务兜底 **`mag_i`** 或剔除该行。

---

### 4.7 备选：按维度「差分」加减分算总分（`matrixVersion = diff-weight-v1`）

当前 **4.4–4.6** 余弦支路 **`S`** 是 **仅方向**；**4.6.1** 的 **`Sm`** 在方向之外再看**行模长**。若你希望改成 **「每一维看双方差多少，再按运营手工权重加减分」**，可用下面 **`D`**；切换时只改矩阵构造，**5.2 节** 里把 **`X[i][j]`** 换成 **`D[i][j]`** 即可。

#### 输入

- 仍用 **4.4 节** 的 **`F`**（null 已填 **0.5**），**不强制**先做行归一化（差分通常在 **原始 0～1 刻度**上更直观）。
- 一条 **手工权重向量** `w[0..19]`，建议 **`w[d] >= 0`** 且 **`sum_d w[d] = 1`**（方便把「总分」理解成加权平均惩罚）。
- （可选）每人仍有 **`confidence`**，用于与现方案一致的门控：**`D[i][j] *= min(ci, cj)`**。

#### 逐维差分

对每个维度 **d**：

```
delta[i][j][d] = abs( F[i][d] - F[j][d] )
```

`delta` 在 **0～1** 之间（两用户在该轴上越接近，差越小）。

#### 惩罚项（线性加权，最易实现）

```
penalty[i][j] = sum_{d=0..19}  w[d] * delta[i][j][d]
```

在 `w` 归一化且每维 `delta<=1` 时，**`penalty` 落在 0～1**：  
- 全轴完全一致 → **penalty = 0**  
- 权重都压在「差异最大」的维上时，**penalty** 反映「你们在这些看重维度上差多少」。

#### 总分（契合分，越大越好）

把惩罚翻成「相似分」：

```
rawMatch[i][j] = 1 - penalty[i][j]
D[i][j] = rawMatch[i][j] * min(ci, cj)    // 若不用 confidence，可省略乘法
```

- **`D[i][j]`** 对称，取值约在 **0～1**（与余弦分支不同，这里是 **「1 减去加权平均绝对差」**）。
- 后续 **Top-K、排序、漏斗模拟** 与现稿相同，只是把矩阵从 **`S`** 换成 **`D`**。

#### 「加减分」怎么理解成运营可手调

- **想强调某轴**：把对应 **`w[d]` 调大** → 该轴差一点点 **`penalty` 就涨得快** → **`rawMatch` 掉得快**。  
- **想弱化某轴**：**`w[d]` 调小或置 0`**。  
- 若要坚持「先加固定底分再扣分」，可写成等价形式：  
  `score = BASE - sum_d w[d] * delta`（例如 **BASE=1** 即上式；**BASE=100** 则再整体 `/100` 归一到 0～1 便于和 UI 对齐）。

#### 与余弦方案的关系（选型提示）

| | 余弦 `S`（默认） | 差分 `D`（本备选） |
|--|-------------------|-------------------|
| 含义 | 全局方向是否「同向」 | 逐轴数值是否「接近」 |
| 对「整体像但某关键轴相反」 | 可能仍给中高分 | 可把该轴 **w 拉大** 强烈降分 |
| 对「各轴都中等偏」 | 可能接近 | 差分通常更敏感 |

**实现提示**：**`S`/`Sm`/`D`/`G`** 均可先算满 **`n×n`** 再喂 **5.2**；或只对 Top 候选边稀疏算 **`delta`** / **`contrib_d`**，结果一致。

---

### 4.8 备选：按轴逻辑自定义契合分（`matrixVersion = axis-logic-v1`）

当业务上需要 **同一维上区分「越近越好 / 一高一低才好 / 双高才好 / 双低要惩罚」** 等**不同语义**，且不想被 **4.5–4.6 纯余弦**（不显式看行模长）、**4.6.1 方向+模长**（全局标量强弱）、或 **4.7 线性差分**（纯接近）单独绑死时，可采用本方案：对 **每一维 d** 从 **`(F[i][d], F[j][d])`** 映射到对称的 **`contrib_d`**，再聚合成 **`n×n`** 的 **`G[i][j]`**，供 **5.2** 与余弦、差分分支**同一套 Top-K / 排序**使用。**P0 默认用连续隶属度 + softAND/OR（见下节）**；硬布尔谓词仅作**对照**。

#### 与 **F / V** 的关系

- **隶属度与分区**在 **4.4** 的原始刻度 **`F[i][d] ∈ [0,1]`**（null 已填 **0.5**）上定义，便于运营理解「高 / 低 / 中间带」。  
- **不必**先做 **4.5** 的 L2 行归一化再算 **`G`**；若同一批次混用余弦与轴逻辑，代码里可维护两套：**`V`** 用于 **`S`**，**`F`** 用于 **`G`**。

#### 每维：推荐「隶属度 + 软逻辑」（P0 默认，稳定平滑）

对固定维 **d**，设 **`x = F[i][d]`**，**`y = F[j][d]`**，均在 **[0,1]**。要求 **`contrib_d(x,y) = contrib_d(y,x)`**，以保证 **`G[i][j]=G[j][i]`**，与 **5.2** 无向分、Top-K 叙述一致。

**1）平滑「偏高 / 偏低」隶属度（梯形，可调带宽）**

每维配置 **`0 < τ_lo < τ_hi < 1`**，再各给带宽 **`δ_hi, δ_lo > 0`**（如 **0.05～0.1**）。**`clamp(t,0,1)`** 表示截断到 **[0,1]**：

```
m_hi(x) = clamp( (x - (τ_hi - δ_hi)) / δ_hi, 0, 1 )      // 从 τ_hi-δ_hi 到 τ_hi，从 0 平滑升到 1
m_lo(x) = clamp( ((τ_lo + δ_lo) - x) / δ_lo, 0, 1 )       // 从 τ_lo+δ_lo 到 τ_lo，从 0 平滑升到 1
m_mid(x) = clamp( 1 - m_hi(x) - m_lo(x), 0, 1 )           // 中间带（可选与其它项混合）
```

**带宽提示**：应配置使 **`τ_hi - δ_hi`** 与 **`τ_lo + δ_lo`** 不交叉（**`τ_hi - δ_hi ≥ τ_lo + δ_lo`**），否则 **`m_hi` 与 `m_lo`** 会同时偏高，语义上「又高又低」冲突；若业务刻意要重叠过渡区，可缩小 **`δ`** 或只用 **`m_mid`** 吸收中间语义。

**2）软逻辑门（fuzzy 与/或/异或）**

对任意 **`a,b ∈ [0,1]`**：

```
softAND(a,b)  = min(a, b)
softOR(a,b)   = max(a, b)
softXOR(a,b)  = a + b - 2*a*b          // {0,1} 上为布尔 XOR；(0,1) 内光滑
```

**3）按 `mode` 合成该维 `contrib_d`（建议映射到 [0,1]）**

记 **`s_sim = 1 - abs(x - y)`**。

| `mode`（示例） | 对称 **`contrib_d(x,y)`** |
|----------------|---------------------------|
| **`similar`** | **`s_sim`** |
| **`both_high`** | **`softAND( m_hi(x), m_hi(y) )`** |
| **`both_low_penalty`** | 令 **`p = softAND(m_lo(x), m_lo(y))`**，再 **`contrib = 1 - α * p`**，**`α∈(0,1]`** |
| **`complement`** | **`softOR( softAND(m_hi(x), m_lo(y)), softAND(m_lo(x), m_hi(y)) )`** |
| **`mid_neutral`** | 如 **`m_mid(x) * m_mid(y)`** 等，再与上式按 **`β∈[0,1]`** 混合 |

多目标时可用 **`contrib_d = Σ_k β_k * term_k`**（**`β_k≥0`**, **`Σβ_k=1`**）。无项命中时用 **`default_d`**（如 **0.5**）。

---

#### 硬阈值布尔（可选：调试对照，不推荐作线上唯一实现）

取 **`H(x)=1[x>τ_hi]`**、**`L(x)=1[x<τ_lo]`** 等与 **AND/OR/XOR** 组合时在 **`τ`** 处**不连续**；**双低 / 中间带** 须显式 **ELSE**。**线上默认**用**上一小节**的 **`m_hi/m_lo` + softAND/OR**。

---

每维输出 **`contrib_d(x,y) ∈ [0,1]`**（或再经 **`rawG`** 内权重归一）。

#### 聚合为 **`G[i][j]`**

```
rawG[i][j] = sum_{d=0..19}  w[d] * contrib_d( F[i][d], F[j][d] )    // 与上文 x,y 记号一致
G[i][j]    = normalize( rawG[i][j] ) * min(c_i, c_j)    // normalize 可选：例如除以 sum_d w[d] 映射到 0～1
```

- **`w[d]`**：非负权重，**`sum_d w[d] = 1`** 时 **`rawG`** 可自然理解为 **0～1** 的加权平均（若每个 **`contrib_d`** 已在 **0～1**）。  
- **`confidence`**：与 **4.6** 一致，用 **`min(c_i, c_j)`** 整体降权不完整问卷对。

**`G` 即该 `matrixVersion` 下进入 5.2 的 `X`。**

#### 配置形态（实现提示）

- 每维可列为：**`mode`**（**`similar` / `complement` / `both_high` / `both_low_penalty` / `mid_neutral` / `custom_expr`**）、**`τ_hi, τ_lo, δ_hi, δ_lo`**、**`α`（双低惩罚强度）**、**`w[d]`**。  
- **`custom_expr`**：须**对称**于 **`(x,y)`**；在安全沙箱或白名单算子内求值（仅允许 **`x,y`、常数、`min,max,abs,clamp`** 等）。  
- **测试**：对每维扫网格 **`(x,y) ∈ [0,1]^2`** 出热力图，检查 **双低 / 双高 / 对角 / 互补带** 是否符合预期。

#### 与余弦、差分的关系（选型）

| | 余弦 **`S`** | 方向+模长 **`Sm`**（**4.6.1**） | 差分 **`D`** | 轴逻辑 **`G`** |
|--|--------------|------------------------------|--------------|----------------|
| 核心问句 | 归一化后方向像不像？ | 方向（**`C`**）与整体强弱（**`M`**）同时进分 | 每维数值近不近？ | **每维**「要什么图案」？ |
| 强弱 / 双高双低比例 | 不显式看行模长 | **显式**看 **`mag`** 接近度 | 看逐维差 | 看 **`m_hi/m_lo`** 等 |
| 实现成本 | 低（矩阵乘） | 低（在 **`S0`** 上多 O(n) 标量） | 低 | 中（配置 + 连续式） |

#### 后续扩展：维间关联（可选 **P1**）

若出现 **「仅在维 a 为互补且维 b 不双低时，维 c 才加分」** 这类规则，可在 **`rawG`** 之上再加少量 **交叉项** 或第二层 **`h( contrib_0..19 )`**；**P0** 建议先 **各维独立** 的 **平滑 `contrib_d`（4.8 默认小节）**，有数据后再加 **稀疏 `W_ab`**（见 **4.2** P1 提示）。

---

## 5. 阶段 B：倒序双人契合度列表

### 5.1 为什么不全量 N×N 模拟

- 大模型成本与延迟随边数爆炸；矩阵阶段应 **纯数值/矩阵运算**（CPU/GPU 均可，易批处理）。

### 5.2 推荐算法：硬过滤 + Top-K 稀疏化 + 无向对分数 + 全局排序

以下用 **对对矩阵 `X`**（见 **4.2**）：默认 **`X=S`**（**4.6**）；`matrixVersion = cosine-length-v1` 时 **`X=Sm`**（**4.6.1**）；`matrixVersion = diff-weight-v1` 时 **`X=D`**（**4.7**）；`matrixVersion = axis-logic-v1` 时 **`X=G`**（**4.8**）。为与旧稿对齐，下文仍可出现符号 **`S[i][j]`**，实施时一律读作 **`X[i][j]`**。**`matrixScore` 定义不变**。记 **硬过滤**：`allowed(i, j)` 为真当且仅当用户 **j** 属于 **i** 的可行邻居集 `N(i)`，且 **i** 属于 `N(j)`（无向合法）；并排除 `i == j`。

#### 术语：有向分、无向对、无向分（`matrixScore`）

- **有向边 / 有向分**：**Top-K** 步骤里出现的是 **(i → j)** 有向边；边上的分 **`score_ij = X[i][j]`** 可理解为「在矩阵里 **i 与 j** 这一条**有序索引**上的值」。**`X` 对称时** **`X[i][j] = X[j][i]`**，数值与箭头方向无关，但**构图**上仍区分 **i→j** 与 **j→i**（两人各自提名自己的 Top-K）。
- **无向对**：无序对 **`{A,B}`** 在实现里规范成 **`a = min(A,B), b = max(A,B)`**；把多人产生的有向边并集时，**`(i,j)` 与 `(j,i)` 合并为同一无向对**，避免重复进入排序列表。
- **无向分**：对无向对 **`(a,b)`** 定义的 **`matrixScore(a,b) = (X[a][b] + X[b][a]) / 2`**。**当 `X` 对称**（**4.6 / 4.6.1 / 4.7** 及 **4.8** 推荐平滑 **`contrib_d`** 时成立）时 **`matrixScore(a,b) = X[a][b]`**，与把谁写在左边无关，**`L` 中排序用的就是这个无向分**。若将来使用**非对称** `X`，仍保留该平均式或改用业务规定的对称化 **`sym(i,j)`**，并在 **`matrixVersion`** 子版本里写清。

#### 步骤 1：每人保留 Top-K 条有向候选边（K=32）

对每个用户 **i**：

1. 构造候选集合 `C_i = { j | j != i 且 allowed(i,j) }`。
2. 对 `C_i` 中每个 **j**，查矩阵元素 **score_ij = X[i][j]**（**`S`/`Sm`/`D`** 及 **4.8 推荐构造下的 `G`** 均为对称，故 **`X[j][i] = X[i][j]`**）。
3. 将 `C_i` 按 **score_ij 从高到低** 排序，只保留前 **K=32** 个 **j**，得到有向边集合：  
   `E_i = { (i, j_1), (i, j_2), ..., (i, j_K) }`（若候选不足 K，则全保留）。

#### 步骤 2：合并所有用户的 Top-K 边，得到无向候选对集合

初始化空集合 **P**（元素为无向对）。

对每个用户 **i**，对 `E_i` 中每条有向边 **(i, j)**：

- 令 **a = min(i, j)**，`b = max(i, j)`，把无向对 **(a, b)** 插入 **P**（用集合或哈希去重，避免重复插入）。

**直觉**：只要 **i** 把 **j** 放进自己的 Top-K，或 **j** 把 **i** 放进自己的 Top-K，该对就会进入 **P**（并集，不是必须双向都在对方 Top-K）。

#### 步骤 3：无向对的最终矩阵分（取平均）= **无向分**

对 **P** 中每个无向对 **(a, b)**（`a < b`）：

```
matrixScore(a, b) = ( X[a][b] + X[b][a] ) / 2
```

**注**：**`matrixScore` 即上文定义的「无向分」**。**`X` 对称**时 **`X[a][b] == X[b][a]`**，上式等于 **`X[a][b]`**。仍写平均是为了与「未来若改为非对称 `X`」兼容。

#### 步骤 4：全局倒序排序（所有进入 P 的对）

将 **P** 中所有无序对按 **matrixScore 从高到低** 排序：

- 若分数相同，可用二级键打破平局（建议：**(userId_a, userId_b) 字典序**，保证排序稳定、可复现）。

输出列表 **`L`**：`[ (a, b, matrixScore), ... ]`，**第一名 = 在集合 P 内矩阵意义下最契合的一对**。

**与「全员两两全排序」的关系（重要）**  
每人只保留 **Top-K**，因此 **`L` 排序的是稀疏并集 `P` 里的对**，不是全池 **N×(N-1)/2** 条边。若业务明确要求 **全量两两榜单**：把步骤 1 改为「对每个 **i**，取 **C_i 全部邻居**（等价 **K = n-1** 或取消 Top-K）」，步骤 2–4 不变，即可对 **所有 allowed 无向对** 用同一 **matrixScore** 排序。

#### 步骤 5：与阶段 C（模拟）的衔接（M=200）

只对 **`L` 中前 M=200 条** 无向对 **进入模拟漏斗第一层（L1）**；后续按 **6.3–6.5 节** 逐级淘汰，**并非每对都跑满全部分层**；其余对若需要可只存 **matrixScore** 不落模拟。

#### 复杂度（便于实现选型）

- 构造 **F** 与 **`X`**：**余弦支路**为 **F, V, S0, S**，主要成本 **O(n^2 * 20)** 若朴素算 **`S0`**；**轴逻辑支路**为每对每维 **`contrib_d`**，朴素亦为 **O(n^2 * 20)**。**n** 很大时对 **`X`** 可用近似近邻（ANN）替代全量算分，但 **5.2** 排序逻辑不变。
- Top-K 每用户：O(度 log 度) 或 O(n log n) 若每用户扫全体邻居。
- 排序 **|P|** 条：`|P| <= n*K` 量级（常数因子），**O(n K log(nK))**。

### 5.2.1 「每人 K 个最近向量」与 Top-K；以及互补的数学接入

#### 与 **Top-K（步骤 1）** 的关系

- 把每人 **20 维**写成向量后，**「找离自己最近的 K 个向量」**是否等价于文档里的 **Top-K**，**完全取决于「距离」是否与你选的 `X` 单调一致**。  
- **余弦默认支路**：**`V[i]`** 为单位向量，**`S0[i][j] = V[i]·V[j]`**。此时有恒等式 **`||V[i]-V[j]||^2 = 2 - 2 * S0[i][j]`**（欧氏平方距离）。故 **「按 `S0` 从高到低取 K」** ⇔ **「在 `V` 空间里按欧氏距离取 K 个最近邻」**（顺序相反但同一批邻居）。**仅换「KNN」说法而不改度量，不会自动比 Top-K 更好**，只是实现可改用 **ANN（近似最近邻）** 在 **`n` 很大**时避免朴素 **O(n^2)**。  
- **若用未归一化的 `F[i]`** 在 **[0,1]^20** 上算 **欧氏最近 K**：与 **`V`+余弦** 的邻居集**一般不同**——此时「近」含**绝对强弱**，更接近直觉上的「数值离得近」，语义上接近 **`Sm`（4.6.1）** 或 **`D`（4.7）** 一类，**需在 `matrixVersion` 里显式选定**，避免与 **`S`** 混谈。

#### 互补等「非纯相似」如何用数学接进同一套流程

**「近邻」本身只表达相似**；互补要在 **`score(i,j)`** 里**单独定义**，再选一种与 **`X` 一致**的检索方式：

1. **双向量（自我 / 期望）**（**P1**，问卷能拆时）：每人 **`p_i`（我是）**、**`q_i`（我要）`**。互补可定义为 **`sim(p_i, q_j)`** 与 **`sim(p_j, q_i)`** 的对称组合；**K 近邻**应在 **`q` 对 `p` 的空间**或**拼接向量**上建索引，而不是只在 **`p_i` 与 `p_j`** 之间比「近」。  
2. **先轴变换再入向量**：对需要互补的维，先用 **4.8** 的 **`contrib_d`**（如 **`complement`**）把 **`(x,y)`** 压成标量或低维特征，再**拼回**固定维向量；在该向量上做 **KNN** 或先算 **`G`** 再 **Top-K**，语义上「互补已进特征」。  
3. **双分数合成**：例如 **`X = α * X_near + (1-α) * X_comp`**，其中 **`X_near`** 来自 **`V` 余弦或欧氏**，**`X_comp`** 来自 **`G`**（或另一套对称 **`contrib`**）；**Top-K** 在合成后的 **`X`** 上做。或**两段**：先用 **`G`** 或硬规则缩池，再在子集上做 **KNN**。  
4. **度量学习（P1）**：学 **`M`（半正定）** 使 **`dist(i,j) = sqrt((v_i-v_j)^T M (v_i-v_j))`** 在标注上「好对（可含互补）」更近——可行，但需**数据与版本号**，本 P0 文档不展开。

**结论**：**能用数学表达互补**，并与 **KNN / ANN** 并存；关键是**先定义 `X`（或分段管线）**，再令 **「最近邻」所用的距离与 `X` 排序一致**。**不要**默认「**`p_i` 与 `p_j` 欧氏最近**」会自动奖励互补。

### 5.3 列表条目结构（建议）

每条至少包含：

- `userA`, `userB`
- `matrixScore`（见 **4.6 / 4.6.1 / 4.7 / 4.8** 与 **`matrixVersion`**）
- `matrixVersion`（权重/公式版本）
- `hardFilterPassReason`（可选，调试）

---

## 6. 阶段 C：Ditto 式循序渐进模拟漏斗（`simulationVersion = funnel-v2`，P0 默认）

本节将模拟从「平铺三场景」升级为 **由浅入深的漏斗**：越往后样本越少、单对成本越高，整体更接近 Ditto「先 vibe、再对话、再价值观与场景」的路径。产品上希望在**晋级到深层的对上**积累**足够多的对话轮次**，再综合 **Evaluator** 输出判断「是否真合适」，因此 **P0 默认采用 `funnel-v2`**（每层轮数见 **6.5**）。若批次成本更敏感，可切换 **`funnel-v1`**（轻量，见 **6.5.1**）。矩阵与排序见 **4.3**、**4.8**（若启用轴逻辑）、**5.2**；人设输入为 **20 维 float + `confidence`**（与 2.2 节一致）。

### 6.1 与参考架构的对应关系

| Ditto 概念 | 本方案落点 |
|------------|------------|
| Building Persona | 问卷写库 + 模拟 prompt 约束（不做照片吸引力分析，可 P1） |
| Pre-Date / Pool | 快筛矩阵 **`X`（S / Sm / D / G）** + 列表 **L**，取 Top **M** 进入漏斗 |
| Date Simulations（渐进） | **`funnel-v2`**：**L1 → L2 → L3**，逐层淘汰；默认 **5 / 7 / 12** 轮/层（**6.5**），轻量见 **`funnel-v1`（6.5.1）** |
| Rank & Approve | **L3 结束后**按终评排序，产出短名单 |
| Date Planning | **P1**：邀约话术/日程草案（本 P0 不写死实现） |
| Memorize + Iterate | **P1**：反馈回写画像或权重（本 P0 仅占位） |

### 6.2 触发范围（入口规模）

- 对 **`L` 前 M=200** 对 **全部跑 L1**（相对 L2/L3 仍偏浅，但 **`funnel-v2`** 下 L1 已有 **5** 轮对话，非极简）。  
- **L2 / L3** 仅对 **6.4 节**筛选后的子集继续；未晋级对持久化 **L1 分数与摘要** 即可。

### 6.3 漏斗总览（数据流）

```
列表 L（matrixScore 倒序）
    │ 取 Top M=200
    ▼
┌───────────────────────────────────────┐
│ L1 第一印象 + Vibe（默认 funnel-v2）   │  每对 5 轮对话 + 1 次关卡评估
└───────────────────┬───────────────────┘
                    │ 保留前 M1=120（按 L1 分数）
                    ▼
┌───────────────────────────────────────┐
│ L2 对话流 + 轻摩擦（中）               │  每对 7 轮 + 1 次关卡评估
└───────────────────┬───────────────────┘
                    │ 保留前 M2=60
                    ▼
┌───────────────────────────────────────┐
│ L3 价值观 + 场景模拟（深）             │  每对 12 轮 + 1 次终评
└───────────────────┬───────────────────┘
                    │ 全量排序
                    ▼
            短名单 Top M3=30（待 approve / 曝光）
```

### 6.4 晋级规则（默认：按分层分数截断 + 可选地板）

对每一层结束时的评分 **`stageScore`**（由该层 **Evaluator** 输出，范围建议标到 **[0,1]**）：

| 边界 | 默认值 | 规则 |
|------|--------|------|
| 进入 L1 的对数 | **M = 200** | 即 **L** 前 200 条 |
| L1 → L2 | **M1 = 120** | 跑完 L1 后：先剔除 **`stageScore_L1 < 0.45`** 的对（**地板**，默认 **开启**）；在剩余对中按 **`stageScore_L1` 从高到低** 取前 **120** 进入 L2；若剩余不足 120，则 **有多少进多少**。 |
| L2 → L3 | **M2 = 60** | 跑完 L2 后，按 **`stageScore_L2` 从高到低** 取前 **60**；可选地板 **L2 < 0.50** 不晋级（默认 **关闭**，避免误杀；若开启写入 `simulationVersion` 子版本）。 |
| L3 结束短名单 | **M3 = 30** | 跑完 L3 后，按 **`finalScore`（终评）** 倒序取前 **30** 对作为 **Rank 输出**（产品可再人工 approve）。 |

**淘汰率目标（非硬约束，仅验收参考）**：L1 末约 **40%** 出局；L2 末约 **50%** 在晋级者中再出局；L3 全跑完再截 **Top30**。

### 6.5 各层「场景」定义（`funnel-v2`，P0 默认）

**约定**：每层内 **1 轮 = 双方各 1 句**；每层 **每轮 1 次 LLM 调用** 同时生成 A/B 两句（与旧版 6.x 一致，便于解析 JSON）。

| 层 | 主题（对齐 Ditto 渐进） | 每对 **对话轮数** | 该层结束 **Evaluator 次数** |
|----|-------------------------|-------------------|-----------------------------|
| **L1** | **First Impression + Vibe**：初见、氛围、是否愿意继续聊 | **R_L1 = 5**（共 10 条消息） | **1**（输出 `stageScore_L1`、redFlags 计数、是否建议晋级） |
| **L2** | **Conversation Flow + 轻摩擦**：日程冲突/误解类小摩擦，看修复与节奏 | **R_L2 = 7**（共 14 条消息） | **1**（`stageScore_L2`） |
| **L3** | **Shared Values + Scene**：金钱/家庭/边界/中长期取向；可多节拍推进 + 场景收尾 | **R_L3 = 12**（共 24 条消息） | **1**（`finalScore` + `greenFlags` / `redFlags` / `recommendedNextStep`） |

**`funnel-v2` 设计意图**：在仍保持「浅→深」漏斗与 **M / M1 / M2** 截断的前提下，**显著增加打满三层时的对话轮数**（相对旧 **`funnel-v1`**），让节奏、摩擦修复、价值观与场景线索有时间展开，从而更接近「判断两人**是否真的合适**」所需的信息量；**并非**宣称已等价真人长期相处。

三层合计（打满 **L1→L2→L3**）：**5 + 7 + 12 = 24** 轮生成 + **3** 次评估 = **27 次 LLM 调用 / 对**。大量对仍会在 L1/L2 提前结束，仅晋级深层的对承担更高轮次。

**批次粗算（`funnel-v2`，默认 M / M1 / M2 / M3 不变）**

- L1：`200 × (5 + 1) = 1200` 次调用  
- L2：`120 × (7 + 1) = 960` 次  
- L3：`60 × (12 + 1) = 780` 次  
- **合计约 2940 次 / 批**（高于 **`funnel-v1`** 的 **1380** 次 / 批，换来更深对话；仍低于「200 对整批打满 **27** 次」的 **`200×27=5400`**，因漏斗淘汰。）

### 6.5.1 轻量递进漏斗 `funnel-v1`（备选）

成本敏感或灰度时可采用；**5.2** 入口规模不变，仅每层轮次减少。

| 层 | 每对 **对话轮数** | 该层结束 **Evaluator** |
|----|-------------------|------------------------|
| L1 | **R_L1 = 2**（4 条消息） | **1** |
| L2 | **R_L2 = 3**（6 条） | **1** |
| L3 | **R_L3 = 4**（8 条） | **1** |

打满全漏斗：**2+3+4 = 9** 轮 + **3** 次评估 = **12 次 LLM 调用 / 对**；批次粗算 **合计约 1380 次 / 批**（同前稿）。

### 6.5.2 为何不采用「每阶段约 50 轮」这类极深对话

若指 **L1、L2、L3 每一层都对同一对跑满约 50 轮**（仍按本文「1 轮 = 双方各 1 句、每轮 1 次生成调用」），在 **当前批量漏斗（M=200 进 L1、再截断到 M1/M2）** 的前提下，**一般不作为 P0 现实默认**，原因如下。

1. **调用量与成本**  
   - 仅 **L1**：**`200 × (50 + 1) = 10200`** 次生成+评估量级，已约为 **`funnel-v2` 整批（约 2940）** 的 **3 倍以上**，而 L1 仍是入口最广的一层。  
   - 若三层各 **50** 轮且大量对打满，整批调用会再上一个数量级；与「矩阵先筛、模拟只做头部」的**成本控制**目标冲突。

2. **边际收益递减**  
   - 无新场景节拍、无新评估维度时，轮数堆到几十轮以后，模型容易**重复套路**，对「是否真合适」的**额外可区分信号**往往变弱；更划算的是 **少而结构化**的推进（明确冲突点、修复尝试、价值观切口），而不是同一层内线性堆轮数。

3. **上下文与工程**  
   - 轮数极高时，完整 transcript 会逼近或超过常见模型的**上下文窗口**，必须引入**摘要再注入、滑动窗口、分层记忆**等机制，否则后期轮次质量下降；这已超出「只加 R」的轻量改动，应单独版本化（例如 `simulationVersion` 子版本 + 专用记忆管线）。

4. **若产品仍要「极深」**  
   - 更现实的路径是：**只对极少数终局候选**（例如已进 **L3** 且 **`finalScore` 靠前**的 **Top N**）再开「加深包」或第二模拟版本，而不是对 **200 进 L1** 的每一对都做 **50** 轮。

**结论**：「每阶段 **50** 轮」不是数学上不可能，但在**批量匹配 + 漏斗截断**的产品形态下，**默认不现实**；当前 **`funnel-v2`（5 / 7 / 12）** 是在「更深对话」与「整批可承受」之间的折中。若商业上确认要为 **Top N** 买「极深单对模拟」，宜单独定价、单独版本与单独容量规划，不宜与 **M=200** 的 L1 入口绑死。

### 6.6 AI 协同（每层不变）

每层仍采用 **逻辑 3+1**：

| 角色 | 职责 |
|------|------|
| **Facilitator** | 场景设定、回合推进 |
| **Persona-A / Persona-B** | 严格按问卷特征（20 维 + confidence）发言（可合并为单次结构化输出） |
| **Evaluator** | 该层结束打分 + 晋级信号（结构化 JSON） |

**工程默认**：同一 LLM **串行**多轮（成本低、易审计）；与旧版说明一致。

### 6.7 持久化输出（建议 schema）

对无向对 **(A,B)** 建议存：

- `simulationVersion`：`funnel-v2`（或 `funnel-v1` / `flat-v0`，见 **6.5 / 6.5.1 / 6.8**）
- `layer1`：`transcriptSummary`、`stageScore_L1`、`evaluatorJson`
- `layer2`：同上（若未晋级可为 null）
- `layer3`：`transcriptSummary`、`finalScore`、`greenFlags`、`redFlags`、`recommendedNextStep`
- `rankInBatch`：本批 L3 后的名次（可选）

### 6.8 其它 `simulationVersion`

- **`flat-v0`**：原先 **S=3、每场景 R=4、共约 13 次/对** 的平铺方案，仅作对照与回归基线。  
- **`funnel-v1`**：轻量三层漏斗（**6.5.1**），每对打满约 **12** 次调用。  
- **P0 产品默认**：**`funnel-v2`**（**6.5**），在晋级深层的对上使用更多轮次以支撑「是否真合适」的模拟判断。

---

## 7. 与现有代码的关系（落地提示）

- 矩阵特征可直接读 Prisma `UserProfile` 的 G1-R 列与 `confidence`（与 `scoreQuestionnaireG1r` 写库一致）。
- 现 worker 已有规则匹配分 `apps/worker/src/jobs/matching-score.ts`；AI 模拟可作为 **并行评分通道**，通过 `matrixVersion` / `simulationVersion` 区分。
- 合规：对话模拟不得使用可识别 PII；仅用画像特征与脱敏标签。

---

## 8. 修订记录

- v0.1：草案，含 P0 默认场景数 `S=3`、每场景轮数 `R=4`、AI 角色 **3+1**、每对约 **13 次** LLM 调用（按 6.5 节推荐实现）。
- v0.2：将 LaTeX 公式改为纯文本/伪代码，便于在 Cursor 默认 Markdown 预览中阅读。
- v0.3：新增矩阵 **F / V / S0 / S** 定义与 **5.2 全链路排序算法**。
- v0.4：阶段 C 改为 **循序渐进漏斗 `funnel-v1`**（L1/L2/L3 + 截断 M1/M2/M3 + 批次调用量估算）；旧平铺方案保留为 **`flat-v0`**。
- v0.5：新增 **4.7**「差分加权总分」备选矩阵 **`D`**（`matrixVersion = diff-weight-v1`），与余弦矩阵 **`S`** 可切换。
- v0.6：去掉文档内「已定稿」类元提示与独立定稿记录节，章节编号顺延。
- v0.7：新增 **4.8**「按轴谓词 + AND/OR/XOR 与分区」的 **`axis-logic-v1`** 方案；**4.2 / 5.2** 引入统一记号 **`X`**（**S / D / G**）；**6.1** 表格与复杂度表述同步。
- v0.8：**P0 默认模拟**改为 **`funnel-v2`**（L1/L2/L3 轮次 **5 / 7 / 12**，打满约 **27** 次调用/对；批次约 **2940** 次）；原 **`funnel-v1`**（**2/3/4** 轮，**12** 次/对）移至 **6.5.1** 备选；**1.1** 与 **6.3–6.8**、持久化字段同步。
- v0.9：新增 **6.5.2**，说明「每阶段约 **50** 轮」在批量漏斗下一般不现实及替代路径（**Top N** 加深）。
- v0.10：**5.2** 增加术语 **无向分**（即 **`matrixScore`**）及非对称 **`X`** 的说明；**4.8** 将 **P0 默认**改为 **梯形隶属度 + softAND/OR + 对称 `contrib_d` 表**，硬布尔改为可选；修正 **`G[i][j]`** 笔误与 **`F[i][d]`** 记号统一。
- v0.11：新增 **4.6.1** **`cosine-length-v1`**（**`Sm`**：余弦方向 **`C`** × 行模长接近度 **`M`**，或凸组合）；**4.2 / 4.3 / 5.2 / 6.1 / 4.8 选型表** 同步 **`X=Sm`**。
- v0.12：新增 **5.2.1**（**`V`+欧氏 KNN** 与 **`S0` Top-K** 的等价条件、**`F` 欧氏** 与 **`S`** 的差异、**ANN** 实现位、**互补** 与 **`p/q`、轴变换、`G`、双分数、度量学习** 的接入方式）。

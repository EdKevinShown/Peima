# Hoist Angular DevKit so @nestjs/cli can resolve @angular-devkit/schematics-cli/bin/schematics.js
# (pnpm isolated node_modules + Windows junctions across peima/Peima paths otherwise break `nest start`).
public-hoist-pattern[]=*@angular-devkit/*

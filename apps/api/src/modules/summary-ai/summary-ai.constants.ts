/** Structured log `event` for Summary AI slice (P6.5). */
export const SUMMARY_AI_LOG_EVENT = "summary_ai";

/** Rule-layer `sourceVersion` when model path is not used or fails. */
export const SUMMARY_RULE_SOURCE_VERSION = "p6.5-summary-rule-v1";

/** Bumped when prompt instructions change. */
export const SUMMARY_PROMPT_VERSION = "p6.5-summary-prompt-v1";

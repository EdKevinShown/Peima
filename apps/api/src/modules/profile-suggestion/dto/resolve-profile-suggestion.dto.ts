/**
 * P2 v1: accept/dismiss have no body. Reserved for future fields (e.g. client note).
 * If wired later, keep class empty so `{}` passes ValidationPipe.
 */
export class ResolveProfileSuggestionDto {}

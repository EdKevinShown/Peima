/**
 * STUB — M4.1 four-source agreement is intentionally out of scope for M4.4-M1 summary.
 * Tracked in m42AlignmentHint; full comparison remains m4-2-batch-regression-report.mjs.
 */
describe("matching observability · M4.1 four-source (stub)", () => {
  it.todo(
    "document that summary notes redirect to m4-2-batch-regression-report.mjs (no online recompute)",
  );

  it.todo(
    "optional: link sanitized run record fields when M4.2 report exists in workspace",
  );
});

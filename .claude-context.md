# 配吗 (Peima) 项目上下文文档

**项目日期**: 2026年4月8日  
**当前模型**: Claude Sonnet 4.6 (VSCode)  
**上次更新**: P5 全部完成，Code Review 修复已合入并联调通过

---

## 项目概述

**配吗** 是一个面向中文市场的 **AI 关系匹配与互动辅助系统**。

它不是传统的刷脸社交 App，也不是单纯的 MBTI 测试产品，而是一个由以下环节组成的完整系统：

> 照片上传与形象初筛 → 基础条件过滤 → 轻量关系画像 → 规则精筛 → AI 关系模拟 → 最终匹配 → 聊天与关系助手 → 反馈闭环

### 核心理念

系统围绕真实用户的关系决策路径设计：**先"有没有感觉"，再"合不合适"，最后"能不能走下去"**。

### 用户核心流程

1. **注册与填写基础资料** — 年龄、地区、身高、学历、职业、婚恋目标（只需填写一次）
2. **上传照片** — 系统提取视觉特征、识别外在风格、判断气质标签，建立"第一印象"
3. **设置基础偏好** — 告诉系统你想找什么样的人（年龄、地区、身高、学历、风格等）
4. **6人预览池** — 系统生成初步候选池（偏好型2人 + 视觉匹配型2人 + 系统推荐型2人）
5. **轻量关系画像问卷** — 补充深层信息用于精筛
6. **每日批次匹配** — Worker 定时执行，输出唯一最终匹配对象
7. **聊天与AI辅助** — 与匹配对象聊天，系统提供摘要、Copilot建议、开场方向
8. **反馈闭环** — 用户反馈持续更新画像，让下一轮推荐更准确

**仓库**: `/Users/zekaihuang/Desktop/peima/-`  
**当前分支**: `feat/p5-phase-2-complete`

---

## 技术栈

### 后端
- **框架**: NestJS (v10.4.15)
- **数据库**: PostgreSQL + Prisma ORM
- **认证**: JWT (Passport.js)
- **调度**: @nestjs/schedule (定时任务)
- **缓存**: 内存缓存（1小时TTL）

### 前端
- **框架**: React 18 + Vite
- **状态管理**: React Hooks
- **样式**: Vanilla CSS (响应式设计)
- **User Web**: `/apps/web` — 用户端产品前端
- **Admin UI**: `/apps/admin` — 后台管理界面

### Worker
- **框架**: NestJS (独立服务)
- **职责**: 批处理与调度服务（每日批次匹配）

### 包管理
- **包管理器**: pnpm (monorepo)
- **工作区**: apps/, packages/
- **数据库包**: `@peima/database` (Prisma 6.19.3)

---

## Monorepo 结构

```
peima/
├─ apps/
│   ├─ web/          # 用户端 Web (React + Vite)
│   ├─ api/          # 核心后端 API (NestJS)
│   ├─ worker/       # 批处理与调度服务
│   └─ admin/        # 后台管理界面
│
├─ packages/
│   ├─ shared/       # 共享类型、常量
│   ├─ config/       # 共享配置
│   ├─ database/     # Prisma schema、迁移、Client
│   ├─ ai-prompts/   # AI 提示词资产（P6预留）
│   ├─ scoring/      # 评分逻辑
│   └─ sdk/          # 第三方服务封装（预留）
│
├─ infrastructure/
│   ├─ nginx/        # Nginx 配置
│   ├─ docker/       # Docker 配置
│   ├─ scripts/      # 运维脚本
│   └─ monitoring/   # 监控配置
│
├─ docs/             # 分阶段文档
└─ report/           # 项目报告PDF
```

---

## 项目阶段进度

| 阶段 | 名称 | 状态 | 关键内容 |
|------|------|------|----------|
| **P0** | MVP 核心闭环 | ✅ 稳定 | 注册、照片、偏好、预览池、问卷、批次匹配、聊天 |
| **P1** | 差异化能力 | ✅ 完成 | AI关系模拟、匹配解释、风险提示、聊天模拟摘要 |
| **P2/P2.5** | 用户闭环 | ✅ 完成 | 聊天摘要、Copilot建议、用户反馈、画像更新、analytics |
| **P3** | 关系时间线 | ✅ 完成 | 关系时间线API、/chat/timeline页面、长会话分页 |
| **P4** | 产品化补完 | ⏳ 待开发 | 聊天周边体验、提醒、约会推荐、时间轴增强 |
| **P5** | 治理与运营 | ✅ 完成 | 建议治理、RBAC、审计日志、admin工具、分析仪表板 |
| **P6** | AI 生产链演进 | 📋 远期 | 多Agent、模型替换、simulation流水线、全局模型优化 |

### P5 完整开发进度（全部完成）

| 子阶段 | 状态 | 内容 |
|--------|------|------|
| P5 Phase 1+ | ✅ 完成 | 批量操作、CSV导出、RBAC UI、版本历史 |
| P5 Phase 2.1 | ✅ 完成 | 审计日志系统（追踪、查询、导出） |
| P5 Phase 2.2 | ✅ 完成 | RBAC数据库持久化、角色过期管理、Admin UI增强 |
| P5 Phase 2.3 | ✅ 完成 | WebSocket实时通知、事件总线、通知中心UI、Toast |
| P5 Phase 2.4 | ✅ 完成 | 分析仪表板（系统概览、趋势图、分布面板） |
| P5 Bug修复 | ✅ 完成 | 审计日志过滤/导出修复、失败操作记录、权限async修复、history old/new 修复、audit detail 修复、env-admin 通知修复 |

---

## 后端模块 (apps/api)

当前已落地的核心业务模块：

| 模块 | 职责 |
|------|------|
| `auth/` | 注册、登录、JWT校验与鉴权 |
| `users/` | 用户资料与基础画像 |
| `preferences/` | 用户偏好与筛选条件 |
| `images/` | 用户图片相关能力 |
| `preview-pool/` | 预览池生成与读取 |
| `questionnaire/` | 问卷题目与提交评分 |
| `matching/` | 入队、状态查询、最终结果 |
| `chat/` | 会话、消息、摘要、P3关系时间线API |
| `feedback/` | 结构化反馈 |
| `profile-suggestion/` | 画像建议创建、查询、accept/dismiss |
| `behavior-signal/` | 行为信号追加与查询 |
| `analytics/` | 只读统计概览 |
| `copilot/` | 会话级规则建议（只读，不落库） |
| `common/rbac/` | P5: 角色权限管理 |
| `common/audit/` | P5: 审计日志系统 |
| `common/events/` | P5 Phase 2.3: 事件总线 (EventEmitter2) |
| `modules/notifications/` | P5 Phase 2.3: WebSocket通知 + REST API |

**注意**: simulation、explanation、visual-profile、reminders 等能力在历史文档中出现过，但当前仓库中不是独立模块，属于P4/P5/P6规划方向。

---

## Worker 服务 (apps/worker)

围绕匹配批次的调度与处理服务：

```
apps/worker/src/
├─ main.ts                          # Worker 入口
├─ schedulers/
│   └─ daily-match.scheduler.ts     # 每日匹配批次调度
└─ jobs/
    ├─ batch-match.processor.ts     # 批次匹配逻辑执行
    ├─ matching-score.ts            # 评分逻辑
    └─ match-insights-placeholder.ts # P1 matchInsights 占位
```

---

## 前端页面 (apps/web)

核心用户页面：

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | `/login` | 登录注册 |
| 问卷页 | `/questionnaire` | 轻量关系画像问卷 |
| 预览池页 | `/preview-pool` | 6人候选预览 |
| 匹配等待页 | `/matching-waiting` | 等待每日批次匹配 |
| 最终匹配页 | `/final-match` | 唯一匹配结果展示 |
| 聊天页 | `/chat` | 与匹配对象聊天 |
| Copilot页 | `/copilot` | AI 建议 |
| 关系时间线 | `/chat/timeline` | P3 关系时间线 |

### 前端API封装 (apps/web/src/api/)

- `auth.*` — 登录、注册、身份校验
- `questionnaire.*` — 问卷获取与提交
- `previewPool.*` — 预览池生成与读取
- `matching.*` — 匹配状态与最终结果
- `chat.*` — 聊天、摘要、时间线
- `copilot.*` — Copilot 只读建议
- `feedback.*` — 反馈提交
- `profile.*` — 画像建议相关

---

## P5 Phase 2.3 WebSocket 通知架构

### 事件驱动设计
```
业务服务 (RBAC/Audit/Suggestion) → EventEmitter2 → NotificationGateway
                                                    ├─ 持久化到 Notification 表
                                                    └─ 通过 Socket.io 实时推送
```

### 事件类型
- `notification.role_changed` — 角色分配/撤销
- `notification.suggestion_updated` — 建议批量操作
- `notification.audit_alert` — 关键操作/失败告警
- `notification.system_message` — 系统消息广播

### WebSocket 房间
- `user:{userId}` — 用户专属房间（JWT认证后自动加入）
- `role:{roleCode}` — 角色房间（按用户角色加入）

### 通知 REST API (`/notifications/`)
| 方法 | 端点 | 功能 |
|------|------|------|
| GET | `/notifications` | 分页查询通知 |
| GET | `/notifications/unread-count` | 未读数量 |
| PATCH | `/notifications/:id/read` | 标记已读 |
| PATCH | `/notifications/read-all` | 全部已读 |

---

## P5 数据库架构

### `user_roles` 表 (角色分配)
```sql
id, userId, roleCode, grantedAt, grantedBy, expiresAt, createdAt, updatedAt
```
- 唯一约束: `(userId, roleCode)`
- 索引: `roleCode`, `expiresAt`

### `role_permissions` 表 (权限定义)
```sql
id, roleCode, permissionCode, resource, conditions(JSONB), createdAt
```
- 唯一约束: `(roleCode, permissionCode)`

### `notifications` 表 (P5 Phase 2.3 通知)
```sql
id, userId, type, title, message, actionUrl, read, metadata(JSON), createdAt
```
- 索引: `(userId, read, createdAt DESC)`, `(userId, createdAt DESC)`

### `audit_log` 表 (审计日志)
```sql
id, userId(FK→users.id), action, entityType, entityId,
oldValues(JSON), newValues(JSON), status('SUCCESS'|'FAILED'),
errorMessage, duration, ipAddress, userAgent, changeReason, createdAt
```
- `userId` 有外键约束，**不能传 "SYSTEM"**（会触发 FK 违反，被静默吞掉）
- `status` 默认为 `'SUCCESS'`，失败时需显式传 `'FAILED'` + `errorMessage`

---

## RBAC 角色与权限

### 角色
```
REGULAR_USER    → 普通用户
OPERATOR        → 运营人员（建议管理）
DATA_ANALYST    → 数据分析人员
ADMIN           → 管理员（完全权限）
```

### 关键权限
```
VIEW_AUDIT_LOG, EXPORT_DATA, MANAGE_PERMISSIONS, VIEW_GLOBAL_ANALYTICS
```

### RBAC 三层缓存架构
```
请求 → 内存缓存(1小时TTL) → PostgreSQL数据库 → 环境变量(降级方案)
```

---

## 核心API端点

### RBAC 管理 (`/rbac/`)
| 方法 | 端点 | 功能 |
|------|------|------|
| GET | `/rbac/permissions` | 获取权限和角色清单 |
| GET | `/rbac/users` | 列表所有用户及角色 |
| GET | `/rbac/users/:userId` | 获取用户详细角色信息 |
| POST | `/rbac/users/:userId/role` | 分配角色(支持过期时间) |
| PATCH | `/rbac/users/:userId/roles` | 批量更新用户角色 |
| DELETE | `/rbac/users/:userId/role/:roleCode` | 撤销角色 |

### 审计日志 (`/audit-logs/`)
| 方法 | 端点 | 功能 |
|------|------|------|
| GET | `/audit-logs/` | 查询审计日志 |
| GET | `/audit-logs/:id` | 查询单条审计日志详情 |
| GET | `/audit-logs/entity/:type/:id` | 对象变更历史 |
| GET | `/audit-logs/user/:userId` | 用户操作记录 |
| POST | `/audit-logs/export` | 导出审计日志 |

---

## 关键文件位置

### P5 RBAC 系统
- `/apps/api/src/common/rbac/rbac.service.ts` — RBAC 业务逻辑
- `/apps/api/src/common/rbac/rbac.controller.ts` — API 端点
- `/apps/api/src/common/rbac/rbac.guard.ts` — 权限守卫
- `/apps/api/src/common/rbac/dto/assign-role.dto.ts` — DTO

### P5 审计日志
- `/apps/api/src/common/audit/audit.service.ts` — 审计逻辑
- `/apps/api/src/common/audit/audit.controller.ts` — 审计端点
- `/apps/api/src/common/audit/audit.middleware.ts` — 自动审计中间件

### P5 Phase 2.3 通知系统
- `/apps/api/src/common/events/notification.events.ts` — 事件定义
- `/apps/api/src/common/events/events.module.ts` — EventEmitter 模块
- `/apps/api/src/modules/notifications/notification.gateway.ts` — WebSocket 网关
- `/apps/api/src/modules/notifications/notification.service.ts` — 通知服务
- `/apps/api/src/modules/notifications/notification.controller.ts` — REST API

### P5 Phase 2.4 分析仪表板
- `/apps/api/src/modules/analytics/analytics.repository.ts` — `getAdminDashboard()` 聚合查询
- `/apps/api/src/modules/analytics/analytics.service.ts` — 仪表板服务
- `/apps/api/src/modules/analytics/analytics.controller.ts` — `GET /analytics/admin-dashboard`
- `/apps/api/src/modules/analytics/admin-dashboard.types.ts` — `AdminDashboardStats` 类型
- `/apps/admin/src/pages/AnalyticsDashboardPage.jsx` — 仪表板前端页面
- `/apps/admin/src/pages/analytics-dashboard.css` — 仪表板样式
- `/apps/admin/src/api/analytics.js` — 前端 API 封装

### Admin UI
- `/apps/admin/src/pages/RbacPermissionManager.jsx` — 角色管理页面
- `/apps/admin/src/pages/AuditLogPage.jsx` — 审计日志查询与导出页面
- `/apps/admin/src/styles/rbac-permission-manager.css` — 样式
- `/apps/admin/src/components/NotificationCenter.jsx` — 通知中心面板
- `/apps/admin/src/components/NotificationToast.jsx` — Toast 浮动提示
- `/apps/admin/src/hooks/useNotifications.js` — WebSocket 通知 Hook

### 数据库
- `/packages/database/prisma/schema.prisma` — 数据模型定义
- `/packages/database/prisma/migrations/` — 迁移文件

---

## 开发命令

```bash
# 启动数据库
docker compose up -d

# 生成 Prisma 客户端
pnpm --filter @peima/database build

# 启动服务
pnpm dev:api        # http://localhost:3000
pnpm dev:admin      # http://localhost:5174
pnpm dev:web        # http://localhost:5173

# 数据库操作
pnpm --filter @peima/database db:migrate
pnpm --filter @peima/database db:generate
pnpm --filter @peima/database db:studio
```

---

## 代码约定

- 使用 async/await
- 所有 API 调用需要 Authorization 头
- 权限检查使用 `@RequirePermission(Permission.ENUM)`
- 使用 Prisma ORM，重要操作记录审计日志
- Git 提交前缀: `feat:`, `feat(audit):`, `feat(rbac):`, `feat(admin-ui):`

---

## ⚠️ Code Review 关键注意点

### 1. `rbacService` 方法全为 async，必须 await
`requirePermission()` 和 `checkPermission()` 均为 `async` 方法。**不加 `await` 会返回一个永远为 truthy 的 Promise**，导致权限检查完全失效（安全漏洞）。
```typescript
// ❌ 错误 — 权限被静默跳过
this.rbacService.requirePermission(userId, Permission.X);

// ✅ 正确
await this.rbacService.requirePermission(userId, Permission.X);
```

### 2. 审计日志必须覆盖失败路径
所有写操作的 `auditService.recordAction` 调用必须在 try/catch 中同时处理成功和失败：
```typescript
try {
  // ... 执行操作
  await auditService.recordAction(userId, 'ACTION', 'ENTITY', { status: 'SUCCESS', ... });
} catch (error) {
  await auditService.recordAction(userId, 'ACTION', 'ENTITY', {
    status: 'FAILED',
    errorMessage: (error as Error).message,
  });
  throw error; // 必须重新抛出，不能吞掉
}
```

### 3. AuditService 的 userId 不能是 "SYSTEM"
`audit_logs.userId` 有 FK 约束指向 `users.id`。传 `"SYSTEM"` 会触发 FK 违反（被 auditService 内部 try/catch 静默吞掉，日志丢失）。

### 4. 审计日志实际 action 值
数据库中存储的 action 值（非 CREATE/UPDATE/DELETE）：
```
ROLE_ASSIGN, ROLE_REVOKE, ROLE_UPDATE,
SUGGESTION_BULK_OPERATE, SUGGESTION_UPDATE, SUGGESTION_EXPORT, SUGGESTION_HISTORY_READ
```

### 5. 审计日志导出使用 `@Query('format')`
`POST /audit-logs/export?format=csv` — format 通过 query string 传递，后端用 `@Query('format')` 读取（不是 `@Body`）。

### 6. 本轮 Code Review 修复已合入
- `GET /audit-logs/:id` 已接入真实查询，非法 id 返回 `404`
- 审计导出支持携带当前筛选条件，`QueryAuditLogsDto` 已补 `format?: string`
- `SuggestionHistory` 写入/比较统一为 `{ old, new }` 结构
- `SUGGESTION_BULK_OPERATE` / `SUGGESTION_EXPORT` / `SUGGESTION_UPDATE` 已纳入审计告警动作
- env 白名单中的 admin 也能收到离线持久化通知
- Admin 通知中心乱码已清理，Audit 页面 action 过滤项与真实 action 值对齐

### 7. `removeRoleFromUser` 使用 `deleteMany`
避免对 env-only 角色调用 `delete()` 时触发 Prisma P2025（记录不存在）错误。

### 8. 新模块引入 AuditService 需导入 AuditModule
在模块的 `imports` 数组中加入 `AuditModule`，否则 NestJS DI 无法解析 `AuditService`。

### 9. 本轮联调结论
- `GET /audit-logs/:id` 返回真实详情且含 user 信息；假 id 返回 `404`
- suggestion update / bulk operate 后可写入版本历史，`compare` 返回 `fromVersion / toVersion / changes`
- 按 `action=SUGGESTION_BULK_OPERATE` 过滤后导出 CSV，结果与列表一致
- `notifications` REST 未读数正常；`audit_alert` 可持久化到通知表
- 普通用户访问 `/analytics/admin-dashboard` 返回 `403`；admin 返回全局数据
- 建议中心 stats 越权已修复，普通用户统计范围锁定为本人

---

**最后一次更新**: 2026-04-08  
**作者**: Zekai Huang
